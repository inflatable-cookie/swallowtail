# 2026-07-30 Packaged Provider-Wide Activity Proof

## Outcome

Card 136 is complete. Observable activity now has extracted-package evidence
across every public facade and representative rich, thin, direct, realtime,
not-applicable, and unverified-newer profiles.

No package was published. No retained candidate, tag, push, or release state
changed.

## Package Evidence

- 23 local archives assembled.
- The complete extracted workspace compiled.
- An ephemeral clean snapshot reproduced the archives exactly.
- 35 packaged facade suites covered all 26 production routes.
- Ten named activity profiles covered:
  - rich Codex lifecycle
  - shared ACP lifecycle
  - non-ACP harness lifecycle
  - completion-only headless activity
  - direct tool lifecycle
  - direct reasoning-summary activity
  - realtime separation
  - catalogue non-applicability
  - serving non-applicability
  - unverified-newer preservation
- Live credentials were absent and no provider call occurred.

The synthetic source commit existed only inside the temporary proof workspace.
It is not canonical history or a release candidate and was deleted after the
proof.

## Verifier Repair

The package gate exposed stale lifecycle inventory. Gemini CLI headless now
implements provider-session management, but the verifier still expected four
management adapters.

The verifier now requires Gemini, runs its packaged headless management suite,
and records:

- 14 lifecycle suites
- five management adapters
- five supported routes
- three unsupported routes
- eighteen not-applicable routes

## Consumer Evidence

The candidate gate copied Nucleus and Soundcheck into isolated temporary
workspaces and verified their existing Swallowtail integration. It did not
edit either consumer repository.

## Validation

Passed:

- `effigy package:verify-local`
- `effigy package:candidate:facades`
- `effigy package:metadata`
- `effigy package:api`
- `effigy package:docs`

`effigy qa` passed docs, route and matrix gates, then completed the all-target
workspace check. Its redundant full-workspace test phase was stopped by the
operator after disproportionate runtime. No test had failed. The exact
extracted-package and packaged-profile selectors above remain this card's
acceptance evidence.

## Next

Card 137 is ready. Produce bounded Nucleus and Soundcheck activity handoffs
using only public prepared and runtime APIs. Keep persistence, grouping,
collapsed presentation, review, and thread ownership downstream.
