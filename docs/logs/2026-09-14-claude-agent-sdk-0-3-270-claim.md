# 2026-09-14 Claude Agent SDK 0.3.270 Claim

## Result

The `claude-agent.sdk` route now binds the exact one-point
package/native tuple SDK `0.3.270` / native `2.1.270` through all nine
published hops after the `0.3.259` ceiling. Behavior revision, wire, Node
`22.23.2`, sidecar source-tag axes, claim ids, and `QualifiedOnly` posture
stay; `0.3.259` becomes unqualified rather than a second point, and
`0.3.271` does not exist. The sidecar's exact startup gate moves with the
route; the fake-SDK check-only corpus now simulates the `0.3.270` bundle
with structurally identical hooks, status rows, and identity shapes.

Research 301 acceptance stays bound to `0.3.259`/`2.1.259` and does not
transfer. The registered-tool qualification additionally requires the
compiled tuple to equal the frozen live tuple, so the rebound route
projects `registered_tools` and `consumer_tool_exchange` unqualified with
the reason `live_tuple_not_compiled`, and both feature-matrix cells return
to producer-gap with Research 315 as the cross-reference. The follow-up is
a separately authorized live requalification on the new tuple; nothing here
presumes its outcome.

Identity evidence stays in the prior commit: downloaded artifacts were
hashed and never executed, every hop's wrapper/native coupling, tarball
digests, native commits, platform payloads, and 15-file inventory are
frozen, and all 42 declaration deltas are classified unmapped.

Focused, affected-package, example, API, route, docs, Northstar, format, and
diff gates pass without provider work.

## Next

Return the exact outcome to Chatterbox, then compile and dispatch the Grok
Build catalogue as the next Research 308 family without asking Tom to repeat
authorization.
