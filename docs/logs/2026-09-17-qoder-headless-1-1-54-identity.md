# 2026-09-17 Qoder Headless 1.1.54 Identity

g05.080 re-probed official npm `@qoder-ai/qodercli`: stable `latest` is
`1.1.54`, beta is `1.1.54-beta.1`, and `1.1.55` is unpublished. Research 328
freezes the `1.1.52` → `1.1.53` → `1.1.54` registry metadata, tarball
digests, and selected-source inventory in the Qoder adapter fixture.

The selected `qodercli --print --output-format stream-json` route remains
wire-compatible. The `1.1.30` forwarded `--max-turns` AgentLoop authority is
present at the current endpoint. Per the recorded ruling, the claim card will
make `--max-turns 8` an explicit adapter-owned bound, record the reachable
`error_max_turns` terminal, and move to one exact `1.1.54` point on a new
behavior revision. The old `1.1.25` point is not retained.

No prompt, login, credential, installation, host update, or downloaded
artifact execution occurred.
