# 296 Claude SDK Registered-Tool Live Open Rejection

Status: complete; promoted into g05 card 144
Owner: Tom
Created: 2026-09-08

## Finding

Bovine Desktop completed the Card 132 gate against source-linked Swallowtail
`04e9b2dd9058783b7186a33a6c13dd74882a5925`, reverified from a separate clean
detached checkout. The single authorized primary session open returned
`typed_failure` at stage `open` with code
`open.failed.swallowtail.claude-agent.sdk.open_rejected`.

No turn began. No registered-tool dispatch, deny control, pending-cancellation
control, stale/foreign-callback control, or retry occurred. The immutable
capsule contains no failed-open runtime identity record or observed joined
cleanup receipt, so both facts remain unavailable rather than inferred. The
two Contract 061 cells stay unqualified.

## Frozen Identity

Desktop records Northstar task
`0a594fd5-a966-4192-aeb6-fa98f5493d1d`, PR 170 accepted restacked head
`bd83f6b038151afd7e51ca19b944b22e9db72799`, independent review comment
`5586693748`, merge `4cf8049762d5e7891d8b1c8c458af0ef85ed97d6`, and canonical closeout
`ee47e160ac8e3ec5db1fa5f0444999e5cd630b2d`.

Capsule SHA-256:
`e0460a54776a5644ff2c54bc412a52d81b0d84f04ee34f56971b78181e501433`.

The frozen tuple is SDK `0.3.259`, native `2.1.259` on Darwin arm64 at
SHA-256 `884baa38fe1a624be25c4a91568bf5a08b5cf4e7d7acf29b7760e3525d964898`,
Node `22.23.2`, sidecar source tag
`swallowtail-claude-agent-sdk-sidecar@0.4.4`, carrier
`swallowtail-claude-agent-sdk-registered-tool-mcp-v1`,
`private-loopback-http` plus `mediated-stdio-proxy`, MCP `2025-11-25`, model
`claude-sonnet-5`, default permission, persistence false,
`strictMcpConfig: true`, empty setting sources, and omitted `allowedTools`.
The capsule separately records the frozen SDK tarball digest and observed
loaded-module digest; neither is substituted for the other.

## Provider-Free Evidence

The accepted Desktop revision passed source-consumer 4/4 and mediated-stdio
15/15 after hardening future evidence capture. It made future fields
observation-derived and improved failed-open teardown without repeating the
provider attempt. Release and source-independent dependency selectors remain
deferred because the machine-local Longhorn candidate receipt is absent; that
is an environment prerequisite, not a provider or release verdict.

Current Swallowtail retains the sidecar's bounded rejection subcode in the
safe diagnostic message, but the generic `RuntimeFailure` result does not give
the consumer a structured failed-open receipt. The immutable Desktop capsule
therefore cannot distinguish construction, initialization, account, or MCP
status rejection and cannot positively record the cleanup the open guard
performed. Card 144 owns that producer observability and diagnosis gap.

## Card 132 Disposition

Card 132's typed-failure branch moves no feature or qualification row and
permits no replay. The gate is stopped with evidence. Card 144 is the next
producer card: reproduce every bounded open-rejection class provider-free,
surface exact structured failure and cleanup evidence additively, and repair a
producer defect only if the deterministic route identifies one.

Any new live gate requires separate operator authorization after Card 144
returns a precise diagnosis. No tag, release, feature classification, parity,
or broader acceptance follows.
