# 073 Post-SDK Coverage Evidence

Status: completed
Owner: Tom
Updated: 2026-07-21
Milestone: `../022-post-sdk-coverage-checkpoint.md`

## Objective

Select the next high-information Swallowtail proof from current authoritative
evidence after the Bedrock Runtime and control-plane SDK tranche.

## Scope

- inventory uncovered harness, direct API, shared protocol, SDK, catalogue,
  and attached-runtime behavior against the ten realized production routes
- recheck current official provider or maintained-project documentation,
  releases, access mechanisms, endpoint audiences, model catalogues, protocol
  authority, lifecycle, and topology
- compare Claude Code, Cursor, Pi, Qwen Code, remaining hosted APIs, ACP
  implementations, and attached runtimes only where an exact supported or
  maintainer-owned surface exists
- rank candidates by new contract and conformance pressure, deterministic
  fixture quality, support authority, and seamless host integration
- promote one research delta and recommend one bounded proof or prerequisite

## Out Of Scope

- provider implementation
- live authentication, subscription mutation, or paid inference
- implicit provider, credential, endpoint, billing, deployment, or consumer-
  routing preference
- repeating an existing transport shape only to add a provider name

## Acceptance Criteria

- [x] current primary or maintained-project sources support every live claim
- [x] realized coverage and remaining gaps are compared concretely
- [x] authentication, entitlement, metering, endpoint, support, model
      catalogue, lifecycle, and topology remain separate
- [x] the recommendation names the new architectural information it proves
- [x] missing shared contracts and deterministic fixture seams are explicit
- [x] no genuine policy-significant tie remains; Cursor's repository and
      remote-workspace policy is deferred explicitly

## Evidence

- Research 015 compares the ten realized production routes with current
  OpenAI, Cursor, Claude, Qwen, Pi, Kimi, ACP, Z.AI, DeepSeek, Alibaba,
  Ollama, and vLLM surfaces
- OpenAI Responses background mode is selected because the provider operation
  can outlive one SSE attachment and supports status retrieval, cursor
  reattachment, and native idempotent cancellation
- `store=false` still requires temporary provider retention for background
  execution; the route cannot reuse a zero-retention direct-run claim
- the exact access surface is the provider-supported OpenAI public API with an
  API-key lease, API endpoint audience, provider metering, and provider support
- ChatGPT, Codex, harness, subscription, and community OAuth access remain
  excluded and independent
- Cursor Cloud Agents rank second but require a later operator decision on
  GitHub integration, provider-owned VM, repository mutation, artifact, and
  durable-agent deletion authority
- Claude, Qwen, Pi, Kimi Rust Wire, ACP SDK or remote transport, remaining
  hosted APIs, and attached runtimes either repeat realized shapes, require a
  language sidecar, or remain experimental, Preview, or unfinished
- Contract 021 is required for remote operation identity, connection versus
  operation lifetime, bounded cursor reattachment, temporary retention,
  cancellation truth, lease lifetime, and joined cleanup
- the dated loopback HTTP/SSE corpus can prove all first-subset behavior
  without a credential, account, external request, or paid inference
- roadmap 023 and cards 074-075 compile the exact contract, fixture,
  production, and conformance sequence

## Validation

- authority-map and contract consistency review
- `effigy qa:docs`
- `git diff --check`

## Stop Conditions

- evidence depends on private credentials, unsupported OAuth reuse, an
  unmaintained protocol, or an unowned serving lifecycle
- the leading choice would establish provider or product policy absent from
  the authority surfaces

## Auto-Continuation

No. Card 074 is the sole ready continuation.
