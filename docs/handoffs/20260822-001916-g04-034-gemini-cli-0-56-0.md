---
title: g04.034 Gemini CLI 0.56.0 worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Tom
created: 2026-08-22
updated: 2026-08-22
handoff_path: /Users/tom/Dev/projects/swallowtail/docs/handoffs/20260822-001916-g04-034-gemini-cli-0-56-0.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, currentness, gemini]
---

## What This Thread Was Doing

The orchestrator merged g04.033, retained both Pi routes, then resumed the sole
Next Task: Gemini CLI currentness. It re-probed installed `gemini 0.53.0` and
official npm/GitHub stable `0.56.0`, compiled g04.034, and made cards 093-094
ready for one identity-before-claim run across the separate ACP and headless
axes.

This is the handoff from the orchestrator to one bounded implementation thread.
It stands alone; no copied transcript or second prompt is needed.

## Why It Matters

Swallowtail currently guarantees ACP only at `0.51.0` and headless through
`0.52.0`, while the installed host is `0.53.0` and official stable is `0.56.0`.
The routes are useful only through the enterprise/API-key posture selected by
the operator. Consumer-account login is no longer a supported product path.

The family therefore needs an honest binary outcome: qualify both selected CLI
surfaces deterministically at the current stable, or leave both claims
unchanged and return for an explicit keep-or-remove decision.

## Current State

Here is the state the worker is inheriting:

- **Repository:** `/Users/tom/Dev/projects/swallowtail`
- **Repository posture:** strict-ready Northstar
- **Planning branch:** `main`
- **Planning base commit:** `eb3445284431c975c41ea61c7570ee9e9f9f44b3`
- **Pushed main verification:** local `HEAD` and `origin/main` both equalled
  `eb3445284431c975c41ea61c7570ee9e9f9f44b3` before this handoff was created.
  Fetch again at worker startup; the later main tip contains this handoff.
- **Planning checkout:** clean on `main` after the pushed planning commit; this
  handoff is a follow-up commit on the same branch
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight
- **Planning artifacts included at the base:** g04.034; ready cards 093-094;
  the currentness planning log; corrected completion state for Pi cards 089-092
- **Worker branch:** `g04-034-gemini-cli-0-56-0`
- **Worker worktree:** launcher-provided dedicated worktree first. Manual
  fallback path is
  `$AGENTS_WORKTREE_CONTAINER_DIR/swallowtail-g04-034-gemini-cli-0-56-0`
- **Worktree creation command:** only if the current context is unusable and
  `.agents.local.env` defines `AGENTS_WORKTREE_CONTAINER_DIR`:
  `git worktree add -b g04-034-gemini-cli-0-56-0 "$AGENTS_WORKTREE_CONTAINER_DIR/swallowtail-g04-034-gemini-cli-0-56-0" origin/main`
- **Worktree policy:** use a clean, dedicated, non-`main` registered worktree
  supplied by the launcher even if its generated path or branch differs from
  the placeholders above. Record the actual values and do not create another
  worktree. If the current context is unusable, use the named worktree when it
  matches; only then read `.agents.local.env`, require
  `AGENTS_WORKTREE_CONTAINER_DIR`, and ask the operator if it is absent. Never
  use `/tmp`, `TMPDIR`, or a guessed path for the worktree.
- **Active spec lane:** none. Contract 029 and the promoted Gemini research own
  the qualification boundary.
- **Roadmap milestone:**
  `docs/roadmaps/g04/034-gemini-cli-0-56-0-useful-newer.md`
- **Ready cards, in order:**
  `docs/roadmaps/g04/batch-cards/093-gemini-cli-0-56-0-identity.md`, then
  `docs/roadmaps/g04/batch-cards/094-gemini-cli-0-56-0-claim.md`
- **Allowed runway:** identity and claim for Gemini CLI `0.56.0` across the
  separate `gemini-cli.acp-agent` and
  `gemini-cli.headless-stream-json` axes; one PR
- **Remaining card budget:** two cards, one PR
- **Dispatch topology:** serial; identity must settle both segment shapes
  before the claim card starts
- **Parallel safety check:** both cards share official artifacts, Gemini
  selection claims, fixtures, architecture, guides, matrices, and closeout
  state. No safe split.
- **Canonical refs:** `AGENTS.md`; `docs/contracts/001-working-rules.md`;
  Contracts 015, 029, 033, 038, and 039;
  `docs/architecture/system-architecture.md`; Research 045, 059, and 159;
  `docs/guides/version-currentness-checkpoint.md`;
  `docs/guides/gemini-cli-prepared-integration.md`;
  `.agents/skills/version-currentness/SKILL.md` and `reference.md`
- **Observed identity:** host `gemini 0.53.0`, SHA-256
  `4a8f99947eae4e1ff501269ba8b9ca2d1216db044fb75e01f4ee86fd1d8f175e`;
  npm `@google/gemini-cli@0.56.0`; GitHub `v0.56.0`, published
  2026-08-19; darwin-arm64 unsigned asset SHA-256
  `be0c20ccf8b6be6ce01654736847168a9328e92db4db4c0d0b776de70703fb8f`
- **Published stable points after existing ceilings:** `0.53.0`, `0.53.1`,
  `0.54.0`, `0.54.4`, `0.55.1`, `0.56.0`
- **Access posture:** enterprise-owned Gemini API key through the existing
  provider-supported API-key profile; no browser login or individual-account
  service
- **Model capability profile:** capable coding model with medium or higher
  reasoning; frontier review for any lifecycle, authentication, or public API
  change
- **Tool/runtime restrictions:** use Effigy selectors; official artifacts may
  be downloaded only to `/tmp`; do not send prompts, authenticate, install,
  update the host, inspect account state, or run live probes
- **Planning validation:** `effigy qa:northstar`; roadmap, g04, batch-card, and
  log index gates; roadmaps next-action gate; `git diff --check`
- **Required final validation:** `cargo fmt -p swallowtail-adapter-gemini`;
  focused and affected-package gates for `swallowtail-adapter-gemini`;
  `effigy qa:routes`; `effigy qa:northstar`; research, logs, roadmaps, g04,
  and batch-card index gates; roadmaps next-action gate; `git diff --check`
- **PR base/head:** `main` / selected worker branch
- **PR URL:** pending
- **Review state:** awaiting worker PR
- **Merge authorisation:** not granted; merge is a separate
  operator-authorised action

## Boundaries

Please keep this run inside the named runway:

- **In scope:** exact host/npm/GitHub identity; selected-source and selected-help
  comparison; secret-free Gemini `0.56.0` identity fixture; Research 182;
  separate ACP/headless segment decisions; compatible claim extension or
  adapter-private milestone when proved; focused tests; current architecture,
  guide, matrix, changelog, log, roadmap, and Next Task closeout.
- **Out of scope:** browser login; individual-account service; Gemini Live;
  Gemini Models; new flags, auth modes, nested fields, or provider features;
  transcript import/history/management; another route family; per-route feature
  implementation; workspace `qa`, broad `qa:docs`, live probes, MSRV, consumer
  checks, release, or tag work.
- Keep ACP and headless as separate version axes, claims, behavior revisions,
  transports, operations, and lifecycle identities. Sharing one npm package
  does not flatten them.
- The existing enterprise/API-key access profile is the only selected access
  boundary. Do not use browser login or account state as evidence.
- Preserve the historical ACP activity and headless stream-JSON decoder
  corpora unless selected adapter mapping actually changed.
- Do not rewrite Contracts 015 or 038 merely to mirror a moving ceiling. Their
  exact-source statements are historical proof. Stop if current evidence needs
  a new contract decision.
- If either selected axis is a stop, do not edit either production claim. Mark
  the identity result honestly and return for the keep-or-remove decision.
- Do not invent architecture, change contracts, widen the roadmap, or choose an
  unresolved product, API, lifecycle, or authentication decision.
- Work only in the selected clean worker worktree. Never edit the
  orchestrator's planning checkout or an unrelated dirty checkout.
- Do not merge the PR.

## Important Context

- **Planning lineage:** Research 045 qualified headless `0.51.0..=0.52.0`;
  Research 059 qualified ACP bounded writes at `0.51.0`; Research 159 recorded
  Gemini as deferred while host and official stable moved. The operator has now
  reopened the family with enterprise/API-key access as the only useful path.
- **Why these cards are ready:** Contract 029 defines the identity-before-claim
  loop, the current host and official stable are corroborated, the selected
  protocol subsets and historical corpora already exist, and the stop outcome
  is explicit.
- **Decisions and preferences:** qualify the current official stable or return
  for removal; do not preserve a half-current family; do not infer support from
  account login; do not start route-feature work in this PR.
- **Open tensions:** official `0.56.0` may have changed selected ACP auth or
  headless framing despite stable package identity. Treat unmapped additions as
  unmapped. A new flag is not a reason to widen scope. Stop if deterministic
  evidence cannot settle mapped behavior.
- **Report after:** card 093 identity/segment decision, then card 094 claim and
  PR. Report sooner on any stop condition.
- **Report to:** the operator, who will relay progress to the orchestrator

## Suggested Next Move

This handoff explicitly activates worker mode. Read it from the top. Before
broad repository reads, run the startup worktree-safety preflight in
`## Completion Protocol`. Accept a clean launcher-provided non-`main` worktree
as authoritative and do not create a second one.

Once the worktree is safe, read `AGENTS.md`, g04.034, cards 093-094, Contract
029, the version-currentness skill and reference, Research 045/059/159, Gemini
`selection.rs`, both frozen corpus directories, and the prepared guide. Start
with card 093. Re-probe official stable before downloading anything. Continue
to card 094 only when neither axis is a stop. Open one PR and stop.

## Completion Protocol

### Before you start

1. Read this handoff path. Its `worker_mode: implementation` and
   `dispatch_authority: orchestrator` metadata activate worker mode. Then run
   one read-only probe before broad reads: `git rev-parse --show-toplevel`,
   `git branch --show-current`, `git status --porcelain`, and
   `git worktree list --porcelain`.
2. If the current root is a registered worktree, its status is empty, and its
   branch is not `main`, accept it as the launcher-provided worktree. Record its
   actual root/branch and do not compare them with the placeholders or create
   another worktree because they differ.
3. Only if the current context is `main`, dirty, unregistered, or unusable
   should you inspect the named worktree. If that cannot be used, read
   `.agents.local.env` and require `AGENTS_WORKTREE_CONTAINER_DIR`; ask the
   operator if it is absent. Create a unique worktree and branch under that
   container from `origin/main`. Never use `/tmp`, `TMPDIR`, or a guessed path;
   never clean, reset, stash over, or discard the original checkout. If the
   launcher supplied a dirty or `main` worktree, stop and report it instead of
   silently creating a second worktree.
4. From the selected worktree, run `git fetch origin`, confirm `HEAD` equals
   `origin/main`, confirm
   `git merge-base --is-ancestor eb3445284431c975c41ea61c7570ee9e9f9f44b3 HEAD`
   succeeds, and confirm this handoff file exists in `HEAD`.
5. Read the milestone, cards, `AGENTS.md`, canonical refs, and selected Gemini
   source/corpora named above.
6. Run `effigy tasks`, `effigy doctor`, and `effigy test --plan`. Record what
   actually ran and distinguish inherited findings from new ones.

### While you work

- Execute card 093, then card 094. Keep commits aligned with identity and claim
  chunks.
- Use official npm/GitHub sources and `/tmp` artifacts. Freeze only secret-free
  evidence in the repository.
- Compare selected mapped subsets; do not map additions merely because they
  appear in full help or changelog output.
- After card 093, report exact identity, segment decision per axis, fixture and
  research paths, and validation. Stop if either axis is a stop.
- Stop on a moved official point, uncorroborated artifact, required provider
  work, authentication dependence, public lifecycle change, new public
  operation, contract gap, or scope expansion.

### When the assigned runway is complete

1. Run every final gate named by card 094 plus any card 093 gate not rerun on
   the final tree.
2. Update cards 093-094, g04.034, Research 182, identity/claim logs, research
   and log indexes, standing lane, roadmap front doors, architecture, Gemini
   guide, route/feature matrices, changelog, and the sole Next Task honestly.
   The Next Task should compile the first numbered per-route feature milestone;
   do not implement it.
3. Push the selected worker branch.
4. Open a reviewable PR against current pushed `main`. The planning base above
   predates the handoff commit and is not self-referential.
5. In the PR body, link Contract 029, Research 045/059/159/182, g04.034, cards
   093-094, changed surfaces, exact artifacts/digests, segment decisions,
   validation, and unresolved items.
6. Report the PR URL, exact head commit, identity/claim evidence, and checks to
   the operator. Do not merge.

### Review and merge path

The orchestrator will review the PR against the canonical refs, diff, evidence,
and checks. Current review state: awaiting worker PR.

The orchestrator and worker share the GitHub identity `betterthanclay`, so the
orchestrator records its verdict as a PR comment rather than formal approval.
Requested changes are: none yet. The operator must explicitly authorise merge.

- **Closeout refs:** Research 182; cards 093-094; g04.034;
  `docs/roadmaps/README.md`; `docs/roadmaps/g04/README.md`;
  `docs/roadmaps/standing-lanes.md`; `docs/logs/README.md`; Gemini current
  architecture, guide, route matrix, feature matrix, and changelog surfaces

### Handoff closeout

Leave claims unchanged on a stop. Otherwise leave the identity, claim, cards,
milestone, logs, indexes, and Next Task honest. Do not make the family look
qualified before the PR is reviewed and merged.
