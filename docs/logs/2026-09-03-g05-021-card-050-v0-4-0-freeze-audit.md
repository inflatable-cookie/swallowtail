# 2026-09-03 g05.021 Card 050 v0.4.0 Freeze Audit

Status: completed; accepted via PR 198 exact head `2f4923b8`, merged as `835fe9ff`
Card: g05.021 Card 050
Audited head: `b7f804b5940d666d81f7b43fb562be5797c59575`

## Result

Card 050 independently audited the fresh canonical `main` head against the
annotated `v0.3.3` tag. The audit freezes the exact package, dependency, route,
semantic API, and guaranteed-behavior evidence needed by Card 051. The full
machine-readable evidence is in [Research 281](../research/281-v0-4-0-compatibility-and-freeze-audit.md)
and its linked ledgers.

The source remains a coordinated pre-1.0 `0.4.0` candidate input under
Contract 036. The two required breaking surfaces are the OpenAI Background
`minimal` removal and the caller-bounded interactive close signature/cleanup
boundary. The audit also records the Kimi ACP range/isolation restriction, the
Kimi headless v1/v2 identity correction, additive reserved-reap/tree-evidence
surfaces, and both current integration-only sidecar routes.

## Frozen census

- local `HEAD` and `origin/main`: exact `b7f804b5…`;
- `v0.3.3` tag object: `ca30b367…`; peel and merge base:
  `51d18620…`;
- 826 commits and 2,694 changed files in the full range;
- 40 packages at both endpoints;
- immutable release routes remain 47;
- current integration/route and lifecycle rows are 49, with only
  `pi.sdk-sidecar` and `claude-agent.sdk` added beyond the immutable release
  inventory; the route ledger has 47 historical `yes` rows and exactly those
  two `no` rows; Card 051's 49-route candidate boundary includes both, and Pi's
  exact release-note, consumer, and rollback treatment is fixed audit input;
- 14 package APIs are unchanged from `v0.3.3`; 26 have additive lines; the
  only semantic removal is the approved zero-argument `InteractiveSessionHandle::close`;
- 20 route-local rows are unchanged, 29 route rows changed or were added, and
  17 lifecycle rows changed or were added; the shared close break is marked on
  all 25 current interactive rows.

No open mergeable feature/currentness PR was present. Research 276's freeze,
parked Kimi `0.40.1`, watcher `2.1.251` stop, Gemini deferral, Kimi ACP cap,
and 249/518 Contract 061 projection boundary remain unchanged.

Card 051 does not choose whether Pi is included: its existing 49-route
candidate requirement fixes both current-only routes as candidate additions.
The current changelog lacks a dedicated Pi route-add entry, so Card 051 must
add the exact Pi `Added` wording, consumer provisioning, and v0.3.3 rollback
treatment recorded in Research 281 before promotion.

## Safety and scope

The Contract 036 API toolchain was regenerated into temporary output with
`cargo-public-api 0.52.0` and `nightly-2026-08-05`. All 40 current outputs
matched either immutable `v0.3.3` or current unreleased evidence. Historical
baselines are byte-for-byte unchanged; no release preparation, version or
requirement mutation, changelog promotion, candidate baseline, tag, provider
call, consumer-repository mutation, application smoke, or papercut repair was
performed. The repair adds only a deterministic assertion to the existing
route QA gate; it changes no runtime behavior.

The authenticated working-application smoke remains an operator question for
Card 052: supply repository/application, exact route, command, exact candidate
SHA/tag consumption, credential/provider authority, permitted mutations,
evidence/redaction, cleanup authority/evidence, and retry budget.

## Validation

The exact listed Card050 tier passed on this tree:

- `effigy package:metadata` — 40 crates at source version `0.3.3`, Rust
  `1.95`;
- `effigy package:api` — 40 packages, approved removals only;
- `effigy qa:routes` — 87 activity operations, 49 routes, and the exact
  47-historical/2-current-only route-ledger membership assertion;
- roadmap indexes, batch-card index, roadmap status, next-action, links,
  roadmap numbers, and `effigy qa:northstar` — passed;
- `git diff --check` — passed.

Card 050 lists 12 commands even though its prose says 11; all 12 listed
commands ran. No candidate release gate, workspace test suite, provider call,
or application smoke ran. The API generation emitted one existing Ollama
rustdoc unresolved-link warning; it did not fail generation or validation.

## Acceptance

The independent exact-head review accepted the audit and freeze census at PR 198
exact head `2f4923b87be91a9b18be88ded5cbfae760709f30`, merged as
`835fe9ff239f4d33c86276af50f229977f0a429c` on canonical `main`. Card 050 is
completed and accepted. Card 051 is ready as the sole Next Task and must stop
before the single mutating `effigy release prepare --yes` for separate explicit
operator authorization. Card 052 remains planned, blocked on completed card 051
plus the complete authenticated application-smoke authority packet.
