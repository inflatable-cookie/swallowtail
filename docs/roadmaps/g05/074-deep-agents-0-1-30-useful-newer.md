# g05.074 Deep Agents 0.1.30 Useful Newer

Owner: Tom
Created: 2026-09-14
Depends on: Contract 029; Research 153, 157, 206, 308, and 321; g05.073
Vision tags: route currentness, Deep Agents, ACP, exact pin

## Outcome

Reopen the exact `deepagents-acp.package` pin and qualify official npm
`deepagents-acp@0.1.30` for `deepagents.acp` only if a complete provider-free
package-tree, ACP-wire, authority, failure, and lifecycle comparison admits it.
Preserve `QualifiedOnly` and one exact maintained point. Do not infer a range
across the five published successors after `0.1.25`.

Planning rechecked the official npm channel on 2026-09-14. `latest` is
`0.1.30`, published `2026-09-09T17:34:53.193Z`, with tarball shasum
`38b3b2eb663d905ce7628bd8f02702276b948520` and integrity
`sha512-moNlTkJN7JyowNXETdEaGJenpFtZ45xUO+oJYUYBOuebi4FuB0ChGhq7jblPY/JdnFbtX44Waiqotq/3O1fM7A==`.
The stable successor set is `0.1.26..=0.1.30`. The ACP registry remains stale
at discovery-only `0.1.7`, and `deepagents-acp` remains absent from this host.

## Ready-State Rubric

- [x] Tom authorized the complete Research 308 currentness campaign, including
      reopening exact-pin families.
- [x] g05.073 completed and advanced Mistral Vibe to exact `2.25.4`; its
      lifecycle closeout is on canonical `main`.
- [x] Official npm `0.1.30`, all five stable successors, stale ACP-registry
      metadata, and absent host CLI are identified separately.
- [x] Research 157 fixes selected ACP identity and lifecycle; Research 206
      keeps explicit model selection outside the deliver-now set.
- [x] No prompt, package installation, host-key inspection, or provider credit
      is needed for the identity and claim decision.

## Work

1. Re-probe npm latest and time metadata, exact manifests, tarball URLs,
   shasums, integrity values, repository identity, ACP-registry discovery
   metadata, and installed `--version` availability. Retrieve exact official
   tarballs for `0.1.25..=0.1.30` into `/tmp`, verify every digest, and freeze
   Research 322. Do not install or execute a downloaded package.
2. Build a deterministic per-hop full-tree and mapped-module ledger. Classify
   every changed input feeding the selected `deepagents-acp` no-extra-argv
   child: package bin and dependency versions, CLI parsing/defaults, ACP
   initialize and `agentInfo`, `session/new`, `session/prompt`, updates and
   content, stop reasons, request correlation, cancel, permission callback
   options and failure fallback, host-owned auth/config, working-resource and
   local-filesystem fallback, process/task/deadline ownership, cleanup, and
   in-process session persistence. Treat changelogs as discovery only and
   commit identity before production claim edits.
3. Keep `npx`, library `startServer` / `DeepAgentsServer`, custom `tsx`, Python
   Deep Agents, explicit `--name` / `--model` / `--workspace` / `--skills` /
   `--memory` / `--debug` / `--log-file`, `session/load`, modes, slash
   commands, HITL `interruptOn`, images/audio/resources, client MCP, usage, and
   persistent permissions unmapped unless exact selected-source evidence makes
   them unavoidable. Do not turn registry advertisement or a dependency
   capability into selected route behavior. Research 206's empty model-
   selection set remains until its pre-spawn agreement, fail-closed invalid,
   and confirmation gates are independently met.
4. If the selected contract is unchanged or mechanically adaptable without a
   new public lifecycle, authority, or operation, rebind the one exact claim
   point to `0.1.30`. Keep `deepagents.acp.stdio-v1` only when mapped wire and
   lifecycle are unchanged. Do not retain `0.1.25` as a second exact point or
   infer a semver range.
5. Stop with the smallest exact counterexample if a mapped wire, lifecycle,
   authentication, permission, failure, working-resource, process-authority,
   or session-persistence boundary changed materially, or base compatibility
   needs a live provider session. Recheck npm latest immediately before
   identity commit and push. Provider-free retrieval, parsing, classification,
   correction, and validation may rerun normally.

## Dispatch Manifest

| Field | Task 074 |
| --- | --- |
| Readiness | ready |
| Prerequisites | Contract 029; Research 153, 157, 206, 308, and 321; completed g05.073; clean pushed `main`; official npm package metadata/tarballs; ACP registry as discovery only |
| Completion conditions | baseline plus all five successors are frozen and classified; stale registry identity stays explicit; identity precedes claim; one honest exact-`0.1.30` decision lands; QualifiedOnly, selected ACP authority, and Research 206 boundaries survive; exact-head review and named gates pass |
| Owned mutable paths | Deep Agents selection, command, prepared route, ACP driver/decoder, tests, and fixtures under `crates/swallowtail-adapter-deepagents/**`; Research 322 and one research-index line; exact Deep Agents prepared-guide, route/activity/feature matrices, architecture ceiling, standing-lane, `[Unreleased]`, identity-log, claim-log, and task-result lines; `PAPERCUTS.md` append only |
| Reserved shared closeout surfaces | lifecycle task record and generated projections; submitted-handoff deletion belongs to the repository hook |
| Forbidden paths | other adapters; contracts; historical research/captures/releases; live provider probes; package installation/execution; model-selection delivery; release/tag/workflow files |
| Approved concurrent siblings | none on shared currentness surfaces; this campaign is serial |
| Worker capability class | evidence-first Rust currentness worker; exact npm tarball ledger and ACP boundary reconciliation; no provider credentials |
| Acceptance evidence | npm baseline plus five successor identities and complete trees; ACP-registry stale identity; per-hop mapped-module ledger; mutation-sensitive identity/wire/permission/failure/resource/lifecycle fixtures; current docs/claim agreement |
| Review oracle | skipped package hop, inferred range, unclassified mapped delta, changelog-only inference, registry/package flattening, `agentInfo.version`/npm conflation, model-selection or capability widening, lost auth/resource/permission/cleanup boundary, provider reliance, or downloaded-package execution |
| Oracle gate | not required — Contract 029 and Research 157/206 settle this provider-free exact-pin comparison and its falsifiable stop conditions; no new auth, permission, secret, or execution policy is delegated |
| Stop conditions | official-package identity disagreement; selected public lifecycle/authority/authentication/permission/failure/resource/persistence change; new driver/facade required; provider/live evidence required for base compatibility |
| Escalation owner | operator via Chatterbox for new policy; Queue coordinator for mechanical blockers |

## Boundaries

One route family and one exact-pin decision. No prompt, inference, package
installation or execution, `npx`, login, host-key inspection, credential use,
live ACP/provider run, host mutation, release, tag, publication, or consumer
work. Do not infer compatibility from adjacent versions, semver, changelogs,
ACP registry metadata, dependency capabilities, or advertised operations.

## Validation

- `cargo fmt -p swallowtail-adapter-deepagents -- --check`
- `effigy validate:focused swallowtail-adapter-deepagents`
- `effigy package:verify-affected swallowtail-adapter-deepagents`
- `effigy check:examples`
- `effigy package:api`
- `effigy qa:routes`
- `effigy qa:northstar`
- focused Deep Agents identity, package-tree, selection, command, initialize,
  session, permission, failure, activity, deadline, and cleanup selectors
- relevant research/log/roadmap/number/lifecycle/next-action checks
- `git diff --check`

## Acceptance

- [ ] official npm identities reproduce for baseline and every successor
- [ ] every changed selected input is classified
- [ ] identity evidence precedes the claim decision
- [ ] exact QualifiedOnly shape and route boundaries remain truthful
- [ ] stale registry metadata and advertised siblings stay independently gated
- [ ] official npm latest is rechecked at both boundaries

## Next Task

Return the exact outcome to Chatterbox, then compile and dispatch Antigravity
as the next Research 308 family without asking Tom to repeat authorization.
