# 2026-08-26 Pi RPC 0.84.3 Claim

## Result

Card 191 raised `pi.package` through exact official `0.84.3`
(`0.84.0..=0.84.3`) as a compatible extension of
`pi.rpc.strict-lf-v0.84.0-message-update-delta`. No new milestone.
Unpublished `0.83.1` stays incompatible. Later stables stay
AllowUnverified; the synthetic later point is now `0.84.4`. Unused
`toolcall_start` extras, `--`, `powershell`, bundled bin path, and
streaming `usage` stay unmapped. Oh My Pi and the SDK sidecar stay
separate.

g04.068 is standing currentness, completed. Next Task pointer and g04
generation status were left alone.

## Validation

- `cargo fmt -p swallowtail-adapter-pi` passed
- `scripts/validate-focused-packages.sh swallowtail-adapter-pi` passed (90 tests, clippy `-D warnings`)
- `scripts/verify-affected-packages.sh swallowtail-adapter-pi` passed

No `effigy` binary on this host; the named scripts are the task equivalents.

## Next

Resume the generation's actual Next Task. This family is done.
