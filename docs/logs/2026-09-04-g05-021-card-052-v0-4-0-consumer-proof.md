# g05.021 Card 052 v0.4.0 Consumer Proof

Date: 2026-09-04
Status: complete; tag authority granted and executed 2026-09-04, see [the annotated tag record](./2026-09-04-v0-4-0-annotated-tag.md)

## Candidate

- Exact source: `56f3913ac99af44b6ff45384cfc53a0adea587ba`
- Packages: 40
- Candidate routes: 49
- Immutable `v0.3.3` routes: 47
- Added routes: `pi.sdk-sidecar`, `claude-agent.sdk`
- Local gates: all 11 passed before candidate freeze
- Canonical exact-SHA CI: run 33853812785, all six jobs passed
- Local and remote `v0.4.0` tags: absent

Research 281 holds the complete compatibility, API, route, behavior, and
immutable-baseline evidence. The candidate remains in canonical `main`; later
planning and closeout commits do not replace its identity.

## Source Consumer

`effigy package:source-consumer` passed from a clean detached checkout of the
candidate. The temporary Cargo consumer resolved its selected Swallowtail
packages at the exact revision and exercised the normal prepared Codex path.
The temporary worktree was removed.

## Authenticated Application Smoke

Nucleus PR 6 exact head `0fa06e4b38c25d6251704bd71da2d3352ec8367a`
was independently accepted and merged as
`e8de352c3b39a28ec97b425eeed674ecacccac1f`. Its five selected Swallowtail
dependencies resolve to candidate `56f3913a`, with no patch override.

The operator authorized one normal Nucleus Agent Chat local Codex smoke through
`codex.app-server` and protocol facade `codex-app-server-v2`, using the
provider-managed ChatGPT login and no API-key environment variable. Exactly
this ignored test ran:

```text
local_codex_chat::tests::live_smoke_tests::live_chat_receives_active_task_context_without_polluting_history
```

Attempt one passed; the authorized retry was unused. One test passed and 2,158
were filtered out. The provider completed one benign turn. No tool call,
clarifying question, cancellation, or timeout occurred. The active-task title
reached the provider, while stored operator history remained verbatim and
contained no injected context marker.

Cleanup was clean. The test-owned temporary directory was removed, no run-owned
process remained, and the Nucleus checkout stayed clean. The only mutation was
shared build cache. This record retains no prompt text, response content,
credential, account data, private payload, or host path.

## Exact Tag Decision

Card 052 and milestone g05.021 are complete. Review or merge of this record does
not authorize a release mutation. The requested decision is whether to
authorize both local annotated-tag creation and tag push with:

- source commit: `56f3913ac99af44b6ff45384cfc53a0adea587ba`
- canonical branch: `main`
- remote: `git@github.com:inflatable-cookie/swallowtail.git`
- tag: `v0.4.0`
- annotated message:

```text
Swallowtail v0.4.0

Coordinated pre-1.0 source minor over v0.3.3: 40 packages and 49 production
routes, including pi.sdk-sidecar and claude-agent.sdk. Canonical exact-SHA CI
run 33853812785 and the Nucleus normal authenticated Agent Chat consumer proof
passed against 56f3913ac99af44b6ff45384cfc53a0adea587ba.
```

The source-only release contains no crates.io publication, GitHub Release
object, binary, sidecar, installer, or model artifact.
