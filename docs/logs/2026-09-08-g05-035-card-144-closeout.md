# 2026-09-08 g05.035 Card 144 Closeout

Card 144 turned Card 132's opaque Claude SDK registered-tool
`open_rejected` into a bounded failed-open receipt. The additive prepared
route reports the underlying route code, observation-derived open stage,
bounded sidecar subcode, provider-readiness truth, and three-way cleanup
disposition. Provider-free fixtures cover construction, initialization,
account and MCP readiness, readiness validation, deadline, joined cleanup,
unconfirmed cleanup, and the exact Card 132 registered-open request against
the frozen `0.3.259` sidecar.

The deterministic reproduction found no producer defect: the request matches
the frozen Card 132 tuple and the live construction-class cause remains
unresolved and live-only. Both Contract 061 registered-tool cells remain
unqualified and owned by the producer gap. No live retry, qualification,
availability, release, or broader acceptance follows.

PR 294 was independently accepted at exact head
`67e69e577518fdf0486998d47720e045a541bd7e`; review comment `5588499602`
resolved the two round-one receipt findings and found no blocking issue. It
merged into `main` as
`cc53c81ad903a562830d721b39a669aee010d037`.

Named validation passed: formatting, focused validation (477 tests),
affected-package verification, mediated-stdio validation (15/15), semantic
API, route QA, docs QA, Northstar QA, and `git diff --check`. Hosted PR checks
passed except for the configured skipped Pinned MSRV floor tests job. The
pre-existing environment-sensitive
`wrapper_death_preserves_partial_capture_journal` flake was reproduced on the
clean base and remains recorded in `PAPERCUTS.md`; the semantic API check's
redundant-link rustdoc warning remains non-blocking. No named Card 144
acceptance failure was deferred.

The active Next Task pointer remains unchanged; no new planning direction was
introduced.
