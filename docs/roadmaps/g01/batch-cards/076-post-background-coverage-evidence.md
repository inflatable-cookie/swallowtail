# 076 Post-Background Coverage Evidence

Status: completed
Owner: Tom
Updated: 2026-07-21
Milestone: `../024-post-background-coverage-checkpoint.md`

## Objective

Select the next high-information Swallowtail proof from current authoritative
evidence after the OpenAI background Responses tranche.

## Scope

- inventory uncovered lifecycle, access, protocol, catalogue, topology, and
  conformance behavior against twelve realized production drivers
- recheck current official provider or maintained-project documentation,
  releases, access mechanisms, endpoint audiences, model catalogues, protocol
  authority, lifecycle, and topology
- compare cloud harnesses, provider-native harness SDKs, remote ACP, direct
  hosted APIs, and attached local runtimes only where an exact supported or
  maintainer-owned surface exists
- include Cursor Cloud Agents, Claude Code surfaces, current Qwen and Pi
  routes, Kimi and GLM direct access, DeepSeek and Alibaba/Qwen APIs, ACP
  transports, Ollama, vLLM, and SGLang where they expose unresolved shapes
- treat `AmbientHost`, `ProviderEnforced`, and `HostEnforced` as independent
  authority profiles; avoid a container or outer sandbox prerequisite
- rank candidates by new architectural information, deterministic fixture
  quality, support authority, seamless operation, and implementation cost
- promote one research delta and recommend one bounded proof or prerequisite

## Out Of Scope

- provider implementation
- live authentication, repository mutation, subscription mutation, remote
  agent deletion, or paid inference
- implicit provider, credential, endpoint, billing, deployment, isolation, or
  consumer-routing preference
- repeating an existing JSONL, HTTP/SSE, SDK, ACP, WebSocket, or local-runtime
  shape solely to add a provider name

## Acceptance Criteria

- [x] current primary or maintained-project sources support every live claim
- [x] realized coverage and remaining gaps are compared concretely
- [x] authentication, entitlement, metering, endpoint, support, model,
      repository, lifecycle, isolation, and topology remain separate
- [x] the recommendation names the new contract and conformance pressure
- [x] missing shared contracts and deterministic fixture seams are explicit
- [x] no policy-significant tie is hidden; unresolved product authority returns
      to the operator before implementation

## Evidence

- Research 016 rechecks current official or maintained-project evidence across
  cloud harnesses, local harnesses, ACP, direct APIs, and attached runtimes.
- Claude Managed Agents ranks first because it adds a provider-hosted harness,
  versioned agent configuration, owned remote environment and session,
  durable retention, provider rescheduling, persisted event recovery, callback
  waits, and deletion truth.
- The bounded proof uses no repository, files, provider built-ins, external
  sandbox network, MCP, skills, memory, GitHub, or local container.
- Contract 022 promotes the ownership, retention, recovery, event-authority,
  callback, cancellation, deletion, metering, and cleanup boundary.
- Roadmap 025 and cards 077-079 compile the records-and-fixtures, production-
  driver, and conformance runway.
- Cursor remains policy-bound because its useful route couples repository,
  GitHub, provider-VM mutation, artifact, and durable-agent authority.

## Validation

- authority-map and contract consistency review
- `effigy qa:docs`
- `git diff --check`

## Stop Conditions

- evidence depends on private credentials, unsupported OAuth reuse, an
  unmaintained protocol, an unowned serving lifecycle, or mandatory heavy
  containment absent from the provider route
- the leading choice would establish consumer repository, remote mutation,
  durable deletion, credential, routing, or deployment policy

## Auto-Continuation

No. Card 077 is the sole ready continuation.
