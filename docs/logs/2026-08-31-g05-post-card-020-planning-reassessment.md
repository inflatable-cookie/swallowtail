# 2026-08-31 g05 Post-Card-020 Planning Reassessment

Status: complete; strict-paused
Owner: Tom
Generation: g05

## Result

g05 has no ready milestone or batch card after card 020's merged Linux live
evidence stop. Do not reopen the Claude watcher proof, reuse either consumed
turn, merge PR 127, dispatch Darwin, or infer a watcher claim from `JoinedZero`.
Research 256's empty Qoder roster also leaves cards 005-006 planned behind a
positive-evidence gate.

The clearest next planning candidate is the open consumer route-feature and
control projection note. Its reviewed 767-row census is complete enough for a
contract discussion. It is not runtime authority and is not silently promoted
by this reassessment.

## Authority-Resolved Defaults

Existing authority settles these boundaries without another operator choice:

- Contract 047 remains an immutable selection snapshot. Source-evidence,
  readiness, catalogue, preparation, or negotiated-session changes produce a
  replacement projection rather than in-place truth.
- Every projected fact stays bound to the exact configured instance and
  revision, route, model when applicable, operation shape, and evidence source.
- Selection summary, session-start controls, and active-session observations
  stay distinct. A catalogue or post-open option list does not authorize
  mutation.
- Consumer display copy, localization, grouping, persistence, routing, and
  composer policy remain downstream. Swallowtail may expose only bounded
  semantic identity and safe evidence-backed diagnostics.
- Contracts 037, 047, and 057 keep their current preparation, selection, and
  pre-session lifecycle authority. A new surface must compose them rather than
  absorb or weaken them.

## Operator Decisions Still Required

One small promotion decision can change the plan materially:

1. Should the projection census become g05's next provisional spec and
   contract-discussion lane?
2. If yes, should the durable boundary be one dedicated composing contract
   (recommended), or narrowly scoped amendments to Contracts 037, 047, and
   057?
3. Should the first contract define a closed portable availability-reason
   taxonomy, or retain existing source dimensions plus bounded safe reasons
   until cross-route evidence proves a closed taxonomy?

Recommendation: open a provisional spec, target one composing contract, and
defer a closed availability-reason enum. The census proves cohesive projection
need and exact lifecycle distinctions, but it does not prove one exhaustive
cross-route reason vocabulary.

## Current State

- posture: `strict-paused`
- authority mode: orchestrator planning
- active generation: g05, seven numbered roadmaps
- ready cards: none
- active spec: none
- worker handoff: none
- provider authorization: none

No g05.008 roadmap, batch card, contract, architecture claim, or worker handoff
is created before the promotion decision.

## Validation

- `effigy tasks` and `effigy test --plan` resolved normally
- `effigy doctor` reported the current 391 god-file findings (342 warnings / 49
  errors) plus one generated-in-source warning; this planning pass did not widen
  that scope

## Next

Settle the projection promotion decision and the two remaining contract forks.
Only then compile a provisional spec or roadmap.
