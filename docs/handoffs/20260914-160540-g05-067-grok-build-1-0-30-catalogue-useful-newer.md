---
title: g05.067 Grok Build 1.0.30 catalogue useful-newer qualification
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
status: ready-to-launch
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Tom instructed Chatterbox to action the full Research 308 currentness campaign, including Kimi and Oh My Pi, and continue without repeated approval; g05.065 is complete and the Grok Build catalogue is next."
roadmap: docs/roadmaps/g05/067-grok-build-1-0-30-catalogue-useful-newer.md
queue:
  capability: general
  skipPRReview: false
  notifyOriginOnCloseout: true
---

# g05.067 Grok Build 1.0.30 Catalogue Useful Newer

## What This Thread Was Doing

Chatterbox closed g05.065 and compiled the eighth Research 308 family. The
separately prepared Grok catalogue is exact-qualified at `1.0.25`, while both
official npm stable and the installed executable are `1.0.30`.

## Why It Matters

The current exact claim rejects the installed stable catalogue even though the
ACP route is already qualified through `1.0.30`. ACP compatibility cannot be
transferred: the catalogue needs its own prompt-free command, grammar,
membership, auth, lifecycle, and redaction evidence.

## Current State

- Canonical `main` is g05.065 closeout
  `9680409804e9072ebfec9feb275383a0ae2288b0`.
- PR #328 merged accepted head
  `a1e2dc8fceff882f9144cce04ab44db3d145b6e2` as
  `0c4f86edeba7915b607e7c62355d380e3f943221`; review comment
  `5665982546`.
- npm `latest=1.0.30`; `1.0.31` is alpha only. Installed is
  `grok 1.0.30 (04b7ffed98c6) [stable]`.
- Research 314 freezes official package/platform identity for every stable
  `1.0.26..=1.0.30` and proves the installed `1.0.30` executable equals the
  official Darwin arm64 payload.
- g05.053 admits exact `1.0.25` catalogue as authenticated non-inference
  metadata. Its final listing returned ordered `grok-4.6` default then
  `grok-4.5` with no prompt, model session, inference, tool, update, retry, or
  survivor.
- Provider suppression is not an acceptance requirement. Auth refresh or
  bounded catalogue metadata traffic is allowed.

## Boundaries

One family and one exact catalogue point. No model prompt, inference, model
session, tool dispatch, install, host update, downloaded-artifact execution,
release, tag, publication, or consumer mutation. Authenticated metadata is
allowed. Zero credit is not a blocker unless the catalogue command returns a
typed billing failure. Provider-free work and justified non-inference
observation reruns are not artificially one-shot.

## Important Context

Read Contract 029, Contracts 020 and 047, Research 306, 308, 314, g05.053, and
g05.067 before editing. Use the repository `version-currentness` skill and
Effigy selectors. Recheck official stable at both boundaries. Reproduce the
relevant Research 314 identities and classify the catalogue-specific path at
every hop from `1.0.25` to `1.0.30` before changing the claim.

Preserve exact `--no-auto-update models`, bounded output, shipped bullet
grammar, redaction, and joined cleanup. Run provider-free gates first, then the
installed exact `1.0.30` prompt-free catalogue. Persist a redacted capsule
before assertions and record every attempt. Live listing owns membership,
order, and default; embedded metadata supplements matching IDs only. Do not
hard-code models or infer from ACP.

## Suggested Next Move

Freeze Research 316 and the catalogue-specific all-hop ledger. Commit identity.
Run the provider-free gate, then observe the installed prompt-free catalogue,
make the smallest exact-claim update supported by the result, validate, push,
open a PR, and hand its exact head to independent review.

## Completion Protocol

The Queue owns worker launch, independent review, merge, lifecycle closeout,
and handoff deletion. Return the exact stable and installed identities,
catalogue claim outcome, capsule path/digest and ordered IDs/default, command
and non-inference verdict, validation, review marker/comment, merge SHA, and
closeout SHA. Next is Command Code. No release or tag follows.
