# Deep Agents ACP 0.1.25 identity corpus

Secret-free source identity for `deepagents.acp` before any Swallowtail
package or claim exists.

Official npm `deepagents-acp@0.1.25` plus ACP docs freeze the selected
stdio wire: host-approved `deepagents-acp` with no extra argv, initialize,
`session/new`, one bounded `session/prompt` with field `prompt`, cancel,
and joined cleanup.

No live ACP initialize. No provider prompt. No install. No host
`deepagents-acp`. Library embed, custom `tsx`, `npx`, and advertised
`session/load` are not this corpus.

No fixture contains a credential, host path, account identity, provider
payload, or real session id.
