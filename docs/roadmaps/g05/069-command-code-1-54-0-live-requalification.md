# g05.069 Command Code 1.54.0 Live Requalification

Status: planned; this stub names the follow-up only and claims no execution authority
Owner: Tom
Created: 2026-09-14
Depends on: Contract 029; Contract 043; Research 116, 118, and 317; g05.068
Vision tags: route currentness, Command Code, live evidence, exact pin

## Outcome

Run one separately authorized live requalification gate for the exact
`command-code.headless` `1.54.0` point. Reproduce the existing accepted
`1.15.1` evidence shape for authenticated structured completion, tool
lifecycle, usage, credit failure, and the Contract 043 two-turn private
exact-id continuation. On acceptance, reconcile the corresponding live
feature and activity cells to the exact tuple; on a typed stop, keep the
current version-bound gate and record the reason. This task does not alter the
provider-free currentness qualification in g05.068.

## Ready-State Rubric

- [x] g05.068 names the exact `1.54.0` point and keeps Research 116 and 118
      bound to `1.15.1`.
- [ ] Chatterbox compiles the live gate and the queue dispatches it with
      explicit provider-operation authority.
- [ ] The gate's exact executable, account, model, prompt, and cleanup
      evidence are fixed before any provider operation.

## Work

1. Run only the separately authorized exact-`1.54.0` live gate; no broad
   provider exploration, install, login, update, or consumer mutation.
2. Compare the result with Research 116 and 118 without rewriting those
   immutable records. Move feature/activity cells only on matching accepted
   evidence; otherwise preserve their version-bound gate and record a typed
   stop.
3. Keep `QualifiedOnly`, the exact version tuple, private exact-id resume, and
   all route authority boundaries unchanged.

## Boundaries

One exact Command Code version and one authorized live gate. No range, release,
tag, publication, downloaded-artifact execution outside the gate, public
catalogue/import/export/resume operation, or unrelated provider work.

## Validation

Focused Command Code tests, route and activity/feature matrix QA, and the
named docs gates. The live gate supplies its own exact provider evidence.

## Acceptance

- [ ] the exact-`1.54.0` live gate runs or records a typed stop
- [ ] Research 116/118 remain immutable and version-bound
- [ ] feature and activity cells agree with the accepted live outcome

## Result

Planned; not started.

## Next Task

Chatterbox compiles and dispatches this separately authorized live gate after
g05.068 closeout.
