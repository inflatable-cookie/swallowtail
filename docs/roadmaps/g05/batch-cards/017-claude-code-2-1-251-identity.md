# 017 Claude Code 2.1.251 Identity

Status: ready
Owner: Tom
Milestone: [g05.005 Claude Code 2.1.251 Useful Newer](../005-claude-code-2-1-251-useful-newer.md)
Created: 2026-08-30

## Task

Freeze official npm `@anthropic-ai/claude-code` `2.1.251` identity and selected
mapped-protocol evidence for the headless and response-only axes. Name the
Contract 029 segment shape. Do not edit production claims in this card.

## Method

1. Re-probe npm `latest` and the installed host identity at card start. Stop if
   the official point moved or the exact artifact cannot be corroborated.
2. Record published `2.1.242`, `2.1.243`, `2.1.245`, `2.1.246`, `2.1.247`,
   `2.1.248`, `2.1.250`, and `2.1.251`; keep unpublished `2.1.244` and
   `2.1.249` as gaps.
3. Extract official wrapper and native artifacts under `/tmp`. Freeze host and
   official digests without installing or changing the host.
4. Compare the selected mapped headless and response-only flags, stream shape,
   terminal behavior, and decoder assumptions against Research 202 and the
   retained corpora. Treat watcher candidate flags and unrelated additions as
   unmapped.
5. Write a secret-free `claude-code-2.1.251` identity/protocol fixture and
   Research 261. Keep historical decoder specimens.
6. Name compatible-extension, private-milestone, new-driver-or-facade, or stop.

No prompt, login, live session, install, host update, or watcher call.

## Expected Shape

Compatible-extension if the selected mapped stream-JSON subsets are unchanged.
Keep both existing claim ids and behavior revisions. Do not infer watcher
support or widen feature-specific exact version sets from base-route identity.

## Acceptance

- exact host and official `2.1.251` identities are frozen
- every published intermediate and both unpublished gaps are explicit
- selected mapped behavior is compared; unmapped additions stay unmapped
- Research 261 names the segment disposition
- production selection claims still end at `2.1.241`
- card 018 auto-continues only for compatible-extension or an already-governed
  private milestone

## Validation

- `effigy validate:focused swallowtail-adapter-claude-agent` at card 018
- `effigy qa:northstar`
- `git diff --check`

## Stop Conditions

- npm `latest` moves during the run
- official artifact identity cannot be corroborated
- selected mapped stream or lifecycle behavior differs from the retained
  revision
- evidence requires a provider prompt, login, install, or live session
- the change needs a new public operation or would flatten Claude Code onto
  Claude Agent ACP

## Auto-Continuation

Yes, to card 018 only after this card records an admitted segment shape.
