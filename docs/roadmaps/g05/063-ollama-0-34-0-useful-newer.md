# g05.063 Ollama 0.34.0 Useful Newer

Owner: Tom
Created: 2026-09-14
Depends on: Contract 029; qualified Ollama `0.32.15`; Research 174 and 308; g05.062
Vision tags: route currentness, Ollama, compatibility

## Outcome

Qualify current official Ollama `0.34.0` on the attached native-runtime route
if deterministic GitHub tag and selected-source evidence admits it. Preserve
the `0.14.0` baseline, exact `0.32.2` and `0.32.10` exclusions, claim identity,
behavior revision, `AllowUnverified`, attached-runtime ownership, and all
feature-specific evidence boundaries not independently proved at the new
points.

Planning rechecked official GitHub stable `v0.34.0`; the installed client
reports `0.33.3` and no server is running. Published non-prerelease releases
after `0.32.15` are `0.33.0`, `0.33.1`, `0.33.2`, `0.33.3`, and `0.34.0`.
The worker must recheck official latest and include every added stable.

## Ready-State Rubric

- [x] Tom authorized the complete Research 308 currentness campaign.
- [x] g05.062 completed and its lifecycle closeout is on canonical `main`.
- [x] Current claim, official channel, full planning-time hop set, and host
  observation are known.
- [x] No prompt, server start, login, installation, host update, or provider
  credit is needed.

## Work

1. Re-probe official GitHub releases and observe the installed client safely.
   Retrieve the tagged source for the previous ceiling and every published
   stable through current into `/tmp`. Freeze Research 313 and a secret-free
   exact tag/commit/tree and selected-source identity corpus without executing
   downloaded artifacts or contacting an Ollama runtime.
2. Classify every changed shipped file feeding `GET /api/version`, `GET
   /api/tags`, `GET /api/ps`, `POST /api/show`, and `POST /api/chat`, including
   request/response types, NDJSON streaming, errors, model capability/detail,
   structured output, reasoning and generation controls, session transcript,
   cancellation, lifecycle, and attached-runtime ownership. Commit identity
   evidence before production claim edits. Release notes are discovery only.
3. Record one Contract 029 segment decision. Raise the existing maintained
   segment only where the complete hop evidence admits it. Preserve the two
   historical exclusions and independently bounded feature evidence; do not
   flatten Ollama Cloud, generate, embeddings, tools, model management, server
   lifecycle, or llama.cpp onto `ollama.attached`.
4. A material public lifecycle, authority, authentication, or selected-wire
   change stops with the smallest exact counterexample and a concrete
   follow-up. Recheck official latest immediately before the identity commit
   and push. Provider-free diagnosis and reruns remain allowed normally.

## Dispatch Manifest

| Field | Task 063 |
| --- | --- |
| Readiness | ready |
| Prerequisites | Contract 029; Research 174 and 308; completed g05.062; clean pushed `main`; official Ollama GitHub releases/tags |
| Completion conditions | every published stable after `0.32.15` is frozen; identity precedes claim; one honest segment outcome lands; historical exclusions and route boundaries survive; exact-head review and named gates pass |
| Owned mutable paths | Ollama selection/tests/fixtures under `crates/swallowtail-adapter-ollama/**`; Research 313 and one research-index line; exact Ollama prepared-guide, route-matrix, feature-matrix, architecture ceiling, standing-lane, `[Unreleased]`, identity-log, claim-log, and task-result lines; `PAPERCUTS.md` append only |
| Reserved shared closeout surfaces | lifecycle task record and generated projections; submitted-handoff deletion belongs to the repository hook |
| Forbidden paths | other adapters, contracts, historical research/releases, Ollama Cloud/generate/embeddings/tools/model-management/server-lifecycle expansion, public API expansion without a returned planning decision |
| Approved concurrent siblings | none on shared currentness surfaces; this campaign is serial |
| Worker capability class | evidence-first Rust currentness worker; official GitHub retrieval and deterministic tagged-source comparison; no provider credentials |
| Acceptance evidence | official release/tag/commit/tree identities, host observation, complete all-hop mapped-source ledger, mutation-sensitive fixtures, current docs/claim agreement |
| Review oracle | skipped published stable, unclassified mapped delta, release-note-only inference, lost exclusion, attached/cloud or Ollama/llama.cpp flattening, live-runtime reliance, or unjustified feature widening |
| Stop conditions | official-channel disagreement; major-line reset; mapped public lifecycle/authority/authentication change requiring policy; provider/live runtime evidence required |
| Escalation owner | operator via Chatterbox for new policy; Queue coordinator for mechanical blockers |

## Boundaries

One family only. No prompt, model inference, live catalogue, runtime API call,
server start, login, credentials, install, host update, downloaded-artifact
execution, model pull/unload/delete, release, tag, publication, or consumer
mutation. The installed client is observation only; the external runtime stays
unmanaged.

## Validation

- `cargo fmt -p swallowtail-adapter-ollama -- --check`
- `effigy validate:focused swallowtail-adapter-ollama`
- `effigy package:verify-affected swallowtail-adapter-ollama`
- `effigy check:examples`
- `effigy package:api`
- `effigy qa:routes`
- `effigy qa:northstar`
- relevant research/log/roadmap/number/lifecycle/next-action checks
- `git diff --check`

## Acceptance

- [x] official identity and every published stable are reproducible
- [x] every changed selected input is classified
- [x] identity evidence precedes production claim edits
- [x] the maintained segment and historical exclusions remain truthful
- [x] attached-runtime ownership and exact feature boundaries survive
- [x] official latest is rechecked at both boundaries

## Result

Research 313 froze the official tag/commit/tree and selected-file identities
for all five published hops after `0.32.15` (`0.33.0`, `0.33.1`, `0.33.2`,
`0.33.3`, `0.34.0`) with host client `0.33.3` observed and no runtime
started. The eight selected structs are byte-identical through `0.34.0` and
routes through `0.33.2`; the only in-range types delta is the unselected
experimental model-recommendation mappings surface. The compatible-extension-
with-stop claim qualifies `0.33.0` through `0.33.2`, keeps the baseline,
claim identity, behavior revision, historical exclusions, decoder specimen,
and `AllowUnverified`, and leaves `0.33.3` and `0.34.0` visibly unverified
on the named strict-decoder stop: `0.33.3` plumbs additive optional
`prompt_eval_cached_count` into selected `/api/chat` NDJSON records and the
`ollama.native-text-v1` chat decoder fail-closes on that unmapped key, proved
by the frozen counterexample record with a decoder-tolerance follow-up named.
No provider operation or host mutation occurred. Validation is named in the
handoff; PR review remains queue-owned.

## Next Task

Return the exact outcome to Chatterbox, then compile and dispatch Grok Build ACP
as the next Research 308 family without asking Tom to repeat authorization.
