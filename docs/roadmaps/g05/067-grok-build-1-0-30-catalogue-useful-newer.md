# g05.067 Grok Build 1.0.30 Catalogue Useful Newer

Owner: Tom
Created: 2026-09-14
Depends on: Contract 029; Contracts 020 and 047; Research 306, 308, and 314; g05.053; g05.065
Vision tags: route currentness, Grok Build, model catalogue, authenticated metadata

## Outcome

Requalify the separately prepared Grok Build catalogue operation from exact
`1.0.25` to current official and installed `1.0.30` if package evidence and a
prompt-free catalogue observation admit it. Preserve `QualifiedOnly`, exact
argv `--no-auto-update models`, bounded capture, shipped bullet parsing,
redaction, joined cleanup, and the authenticated non-inference metadata
boundary settled by g05.053. Do not widen the Grok ACP execution claim or use
its compatibility window as catalogue evidence.

Planning rechecked official npm `latest=1.0.30`, with `1.0.31` on `alpha` only,
and installed `grok 1.0.30 (04b7ffed98c6) [stable]`. Research 314 already
freezes official package and platform identity for every stable
`1.0.26..=1.0.30`; this task may reuse that immutable corpus but must classify
the catalogue-specific command, output grammar, membership source, auth path,
and lifecycle at the new exact point.

## Ready-State Rubric

- [x] Tom authorized the complete Research 308 currentness campaign.
- [x] g05.065 completed and its lifecycle closeout is on canonical `main`.
- [x] Official and installed Grok are both exact stable `1.0.30`.
- [x] g05.053 settled authenticated catalogue metadata as acceptable and
      rejected provider suppression as an acceptance gate.
- [x] The existing operation sends no prompt and opens no inference session.

## Work

1. Recheck npm stable/dist-tags and installed `grok --version`. Reproduce the
   relevant Research 314 package/platform identities for `1.0.25` and every
   stable through `1.0.30`. Freeze Research 316 and a catalogue-specific
   all-hop ledger without executing downloaded artifacts.
2. Classify every changed input feeding `grok models`: exact argv and flag
   placement, authentication preamble, bullet grammar, membership/default
   source, supplemental embedded metadata, output/error bounds, update
   suppression, credential redaction, deadline, process exit, and cleanup.
   Commit identity evidence before changing the claim.
3. Run the existing provider-free fake-process and local-host gate. Then use
   the installed exact `1.0.30` executable for prompt-free authenticated model
   catalogue observation. The operation may refresh authentication or bounded
   catalogue metadata. It must not send a prompt, open a model session, invoke
   inference, dispatch a tool, update the harness, or retain provider state.
   Persist a redacted capsule before assertions. Provider-free diagnosis and
   reruns are normal; a failed observation may be repeated after a concrete
   harness or parser repair because this operation carries no inference
   authority. Record every executed observation instead of inventing a
   one-shot budget.
4. On acceptance, move the exact catalogue claim to `1.0.30`; do not retain
   `1.0.25` as a second accepted point merely to disguise incomplete evidence.
   Live listing owns membership, order, and default. Frozen embedded metadata
   may supplement exact matching IDs only; unknown valid IDs pass through and
   absent source fields remain absent.
5. Stop with the smallest typed counterexample if `1.0.30` prompts, opens a
   model session, invokes inference or a tool, mutates the harness, cannot
   close cleanly, leaks raw account data, or changes the public catalogue
   boundary. Recheck npm stable immediately before identity commit and push.

## Dispatch Manifest

| Field | Task 067 |
| --- | --- |
| Readiness | ready |
| Prerequisites | Contract 029; Contracts 020 and 047; Research 306, 308, and 314; completed g05.053 and g05.065; clean pushed `main`; installed exact `1.0.30` |
| Completion conditions | catalogue-specific identity and all-hop classification precede claim; one honest exact `1.0.30` decision lands; accepted observation is redacted and reproducible; non-inference boundary, lifecycle, and separate ACP authority survive; exact-head review and named gates pass |
| Owned mutable paths | Grok catalogue selection, driver, tests, and fixtures under `crates/swallowtail-adapter-grok/**`; Research 316 and one research-index line; exact Grok catalogue guide, route-matrix, feature-matrix, architecture ceiling, standing-lane, `[Unreleased]`, identity-log, claim-log, and task-result lines; `PAPERCUTS.md` append only |
| Reserved shared closeout surfaces | lifecycle task record and generated projections; submitted-handoff deletion belongs to the repository hook |
| Forbidden paths | Grok ACP compatibility and registered-tool claims; other adapters; contracts; historical research, capsules, releases, and tagged baselines; Desktop or other consumers |
| Approved concurrent siblings | none on shared currentness surfaces; this campaign is serial |
| Worker capability class | evidence-first Rust currentness worker; deterministic package comparison and bounded prompt-free authenticated catalogue observation |
| Acceptance evidence | npm/host identity; Research 314 reproduction; catalogue-specific all-hop ledger; mutation-sensitive parser/driver/local-host fixtures; redacted exact-`1.0.30` observation capsule; current docs/claim agreement |
| Review oracle | ACP-to-catalogue inference, skipped stable, unclassified mapped delta, changelog-only inference, stale `1.0.25` acceptance, hard-coded model membership, raw account leakage, prompt/session/inference/tool activity, artificial one-shot failure, or unjustified range widening |
| Stop conditions | official-channel disagreement; public catalogue/auth/lifecycle boundary change; prompt, model session, inference, tool dispatch, update, unjoined cleanup, raw account leakage, or a provider result that cannot be classified after ordinary diagnosis |
| Escalation owner | operator via Chatterbox for new policy; Queue coordinator for mechanical blockers |

## Boundaries

One family and one exact catalogue point. No model prompt, inference, model
session, tool dispatch, install, host update, downloaded-artifact execution,
release, tag, publication, or consumer mutation. Authenticated catalogue
metadata traffic is allowed and is not inference authority. Zero provider
credit is not a blocker unless the metadata command itself returns a typed
billing failure. Alpha `1.0.31` is not the stable target.

## Validation

- `cargo fmt -p swallowtail-adapter-grok -- --check`
- `effigy validate:focused swallowtail-adapter-grok`
- `effigy package:verify-affected swallowtail-adapter-grok`
- `effigy check:examples`
- `effigy package:api`
- `effigy qa:routes`
- `effigy qa:consumer-docs`
- `effigy qa:northstar`
- focused Grok catalogue parser, driver, local-host, capsule, and identity selectors
- relevant research/log/roadmap/number/lifecycle/next-action checks
- `git diff --check`

## Acceptance

- [ ] exact `1.0.30` package, executable, and catalogue identities reproduce
- [ ] every changed catalogue input is classified
- [ ] the observation sends no prompt and opens no inference session
- [ ] membership, order, default, redaction, bounds, and cleanup are truthful
- [ ] exact catalogue and ACP claims remain independent
- [ ] official stable is rechecked at both boundaries

## Result

Ready for dispatch.

## Next Task

Return the exact outcome to Chatterbox, then compile and dispatch Command Code
as the next Research 308 family without asking Tom to repeat authorization.
