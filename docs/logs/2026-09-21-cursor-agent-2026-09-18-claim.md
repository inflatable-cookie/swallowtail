# 2026-09-21 Cursor Agent 2026.09.18 Claim

## Result

Raised the `cursor-agent.release-date` ceiling from `2026.09.10-fd3934a`
to official `2026.09.18-9a7762b` as a compatible extension. Research 331
froze both published hops (`2026.09.15-d2fe57e`, `2026.09.18-9a7762b`)
with a complete tree inventory before any claim edit. Selected CLI
definitions and stream-json event keys persist. Selected ACP initialize
subset is unchanged on Swallowtail's `fs`-only client; negotiated
`sessionCapabilities.subagents` stays unmapped. All nine exact
date-plus-build milestones are now qualified on the three distinct
catalogue, ACP, and headless claims; dates between them stay incompatible
and later dates stay visible `UnverifiedNewer`. Historical milestones,
exact feature-specific sets, claim identities, and `AllowUnverified`
survive. Worker/persist flags, SEA/native packaging, ACP load/replay, and
continuation recovery stay unmapped or blocked. The prepared guide, route
and lifecycle matrices, feature matrix, architecture ceiling, and
changelog name the new ceiling. Official latest was rechecked at the
identity commit, after the Ask-fixture repair, and again after focused
validation: still `2026.09.18-9a7762b`. No provider operation or host
mutation occurred. Next Task, generation status, handoffs, and northstar
loop state were left untouched.

Ask-mode unqualified fixtures moved from calendar-gap `2026.09.11` /
`2026.09.12` to later `UnverifiedNewer` `2026.09.19` / `2026.09.20` so
preflight still admits the binding and Ask can reject
`ask_mode_unqualified`. `validate:focused swallowtail-adapter-cursor`
passed (72 tests, clippy `-D warnings`).
`package:verify-affected swallowtail-adapter-cursor` passed from an
isolated worktree; this checkout's repo root `/workspace` is a substring
of the historical decoder specimen `cwd` `/fixture/workspace`, so the
same selector false-positives here. The specimen was not rewritten.

## Next

Standing currentness continues on other official-newer families. Gemini
stays skipped here.
