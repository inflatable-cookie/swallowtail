# Ollama Think Max V0.14.0 Through V0.32.15

This secret-free corpus freezes exact tagged-source and current official
specimens for native `ChatRequest.think: "max"` on the qualified
`ollama.runtime` window. It does not start a runtime, send a prompt, or claim
exact model-level membership, effective reasoning, or dispatch confirmation.

Every listed tag retains `ChatRequest.think` as `*ThinkValue` and `/api/chat`
as the native request surface. The corpus distinguishes wire-parser acceptance
from selected-model exact membership. Generic `capabilities: ["thinking"]` on
`/api/show` is not level membership.

No fixture contains credentials, model files, endpoint values, prompts, or live
inference responses.
