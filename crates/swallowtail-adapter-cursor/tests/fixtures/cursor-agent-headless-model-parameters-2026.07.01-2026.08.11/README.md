# Cursor Agent Headless Model Parameters 2026.07.01 Through 2026.08.11

This secret-free corpus freezes exact qualified CLI help and official
documentation specimens for Cursor headless `--model` bracket parameters. It
does not infer support from catalogue ids, SDK descriptors, or account state.

All four qualified calendar builds publish the same `--model` help excerpt and
the same executable SHA-256. The selected CLI catalogue (`cursor-agent models`)
returns plain model ids without parameter descriptors.

No fixture contains credentials, account identity, prompts, or provider
payloads.
