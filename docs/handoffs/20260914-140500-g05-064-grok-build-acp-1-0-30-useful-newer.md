---
title: g05.064 Grok Build ACP 1.0.30 useful-newer qualification
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
status: ready-to-launch
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Tom instructed Chatterbox to action the full Research 308 currentness campaign, including Kimi and Oh My Pi, and continue without repeated approval; g05.063 is complete and Grok Build ACP is next."
roadmap: docs/roadmaps/g05/064-grok-build-acp-1-0-30-useful-newer.md
queue:
  capability: general
  skipPRReview: false
  notifyOriginOnCloseout: true
---

# g05.064 Grok Build ACP 1.0.30 Useful Newer

## What This Thread Was Doing

Chatterbox closed g05.063, rechecked the next authorized family, and compiled
the sixth Research 308 task. Installed Grok and official npm stable both report
`1.0.30`; all stable points `1.0.6..=1.0.30` need one complete ACP-only
classification. Published `1.0.31` is alpha, not the stable target.

## Why It Matters

Swallowtail's maintained Grok Build ACP segment ends at `1.0.5`. A large set
of stable versions now sits above that ceiling. The claim must either advance
from complete artifact evidence or stop on the first mapped incompatibility,
without borrowing authority from the separate model-catalogue operation.

## Current State

- Canonical `main` includes g05.063 closeout `eeac1a613647757b9adfff8617a9a92740fabbe9`.
- Installed observation: `grok 1.0.30 (04b7ffed98c6) [stable]`.
- npm `latest=1.0.30`; `alpha=1.0.31`.
- Existing ACP ceiling: `1.0.5`, behavior
  `grok-build.acp-v1.cached-token-model-4-6-v3`, `AllowUnverified`.
- Exact catalogue operation remains independently qualified at `1.0.25` and
  is outside this task.
- The task and manifest are in
  `docs/roadmaps/g05/064-grok-build-acp-1-0-30-useful-newer.md`.

## Boundaries

One family and ACP only. No prompt, inference, live ACP initialize/session,
catalogue listing, login, credential use, install, host update, downloaded
artifact execution, release, tag, publication, or consumer mutation. Do not
treat alpha `1.0.31` as stable. Do not widen catalogue or registered-tool
claims, erase historical gaps, or infer feature-specific support.

Provider-free diagnosis and reruns are normal engineering work. Do not impose
an artificial one-attempt limit on npm retrieval, unpacking, comparison,
fixture repair, or validation. Stop only on the task's substantive contract
conditions or a real external blocker.

## Important Context

Read Contract 029, Research 163, 294, 308, and the g05.064 task before editing.
Use the repository `version-currentness` skill and Effigy selectors. Inventory
every stable hop, not only `latest`; freeze package/platform identity before
claim edits. Classify selected `grok agent stdio` inputs across authentication,
cached-token session creation, ACP wire/activity, model selection, permissions,
registered tools/MCP courier, attachments, cancellation, failures, cleanup,
lifecycle, config, and process ownership. Changelogs locate evidence but do not
prove compatibility.

## Suggested Next Move

Run the provider-free identity phase from a clean task workspace, write
Research 314, commit it, then make the narrow ACP claim decision. If a mapped
change prevents extending through `1.0.30`, retain the smallest exact stop and
name the repair. Otherwise extend the existing maintained segment. Run the
task's focused validation, push, open a PR, and hand the exact head to an
independent reviewer.

## Completion Protocol

The Queue owns worker launch, independent review, merge, lifecycle closeout,
and handoff deletion. Return exact stable identity, classified hop set, claim
outcome, Research 314 path, validation, review marker/comment, merge SHA, and
closeout SHA. The next authorized family is the Claude Agent SDK tuple. No
release or tag follows from completion.
