# 2026-09-15 Kimi Code 0.43.0 Installed Identity

Research 325 froze official npm and GitHub `@moonshot-ai/kimi-code` `0.39.1`,
`0.40.0`, `0.40.1`, `0.41.0`, `0.42.0`, and `0.43.0`. Every npm `integrity`,
`shasum`, tarball SHA-256, file count, unpacked size, `bin` entry, and
`dist/main.mjs` digest was recomputed from the downloaded tarball; every GitHub
annotated tag was resolved to its commit and tree and every tagged source
archive was downloaded and hashed. The installed host stays `kimi 0.34.0` at
the Research 270 digest, observed only through `--version` and its digest.

One deterministic tagged-source `path -> SHA-256` inventory per point produced
the changed-path sets directly, and the documented selected surfaces were
reduced to a git-blob ledger with a `null` for absent paths, so a silent edit
to any selected surface fails the ledger, the per-hop lists, the bundle oracle,
and the live claim tests together. A separate bundle oracle extracts the
`AcpProcessService` class and seven headless emission surfaces from all six npm
`dist/main.mjs` bundles.

ACP stops unchanged. The g05.017 A2 ruling is binding, and
`acpTerminalRunner.ts` is git blob `9016d48b` at all six points while the
bundled `AcpProcessService` is SHA-256 `7c58e045` at all six — the exact digest
the frozen `kimi-code-0.39.0-acp-authority` corpus records for `0.39.0` and
`0.39.1`. The `terminal: false` branch still delegates to a direct local
host-process spawn, Swallowtail always advertises `terminal: false`, and no
adapter or runtime control contains it. The ceiling stays at `0.38.0`, the
exact `0.39.0`/`0.39.1` exclusions do not grow, and `0.40.0..=0.43.0` is the
posture-rejected published gap.

Headless extends to `0.43.0`. The `dispatchNativeEvent` switch is
byte-identical across all six points — the same ten case labels and writer
calls — and `prompt-render.ts` and `options.ts` are byte-identical git blobs,
so the JSONL roles, `system.version` preamble, `session.resume_hint`, retry
record, tool record, and terminal shape do not move. The
`TurnStepRetryingPayload` field set is identical even though its file moves at
`0.42.0`. The hops move only internal dependency-injection, telemetry, shutdown
quiescence, prompt-submission, and cancellation mechanics, plus the deletion of
the v1 legacy body and the `KIMI_CODE_LEGACY_FLAG` gate at `0.42.0`.

The `0.41.0` auto-permission-mode drop of the dangerous-command deny guard and
the `0.42.0` trust-gated MCP stderr warning stay unmapped: the first is
provider tool policy behind the mode the print route forces for itself and was
already recorded unmapped by Research 282, and the second is a stderr
diagnostic rather than a `stream-json` stdout record.

The corpus is frozen in
`crates/swallowtail-adapter-kimi/tests/fixtures/kimi-code-0.43.0/` with the
`kimi_code_0_43_0_identity` ledger tests. No downloaded artifact was executed,
installed, or unpacked beyond static extraction, and no prompt, login,
credential, catalogue, live session, host update, local-server start, release,
or consumer mutation occurred.
