# 2026-08-27 g04.080 xAI Responses WebSocket Web Search Compiled

Status: complete
Owner: Tom

## Changed

- reassessed the remaining per-route feature inventory after g04.079
- selected provider-owned web search on `xai.responses-websocket` as the next
  evidence candidate
- compiled g04.080 and serial cards 222-224
- reserved Research 227
- kept delivery conditional on exact WebSocket composition, model/profile
  support, a positive provider-side use bound, and bounded response/citation
  truth

## Why This Lane

The xAI route already owns exact Grok 4.5/4.6 selection, one Responses
WebSocket, structured runs, serial private continuation, reasoning, output
bounds, usage, billed cost, restoration, and joined cleanup. Its one explicit
feature gap is visible in every current request: `tools: []`.

Current primary xAI material says WebSocket `response.create` uses the
Responses create body and documents `web_search` on `grok-4.6`, provider-owned
search-call items, citations, and tool-turn bounds. That makes the seam more
direct than remaining account/billing controls, ambient registries, writable
modes, and process-topology features. It is still not delivery evidence: the
existing facade is exact and dated, so Research 227 must freeze the complete
request, response, model, bound, citation, usage, billing, and lifecycle row.

Codex app-server verbosity was not selected because exact app-server v2 has no
first-class thread/turn verbosity control. Claude autocompact remains shadowed
by opaque environment authority. Ollama `think=max` maps to `high` on the exact
relevant family.

## Deliberate Exclusions

X/image search, code execution, files, MCP, function and consumer tools, raw
tool arrays, caller filters, host networking, sibling routes, live provider
work, currentness, release, merge, generation rollover, and g04 closure remain
outside the lane.

## Execution Shape

One serial worker lane:

1. card 222 freezes exact evidence and promotes Research 227;
2. card 223 runs only for a non-empty exact deliver-now set;
3. card 224 proves route-local acceptance and closes the lane.

The evidence card may stop the milestone honestly. Current documentation or
HTTP Responses support without exact WebSocket and bounded event truth is not
enough.

## Next

Execute g04.080 cards 222-224 serially. Stop after card 222 if Research 227 is
empty or any decision gate fires. Keep g04 open. Contract 029 currentness
remains standing.
