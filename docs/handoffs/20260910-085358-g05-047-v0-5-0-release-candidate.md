---
title: g05.047 v0.5.0 release candidate
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
status: ready-to-launch
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Tom authorized v0.5.0 candidate preparation and its provider-free/source-consumer gates on 2026-09-10 after Contract 036 ruled out v0.4.5. No tag authority was granted."
queue:
  capability: general
  skipPRReview: false
  notifyOriginOnCloseout: true
---

## What This Thread Was Doing

Deliver g05.047: prepare and merge one immutable source-only `v0.5.0`
candidate carrying the registered-only Claude SDK route and post-`v0.4.4`
source.

## Why It Matters

Desktop g02.051 needs the registered-only API from g05.046. The corrected
`v0.4.4` native-mediation guarantee is a breaking guaranteed-behavior shrink
under Contract 036, so the candidate must be `0.5.0`, not `0.4.5`.

## Current State

g05.044, g05.045, and g05.046 are complete. Before this planning promotion,
clean canonical `main` equalled `origin/main` at
`98894512e3042d1e2355c31d9385d7418dbe0e89`. `v0.4.4` is immutable at
`49c9e3b291609c9ebf5b35a284c08302f3b8d5e3`; `v0.5.0` was absent.

## Boundaries

Follow `docs/roadmaps/g05/047-v0-5-0-release-candidate.md` and the release
playbook. Own only coordinated versioning, fresh candidate baselines, release
notes/changelog/version examples, one prepare transaction, provider-free and
exact-source gates, hosted CI, candidate PR, and closeout. Do not contact a
provider, mutate Desktop, create or push a tag, publish, or alter history.

## Important Context

Run exactly one `effigy --json release prepare --yes --check-gates --version
0.5.0`. Preseed the three fresh `0.5.0` baselines first and never edit earlier
ones. PR CI alone is not qualifying hosted evidence; use a green
`workflow_dispatch` or push run at the exact candidate SHA or an identical
tree. Preserve the feature freeze and stop on deterministic failure.

## Suggested Next Move

Freeze the complete `v0.4.4..HEAD` delta and compatibility classification,
write the release note and fresh baselines, then consume the single authorized
prepare transaction.

## Completion Protocol

Open one PR for independent exact-head review. Merge only with green local
gates, exact-source consumer, qualifying hosted CI, and queue verification.
Return candidate head, merge SHA/tree, release receipt digest, hosted run ID,
gate results, and zero provider contact. Stop with local and remote `v0.5.0`
absent. The queue owns merge and closeout; Tom owns any later exact-SHA tag.
