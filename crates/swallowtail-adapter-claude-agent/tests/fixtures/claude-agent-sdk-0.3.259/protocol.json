{
  "selected_mapped_subset": [
    "query() through the '.' entry point only",
    "Options: cwd, model, executable, pathToClaudeCodeExecutable, env, settingSources, skills, plugins, agents, mcpServers, strictMcpConfig, hooks, persistSession, tools, disallowedTools, permissionMode, canUseTool, spawnClaudeCodeProcess",
    "system/init fields: cwd, model, apiKeySource, capabilities",
    "accountInfo(): apiProvider, subscriptionType",
    "canUseTool(toolName, input) -> allow with unchanged updatedInput, or deny",
    "interrupt() and its capability-gated receipt",
    "SDKMessage projection: assistant text and tool_use, user tool_result, result subtype"
  ],
  "mapped_subset_unchanged": true,
  "declaration_deltas": [
    {
      "symbol": "Options.permissionPrompts",
      "kind": "added_optional_input",
      "mapped": false,
      "why_unmapped": "new 'host' | 'none' selector forwarded only when set (`if(x)Y.push(\"--permission-prompts\",x)`); unset keeps the default host surface, so canUseTool still governs admission. The route never sets it and asserts the asset never names it."
    },
    {
      "symbol": "user_message_uuids",
      "kind": "added_optional_field",
      "occurrences": 4,
      "mapped": false,
      "why_unmapped": "additive correlation siblings of user_message_uuid on reply, stream-event, error-result, and success-result records; the route reads neither, and unknown record fields are ignored while unknown record and part types still fail closed."
    },
    {
      "symbol": "task summary",
      "kind": "documentation_only",
      "mapped": false,
      "why_unmapped": "doc comment added above an existing field on background task rows; background tasks are outside this route."
    },
    {
      "symbol": "Settings.managedMcpServers",
      "kind": "added_settings_key",
      "mapped": false,
      "why_unmapped": "managed-settings tier only; the route passes settingSources: [] and strictMcpConfig with no MCP servers, so no settings source is loaded."
    },
    {
      "symbol": "Settings.allowedMcpServers / managedSourcesBehavior",
      "kind": "documentation_only",
      "mapped": false,
      "why_unmapped": "prose rewrite describing how managedMcpServers composes; same suppression applies."
    },
    {
      "symbol": "sdk-tools skill publish fields",
      "kind": "added_optional_field",
      "occurrences": 2,
      "mapped": false,
      "why_unmapped": "publishesRemaining and publishesResetAt on a skill-publishing tool; skills are prohibited on this route."
    }
  ],
  "implementation_invariants_unchanged": {
    "can_use_tool_gating": "canUseTool still pushes --permission-prompt-tool stdio and still conflicts with permissionPromptToolName",
    "spawn_callback": "spawnClaudeCodeProcess receives one SpawnOptions object with command, args, cwd, env, and signal",
    "no_joined_stop": "the bounded waitForExit race remains, and SIGKILL escalation is still unref'd",
    "stderr_drain": "sdk-exit-after-stderr-drained remains a single internal remap"
  },
  "credential_non_custody": {
    "ten_pattern_hits_in_default_entry": 3,
    "hits_are_prose_only": true,
    "exported_functions": 17,
    "login_or_oauth_exports": 0,
    "entry_points_unchanged": true,
    "bridge_and_browser_declarations_byte_identical": true,
    "bridge_and_browser_implementations_changed_but_unreachable": true
  },
  "native_artifact_rotation": {
    "all_eight_platform_binaries_rotated": true,
    "harness_schema_unchanged": true,
    "tested_wrapper_versions_still_stale": true
  },
  "no_prompt": true,
  "no_live_session": true,
  "no_host_change": true,
  "nothing_executed": true
}
