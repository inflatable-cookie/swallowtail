# Qwen Code 0.21.13 currentness corpus

This secret-free identity corpus freezes host Qwen Code `0.21.2` and
official npm `@qwen-code/qwen-code` `0.21.13` before Swallowtail widens
the `qwen-code.package` claim.

Exact npm tarball and selected git-blob identities live in
`identity.json`. Selected flags, exact `--resume`, and the image-only
catalogue filter remain. `0.21.4` adds unmapped nested `goal_state`.
Initialize `effort_status` stays unused.

Unpublished stable `0.20.2` stays a gap. No provider prompt. No live
catalogue. No live headless session. The host install was not replaced.

No fixture contains a credential, host path, account identity, provider
payload, or real session id.
