# 075 Claude Code Headless 2.1.234 Useful Newer

Status: completed
Owner: Tom
Created: 2026-08-18
Depends on: Research 127; Research 132; g03.074
Vision tags: maintained compatibility, exact interfaces, currentness
Contract refs: 001, 011, 029, 032, 036-037, 039, 044, 052
Planning state: cards 237-238 completed

## Problem

Research 127 ranked AllowUnverified host drift after Codex, Grok, and the
Muse pin. Claude Code headless is exact qualified `2.1.220` with
AllowUnverified. This host now runs npm `latest` `2.1.234`, already
classified UnverifiedNewer. Raising the bound is useful-newer support, not
a silent `latest` bump.

Response-only uses the same CLI on a different axis. Mixing the two would
flatten route identity.

## Generation Runway Goal

Freeze Claude Code headless `2.1.234` identity against the `2.1.220`
corpus, then raise the qualified ceiling only if that evidence names a
compatible extension.

## Goals

- [x] rank remaining AllowUnverified host-drift families and pick Claude
      Code headless first
- [x] freeze npm and local `2.1.234` identity and selected-help protocol
- [x] raise the headless claim through exact `2.1.234` on the existing
      behavior revision
- [x] leave response-only, Gemini, and other 127 families for later
      one-family work

## Non-Goals

- mixing `claude-code.response-only-stream-json` into this milestone
- bulk-bumping other AllowUnverified families
- Gemini requalification
- provider prompts, install, update, or publication

## Execution Plan

### Batch 75.1 — Identity Corpus

- [x] Execute card 237.
- [x] record host and npm `2.1.234` identity against exact `2.1.220`
- [x] name compatible-extension for card 238 without changing production
      claims

### Batch 75.2 — Claim And Acceptance

- [x] Execute card 238 after card 237 names compatible-extension.
- [x] raise `claude-code.headless-stream-json` through exact `2.1.234`
- [x] keep baseline `2.1.220`, AllowUnverified, and behavior
      `claude-code.headless.stream-json.v1`
- [x] refresh matrices, guides, and focused Claude Code proof

## Acceptance Criteria

- [x] Claude Code headless is ranked first among remaining AllowUnverified
      host-drift families
- [x] `2.1.234` identity is frozen and the segment shape is named
- [x] production headless claim admits `2.1.220..=2.1.234` as Maintained
- [x] later stables remain visible UnverifiedNewer
- [x] response-only and other 127 families remain untouched

## Decision Gates

- Stop if selected stream-JSON evidence requires a new public operation.
- Stop if qualification depends on a provider prompt or harness install.
- Do not compile response-only or the next 127 family inside this
  milestone.

## Next Planning Checkpoint

After card 238, reassess remaining Research 127 families one at a time.
Next rank: Claude Code response-only, then other AllowUnverified
host-drift. Gemini stays deferred.
