---
title: g05 Kimi active-observation gate and Card 034 worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: planning
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Tom
created: 2026-09-01
updated: 2026-09-01
handoff_path: /Users/tom/Dev/projects/swallowtail/docs/handoffs/20260901-111034-g05-kimi-gate-card034.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g05, contract-061, kimi]
---

## What This Thread Was Doing

The orchestrator reviewed and merged Card 033 through PR 147 at
`10fab5eac15f16199f8da51484f94a8a4755efeb`. Candidate F is reconciled at 89
rows but remains unpromoted behind three `kimi-code.acp` active-observation
families. The operator has now answered all five route-local gate decisions.

This worker compiles those decisions into one exact public-baseline gate and
one ready Candidate F implementation card. It changes planning only. A later
worker implements the reviewed card.

## Operator Decisions

The accepted baseline is exact and route-local:

1. Keep `swallowtail-runtime::EffectiveReasoningSetup` unchanged. Retain exact
   Kimi reasoning and Plan effective/rejected values inside
   `swallowtail-adapter-kimi`.
2. Use a bounded adapter-local Kimi provider-value representation. Preserve
   exact admitted or foreign confirmation tokens only when they satisfy the
   gate's exact non-blank byte bound. Foreign or over-bound values fail closed
   on the projected path and publish no active row. The preserved
   `open_session` keeps its current normalization, failures, handle, and
   cleanup behavior.
3. Add only
   `KimiPreparedSession::open_session_with_projection`; keep `open_session`,
   `load_session`, and `resume_session` public behavior unchanged. Both open
   methods must share one private lifecycle.
4. Add a separate projected catalogue seam over
   `KimiPreparedSessionCatalogue::list_sessions`; keep `list_sessions` and
   pagination behavior unchanged. Catalogue observation stays exact
   session-management truth, never interactive-session truth.
5. Use distinct caller-supplied prepared and active-observation source IDs.
   Add no runtime, testkit, core, contract, registry, callback, generic
   provider payload, or cross-route decision.

Use the existing 128-byte provider-value bound in
`crates/swallowtail-core/src/model_catalog/value.rs` as the planning precedent
for the adapter-local Kimi token. The gate must fix one exact adapter-owned
constant/type and its validation/failure behavior; it must not expose the raw
ACP response or add a shared constant. If current public types make that exact
surface impossible without widening shared API, stop and report the conflict
instead of weakening the decision.

## Current State

- **Repository:** `/Users/tom/Dev/projects/swallowtail`
- **Required planning base:** pushed `main` at
  `10fab5eac15f16199f8da51484f94a8a4755efeb`
- **Planning checkout:** clean and synchronized before this handoff commit
- **Worker branch:** `worker/g05-kimi-gate-card034`
- **Worker worktree:** launcher-generated Paseo worktree
- **Dispatch topology:** serial; this worker owns the shared roadmap, triage,
  log, and front-door surfaces until its planning PR reaches orchestrator
  review
- **Active milestone:**
  `docs/roadmaps/g05/009-contract-061-consumer-projection-realization.md`
- **Completed evidence:** Card 033; PR 147; Batch 9.4 checkpoint; reviewed
  767-row census; Claude Agent and Cline active-observation gates
- **Coverage before implementation:** 249 proved / 518 remaining
- **Candidate F boundary:** 89 exact rows across `kimi-code.acp` 25,
  `kimi-code.headless` 20, `kimi-code.local-server` 31, and
  `kimi-platform.chat` 13
- **Allowed runway:** compile the Kimi gate and one Card 034 only
- **Required validation:** `effigy qa:docs`; `effigy qa:northstar`;
  `git diff --check`
- **Merge authority:** orchestrator only

## Deliverables

Produce one docs-only planning PR with all of the following:

1. A dated triage gate alongside the Claude Agent and Cline gates. It fixes:
   exact public names and signatures; adapter-local bounded value type and
   bound; compound reasoning-and-Plan acknowledgement states; preserved versus
   projected behavior; projected-open outcome/failure; projected-catalogue
   outcome/failure; source identities; lifecycle; cleanup; diagnostics;
   contribution semantics; and provider-free counterexamples.
2. Card 034 under `docs/roadmaps/g05/batch-cards/`. It owns both Kimi adapter
   packages and all 89 tuples. Name every prepared facade and both accepted
   active-observation seams. Derive the exact four-route emitted/withheld
   ledgers from the census and current source; do not assume 86/3 is the final
   disposition.
3. Exact proof requirements for maximal/minimal prepared runs, effective and
   rejected reasoning, effective and rejected Plan, requested `on` with an
   exact concrete or foreign provider token, omitted options, over-bound and
   malformed confirmations, model-option observation, catalogue success and
   failure, equal sources, stale revision, route/operation/instance/access
   mixtures, lifecycle, posture, omission, support, and availability.
4. Exact preservation requirements: current `open_session`, `load_session`,
   `resume_session`, `list_sessions`, continuation paging, failure codes, and
   cleanup remain unchanged. Projected paths fail closed without publishing
   partial or ambiguous active truth.
5. Exact semantic API boundary. Only the Kimi and Kimi Platform adapter
   baselines may change during implementation. Runtime, testkit, core,
   contracts, architecture, census, provider claims, and compatibility claims
   may not.
6. Reconcile Card 033, Batch 9.4, g05.009, g05 and generation front doors,
   batch-card index, one gate-compilation log plus its index, and the sole
   `docs/roadmaps/README.md` Next Task. The result is `strict-ready`, with Card
   034 as the sole ready implementation card. Do not count its 89 rows as
   proved before implementation review and merge.

## Boundaries

- Docs-only. Do not edit Rust, manifests, baselines, contracts, architecture,
  census, provider claims, compatibility, PAPERCUTS, watcher, skill-discovery,
  currentness, or Batch 9.5 surfaces.
- Do not implement Card 034 in this worker.
- Do not transfer Kimi authority to another route or adapter.
- Do not collapse interactive-session acknowledgement, model-option
  observation, and session-management catalogue observation into one source or
  one lifecycle.
- Do not infer exact values from session existence, prepared success,
  documentation, static diagnostics, or another route.
- Do not merge.

## Review Oracle

Fail the planning PR if any of these remain possible:

- a foreign or over-bound Kimi token publishes as portable reasoning truth;
- requested `"on"` silently replaces the exact provider-effective token on
  the projected path;
- an exact rejection is published from a static failure code without the
  retained confirmation value;
- `EffectiveReasoningSetup` or another shared public type changes;
- the preserved and projected open paths can drift in setup, failure code, or
  cleanup;
- load or resume gains projection authority from the open-only decision;
- prepared catalogue success is presented as observed catalogue truth;
- catalogue observation uses the interactive-session active source or
  operation shape;
- model options become selection, mutation, acknowledgement, or catalogue
  authority;
- equal prepared and active source IDs reach provider work;
- a Kimi ACP active row reaches headless, local-server, or Kimi Platform;
- any ledger reaches 89 through a filter, exception list, duplicate tuple, or
  borrowed identity;
- Card 034 claims proved coverage before its implementation merges.

## Completion Protocol

### Before work

1. Record `git rev-parse --show-toplevel`, branch, clean status, and registered
   worktree. Use the launcher worktree; do not create another.
2. Fetch origin with a bounded non-interactive SSH command. Confirm `HEAD ==
   origin/main`, confirm the required planning base is an ancestor, and confirm
   this handoff's tracked blob matches the absolute file.
3. Read `AGENTS.md`, Contract 061, Card 033, Batch 9.4, the reviewed census,
   the Claude Agent and Cline gates/cards, g05.009, and current Kimi source
   read-only.
4. Run Effigy's cheap orientation route before editing.

### While working

- Re-derive all public signatures and ledger totals. Treat this handoff as the
  decision boundary, not as permission to copy a prior adapter blindly.
- Keep the gate exact enough that the implementation worker makes no product
  or public-baseline choice.
- Use one coherent planning commit unless a later review repair requires a
  separate commit.

### Closeout

1. Run `effigy qa:docs`, `effigy qa:northstar`, and `git diff --check` after
   the complete planning batch.
2. Check every new card acceptance item only if the planning artifact itself
   proves it; implementation acceptance remains unchecked.
3. Push the branch and open one PR against current pushed `main`.
4. Report exact head/base, gate public surface, bounded-value decision,
   four-route ledger totals, changed files, validation, PR URL, and any stop.
5. Stop for orchestrator exact-head review. Do not launch implementation and
   do not merge.

## Next Move

Compile the Kimi public-baseline gate and Card 034 as one reviewable docs-only
PR. After orchestrator merge, a fresh implementation worker receives a new
single-file handoff for Card 034.
