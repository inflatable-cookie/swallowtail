# Codex CLI 0.149.0 currentness corpus

Secret-free identity for official npm `@openai/codex` `0.149.0` before
Swallowtail raises the `codex.cli` ceiling.

The mapped exec JSONL flags and app-server methods remain unchanged from
`0.148.0`. Exec help output is byte-identical. ModelListParams schema is
unchanged. Schema bundle hashes differ but do not affect mapped protocol
surface. No decoder update required. Host install was not present.

No fixture contains a credential, host path, account identity, provider
payload, or real session id.
