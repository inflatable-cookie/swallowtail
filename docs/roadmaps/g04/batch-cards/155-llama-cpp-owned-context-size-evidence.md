# 155 llama.cpp Owned Context-Size Evidence

Status: ready
Owner: Tom
Created: 2026-08-24
Updated: 2026-08-24
Milestone: [g04.056 llama.cpp Owned Context Size](../056-llama-cpp-owned-context-size.md)
Depends on: Research 008; exact route `llama-cpp.owned` `b10069-178a6c449`

## Goal

Freeze exact current official and tagged `b10069` context-size behavior, then
define the smallest positive owned-serving selection whose dispatch,
acceptance, observation, failure, and cleanup Swallowtail can represent
without inventing model or host feasibility.

## Method

1. Fetch the current official `llama-server` documentation and exact official
   `b10069` documentation/source. Record retrieval dates, source identity,
   stable URLs, complete specimen digests, and decisive parser, parameter,
   model-load, context-clamp, property, error, and cleanup paths.
2. Freeze the current `llama-cpp.owned` preparation, serving selection,
   immutable evidence, driver, launch arguments, artifact acquisition,
   readiness probes, failure cleanup, stop, endpoint invalidation, and artifact
   release. Do not launch a server or load a model.
3. Classify caller omission, explicit zero, positive integers, negatives,
   fractional input, representation overflow, values above training context,
   and resource-infeasible values. Keep parser acceptance separate from a
   useful public domain.
4. Establish whether the exact runtime clamps, rejects, expands, or otherwise
   transforms requested context size, and which facts depend on model metadata
   or host resources.
5. Inspect exact `GET /props`, health, catalogue, and startup surfaces. Decide
   whether any selected bounded surface confirms the applied context size.
   Keep requested, dispatched, accepted, effective, and observed states
   distinct.
6. Decide the smallest typed adapter-local selection and immutable prepared
   start evidence/specification needed for any admitted subset. It must remain
   inspectable before effects and agree with the configured driver and argv.
7. Decide whether exact `b10069` needs a new behavior, driver, claim, or
   configured-instance revision. Do not change Contract 029 or production code
   on this card.
8. Replace Research 203's reservation with source-backed domain, state,
   observation, failure, and lifecycle dispositions plus a deliver-now table
   or honest empty set. Do not edit shared closeout surfaces.

Current public documentation and secret-free exact-tag source evidence are
authorized. No model download, executable download, installation, server
launch, model load, credential, external inference request, or paid work is
authorized.

## Acceptance Criteria

- [ ] exact current official and `b10069` evidence is frozen with source
      identity and complete specimen digests
- [ ] parser domain and Swallowtail's useful public domain are distinct
- [ ] omission, zero, positive, negative, fractional, overflow, above-training,
      and resource-infeasible states have explicit dispositions
- [ ] request, dispatch, acceptance, effectiveness, and observation are
      separated
- [ ] model metadata, host memory, allocation, and context-clamp dependencies
      are explicit
- [ ] immutable start evidence/specification and revision posture are explicit
- [ ] Research 203 is promoted with a deliver-now table or honest empty set
- [ ] no production code, shared capability, matrix, contract, currentness, or
      sibling-route change lands
- [ ] `effigy validate:focused swallowtail-adapter-llama-cpp` passes
- [ ] `effigy qa:northstar` and `effigy qa:docs:index:research` pass
- [ ] `git diff --check` passes

Auto-continue to card 156 only when Research 203 admits a non-empty exact
value/profile set with truthful application and lifecycle semantics.

## Stop Conditions

- a useful domain or claimed effect requires live model or host-specific
  resource evidence
- a selected value cannot be represented without implying effective context,
  allocation success, or model fit
- selection needs a shared capability, generic provider-settings map,
  contract/currentness change, sibling route, or breaking API
- artifact, process, endpoint, readiness, failure, stop, or release semantics
  would change

## Out Of Scope

- production binding, shared closeout, another llama.cpp build/route/control,
  live work, release, publication, merge, generation rollover, or g04 closure
