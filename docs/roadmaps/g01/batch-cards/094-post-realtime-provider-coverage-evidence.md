# 094 Post-Realtime Provider Coverage Evidence

Status: completed
Owner: Tom
Updated: 2026-07-22
Milestone: `../032-post-realtime-coverage-checkpoint.md`

## Objective

Revalidate and rank the next provider or transport proof after OpenAI Realtime,
then promote only the shared boundary and implementation lane supported by
current evidence.

## Readiness Gate

Roadmap 031 must be complete with the realtime-media production driver and
eleventh common profile passing full repository QA.

## Scope

- audit the seventeen production descriptors and eleven common profiles
- revalidate Gemini Live, Grok Build, DeepSeek, Z.AI, remote ACP, Cursor, and
  useful attached-runtime candidates against current authoritative sources
- compare missing lifecycle, access, catalogue, credential, topology, media,
  protocol, and cleanup pressure
- keep preview, beta, experimental, maintainer-supported, and provider-
  supported authority explicit
- promote a research delta and any minimum durable contract before compiling
  implementation cards
- ask the operator when equally supported choices would set product policy

## Acceptance Criteria

- [x] every candidate has a current dated authority and access assessment
- [x] realized mechanisms are separated from genuinely missing pressure
- [x] the recommendation names one exact route and bounded first subset, or an
      explicit evidence or operator gate
- [x] no provider, model, endpoint, credential, entitlement, topology, retry,
      or fallback becomes implicit
- [x] any selected implementation remains in g01 with meaningful cards
- [x] the roadmap spine ends with one sole next task

## Completion Evidence

- Research 021 audits all named candidates from current official or maintained
  sources and selects Gemini Live raw server-to-server WebSocket.
- Gemini adds planned connection rollover, private provider-session handles,
  asymmetric PCM formats, and authorization-key migration pressure not proved
  by the seventeen production routes or eleven common profiles.
- Contract 027 makes rollover opt-in and maximum one, separates it from
  reconnect, reattachment, retry, consumer resume, and durable state, and
  requires private handles plus joined old/new connection cleanup.
- The exact first subset binds provider-supported preview `v1beta`, model
  `gemini-3.1-flash-live-preview`, project authorization-key access, project
  billing, explicit formats, voice, thinking, manual activity, two turns, and
  one idle-boundary rollover.
- Grok Build remains later harness breadth. Remote ACP advanced to Active but
  remains behind implementation and hardening gates. Cursor gained a local SDK
  but remains beta and lower-information or policy-heavy. DeepSeek, Z.AI, and
  attached runtimes remain later breadth.
- Roadmap 033 and cards 095-097 compile the proof inside g01. Card 095 is the
  sole ready task.

## Continuation

Roadmap 033 and card 095 own provider-neutral rollover records, pure preflight,
the assertion pack, and the frozen Gemini Live corpus. Production transport
does not begin before that card completes.

## Validation Evidence

- `effigy qa:docs --json` passes link, vision-index, sole-next-action, and
  forbidden-pattern checks.
- `git diff --check` passes.
- Effigy doctor remains at the inherited 19 findings: 12 warnings and seven
  errors. No finding was added by this documentation batch.

## Validation

- configured docs QA
- `git diff --check`
- `effigy doctor` delta review if compiled work changes file shape

## Stop Conditions

- current official or maintained evidence does not settle protocol authority
- provider selection would grant repository, remote mutation, browser-device,
  billing, credential, or model-serving policy not already authorized
- the candidate adds only a provider name over an existing compatible route

## Auto-Continuation

No. Select and compile the next exact lane, or stop for operator direction.
