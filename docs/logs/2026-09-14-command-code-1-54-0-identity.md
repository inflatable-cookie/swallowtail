# 2026-09-14 Command Code 1.54.0 Identity

Research 317 froze official Command Code `1.15.1` plus all 67 published
stable successors through `1.54.0`. The complete extracted package trees have
67 consecutive deltas, no removals, eight additive bundled reference files,
and a byte-identical `dist/index.mjs` entrypoint. Every selected invocation,
AgentEvent, result, usage, failure, stdin, and local lifecycle input was
classified from the official artifacts; unselected CLI, TUI, Provider API,
session-catalogue, and bundled-reference changes remain outside the route.

The provider-free identity decision admits a compatible exact-point rebind to
`1.54.0` with the same `command-code.agent-event-ndjson-v1` behavior and
`QualifiedOnly` posture. Research 116 and 118 remain immutable live evidence
for exact `1.15.1`; their authenticated completion/tool/usage/credit and
private continuation results do not migrate. The identity corpus is frozen in
`crates/swallowtail-adapter-command-code/tests/fixtures/command-code-1.54.0/`.
No downloaded artifact was executed and no provider operation occurred.
