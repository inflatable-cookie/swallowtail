# 2026-08-22 g04.036 Ollama Num Ctx Compiled

## Change

- rechecked the current official Ollama native API, OpenAPI, FAQ, and source
  shape for `num_ctx`
- inspected the existing attached prepared, driver, protocol, fixture, and
  compatibility boundaries
- compiled g04.036 and cards 098-100 for exact evidence, typed binding, and
  deterministic acceptance
- advanced the per-route feature programme to its second numbered milestone

## Decision

`num_ctx` stays an Ollama-local native runner option. It does not become a
portable context-window capability, output-token alias, generic options map, or
catalogue-derived model claim.

The worker must first freeze the exact tagged-source behavior across the
qualified runtime window and classify structured inference and interactive
replay independently. Implementation may cover only the Research 184
deliver-now profiles and positive numeric domain.

The exact value must remain inspectable in prepared evidence and immutable in
the configured low-level driver that emits `options.num_ctx`. The
provider-neutral plan continues to advertise no context-window capability.
The milestone claims qualified dispatch only, not provider acceptance,
effective allocation, model fit, truncation behavior, or memory feasibility.

## Evidence

- the current official OpenAPI defines `num_ctx` as the context length in the
  native model options used by chat requests
- current official source retains `NumCtx` as JSON `num_ctx` in runner options
- the current FAQ documents API selection through the native `options` object
- the prepared route already rejects remote/cloud model detail and binds one
  exact local endpoint, runtime version, model tag, and manifest digest
- current implementation encodes `num_predict` in the same native options
  object but has no route-local context selection or portable request field
- no runtime was started and no model request, authentication, install, pull,
  unload, or host change ran

## Next

Execute cards 098-100 serially in one worker PR. After g04.036 closes, compile
Anthropic Messages `output_config.effort`.
