# g05.064 Grok Build ACP 1.0.30 Useful Newer

Owner: Tom
Created: 2026-09-14
Depends on: Contract 029; qualified Grok Build ACP `1.0.5`; Research 163, 294, and 308; g05.063
Vision tags: route currentness, Grok Build, ACP, compatibility

## Outcome

Qualify current official npm stable `1.0.30` on the Grok Build ACP route only
if deterministic package and platform-artifact evidence admits it. Preserve the
deprecated `0.2.114..=0.2.116` segment, exact `0.2.117` task-control segment,
the `0.2.118..=0.2.121` and `1.0.0..=1.0.3` gaps, claim identity,
`grok-build.acp-v1.cached-token-model-4-6-v3`, `AllowUnverified`, exact feature
sets, and every independently bounded operation.

Planning observed installed `grok 1.0.30 (04b7ffed98c6) [stable]`. Official npm
`latest` is `1.0.30`; `1.0.31` is published only on the `alpha` tag and is not
the stable target. Published stable versions after `1.0.5` are every point
from `1.0.6` through `1.0.30`. The worker must recheck the official channel and
include every added stable.

## Ready-State Rubric

- [x] Tom authorized the complete Research 308 currentness campaign.
- [x] g05.063 completed and its lifecycle closeout is on canonical `main`.
- [x] Current claim, official channel, complete planning-time hop set, and host
  observation are known.
- [x] No prompt, ACP session, login, installation, host update, or provider
  credit is needed.

## Work

1. Re-probe npm `latest`, dist-tags, and the complete published version set;
   observe the installed executable through `--version` only. Retrieve package
   and platform artifacts for `1.0.5` through every stable ending at `1.0.30`
   into `/tmp`. Freeze Research 314 and a secret-free exact package, platform,
   revision, tree, and selected-file identity corpus without executing any
   downloaded artifact.
2. Classify every shipped file feeding selected `grok agent stdio` behavior:
   ACP initialize and authentication state, cached-token session creation,
   prompt and activity flow, model option/selection, permission requests,
   registered tools and the MCP courier, attachments, cancellation, typed
   failures, cleanup, lifecycle, configuration, and process ownership. Commit
   identity evidence before production claim edits. Changelogs are discovery
   only.
3. Record one Contract 029 segment decision for the ACP route. Extend only the
   existing maintained segment where complete all-hop evidence admits it.
   Preserve feature-specific exact sets unless their selected surface is
   directly proved. Do not change the exact `1.0.25` catalogue claim or infer
   ACP execution compatibility from it. Keep the registered-tool carrier and
   every other operation independently bounded.
4. A material public lifecycle, authority, authentication, permission, model,
   or selected-wire change stops with the smallest exact counterexample and a
   concrete follow-up. Recheck official stable immediately before the identity
   commit and push. Provider-free diagnosis and reruns remain normally allowed.

## Dispatch Manifest

| Field | Task 064 |
| --- | --- |
| Readiness | ready |
| Prerequisites | Contract 029; Research 163, 294, and 308; completed g05.063; clean pushed `main`; official npm package and platform artifacts |
| Completion conditions | every stable `1.0.6..=1.0.30` is frozen; identity precedes claim; one honest ACP segment outcome lands; historical gaps, separate operations, and feature bounds survive; exact-head review and named gates pass |
| Owned mutable paths | Grok selection/tests/fixtures under `crates/swallowtail-adapter-grok/**`; Research 314 and one research-index line; exact Grok prepared-guide, route-matrix, feature-matrix, architecture ceiling, standing-lane, `[Unreleased]`, identity-log, claim-log, and task-result lines; `PAPERCUTS.md` append only |
| Reserved shared closeout surfaces | lifecycle task record and generated projections; submitted-handoff deletion belongs to the repository hook |
| Forbidden paths | other adapters, contracts, historical research/releases, the `grok-build.catalogue` operation or its exact `1.0.25` claim, unrelated registered-tool semantics, public API expansion without a returned planning decision |
| Approved concurrent siblings | none on shared currentness surfaces; this campaign is serial |
| Worker capability class | evidence-first Rust currentness worker; official npm retrieval and deterministic package/platform comparison; no provider credentials |
| Acceptance evidence | npm dist-tags/version set, package/platform identities, host observation, complete all-hop mapped-surface ledger, mutation-sensitive fixtures, current docs/claim agreement |
| Review oracle | skipped stable, alpha treated as stable, unclassified mapped delta, changelog-only inference, lost historical gap, catalogue/ACP flattening, live-session reliance, or unjustified feature widening |
| Stop conditions | official-channel disagreement; major-line reset; mapped public lifecycle/authority/authentication/model change requiring policy; provider/live ACP evidence required |
| Escalation owner | operator via Chatterbox for new policy; Queue coordinator for mechanical blockers |

## Boundaries

One family and the ACP route only. No prompt, inference, ACP initialize, live
session, catalogue listing, login, credentials, install, host update,
downloaded-artifact execution, release, tag, publication, or consumer mutation.
The installed executable is observation only. npm `alpha` `1.0.31` is not
stable authority. The ACP registry does not override the npm stable channel.

## Validation

- `cargo fmt -p swallowtail-adapter-grok -- --check`
- `effigy validate:focused swallowtail-adapter-grok`
- `effigy package:verify-affected swallowtail-adapter-grok`
- `effigy check:examples`
- `effigy package:api`
- `effigy qa:routes`
- `effigy qa:northstar`
- relevant research/log/roadmap/number/lifecycle/next-action checks
- `git diff --check`

## Acceptance

- [ ] official identity and every published stable are reproducible
- [ ] every changed selected input is classified
- [ ] identity evidence precedes production claim edits
- [ ] the maintained segment and historical gaps remain truthful
- [ ] catalogue, registered-tool, and exact feature boundaries survive
- [ ] official stable is rechecked at both boundaries

## Result

Ready for Queue dispatch.

## Next Task

Return the exact outcome to Chatterbox, then compile and dispatch the Claude
Agent SDK tuple as the next Research 308 family without asking Tom to repeat
authorization.
