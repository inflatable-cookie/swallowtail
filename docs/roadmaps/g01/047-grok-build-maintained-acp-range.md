# 047 Grok Build Maintained ACP Range

Status: moved to backlog
Owner: Tom
Created: 2026-07-24
Depends on: g01.046
Vision tags: broad harness coverage, maintained compatibility, explicit authority
Contract refs: 005, 006, 009, 010, 011, 012, 013, 015, 017, 023, 029, 032, 033, 034
Planning state: backlog

## Purpose

Add the first xAI harness route and prove maintained-range handling against a
high-frequency ACP release stream without treating provider permissions as a
sandbox or read-only containment.

## Generation Runway

Keep g01 active. Roadmap 047 takes it to 47 numbered roadmaps, inside the
documented 30-50 range. Closeout returns to a deliberate generation checkpoint
before another material lane.

## Goals

- [x] Freeze exact Grok Build package, executable, ACP, authentication,
      configuration, permission, retention, model, and update evidence.
- [x] Publish only evidence-backed compatibility segments, milestones,
      exclusions, and unverified-newer posture.
- [ ] Qualify one activation-only delegated subscription-auth lifecycle.
- [ ] Add exact host-approved installed discovery and private version dispatch.
- [ ] Implement one explicit ambient Grok ACP v1 interactive driver.
- [ ] Prove local and remote-authoritative topology with fixture-first
      lifecycle and no live authentication.

## Non-Goals

- a direct xAI API route; the existing xAI adapter remains separate
- automatic installation, upgrade, downgrade, registry execution, or package
  manager ownership
- API-key injection, login, device auth, logout, or auth-status orchestration
- Swallowtail-owned workspace writes, shell, web search, subagents, memory,
  plugins, hooks, MCP mutation, or custom endpoints
- a bounded read-only guarantee from provider permissions
- a sandbox, container, VM, or process-containment requirement
- inference of compatibility from package semver
- Nucleus or Soundcheck edits

## Execution Plan

### Batch 47.1 — Exact Range Corpus

- [x] Execute card 137.
- [x] Freeze exact registry, npm, executable, protocol, permission,
      configuration, retention, and model evidence.
- [x] Record that no release is qualified; `0.2.111` remains the sole candidate
      behind exact authentication and ambient-access evidence.

### Batch 47.2 — Delegated Authentication And Access

- [ ] Resume held card 138 only through an explicit later operator decision.
- [ ] Prove activation-only use of one pre-existing delegated subscription
      credential without login, mechanism switching, or model work.
- [ ] Promote Spec 003 only if exact evidence supports a shared contract.
- [ ] Retain explicit `AmbientHost` and ambient configuration without a
      bounded read-only claim.

### Batch 47.3 — Discovery And Dispatch

- [ ] Execute card 139.
- [ ] Add host-approved `grok --no-auto-update --version` discovery.
- [ ] Bind exact version assessment and private milestone dispatch into
      configured instance and preflight.

### Batch 47.4 — Production ACP Driver

- [ ] Execute card 140.
- [ ] Add the separate `swallowtail-adapter-grok` crate.
- [ ] Implement explicit ambient ACP v1 sessions over owned stdio with
      qualified delegated OAuth activation.

### Batch 47.5 — Portability And Closeout

- [ ] Execute card 141.
- [ ] Run unchanged profile and provider assertions under both host identities.
- [ ] Close the roadmap and return to a generation-boundary checkpoint.

## Acceptance Criteria

- [ ] exact package and executable versions remain separate from ACP wire,
      schema, SDK, registry, model, and configured-instance versions
- [ ] the support claim covers only proven releases and milestones
- [ ] later exact stable releases may be unverified without becoming qualified
- [ ] ACP v2 Draft and new optional v1 methods do not widen the selected subset
- [ ] permissions, configuration, retention, access, and isolation remain
      independently bound
- [ ] the first route performs no login, installation, update, fallback,
      read-only, or containment claim
- [ ] local and remote-authoritative cleanup joins process, callback, resource,
      state, and credential work
- [ ] one generation checkpoint remains next

## Planning Gaps

No release is qualified. Exact `0.2.111` advertises an authentication method
that differs from the maintained public example, and session allocation
requires a separate authentication request. The operator authorized the
no-prompt delegated-auth probe, but this host has no installed Grok command or
pre-existing Grok state. Card 138 remains blocked pending independently
provisioned exact `0.2.111` subscription state or matching maintained
documentation. Cards 139-141 remain planned.

## Hold Decision

The operator placed this lane on hold on 2026-07-24 because no Grok account is
available for the required delegated-authentication proof. This is not a
support rejection. Card 137 and its exact corpus remain valid; cards 138-141
remain resumable if matching authenticated state or maintained evidence later
becomes available.

Roadmap 048 completed the separate provider-coverage continuation without
absorbing or weakening this authentication boundary. The roadmap-049
checkpoint rejected spending roadmap 050 on an externally blocked duplicate.
The operator approved g01 closure on 2026-07-24. This lane now lives in the
[roadmap backlog](../backlog/grok-build-maintained-acp-range.md); cards 138-141
stay with g01 as backlog evidence and retain their exact promotion gate.

## Evidence Required

- official xAI docs and live ACP registry snapshot
- exact npm launcher and platform-artifact identities
- frozen normalized ACP transcripts for every selected behavior milestone
- separately gated activation-only delegated-auth evidence
- published-version gap and exclusion evidence
- installed discovery, private dispatch, topology, redaction, and cleanup tests
- full repository QA and doctor delta at closeout
