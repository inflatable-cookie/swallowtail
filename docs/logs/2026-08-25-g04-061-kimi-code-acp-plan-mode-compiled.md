# 2026-08-25 g04.061 Kimi Code ACP Plan Mode Compiled

Status: compiled
Owner: Tom
Milestone: g04.061
Cards: 170-172

## What Changed

- reconciled g04.060 merge state and the stale batch/generation indexes
- reassessed the remaining promoted per-route feature inventory after g04.060
- selected Kimi Code ACP negotiated `HarnessMode::Plan`
- compiled g04.061 and cards 170-172 as one serial evidence-first worker lane
- reserved Research 208 and the route-local closeout before dispatch
- kept g04 open and Contract 029 currentness standing

## Selection Basis

Exact official Kimi Code `0.38.0` source already advertises one `mode` select
with `default|plan|auto|yolo`. The exact dispatcher maps `plan` to
`setPlanMode(true)` plus manual permission and rebuilds the option snapshot
after selection. Swallowtail already owns the typed `HarnessMode::Plan`, exact
plan constraint, and negotiated-session-option contract.

This is a stronger fit than the remaining authority-widening or model-semantic
gaps. It uses the selected ACP transport and the same bounded pre-readiness
configuration exchange already proved for Kimi reasoning. It does not expose
provider `auto|yolo`, generic configuration, or a sandbox claim.

The version floor remains an evidence gate. Card 170 must identify the first
qualified version with the complete mode option, request, application, and
effective confirmation path. Cards 171-172 continue only for a non-empty exact
Research 208 set.

## Reassessment Notes

llama.cpp owned reasoning remains blocked on exact model/template semantics.
Codex verbosity still lacks exact selected-model support evidence. Anthropic
adaptive thinking needs private thinking-block stream and continuation
preservation work. Kiro profiles, OpenCode permissions, sandbox families,
skills/memory, and multi-agent controls widen authority or need containment
evidence. Ollama `think=max` still maps to `high` on the relevant exact family.

## Validation Plan

Card 170 uses exact public source and secret-free fixtures only. If it promotes
a non-empty set, cards 171-172 run focused Kimi formatting, validation,
affected-package verification, examples, route/docs/API/index checks, doctor,
and diff checks. No Kimi install, OAuth mutation, login, provider prompt,
external inference request, or paid work is part of default QA.

## Next Move

Execute g04.061 cards 170-172 serially in one isolated worker worktree and open
one PR. Stop honestly after card 170 if Research 208 is empty or the exact
version/application/confirmation gate fails. Do not merge or close g04.
