# Oh My Pi 17.3.8 currentness corpus

Secret-free identity freeze for npm `@oh-my-pi/pi-coding-agent@17.3.8`
before Swallowtail widens `oh-my-pi.package`.

Official artifacts stayed in `/tmp`. Extracted `--version` was not run:
the unpacked tarball needs `@oh-my-pi/pi-natives`. Host install stayed
`omp/17.2.15`. No provider prompt.

Selected `--mode rpc` flags and RPC v2 commands remain. GitHub
`docs/rpc.md` at `v17.3.8` has the same blob as `v17.3.7`. Decoder
specimen stays `oh-my-pi-rpc-17.2.9`. The frozen `17.3.7` corpus stays.
