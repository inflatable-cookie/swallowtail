# 176 Kimi Code Headless Reasoning-Effort Evidence

Status: complete; evidence stop
Owner: Tom
Created: 2026-08-25
Updated: 2026-08-25
Milestone: [g04.063 Kimi Code Headless Reasoning Effort](../063-kimi-code-headless-reasoning-effort.md)
Depends on: Research 017, 046, 056, 066, 068, 074, 159, 179; Contracts 029, 040, 044

## Goal

Determine the exact Kimi Code headless versions, selected models, and effort
values that can be set process-locally without silent fallback, ambient
override, config mutation, or thought disclosure. Promote an honest empty set
if any required fact remains unproved.

## Work

1. Retrieve and digest exact official 0.38.0 documentation and selected source
   for configuration, model resolution, thinking effort, environment parsing,
   command startup, stream-json rendering, and failures. Inspect earlier exact
   qualified tags only when needed to establish a version floor.
2. Record retrieval dates and decisive body or package hashes in Research 210.
   Official packages may be inspected in a disposable directory; do not
   install or execute a prompt.
3. Identify the exact `KIMI_MODEL_*` key or other process-temporary surface,
   parse point, precedence, scope, and lifetime. Distinguish child-only env,
   inherited env, user config, model config, CLI flags, and defaults.
4. Freeze each exact model's `support_efforts`, `default_effort`, provider,
   aliases, and selected-model agreement. Do not prequalify
   `off|on|low|medium|high|xhigh|max` from names alone.
5. Classify missing, empty, malformed, unknown, unsupported, aliased, clamped,
   shadowed, and fallback values. A candidate row is deliverable only if the
   selected value cannot silently become another value or the model default.
6. Audit the current headless builder, prepared evidence, model selection,
   environment ownership, access, one-child boundary, output parser, retry,
   retention, restoration, cancellation, diagnostics, and fixtures.
7. Freeze stream-json disclosure truth, including whether thinking content is
   absent, suppressed, or merely undocumented. Do not promote thought output
   or `ReasoningSummary` activity.
8. Separate planned, dispatched, accepted, effective, and observed claims.
   Name the strongest exact boundary available without a live provider call.
9. Promote Research 210 with one exact deliver-now table or an explicit empty
   set. Update milestone/card state and the reserved closeout honestly.

## Acceptance Criteria

- [x] exact official source bodies/packages, dates, and hashes are recorded
- [x] exact environment/config key, parser, precedence, and lifetime are named
- [x] exact version/model/value support and defaults are explicit
- [x] fallback, alias, clamp, ignore, and ambient override are settled
- [x] stream-json disclosure and provider-state lifetime are explicit
- [x] claim strength is split by planned/dispatched/accepted/effective/observed
- [x] Research 210 contains a non-empty exact table or honest empty set
- [x] no production code, public API, shared contract/runtime, currentness,
      release, merge, generation rollover, or g04 closure changes
- [x] `effigy validate:focused swallowtail-adapter-kimi`, `effigy qa:northstar`,
      relevant indexes, and `git diff --check` pass

## Stop Conditions

- exact process-local transport, precedence, model/value support, or no-
  fallback behavior remains ambiguous
- delivery needs live provider work, user config mutation, synthetic config,
  generic environment access, public thought content, or shared authority

## Out Of Scope

- production binding, ACP/local-server/Python promotion, unrelated Kimi
  controls, live access, currentness, release, merge, rollover, or g04 closure

## Closeout

Research 210 is an honest empty deliver-now set. Exact `0.38.0` documents
`KIMI_MODEL_THINKING_EFFORT` on both the default agent-core-v2 headless path
(`runV2Print` unless `KIMI_CODE_LEGACY_FLAG` is truthy) and the legacy
agent-core path, but headless lacks a fail-closed confirmation transport,
ambient `thinking.enabled = false` disables the override on both engines,
Kimi-protocol normalization can substitute `default_effort` before env overlay,
and there is no headless catalogue snapshot for selected-model agreement.
Swallowtail's qualified v1 source mapping does not force legacy dispatch.
Cards 177-178 stay blocked. No headless reasoning-effort feature ships.
