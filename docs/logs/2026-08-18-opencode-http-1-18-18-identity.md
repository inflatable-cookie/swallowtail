# 2026-08-18 OpenCode HTTP 1.18.18 Identity

## Result

Card 246 froze host/npm OpenCode `1.18.18` against qualified `1.18.10`.
Selected six-route operation objects, delete, import, and continuity
closures are unchanged. `GET /provider`'s transitive Model interleaved
field enum changed, so the next card adds private `surface-19`. No
provider prompt. The attached server was not started.

## Next

Card 247 raises the `opencode.server` ceiling through `1.18.18`.
