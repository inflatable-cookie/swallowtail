# 2026-07-28 Session Continuity Closeout And Pi Selection

Correction: Research 053 and the later Pi resource-gate log supersede the two
contract-ready Pi classifications below. The matrix remained `No`; only the
planned continuation changed.

## Decision

Close the first session-continuity tranche at five realized conversions.
Select Pi RPC load and resume next. Keep Alibaba Conversations and Anthropic
Managed Agent continuity behind a separate retained-provider-session contract
gate.

## Re-Audit

The 58 starting cells now contain:

- five realized conversions
- two remaining contract-ready Pi RPC cells
- four retained hosted-session contract gaps
- one Gemini CLI ACP upstream-ordering block
- ten exact selected-route absences
- 36 operation-shape mismatches

The matrix retains 53 continuity `No` cells: 17 load, 16 resume, and 20 native
close. It contains 432 `No` and 29 `Not applicable` cells overall. All 20
native-close absences remain honest.

## Package Evidence

The dirty-source package gate assembled all 23 archives, compiled the locked
extracted workspace and complete extracted test graph, and executed selected
packaged facade suites. Codex prepared profiles, Claude Agent prepared
facades, and OpenCode prepared facades cover all five converted cells.

Public API baselines changed only for those three adapter crates. API,
examples, docs, Northstar, routes, formatting, lint, focused suites, and the
workspace pass without live access.

## Pi Boundary

Exact Pi `0.80.10` RPC exposes `switch_session`, `get_state`, `get_messages`,
and append-order `get_entries`. The current Swallowtail Pi profile uses
`--no-session` and prohibits provider state. Roadmap 029 therefore requires a
separate persistent-session profile rather than widening the ephemeral path.

Card 096 must revalidate the maintained release, freeze exact replay and
attachment semantics, and confirm the existing contract fit before
implementation.

## Remaining Risks

- Pi extension hooks may cancel a session switch; fixtures must preserve that
  outcome.
- Raw session paths and ids are not attachment authority.
- Process exit remains local cleanup, not provider-native close.
- Unverified-newer releases remain mileage-may-vary under explicit posture,
  not guaranteed continuity support.

## Next

Execute card 096.
