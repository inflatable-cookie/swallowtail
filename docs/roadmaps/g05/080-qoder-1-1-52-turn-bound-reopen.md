# g05.080 Qoder 1.1.52 Turn-Bound Reopen

Status: ready
Owner: Tom
Created: 2026-09-17
Governing refs: Contract 029; Contracts 017 and 023
Depends on: completed g05.070 (typed stop and Research 318); the recorded turn-binding ruling in `docs/roadmaps/standing-lanes.md`

## Outcome

Reopen the exact `qoder.package` point under the operator's recorded
turn-binding ruling and land one honest `qoder.headless` claim at the
then-current published stable. The route's `--max-turns` value becomes a
declared, deliberate adapter-owned bound instead of an inherited accident, and
the `error_max_turns` terminal shape is recorded in the Qoder prepared guide.

## Ready-State Rubric

- [x] g05.070 froze the exact `1.1.25` baseline and all 27 published stable
  successors through `1.1.52` with Research 318 and a mutation-sensitive
  fixture.
- [x] The turn-binding ruling is recorded and names the exact reopen
  requirement: pin an explicit deliberate value and record the bound.
- [x] No prompt, login, credential, installation, or host update is needed for
  the identity and claim decision.
- [x] Scope, acceptance, validation, evidence, and stop conditions are explicit.
- [x] Review oracle is present because the claim is exact, version-bound, and
  partly negative.
- [ ] Official stable is re-probed immediately before the identity commit.

## Decisions

None open. The ruling settles the policy question: the route accepts a declared
turn bound but not an inherited one. This task settles only the concrete value
and its documentation.

## Dispatch manifest

- **State:** ready; provider-free; one lane; serial against other currentness
  families on shared matrices; no automatic successor.
- **Completion:** the adapter pins one explicit `--max-turns` value with a
  recorded rationale; the bound and the `error_max_turns` terminal shape are in
  the Qoder prepared guide; one exact `QualifiedOnly` point lands at the
  then-current published stable with the behavior revision justified; focused
  validation, route and matrix QA, and named docs gates pass; independent
  exact-head review accepts the head.
- **Owned mutable paths:** Qoder selection, prepared route, argv, decoder, and
  fixtures under `crates/swallowtail-adapter-qoder/**`; the new Research record
  and one research-index line; the Qoder prepared guide, route/activity/feature
  matrix rows, architecture ceiling, standing-lane entry, `CHANGELOG.md`
  `[Unreleased]`, identity log, claim log, and this task's result lines;
  `PAPERCUTS.md` append only.
- **Reserved closeout surfaces:** lifecycle task record and generated
  projections; submitted-handoff deletion belongs to the repository hook.
- **Worker:** evidence-first Rust/node currentness worker; exact-pin retrieval,
  classification, and validation; no provider credentials.
- **Excluded:** Qoder ACP, SDK, TUI, or IDE surfaces; skill visibility and the
  g05.039/g05.040 gate; any live print run; release, tag, or publication;
  consumer mutation; inference of a compatibility range from semver continuity.
- **Escalation:** operator via Chatterbox for any new route-policy choice;
  Queue coordinator for mechanical blockers.

## Work

1. Re-probe official npm stable and dist-tags. Freeze identity for the
   then-current published stable and re-confirm the `1.1.30` hop that made the
   argv value authoritative.
2. Choose the `--max-turns` value deliberately. Justify it against the route's
   bounded-limit posture rather than inheriting `8`, and prove with a
   mutation-sensitive fixture that the argv value is what bounds the AgentLoop.
3. Record the chosen bound and the `error_max_turns` terminal shape in the Qoder
   prepared guide, and reconcile the decoder, prepared-route, matrix, and
   architecture surfaces that the bound touches.
4. Commit identity before the claim. Rebind one exact `QualifiedOnly` point and
   keep or mint the behavior revision only as the mapped wire and lifecycle
   require. Do not retain `1.1.25` as a second point to hide incomplete
   evidence.
5. Stop with the smallest exact counterexample if a mapped wire, lifecycle,
   permission, retention, failure, or process-authority boundary changed
   materially beyond the turn binding.

## Acceptance and review oracle

| Invariant | Adversarial counterexample | Required proof |
| --- | --- | --- |
| The route's turn ceiling is a declared adapter-owned value | The adapter still inherits `8` because 1.1.30 made an old default real | argv assertion plus the guide's recorded bound |
| The bound is enforced by the route, not merely described | A run exceeds the bound without an `error_max_turns` terminal | mutation-sensitive fixture over the forwarded option |
| The claim stays one exact point | A range is inferred across the 27 published successors | selection table shows one exact point and the behavior revision |
| Skill visibility is untouched | Package skill files are promoted into selected-run visibility | Research 256's empty disposition and the g05.039/g05.040 gate stand |

## Stop conditions

- Stop on official-channel disagreement or an unreachable channel; record the
  gap rather than guessing.
- Stop if the selected public lifecycle, authority, permission, retention, or
  failure surface changed beyond the turn binding.
- Stop and escalate if pinning the bound requires a new driver, facade, or
  public lifecycle.

## Evidence

On completion, record the identity ledger, the chosen bound and rationale, the
fixture result, validation actually run, PR link, reviewed exact head, merge
commit, and any material limit.

## Next task

Nothing is auto-started. Chatterbox resumes planning on the returned result and
reconciles the standing lane.
