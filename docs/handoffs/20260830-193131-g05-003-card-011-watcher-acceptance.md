---
title: g05.003 card 011 watcher acceptance worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Tom
created: 2026-08-30
updated: 2026-08-30
handoff_path: /Users/tom/Dev/projects/swallowtail/docs/handoffs/20260830-193131-g05-003-card-011-watcher-acceptance.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, rust, claude-code, watcher, live-proof]
---

## What This Thread Was Doing

Card 010 landed the exact Claude Code `2.1.251` watcher candidate binding with
private MCP, settings, skill, Stop continuation, and credential-free fixtures.
Planning then compiled the smallest live counterexample and a one-attempt
authority envelope. The operator has now authorized that attempt and requested
a cheap model.

This dispatches card 011 only. The worker completes the deterministic
acceptance surface, then consumes at most one live Claude provider session with
exact `claude-haiku-4-5`. It returns one PR after proof or one sanitized stop
report after failure. No transcript or second prompt is part of the authority
chain.

## Why It Matters

Swallowtail cannot claim dependable watcher support merely because its local
fixtures can block terminal completion. The exact provider must demonstrate
that an early Stop is blocked, active state returns to the same conversation,
the model explicitly waits or stops, and only joined cleanup admits success.
That is the route proof consumer-facing activity depends on.

## Current State

- **Repository:** `inflatable-cookie/swallowtail`
- **Planning branch:** `main`
- **Planning base commit:** `0d3b2e7277edc60550e5e5ce5ee7be0abaf300ca`
- **Pushed main verification:** local `HEAD` and `origin/main` both resolved to
  `0d3b2e7277edc60550e5e5ce5ee7be0abaf300ca` before this handoff was created
- **Planning checkout:** clean before the readiness and handoff edits
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight
- **Planning artifacts included at the base:** completed card 010 and its
  post-merge card 011 readiness reassessment. This handoff commit adds the
  operator authorization, ready-card state, dispatch log, and this file
- **Worker branch:** `worker/g05-003-card-011-watcher-acceptance`
- **Worker worktree:** `/Users/tom/Dev/worktrees/swallowtail-g05-003-card-011-watcher-acceptance`
- **Worktree creation command:** `git worktree add -b worker/g05-003-card-011-watcher-acceptance /Users/tom/Dev/worktrees/swallowtail-g05-003-card-011-watcher-acceptance origin/main`
- **Worker worktree policy:** use a clean launcher-provided registered
  non-`main` worktree first. Names may differ from the fallback above
- **Required sibling worktree links:** none
- **Active spec lane:** none; Contracts 059-060 and g05.003 are canonical
- **Roadmap milestone:** `docs/roadmaps/g05/003-operation-scoped-watcher-proof.md`
- **Ready cards, in order:**
  `docs/roadmaps/g05/batch-cards/011-watcher-acceptance-and-consumer-projection.md`
  only
- **Allowed runway:** card 011 deterministic watcher acceptance, one exact
  live same-turn proof, and claim/docs closeout conditional on that proof
- **Remaining card budget:** one card; one live provider session; one PR or one
  sanitized stop report
- **Dispatch topology:** serial single-card lane
- **Parallel safety check:** live authority is deliberately single-use and the
  route-feature projection census is outside this lane. No parallel worker may
  run Claude, edit card 011, or edit its claim/closeout surfaces
- **Canonical refs:** `docs/contracts/001-working-rules.md`,
  `docs/contracts/009-async-operation-lifecycle.md`,
  `docs/contracts/010-execution-host-services-and-inputs.md`,
  `docs/contracts/012-interactive-session-options-and-callback-exchange.md`,
  `docs/contracts/013-interactive-session-access-policy.md`,
  `docs/contracts/023-harness-operation-isolation-and-native-boundary.md`,
  `docs/contracts/041-input-callback-and-provider-tool-admission.md`,
  `docs/contracts/044-observable-agent-activity-and-disclosure.md`,
  `docs/contracts/059-operation-scoped-process-watchers.md`, and
  `docs/contracts/060-operation-scoped-watcher-http-bridge.md`
- **Evidence refs:**
  `docs/research/226-claude-code-headless-maximum-turns-evidence.md`,
  `docs/research/241-claude-code-headless-spend-cap-evidence.md`,
  `docs/research/257-claude-code-watcher-seam-evidence.md`,
  `docs/research/260-claude-code-watcher-bridge-transport.md`, and
  `docs/research/261-claude-code-2-1-251-identity.md`
- **Review oracle:** card 011 `## Review Oracle`; especially the smallest
  early-final counterexample and required event ordering
- **Model capability profile:** capable Rust implementation and integration
  worker with exact lifecycle, privacy, and live-evidence judgment
- **Tool/runtime restrictions:** no subagents; no provider access until every
  credential-free implementation and deterministic check passes; then one
  provider session only through the new explicit probe selector. Pin exact
  `claude-haiku-4-5` and a 90-second operation deadline.
  Do not use a moving model alias, fall back to another model, run an existing
  live probe, retry, inspect credentials, login, install/update Claude, mutate
  ambient settings, use shared project material, bind `--max-budget-usd` or
  the separately unqualified-at-`2.1.251` maximum-turns feature,
  merge, release, or start another card
- **Inherited health baseline:** `effigy doctor` reports 390 god-file findings:
  341 warnings and 49 errors, plus one generated-in-source warning and a stale
  graph index. Do not widen this lane into structural cleanup
- **Required validation:**
  `cargo fmt -p swallowtail-adapter-claude-agent -p swallowtail-host-local -p swallowtail-testkit`;
  `effigy validate:focused swallowtail-adapter-claude-agent swallowtail-host-local swallowtail-testkit`;
  `effigy package:verify-affected swallowtail-adapter-claude-agent swallowtail-host-local swallowtail-testkit`;
  credential-free deterministic tests required by card 011;
  one exact opt-in `effigy probe:claude-code-watcher-live` after all prior
  checks pass; `effigy qa:docs`; `effigy qa:northstar`; `git diff --check`
- **PR base/head:** current pushed `main` / selected worker branch
- **PR URL:** pending
- **Review state:** awaiting implementation, single live proof, and exact head
- **Merge authorization:** not authorized

## Boundaries

- **In scope:** add one ignored live watcher test and explicit Effigy selector;
  pin installed Claude Code `2.1.251`, its frozen native digest
  `625869b01e0050f260b2980fac248fd9cef9e462612bded4ec9d3d49ff8969a5`,
  exact `claude-haiku-4-5` and a 90-second deadline; run one
  host-approved bounded watcher with no external-network need; prove the card's
  live event ordering; complete deterministic multi-watcher, controls, races,
  failures, privacy, omission, and cleanup acceptance; update route matrix,
  guides, card, milestone, log, and sole Next Task only after all proof passes.
- **Out of scope:** a second provider request; Sonnet, Opus, alias, or fallback
  model use; API-key billing; `--max-budget-usd`; provider setup; raw process or
  provider material; wider Claude watcher version ranges; cohesive consumer
  route-feature projection; skill discovery; contract changes; arbitrary
  processes; containment or Docker; unrelated cleanup; release work; merge;
  another card.
- **Outcome shape:** one complete contract-valid route proof and reviewable PR,
  or one sanitized evidence stop. Diagnostics alone do not complete a success
  lane. A failed provider request consumes the live authorization and forbids
  retry.
- Complete and validate the test harness before the live request. The identity
  and digest preflight may run without consuming the provider attempt. If exact
  identity, model availability, or existing authentication cannot be confirmed
  without a provider request, stop before contacting Claude.
- Once any request reaches Claude, the single attempt is consumed regardless of
  provider, hook, model, deadline, or test outcome. Never rerun the selector.
- Do not expose commands, arguments, paths, environment, PID, raw output,
  endpoint, bearer, provider payload, credential material, or source artifacts
  in public events, docs, fixtures, logs, PR text, or the stop report.
- The live watcher operation must be pre-approved, local, bounded, inert, and
  require no network. It must last long enough for the model's early completion
  attempt to encounter active state, then permit explicit wait or stop and
  joined cleanup.
- Do not invent architecture, change contracts, widen the roadmap, or choose a
  new product/API/persistence/security policy.
- Work only in the clean worker worktree selected by `Completion Protocol`.
  Never edit the planning checkout or an unrelated dirty checkout.
- Do not merge the PR. Merge remains separately operator-authorized.

## Important Context

- **Planning lineage:** Research 257 admitted Claude's private MCP and Stop
  seam. Cards 008-010 and 014-016 landed lifecycle, supervision, HTTP bridge,
  and exact credential-free route binding. Research 260 froze exact
  `2.1.251`; Research 261 qualified both base-route axes. The post-card-010
  reassessment compiled the one-attempt oracle. The operator then authorized
  one cheap-model live attempt.
- **Why this card is ready:** every technical prerequisite and review oracle is
  explicit. The only former gate was external provider authority, now granted
  once for exact Haiku through existing local state.
- **Decisions and preferences:** manual Northstar worker/PR loop; no internal
  subagents. Exact `claude-haiku-4-5`, not the moving `haiku` alias. The
  maximum-turns feature is not qualified at the exact route point; one provider
  session and the 90-second deadline bound the probe. Research 241 forbids
  pretending `--max-budget-usd` meters subscription allowance. Docker and hard
  process containment are not part of the feature.
- **Open tensions:** provider behavior may not follow the prompt or may expose a
  Stop-hook limitation. That is evidence, not permission to retry, weaken the
  invariant, auto-wait locally, or convert terminal success to a late failure.
  Claim only the exact live-proved point.
- **Report after:** credential-free implementation and deterministic acceptance
  are green, before the sole live request; then report the live result and PR
  or sanitized stop
- **Report to:** the operator, who will relay progress to the orchestrator

## Suggested Next Move

Run the `Completion Protocol` preflight before broad reads. Trace card 010's
watcher binding, current ignored live-probe pattern, host-approved watcher
fixture, model selection, operation deadline, event capture, Stop
continuation, and cleanup. Implement the smallest dedicated live test and
selector, then close every deterministic acceptance row and falsify the diff.

Do not contact Claude during implementation. When the credential-free surface
is green, re-probe the exact installed path/version/digest. Only then run the
new selector once. If it passes, finalize exact claims and open the PR. If it
fails after contact, withhold claims and return the sanitized stop report.

## Completion Protocol

### Before you start

1. This handoff's `worker_mode: implementation` and
   `dispatch_authority: orchestrator` activate worker mode. Before broad reads,
   run `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. If the current root is a registered worktree, its status is empty, and its
   branch is not `main`, accept it as launcher-provided. Record its actual
   root/branch; do not create another worktree because names differ.
3. If current context is `main`, dirty, unregistered, or unusable, inspect the
   named worktree. If unusable, read `.agents.local.env`, require
   `AGENTS_WORKTREE_CONTAINER_DIR`, and ask the operator when absent. Create a
   unique worktree/branch there from pushed `origin/main`. Never use `/tmp`,
   clean, reset, stash over, or discard dirty state. Report a launcher-supplied
   dirty or `main` worktree instead of creating another.
4. From the selected worktree, record this handoff's repository-relative path.
   Confirm `HEAD == origin/main`, confirm
   `git merge-base --is-ancestor 0d3b2e7277edc60550e5e5ce5ee7be0abaf300ca HEAD`,
   and load the tracked handoff with
   `git show HEAD:docs/handoffs/20260830-193131-g05-003-card-011-watcher-acceptance.md`.
   If the absolute dispatch file differs, stop. The tracked copy is canonical.
5. Required sibling worktree links are `none`.
6. Read `AGENTS.md`, `PAPERCUTS.md`, g05.003, card 011, Contracts 001, 009,
   010, 012, 013, 023, 041, 044, 059, 060, Research 226, 241, 257, 260, 261, the
   card 010 implementation and log, the card 011 readiness and authorization
   logs, watcher bridge/registry/process code, current matrix/guides, and the
   Rust-quality profile.
7. Run `effigy tasks`, `effigy doctor`, and `effigy test --plan`. Record the
   inherited baseline; do not execute the broad workspace plan.

### While you work

- Add the ignored live test and explicit `probe:claude-code-watcher-live`
  selector. The test must require `SWALLOWTAIL_LIVE_CLAUDE_CODE_WATCHER=1`,
  reject `ANTHROPIC_API_KEY`, pin exact version/digest/model, use a 90-second
  deadline, snapshot source state, and redact all evidence.
- Build only the single approved watcher operation into the local host fixture.
  Do not allow arbitrary executable, command, shell, arguments, environment,
  paths, network, PID, or raw output through public input or records.
- Prove deterministic acceptance first: multiple bounded watchers; explicit
  model and operator wait/stop; completion races; hook rejection; cancellation;
  deadline; provider failure; bounded redacted summaries; unchanged omission;
  terminal state; joined process/bridge/private-resource cleanup.
- Falsify the exact oracle before live use. In particular, ensure the system
  does not auto-wait, admit early success, or merely replace an already-final
  provider success with local failure.
- Report the credential-free checkpoint to the operator. Then perform identity
  and digest preflight. Stop without contact on drift, unavailable exact Haiku,
  login/setup need, dirty source, missing authority, or a deterministic gap.
- Run only `effigy probe:claude-code-watcher-live`, once. Do not run the
  response-only probe or any other provider test. A request reaching Claude
  consumes authorization. Never rerun it in this handoff.
- Record only sanitized ordering: watcher start; Stop block; same-conversation
  continuation; explicit wait or stop; zero active/unjoined; clean provider
  success; joined bridge/process cleanup. Do not persist raw payload or process
  material.
- On live success, finish the exact `2.1.251` claim, matrix/guide truth, card,
  milestone, log, index, and sole Next Task. On live failure, withhold every
  claim and return the sanitized stop report. Do not open a claim PR based only
  on deterministic evidence.
- Stop on a missing contract seam, privacy leak, identity drift, a second-attempt
  need, or any wider architecture/policy decision.

### When the assigned runway is complete

1. Run every command under Required validation. The live selector is run once
   only and only after the credential-free commands pass.
2. Try to falsify the diff against card 011. Enumerate exact, universal, and
   negative claims; exercise the smallest early-final counterexample; map each
   acceptance row to deterministic or sanitized live proof; reconcile card,
   milestone, log, handoff, matrix, guide, and front-door state.
3. If live proof passed, mark card 011 and g05.003 complete only when every
   acceptance row closes. Choose the next roadmap action from existing
   authority; do not promote the consumer projection note or close g05 without
   operator direction. If live proof failed, keep claims absent and preserve an
   honest stopped state.
4. Push the selected worker branch.
5. After live success, open one reviewable PR against current pushed `main`.
   After live failure, follow card 011's sanitized stop-report outcome and do
   not imply success. This handoff's planning base predates its own commit.
6. In a PR body, link the milestone, card, contracts, changed surfaces,
   sanitized event proof, validation, exact version/model point, and unresolved
   items. Never include secrets or raw provider/process material.
7. Report the PR URL and evidence, or the sanitized stop, to the operator. Do
   not merge or continue.

### Review and merge path

The orchestrator reviews an exact worker head against card 011, Contracts
059-060, the diff, deterministic proof, and sanitized live ordering. With a
shared GitHub identity, the verdict is a PR comment. Blocking findings use
`execution-miss`, `oracle-gap`, `planning-change`, `validation-gap`, or
`integration-drift`. A `planning-change` returns to planning. Requested
changes are `none` until review. Any requested revision that would contact
Claude requires fresh operator authorization; the worker may not rerun the
live probe merely to satisfy review. Merge remains separately authorized.

- **Closeout refs:** card 011; g05.003; g05 and roadmaps front doors; logs
  index and card 011 closeout log; route matrix and guide only after proof

### Handoff closeout

Before calling the runway complete, leave card, roadmap, log, route claims,
and sole Next Task honest. On any live or authority stop, preserve the exact
sanitized blocker and do not make the handoff look complete.
