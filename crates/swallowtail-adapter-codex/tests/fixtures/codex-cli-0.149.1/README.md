# Codex CLI 0.149.1 currentness corpus

Secret-free identity for official npm `@openai/codex` `0.149.1` before
Swallowtail raises the `codex.cli` ceiling.

The mapped exec JSONL flags and app-server methods remain unchanged from
`0.149.0`. Generated v2 and experimental schema bundles are byte-identical.
`ModelListParams` is unchanged. Exec help differs only by unmapped
`--thread-source`. No decoder update required. Host install was not present.

No fixture contains a credential, host path, account identity, provider
payload, or real session id.
