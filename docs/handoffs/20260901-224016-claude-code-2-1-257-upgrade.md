---
title: Claude Code 2.1.257 useful-newer worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Tom
created: 2026-09-01
updated: 2026-09-01
handoff_path: /Users/tom/Dev/projects/swallowtail/docs/handoffs/20260901-224016-claude-code-2-1-257-upgrade.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, version-currentness, claude-code]
---

## Objective

Run the Contract 029 Upgrade Workflow for the Claude Code installed-harness
family only. Re-probe the official channel, freeze identity before claim, and
qualify the current official stable if the mapped headless and response-only
surfaces support an honest compatible extension or separately named milestone.

## State And Scope

- **Repository:** `/Users/tom/Dev/projects/swallowtail`
- **Required ancestor:** `2c1e2ef015413b52bb91301042a39ab7439179fd`
- **Worker branch:** `worker/version-currentness-claude-code-2-1-257`
- **Worker worktree:** Paseo-managed from pushed `origin/main`.
- **Worker class:** day-to-day. This is a bounded one-family identity and claim
  workflow using established Contract 029 patterns; frontier review follows
  because the result widens executable compatibility.
- **Authority:** `AGENTS.md`; `.cursor/skills/version-currentness/SKILL.md` and
  `reference.md`; Contract 029; `docs/roadmaps/standing-lanes.md`;
  `docs/roadmaps/README.md` Next Task; the latest Claude Code identity corpus,
  Research 266, g05.014 cards 037-038, current selection code, and prepared
  guide.
- **Planning observation:** npm official latest is `2.1.257` immediately before
  this handoff. The worker must re-probe rather than trust this observation.
- **Current claims:** headless and response-only latest-qualified are `2.1.252`;
  installed host observation is `2.1.251`.
- **Hard boundary:** watcher admission remains exact `2.1.251`. Do not change
  `CLAUDE_CODE_WATCHER_VERSION`, watcher live authorization, watcher digests,
  watcher mechanism planning, or imply newer watcher readiness.
- **In scope:** Claude Code identity/claim fixtures and proofs; Claude-agent
  adapter claim constants/tests needed for this family; required guide, matrix,
  changelog, research/log/roadmap/card/index/Next Task closeout surfaces.
- **Out of scope:** Claude Agent ACP, Gemini, Kimi, another family, watcher
  production behavior, provider prompts/login/live sessions, installs, host
  updates, contracts, public runtime/core/testkit API, and g05.009/card 034
  state or its 249/518 coverage counts.
- **Parallel partition:** the Gemini Papercuts worker owns only Gemini test
  modules and `PAPERCUTS.md`. This lane owns currentness research/log/roadmap
  and Claude-agent surfaces. No shared mutable path.
- **Merge ordering:** same-repository PRs merge one at a time; restack and
  revalidate if the Gemini papercut merges first.

## Evidence Method And Review Oracle

Use the hardened version-currentness skill from this base. Changelog and release
notes are discovery only. Enumerate every published stable hop after `2.1.252`
through the launch-time official latest. Extract official packages/assets to
`/tmp`; do not execute downloaded binaries.

If all shipped executable files feeding mapped behavior are byte-identical,
freeze a compact exact byte-identity ledger. If any mapped-feeding file changes
or the package tree/refactor shape defeats spot checks, derive a deterministic
recursive artifact inventory with exact per-hop file counts and
added/removed/changed/identical sets or digests. Inspect and classify every
changed file that can affect argv/help selection, app-server wire shape,
lifecycle, failure behavior, permissions, usage, capability advertisement,
session updates, config/mode, load/resume/close/delete, or response-only output.
Bound provider-internal and unmapped changes with mechanistic reasons.

Freeze mutation-sensitive exact file/key sets independently of self-authored
booleans. Keep the existing unpublished gaps, including `2.1.244` and
`2.1.249`, plus any independently unqualified points. Prove the first later
unpublished stable as `UnverifiedNewer` without treating it as a published
artifact. Prove `2.1.257` (or the re-probed official point) is rejected at both
watcher admission seams while the exact `2.1.251` watcher remains admitted.
Keep live-probe revisions independent of the moving route ceiling.

Identity must be the first commit with production claims still at `2.1.252`.
Claim follows only if the evidence closes. Recheck official latest after any
evidence repair and immediately before final push. If it moves, stop and ask;
do not silently chase it. Stop with claims unchanged if mapped behavior differs
without an already-authorized private milestone, identity cannot be
corroborated, a provider/live operation is needed, watcher authorization would
widen or break, a new public operation is required, or evidence remains
unclassified.

## Validation And Completion

Confirm a clean non-`main` worktree, `HEAD == origin/main`, the required
ancestor, and this tracked handoff before edits. Run the version-currentness
family mode for exactly one family.

At claim closeout run:

- `cargo fmt -p swallowtail-adapter-claude-agent -- --check`
- `effigy validate:focused swallowtail-adapter-claude-agent`
- `effigy package:verify-affected swallowtail-adapter-claude-agent`
- `effigy package:api`
- `effigy qa:routes`
- `effigy qa:northstar`
- research, logs, roadmaps, g05, batch-card, and next-action index selectors
- `effigy --json scan god-files`
- `git diff --check`

Falsify artifact identities, exact delta sets, mapped/unmapped classification,
gap membership, both moving ceilings, watcher rejection at the newly qualified
point, and identity-before-claim history. Keep changed files below configured
god-file thresholds or split them without changing semantics.

Push one reviewable PR with identity then claim commits. Report exact head/base,
official recheck, per-axis result, artifact inventory shape, mapped/unmapped
deltas, gaps, watcher proof, validation, public API result, god-file count, and
residuals. Do not merge and do not start another family.

