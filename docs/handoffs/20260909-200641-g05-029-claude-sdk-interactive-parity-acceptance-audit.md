---
title: g05.029 Claude SDK interactive parity acceptance audit
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
status: ready-to-launch
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Tom said 'Continue' on 2026-09-09 after the g05.031 closeout advanced docs/roadmaps/README.md to g05.029 as the sole Next Task; this authorizes the bounded evidence-only acceptance audit and closeout. It grants no provider call, consumer mutation, range widening, release, or tag authority."
queue:
  capability: general
  skipPRReview: false
  notifyOriginOnCloseout: true
---

## What This Thread Was Doing

Close `g05.029 Claude SDK Interactive Parity` by auditing the delivered runway
against all five parent Acceptance clauses. Publish one exact evidence ledger
and honest clause verdicts. Do not add features, repeat provider work, or reopen
the stopped qualified-range gate.

## Why It Matters

The route now has editing, permission modes, Bash, model/effort changes,
resume, MCP, diagnostics, and exact live registered-tool qualification. Those
pieces do not by themselves prove that a consumer drove the promised
multi-turn editing session, that later scope landed in priority order, or that
the `v0.4.1` carrier preserved every required default and gate.

## Current State

- Canonical task: `docs/roadmaps/g05/029-claude-sdk-interactive-parity.md`.
- Cards 080-086, 088, 089, 105, 108, 119-121, 124, and 153 are complete.
  Card 087 stopped because Research 287 admits no qualified range; five exact
  `QualifiedOnly` pins remain.
- Card 080 delivered permission selection, mid-session change, bypass
  rejection, ambient read-write editing, and provider-free pre-effect
  mediation through PRs #221 and #224. Card 081 added mediated Bash in #233.
- `v0.4.1` is tagged at `c3cce7504ffd5eae138a0190f1cd81332db68c3c`.
  Its release record says no application drove that candidate before tagging;
  the first real Desktop open then failed.
- Desktop Card 318 and Swallowtail Card 153 later qualified one live
  registered-tool turn on the exact `v0.4.4` source-linked tuple. Desktop Card
  323 ran later Claude controls. Do not infer editing, multiple turns, or
  permission-mode changes unless the immutable capsules actually record them.
- Durable merge order after Card 082 must be reconstructed rather than assumed:
  PRs #241/#242/#243/#244/#255 do not numerically follow the runway order.

## Boundaries

Documentation and read-only evidence only. Follow the task Dispatch table.
Do not edit runtime/test code, sidecars, manifests, baselines, guides, matrices,
contracts, research, releases, tags, or Desktop. Do not contact Claude or any
provider, consume credit, run an authenticated probe, mutate a consumer, or
widen the five exact version axes.

## Important Context

Score each Acceptance clause literally and independently. Provider-free fake
SDK tests may prove mediation mechanics and permission selection, but cannot
alone satisfy “a consumer drives.” A live registered-tool turn cannot be
called an editing session unless its exact prompt and observations include an
editing tool effect, and cannot be called multi-turn without at least two
correlated user turns in one session. Every tool call must have a recorded
pre-effect consumer decision.

For priority order, publish the planned order, actual worker/PR/merge order,
and dependency edges. Concurrent preparation is distinct from landing; use
merge ancestry for “lands.” Inspect each later delta for edits to the Card 080
permission/editing seam and distinguish additive use from semantic reopening.
For default preservation, compare constructor/profile selection, advertised
tools and access, permission mode, and guaranteed behavior across the
`v0.4.0` tag, `v0.4.1` tag, and current main. Do not treat API additivity as
complete behavioral proof.

## Suggested Next Move

Freeze tag peels, PR heads/merges/reviews, folded closeout records, and the
source-linked Desktop capsule identities. Build five small evidence maps, then
score each clause without running code or contacting a provider.

## Completion Protocol

Update only the owned worker paths. Run `effigy qa:docs`, `effigy
qa:northstar`, and `git diff --check`, then open one PR for independent
exact-head review. The queue owns merge and closeout. Mark g05.029 complete
only if all five clauses are proven; otherwise close it as `stopped` with each
pass, fail, or unproven clause explicit. Preserve the Card 087 stopped gate and
do not invent repair scope. If no ready generation task remains, the
coordinator records a runway-empty closeout for Chatterbox rather than choosing
a planned task. Return source identities, five verdicts, review comment,
PR/merge, closeout commit, and the resulting Next Task/runway state.
