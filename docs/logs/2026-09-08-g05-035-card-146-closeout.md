# 2026-09-08 g05.035 Card 146 Closeout

Card 146 repaired the provider-free Claude SDK MCP-status projection
contradiction. Frozen SDK `0.3.259` evidence permits `name` and `status` plus
optional `serverInfo`, `error`, `config`, `scope`, and `tools`; the shipped
implementation passes native rows through, while the old sidecar rejected
declared metadata. The repaired projection admits exactly the declared seven
keys, discards optional metadata, and keeps undeclared, malformed, foreign,
duplicate, and count-mismatched rows fail-closed. Faithful fake-SDK fixtures,
row-shape and Card 145 tuple corpora, non-leak oracles, and bounded status
fixtures are merged.

PR 296 was independently accepted at reviewed head
`58847bb00e4d15285af503c7023ff8750d1f1a7f`; review comment `5590924478`
resolved the earlier documentation finding in `5590709766` and marked the
final head ready to merge. The PR merged into `main` as
`13dee542e5ef4ab967cb4f0934cc11c35a8768c2`.

Final validation passed: formatting, focused validation (489 tests), affected
package verification, `validate:card116-mediated-stdio` (15/15),
`qa:routes`, `qa:docs`, `qa:northstar`, and `git diff --check`. The final
`qa:routes` pass depends on `ccd58ca6`, which repoints the Claude SDK
`consumer_tool_exchange` matrix cross-reference from completed card 144 to
card 146. That repoint is a temporary bridge: after card 146 becomes complete,
the producer-gap rule will reject a reference to a completed card. Queue or
Chatterbox must promote the next live-gate owner or reclassify the cell under
the Feature Matrix Rule. No live attempt ran, both Contract 061 cells remain
unqualified, and no release or qualification authority follows.

The active Next Task pointer now records that planning direction is needed;
the closeout introduced no new product or planning direction.
