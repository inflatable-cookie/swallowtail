---
title: g05.061 Codex 0.154.0 useful-newer qualification
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
status: ready-to-launch
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Tom instructed Swallowtail on 2026-09-14 to action every family in Research 308 and continue without repeated approval; g05.060 is complete and Codex is next."
roadmap: docs/roadmaps/g05/061-codex-0-154-0-useful-newer.md
queue:
  capability: general
  skipPRReview: false
  notifyOriginOnCloseout: true
---

## What This Thread Was Doing

Continue Tom's authorized Research 308 serial currentness campaign with Codex.
Qualify every published stable after `0.152.1` through current official stable
on exec and app-server where artifact evidence admits each axis.

## Why It Matters

Codex exec and app-server remain qualified only through `0.152.1`; npm and the
official GitHub release had reached `0.154.0` at planning, while the host is
already `0.153.3`. `AllowUnverified` is temporary posture, not current
qualification.

## Current State

- Canonical task: `docs/roadmaps/g05/061-codex-0-154-0-useful-newer.md`.
- Campaign authority: Research 308 and Tom's 2026-09-14 instruction to action
  all named families without repeated approval.
- Existing claims: exec and app-server have distinct historical segments and
  claim ids on `codex.cli`, both maintained through `0.152.1`, with known
  unpublished gaps and `AllowUnverified` retained.
- Host observation: `codex-cli 0.153.3`. Planning official point: npm/GitHub
  `0.154.0`, with six stable hops after the ceiling.

## Boundaries

Follow g05.061 exactly. Codex CLI exec/app-server family only. No prompt, live
exec, live app-server session, login, credentials, install, host mutation,
downloaded-artifact execution, new public operation, other adapter change,
release, tag, publication, or consumer mutation. Provider-free retrieval,
parsing, comparison, correction, and validation may be rerun normally; impose
no artificial attempt budget.

## Important Context

Use the repository `version-currentness` skill, Contract 029, Research 275 and
308, and the frozen `0.152.1` corpus. Inspect all six observed stable hops
`0.153.0`, `0.153.1`, `0.153.2`, `0.153.3`, `0.153.4`, and `0.154.0`, plus any
new stable found by the required rechecks. Classify the complete selected exec,
app-server, schema, and lifecycle surface. Identity must be committed before
any claim edit. Preserve all older segments and gaps. Keep model verbosity and
other feature-specific version sets exact unless their selected surfaces are
independently proved. Apply In-Run Latest Movement before the identity commit
and final push.

Research 311 is reserved; renumber cleanly if canonical main takes it first.
Do not add a task-level `Status:` header: lifecycle state belongs to the
repository hook.

## Suggested Next Move

Recheck npm/GitHub latest, enumerate the stable hop set, and build the
wrapper/platform/source-tree ledger before touching selection code.

## Completion Protocol

Meet every g05.061 oracle, run its named gates, obtain independent exact-head
review, and merge through Queue. The lifecycle hook owns closeout and handoff
consumption. Return identities, per-axis decisions, commits, checks, review,
PR/merge, and canonical closeout. Chatterbox then compiles Cursor Agent from
fresh main under the same standing operator authority.
