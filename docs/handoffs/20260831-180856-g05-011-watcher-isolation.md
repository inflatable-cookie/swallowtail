---
title: g05.011 watcher credential-preserving isolation worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Tom
created: 2026-08-31
updated: 2026-08-31
handoff_path: /Users/tom/Dev/projects/swallowtail/docs/handoffs/20260831-180856-g05-011-watcher-isolation.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, claude-code, watcher, isolation]
---

## What This Thread Was Doing

Card 026 tested the suspected Claude watcher admission failure and disproved
it: `--tools Read,Glob,Grep` filters built-ins, not the operation-private MCP
family. Exact `2.1.251` help and the consumed live envelope instead point to
watcher-only `--bare`, which removes every authentication path that envelope
could use.

The orchestrator tightened card 029 with an explicit isolation review oracle.
This dispatches that one provider-free implementation lane. No transcript or
second prompt is part of the authority chain.

## Why It Matters

The watcher route cannot justify another live turn while its isolation mode
removes configured authentication before MCP initialization. The repair must
preserve authentication without reopening ambient hooks, skills, settings, or
MCP servers and without weakening the private watcher boundary.

## Current State

- **Repository:** `/Users/tom/Dev/projects/swallowtail`
- **Planning branch:** `main`
- **Planning base commit:** `eb5e26dffbbd4b8ad2b9a31fcca2014dc0b13b0a`
- **Pushed main verification:** local `HEAD` and `origin/main` were equal at
  the planning base before this handoff was written
- **Planning checkout:** clean `main`; no implementation edits
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight
- **Planning artifacts included at the base:** completed card 026 evidence,
  ready card 029 with review oracle, and card 029 as the sole Next Task
- **Worker branch:** `worker/g05-011-claude-watcher-isolation`
- **Worker worktree:** Paseo-managed isolated worktree; accept the clean
  launcher-selected non-`main` worktree even when its path or branch differs
- **Worktree creation command:** Paseo `branch-off` from `origin/main`; manual
  fallback only through the Completion Protocol and `.agents.local.env`
- **Required sibling worktree links:** none
- **Active spec lane:** none; Contracts 059-060 are active
- **Roadmap milestone:**
  `/Users/tom/Dev/projects/swallowtail/docs/roadmaps/g05/011-watcher-route-admission-recovery.md`
- **Ready cards, in order:**
  `/Users/tom/Dev/projects/swallowtail/docs/roadmaps/g05/batch-cards/029-claude-watcher-credential-preserving-isolation.md`
- **Allowed runway:** exactly card 029; one provider-free command-isolation
  comparison, the smallest admitted repair if proved, and one PR
- **Remaining card budget:** one card; zero provider turns; one PR; no
  auto-continuation
- **Dispatch topology:** serial single-card lane
- **Parallel safety check:** keep the Claude watcher command, fixture, card,
  and closeout surfaces exclusive to this worker through review
- **Canonical refs:** `AGENTS.md`,
  `docs/contracts/001-working-rules.md`,
  `docs/contracts/023-harness-operation-isolation-and-native-boundary.md`,
  `docs/contracts/041-input-callback-and-provider-tool-admission.md`,
  `docs/contracts/059-operation-scoped-process-watchers.md`,
  `docs/contracts/060-operation-scoped-watcher-http-bridge.md`, and
  `docs/architecture/system-architecture.md`
- **Evidence refs:**
  `crates/swallowtail-adapter-claude-agent/tests/fixtures/claude-code-2.1.251/watcher-tool-admission.json`
  and `docs/logs/2026-08-31-g05-011-card-026-watcher-tool-admission.md`
- **Review oracle:** card 029 `## Review Oracle`; all three smallest
  counterexamples are mandatory
- **Model capability profile:** frontier Rust worker with high reasoning;
  authentication isolation, exact negative claims, and route lifecycle proof
- **Tool/runtime restrictions:** no provider prompt, live probe, login,
  credential read, credential mutation, install/update, ambient configuration
  mutation, watcher claim, currentness, skill-inventory, feature-façade,
  papercut, release, or unrelated cleanup
- **Required validation:** every command under card 029 `## Validation`; no
  live selector or provider command that starts a model turn
- **PR base/head:** current pushed `main` /
  `worker/g05-011-claude-watcher-isolation`
- **PR URL:** pending
- **Review state:** awaiting implementation and exact-head orchestrator review
- **Merge path:** orchestrator after accepted exact-head review and passing
  required checks

## Boundaries

- **In scope:** freeze exact prompt-free parser/help evidence; compare current
  `--bare`, watcher-only `--restricted`, and the smallest exact setting-source
  composition; select a candidate only when the card oracle passes; change only
  the watcher command and deterministic fixtures if proved; open one PR.
- **Out of scope:** provider contact, credential access, live acceptance,
  generic authentication/settings/MCP design, Contracts 059-060 changes,
  capability/version claims, the normal non-watcher route, card 024, other g05
  tracks, papercuts, currentness, releases, or merge.
- **Outcome shape:** one complete contract-valid provider-free repair and
  deterministic proof, or an honest evidence stop with no production edit if
  no candidate proves both authentication preservation and ambient exclusion.
- Do not infer credential availability from a path, environment name, keychain,
  OAuth state, or successful installation. Do not print, hash, inspect, or
  retain credential material.
- Preserve the private MCP authority, Stop hook, injected skill, Plan mode,
  working-resource confinement, fixed built-ins, terminal join, cleanup, and
  exact omission behavior.
- Work only in the clean worker worktree selected below. Do not edit the
  planning checkout, start a nested worker/orchestrator lane, or merge the PR.

## Important Context

- **Planning lineage:** cards 008-010 and 014-016 delivered the portable
  watcher lifecycle, supervision, private HTTP bridge, and exact Claude
  binding. Cards 011 and 020 stopped honestly after live turns created no host
  watcher. Card 026 then eliminated MCP tool suppression and named `--bare` as
  the next pre-initialization blocker.
- **Why this card is ready:** exact `2.1.251` help defines the relevant flags;
  the current operation-private fixture can prove command, MCP, hook, skill,
  lifecycle, and cleanup behavior without credentials or a provider turn; the
  review oracle fixes the allowed authority delta and stop point.
- **Decisions and preferences:** retain isolation first. A candidate that gains
  authentication by reopening ambient user/project/local state is failure, not
  a partial repair. Omission and the ordinary route must remain byte-identical.
- **Open tension:** prompt-free evidence may show that none of the available
  flags can preserve configured authentication under the required isolation.
  If so, stop honestly rather than reading credentials or weakening isolation.
- **Report after:** candidate semantics and counterexample fixtures are
  coherent, then after final validation and PR creation; report sooner for a
  stop condition.
- **Report to:** the Paseo parent/orchestrator through the active control plane.

## Suggested Next Move

Run the Completion Protocol preflight before broad reads. Then read card 029,
g05.011, Contracts 023, 041, 059, and 060, and the card 026 evidence. Trace the
current watcher-only argv/settings construction, freeze the exact prompt-free
flag semantics, and exercise the review-oracle counterexamples before choosing
whether any production edit is justified.

## Completion Protocol

### Before you start

1. This handoff's worker metadata activates implementation-worker mode. Before
   broad reads, run `git rev-parse --show-toplevel`,
   `git branch --show-current`, `git status --porcelain`, and
   `git worktree list --porcelain`.
2. Accept a clean registered non-`main` launcher worktree as authoritative,
   regardless of generated path or branch name. Do not create another.
3. If the launcher context is `main`, dirty, unregistered, or unusable, report
   it. Only then use the named/manual fallback through `.agents.local.env` and
   `AGENTS_WORKTREE_CONTAINER_DIR`. Never guess `/tmp`, clean, reset, stash
   over, or discard another checkout.
4. From the selected worktree, run
   `GIT_SSH_COMMAND="ssh -o ConnectTimeout=10 -o BatchMode=yes" git fetch origin`.
   Require `HEAD == origin/main`, require planning base `eb5e26df` to be an
   ancestor, and load the tracked handoff with
   `git show HEAD:docs/handoffs/20260831-180856-g05-011-watcher-isolation.md`.
   If the absolute dispatch file differs, stop. The tracked copy is canonical.
5. Required sibling worktree links are `none`.
6. Read the milestone, card 029, `AGENTS.md`, the canonical refs, and card 026
   evidence. Follow the repo's Effigy and Northstar Rust everyday-authoring
   routes and record the inherited doctor baseline.

### While you work

- Execute card 029 only. Keep the investigation provider-free and
  credential-blind.
- Model the exact argv and setting-source authority of every candidate before
  editing production. Exercise the ambient-reopening, auth-removal, and
  omission/ordinary-command counterexamples.
- If one candidate passes, implement the smallest watcher-only delta and its
  deterministic proof. If none passes, preserve production and return the
  evidence stop.
- Report meaningful chunks through Paseo with changed files, validation run,
  remaining work, risks, and blockers.
- Stop if a contract or product decision is missing, exact evidence is
  insufficient, scope expands, or validation changes the plan.

### When the assigned runway is complete

1. Run every command under card 029 `## Validation`; do not run a live probe.
2. Falsify the diff against every card 029 review-oracle claim. Confirm no
   credential material, ambient configuration, raw private authority, or
   provider payload entered fixtures, diagnostics, logs, or Git history.
3. Keep card 029 and the sole Next Task unchanged through review. Put execution
   evidence in the PR body; the orchestrator owns post-merge roadmap/log
   closeout.
4. Push the selected worker branch and open one PR against current pushed
   `main`. Link card 029, g05.011, Contracts 059-060, card 026 evidence, changed
   surfaces, validation, and unresolved items.
5. Report the PR URL and exact head through Paseo. Do not merge.

### Review and merge path

The orchestrator will independently review the exact PR head, record its
verdict on the PR, and send any bounded revision request back to this same
worker. Requested changes are: none. A planning change returns to the
orchestrator before revision. Once the exact reviewed head and required checks
are accepted, the orchestrator may merge without another operator prompt.

- **Closeout refs:** card 029, g05.011, batch-card and g05/generation indexes,
  the log index, and the sole roadmap Next Task.

### Handoff closeout

Leave the PR and planning state honest. If no candidate can preserve configured
authentication while excluding ambient authority, report the exact evidence
stop; do not weaken the invariant or manufacture a production change.
