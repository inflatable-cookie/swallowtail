# g05.053 Grok 1.0.25 Catalogue Merge

Merged 2026-09-13: PR #316 landed the exact installed Grok `1.0.25`
catalogue correction at `3e9f29e7` after independent exact-head review
`5649291554` (operator, distinct party from the worker agent).

## Accepted outcome

Exact `--no-auto-update models` is admitted as an authenticated,
non-inference metadata operation under
`HarnessConfigurationPosture::Ambient`. The shipped bullet grammar
(`  * <id> (default)`, `  - <id>`, tolerated auth preamble) owns
membership, order, and default; the frozen embedded document supplements
exact-ID matches only. The accepted final capsule records ordered
`grok-4.6` (default) then `grok-4.5`, zero exit, stdout 113 bytes with zero
stderr, no prompt, no model session, no tool, no inference, joined
cleanup. Both earlier observations are preserved as evidence and were not
retried.

## Review and validation at the exact head

Review `5649291554` verified clean tree, owned-paths-only scope, unchanged
ACP execution compatibility and `grok_build_model_for_version`, immutable
tagged `v0.5.0` and historical evidence, and all named tiers green: `cargo
fmt --check`, `git diff --check`, `validate:focused` 95/95,
`package:verify-affected`, `check:examples`, `package:api`,
`qa:routes` (50 production routes), `qa:consumer-docs`, `qa:northstar`.

## Deferred (non-blocking, no action)

- Two docstrings retain the removed concept's adjective
  ("provider-suppressed"); cosmetic, binding gone.
- Committed `live-capsule.json` is schema v2 while the in-tree probe writer
  emits v3; nothing provider-free parses the fixture and no re-run is
  authorized, so the accepted v2 evidence stands.

## Boundary

Desktop may adopt only the exact accepted source SHA under separate
consumer authority. No inference, release, tag, Desktop mutation, or
consumer pin follows. No task is ready after this merge; the next dispatch
needs a Chatterbox planning ruling.
