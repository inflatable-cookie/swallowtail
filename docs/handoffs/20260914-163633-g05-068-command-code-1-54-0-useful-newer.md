---
title: g05.068 Command Code 1.54.0 useful-newer qualification
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
status: ready-to-launch
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Tom instructed Chatterbox to action the full Research 308 currentness campaign, including exact-pin families, Kimi and Oh My Pi, and continue without repeated approval; g05.067 is complete and Command Code is next."
roadmap: docs/roadmaps/g05/068-command-code-1-54-0-useful-newer.md
queue:
  capability: general
  skipPRReview: false
  notifyOriginOnCloseout: true
---

# g05.068 Command Code 1.54.0 Useful Newer

## What This Thread Was Doing

Chatterbox closed g05.067 and compiled the ninth Research 308 family. Command
Code remains exact-qualified at installed `1.15.1`; official npm stable is
`1.54.0`, 67 published stable points later.

## Why It Matters

The exact old pin rejects the current official harness. The large gap cannot be
treated as semver compatibility or a range: every shipped hop feeding the
selected headless and private-continuation behavior needs deterministic
classification, and live evidence must remain bound to the version that ran.

## Current State

- Canonical `main` is g05.067 closeout
  `f40d2a3f2cfb866591a18ca64cb2aa0a9ca3bed9`.
- PR #329 merged accepted head
  `6cddd6a115bf45fdea8271418843750ca9177510` as
  `3a519b090f658e283e9b0c3bac83852d86a9a52d`; review comment
  `5666473407`.
- npm `latest=1.54.0`, published `2026-09-14T01:59:24.573Z`; alpha,
  beta, and rc tags are ignored. Installed is `1.15.1`.
- There are 67 published stable successors after `1.15.1`. Latest still
  declares Node `>=22`; host Node `22.23.2` satisfies it.
- Current claim is exact `1.15.1`, `QualifiedOnly`, behavior
  `command-code.agent-event-ndjson-v1`.
- Research 116's live structured/tool/usage/credit evidence and Research 118's
  private exact-id continuation evidence ran on `1.15.1` only.

## Boundaries

One route family and one exact-pin decision. No prompt, inference, login,
credentials, live run/turn, install, host update, downloaded-artifact
execution, Provider API, release, tag, publication, or consumer mutation.
Provider-free work may rerun normally. Do not infer a range or transfer old
live evidence.

## Important Context

Read Contract 029, Research 116, 118, 308, 316, g05.068, the current prepared
guide, `selection.rs`, and all Command Code corpora before editing. Use the
repository `version-currentness` skill and Effigy selectors. Recheck official
latest at both boundaries. Retrieve the baseline and every stable successor
to `/tmp`; freeze Research 317 and a mutation-sensitive full shipped-tree
ledger before claim edits.

Classify exact selected argv, stdin, NDJSON events/results/errors, model and
effort, permission mode, retention, private `--resume`, cancellation,
deadlines, redaction, process ownership, and cleanup. New aliases, TUI,
Provider API, mods, taste, session catalogue/export, and other additions stay
unmapped only with a concrete non-effect proof. On a compatible provider-free
result, move the single exact point to `1.54.0`; otherwise land the typed stop.
Gate live-specific feature truth rather than pretending `1.15.1` observations
ran on the new pin.

## Suggested Next Move

Automate the 68-point baseline-plus-successor identity and per-hop tree ledger,
classify the selected surface, commit identity, then make the smallest honest
exact-pin and feature-projection decision. Validate, push, open a PR, and hand
the exact head to independent review.

## Completion Protocol

The Queue owns worker launch, independent review, merge, lifecycle closeout,
and handoff deletion. Return official/host/Node identities, complete hop set,
Research 317 path, segment and live-evidence disposition, validation, review
marker/comment, merge SHA, and closeout SHA. Next is Qoder. No release or tag
follows.
