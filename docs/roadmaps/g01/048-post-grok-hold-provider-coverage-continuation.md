# 048 Post-Grok-Hold Provider Coverage Continuation

Status: completed
Owner: Tom
Created: 2026-07-24
Depends on: g01.046 and completed batch g01.047.1
Vision tags: broad provider coverage, fixture-first delivery, explicit authority
Contract refs: 005, 006, 007, 011, 014, 015, 019, 020, 023, 029, 032, 035
Planning state: completed

## Purpose

Qualify and prove the maintained Claude Agent ACP range while the Grok Build
lane is held for unavailable account evidence.

## Generation Runway

Keep g01 active. Roadmap 048 takes it to 48 numbered roadmaps, still inside the
documented 30-50 range. This lane must return to a deliberate generation
checkpoint at closeout rather than creating g02 automatically.

## Goals

- [x] Revalidate remaining provider, harness, protocol, SDK, and self-hosted
      gaps against current authoritative evidence.
- [x] Select a route whose deterministic development and conformance do not
      require a live developer account.
- [x] Prefer a materially new lifecycle or transport over another near-duplicate
      adapter.
- [x] Keep heavy containers and persistent model serving deferred unless they
      provide uniquely necessary architectural evidence.
- [x] Confirm no missing shared boundary blocks implementation.
- [x] Deliver fixture-first production mapping and cross-topology conformance.

## Execution Plan

### Batch 48.1 — Coverage Evidence And Selection

- [x] Execute card 142.
- [x] Inventory realized routes and current gaps.
- [x] Rank current candidates and select Claude Agent ACP.
- [x] Preserve the Grok hold and exclude unapproved Claude subscription login.

### Batch 48.2 — Claude Agent ACP Range And Corpus

- [x] Execute card 143 against candidate range `0.52.0..=0.61.0`.
- [x] Confirm that no missing shared contract blocks the frozen subset.
- [x] Freeze exact adapter, SDK, native-artifact, ACP, access, lifecycle,
      configuration, and deterministic fixture evidence before production code.

### Batch 48.3 — Claude Agent ACP Production Driver

- [x] Execute card 144.
- [x] Add one separately registered `claude-agent` driver without widening
      Anthropic direct inference or Managed Agents.
- [x] Keep live authentication separately gated.

### Batch 48.4 — Claude Agent ACP Conformance And Closeout

- [x] Execute card 145.
- [x] Prove the selected route under local and remote-authoritative hosts.
- [x] Return to a deliberate generation checkpoint.

## Boundaries

- Grok cards 138-141 remain on hold and are not prerequisites
- no provider implementation during card 142
- no live account, credential, login, paid request, or package installation in
  default tests
- no Claude subscription login, terminal auth, logout, or credential mutation
- no heavy container as an implicit runtime requirement
- no provider, model, endpoint, credential, topology, or support fallback
- no Nucleus, Soundcheck, or Monkey edit
- no automatic g02 creation

## Acceptance Criteria

- [x] exact integration family, driver, transport, instance, access, route,
      artifact, and interface identities remain separate
- [x] the selected proof adds architectural or compatibility information
- [x] deterministic fixtures precede production network or process work
- [x] any user-time account requirement remains explicit without becoming a
      development-time live-test dependency
- [x] qualified support and unverified-newer execution remain distinct
- [x] all live probes remain separately gated
- [x] one generation checkpoint remains next at closeout

## Selected Boundary

Research 032 selects:

- integration family `claude-agent`
- Claude Agent ACP over ACP v1 NDJSON stdio
- qualified adapter range `0.53.0..=0.61.0`, excluding `0.58.0`
- one host-approved Anthropic public-API key lease
- integration-maintainer-supported ACP adapter over the provider-supported
  Agent SDK and public API
- explicit `Ambient` configuration and `AmbientHost` isolation
- provider-native read-tool policy without a sandbox claim

Card 143 qualifies four private behavior milestones at `0.53.0`, `0.54.0`,
`0.60.0`, and `0.61.0`. It rejects `0.52.0`, freezes exact nested Claude Code
`2.1.195..=2.1.217` evidence, and confirms the existing shared contracts are
sufficient. Persistent sessions, terminal authentication, provider switching,
writes, shells, web tools, subagents, terminals, MCP, elicitation, steering,
sandboxing, and installation remain outside the first proof.

## Evidence Required

- realized descriptor, transport, profile, and maintained-range inventory
- current official provider and maintained-project sources
- ranked candidate matrix with account, container, authority, lifecycle,
  compatibility, and information-gain tradeoffs
- explicit missing-contract and operator-policy decisions
- exact Claude Agent ACP range, fixture, driver, topology, cleanup, redaction,
  and validation results

## Outcome

Claude Agent ACP is realized as a separate maintained harness range without
widening Anthropic direct inference or Managed Agents.

The qualified wrapper range is `0.53.0..=0.61.0`, excluding unpublished
`0.58.0`. Four private revisions preserve additive wrapper behavior. Stable
newer versions remain executable only as visible unverified points.

The production route uses ACP v1 stdio, one exact model, one Anthropic
public-API-key lease, `Ambient` configuration, `AmbientHost` isolation, and
provider-native read tools without a sandbox claim. The unchanged long-lived
ACP profile and adapter-local matrix pass under local and remote-authoritative
host identities.

Full QA inventories 658 tests: 654 pass and four installed/live probes remain
ignored. Doctor remains at the inherited 19 findings. Card 146 is the sole
ready generation-disposition checkpoint; no g02 rollover is implied.
