# g05.058 Claude Code 2.1.270 Useful Newer

Owner: Tom
Created: 2026-09-14
Depends on: Contract 029; qualified Claude Code `2.1.257`; Research 273; g05.019
Vision tags: route currentness, Claude Code, compatibility

## Outcome

Qualify current official Claude Code `2.1.270` for the existing headless and
response-only axes if deterministic artifact evidence admits it. Preserve
identity-before-claim, both historical baselines, all unpublished gaps,
`AllowUnverified`, and the strict separation from Claude Agent ACP and SDK.

Official npm and GitHub stable are `2.1.270` at planning. Eleven stable points
were published after the qualified `2.1.257` ceiling: `2.1.258`, `2.1.259`,
`2.1.260`, `2.1.261`, `2.1.263`, and `2.1.265` through `2.1.270`; `2.1.262`
and `2.1.264` are unpublished. The host reports `2.1.258`; it is observation
input, not qualification or update authority.

## Ready-State Rubric

- [x] The operator identified a fresh Claude Code version and asked Swallowtail
      to add support.
- [x] npm and GitHub agree on official stable `2.1.270`.
- [x] Headless and response-only remain `AllowUnverified` through qualified
      `2.1.257` on separate existing behavior axes.
- [x] Every published hop, the two new gaps, evidence order, validation, and
      authority boundaries are explicit.
- [x] No prompt, login, install, host mutation, or provider credit is required.

## Work

1. Re-probe npm and GitHub stable, observe the installed host safely, and
   freeze Research 307 plus a secret-free `2.1.270` identity corpus. Retrieve
   official npm wrapper and platform packages into `/tmp`; do not execute
   downloaded binaries. Inventory every published stable from `2.1.258`
   through `2.1.270`, prove `2.1.262` and `2.1.264` absent, and correlate the
   official GitHub tags/releases.
2. Compare the full shipped artifacts and mapped CLI surface from `2.1.257`
   across every published hop. Classify all changes affecting selected argv,
   stream-JSON framing, result/error/usage decoding, permission and tool
   suppression, session persistence, model/effort selection, MCP isolation,
   process lifecycle, and configuration precedence. Treat changelog text as
   subordinate evidence. Commit identity evidence before any production claim
   edit.
3. Name one Contract 029 outcome: compatible extension, adapter-private
   milestone, new driver/facade stop, or evidence stop. Only for an admitted
   outcome, update the two selection claims and mutation-sensitive tests, the
   prepared guide, exact Claude Code route and feature-matrix version truth,
   `[Unreleased]`, standing-lane state, and claim log in a later commit.
   Preserve watcher exact `2.1.251` and every feature-specific exact-version
   set unless separate evidence expressly admits it.
4. Re-probe official latest immediately before the identity commit and final
   push. Apply Contract 029's In-Run Latest Movement rule. Normal artifact
   retrieval, parsing, comparison, validation, and correction may be retried
   within this task; only substantive evidence or authority failures stop it.

## Dispatch Manifest

| Field | Task 058 |
| --- | --- |
| Readiness | ready |
| Prerequisites | Contract 029; Research 273 and frozen Claude Code `2.1.257` evidence; clean pushed `main`; npm/GitHub consensus on `2.1.270` |
| Completion conditions | Research 307 and a secret-free corpus freeze every published `2.1.257` -> `2.1.270` hop; identity is committed before claim edits; one segment outcome is recorded; an admitted outcome raises only the proved headless and response-only claims and current downstream truth; a stopped outcome leaves claims unchanged; exact-head review and named validation pass |
| Owned mutable paths | `crates/swallowtail-adapter-claude-agent/src/claude_code_selection.rs`; `crates/swallowtail-adapter-claude-agent/src/claude_code_response_selection.rs`; Claude Code selection and identity tests/fixtures under `crates/swallowtail-adapter-claude-agent/tests/**`; `docs/research/307-*.md` and one research-index line; exact Claude Code sections in `docs/guides/claude-agent-prepared-integration.md`, `provider-route-matrix.md`, and `provider-solution-feature-matrix.csv`; `CHANGELOG.md` `[Unreleased]`; one Claude Code identity log and, if admitted, one claim log plus index lines; this task's result; Claude Code currentness paragraphs in `docs/roadmaps/standing-lanes.md`; `PAPERCUTS.md` append only |
| Reserved shared closeout surfaces | lifecycle task record and generated projection blocks; deletion of the submitted handoff belongs to the repository hook |
| Forbidden paths | Claude Agent ACP and SDK claims; watcher fixtures/authorization; feature-specific exact-version sets without direct new evidence; every other adapter; contracts; historical research and release notes; any new public API |
| Approved concurrent siblings | unrelated work only; no shared Claude Code, currentness, or generated lifecycle surfaces |
| Worker capability class | evidence-first Rust currentness worker; npm/GitHub artifact retrieval and deterministic binary/string or package-tree comparison; no provider credentials |
| Acceptance evidence | exact npm metadata/tarballs, GitHub tag/release identity, host version/digest observation, all-hop inventories and mapped-surface classification, mutation-sensitive fixtures/tests, and exact current-doc agreement |
| Review oracle | the smallest counterexample is a skipped published hop, execution of a downloaded artifact, an unclassified mapped delta, a changelog-only claim, a lost historical gap, accidental watcher/SDK/ACP widening, or docs disagreeing with selection code |
| Stop conditions | official-channel disagreement; major-line reset; mapped public lifecycle/capability/authority change; unclassified stream/failure delta; new driver/facade required; provider/live evidence required |
| Escalation owner | operator via Chatterbox for policy/authority; queue coordinator for mechanical blockers |

## Boundaries

One Claude Code family, covering its two existing axes. No prompt, provider
call, login, credential use, package install, host update, downloaded-artifact
execution, watcher live work, Claude Agent ACP or SDK change, release, tag,
publication, or consumer mutation. Provider balance is irrelevant to this
provider-free qualification.

## Validation

- `cargo fmt -p swallowtail-adapter-claude-agent -- --check`
- `effigy validate:focused swallowtail-adapter-claude-agent`
- `effigy package:verify-affected swallowtail-adapter-claude-agent`
- `effigy check:examples`
- `effigy package:api`
- `effigy qa:routes`
- `effigy qa:northstar`
- research, logs, roadmaps, g05, roadmap-number, lifecycle, and next-action checks
- `git diff --check`

## Acceptance

- [ ] official identity is reproducible for every published hop
- [ ] every changed shipped input feeding selected behavior is classified
- [ ] identity evidence lands before any production claim edit
- [ ] only the admitted segment changes claims; a stop changes no claim
- [ ] historical segments, gaps, claim IDs, `AllowUnverified`, watcher bounds,
      and unrelated Claude routes survive
- [ ] official latest is rechecked at both required boundaries

## Next Task

Return the exact artifact and segment decision. No release or second family
follows automatically.
