# 2026-09-14 Codex 0.154.0 Claim

## Result

Codex exec and app-server now qualify official npm `@openai/codex` `0.154.0`
through all six published stable hops after `0.152.1`. The shared `codex.cli`
window extends as a compatible extension on the existing
`codex.exec.jsonl-v1` and `codex.app-server.v2.workspace-roots` behavior
revisions and the `strict-descendant-hard-delete` lifecycle revision.
Newly interior unpublished `0.152.2` is pinned incompatible alongside
`0.149.2`, `0.150.2`, and `0.151.1`; unpublished `0.154.1` is the visible
`UnverifiedNewer` point. Model verbosity, fast mode, personality, plan-mode
effort, and every other feature-specific exact set remain bounded through
the `0.147.0..=0.149.1` probed points. Identity evidence stays in the prior
commit (`bd7430d1`): downloaded binaries were hashed and never executed, the
complete wrapper/platform/source-tree inventories bound every hop, the exec
JSONL wire is byte-identical, the selected schema params are byte-identical
or additive-only, and the default-off `exec --worktree` opt-in plus the
remaining feeding deltas stay unmapped.

Focused, affected-package, semantic API, route, docs, Northstar, format, and
diff gates pass without provider work.

## Next

Return the exact outcome to Chatterbox, then compile and dispatch Cursor
Agent as the next Research 308 family without asking Tom to repeat
authorization.
