# Aider Headless Route

Status: deferred
Owner: Tom
Source: g03.095 and cards 295-298

## Deferred Work

Add Aider through its mature scripted CLI surface only if text and Git
mutation can be represented without pretending it is structured ACP.

g03.095 and cards 295-298 remain source evidence. They were not started.

## Promotion Gate

Promote only when:

- current contracts can represent instruction input, worktree authority,
  stdout/stderr, exit behavior, Git mutation, cancellation, and cleanup
  without a fake structured-run or ACP claim
- the active generation has capacity for a new installed-harness route
- the operator approves the exact artifact and mutation boundary

There is no implied revisit date.
