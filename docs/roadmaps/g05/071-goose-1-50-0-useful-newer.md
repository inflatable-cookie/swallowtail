# g05.071 Goose 1.50.0 Useful Newer

Owner: Tom
Created: 2026-09-14
Depends on: Contract 029; Research 148, 250, 253, 308, and 318; g05.070
Vision tags: route currentness, Goose, ACP, exact pin

## Outcome

Reopen the exact `goose.release` pin and qualify current official GitHub
`v1.50.0` for `goose.acp` only if complete provider-free tag, source-tree,
CLI, ACP wire, authority, and lifecycle evidence admits it. Preserve
`QualifiedOnly` and one exact maintained point; do not infer a range across
the four published stable releases after `1.46.0`.

Planning rechecked the canonical `aaif-goose/goose` release channel:
`v1.50.0` is latest stable, published `2026-09-08T19:32:41Z`, at lightweight
tag commit `4cc49a8485f7960efeceba37ac5f572459c52c7c`. Stable successors are
`v1.47.0`, `v1.48.0`, `v1.49.0`, and `v1.50.0`; their exact tag commits are
known. `goose` is absent from this host. The repository move from
`block/goose` is already recorded and does not change the release axis.

## Ready-State Rubric

- [x] Tom authorized the complete Research 308 currentness campaign, including
      reopening exact-pin families.
- [x] g05.070 completed as an honest typed stop and its lifecycle closeout is
      on canonical `main`.
- [x] Official `v1.50.0`, absent host CLI, and all four stable successor tags
      are identified separately.
- [x] Research 148 fixes the selected ACP wire; Research 250 and 253 fix the
      builtin and mode authority boundaries.
- [x] No prompt, configure/login, installation, host update, or provider credit
      is needed for the identity and claim decision.

## Work

1. Re-probe GitHub latest stable, releases, exact tag commits, release-asset
   metadata and installed `--version` availability. Retrieve the exact tagged
   source trees for `v1.46.0..=v1.50.0` into `/tmp`. Freeze Research 319 and
   exact tag/tree, workspace-version, selected-crate, ACP dependency/schema,
   CLI-command, and published Darwin-arm64 asset identities without installing
   or executing a downloaded artifact.
2. Derive a deterministic per-hop source-tree ledger and mapped-module closure.
   Classify every changed input feeding `goose acp`: CLI dispatch and empty
   builtin/scheduler selection, initialize negotiation and capabilities,
   `session/new`, one `session/prompt`, cancel, content blocks, stop reasons,
   activity/update frames, permission callbacks, session modes, host provider/
   model failure, correlation and bounds, cwd/resource authority, connection,
   task/process ownership, deadline, and joined cleanup. Changelogs are
   discovery only; commit identity before production claim edits.
3. Keep `goose serve`, HTTP/WebSocket/TLS, desktop/TUI, recipes, ACP-provider
   adapters, `--with-builtin`, `--enable-scheduler`, persistent permission,
   load/list/close, fork/steer/set-model, client MCP servers, and provider
   configuration unmapped only with concrete non-effect proof. Do not convert
   advertised ACP capabilities into Swallowtail operations.
4. If the selected contract is unchanged or mechanically adaptable without a
   new public lifecycle, authority, or operation, rebind the one exact claim
   point to `1.50.0`. Keep the behavior revision only when mapped wire and
   lifecycle are unchanged. Do not retain `1.46.0` as a second exact point or
   infer a compatibility range.
5. Stop with the smallest exact counterexample if a mapped wire, lifecycle,
   permission, authentication/provider configuration, builtin, mode, failure,
   or process-authority boundary changed materially, or if base compatibility
   requires a live provider session. Recheck official stable immediately
   before identity commit and push. Provider-free retrieval, classification,
   correction, and validation may rerun normally without artificial limits.

## Dispatch Manifest

| Field | Task 071 |
| --- | --- |
| Readiness | ready |
| Prerequisites | Contract 029; Research 148, 250, 253, 308, and 318; completed g05.070; clean pushed `main`; official GitHub tags and releases |
| Completion conditions | all four published stable successors are frozen and classified; identity precedes claim; one honest exact-`1.50.0` decision lands; QualifiedOnly and route-family boundaries survive; exact-head review and named gates pass |
| Owned mutable paths | Goose selection, prepared route, ACP decoder/connection, tests, and fixtures under `crates/swallowtail-adapter-goose/**`; Research 319 and one research-index line; exact Goose prepared-guide, route/activity/feature matrices, architecture ceiling, standing-lane, `[Unreleased]`, identity-log, claim-log, and task-result lines; `PAPERCUTS.md` append only |
| Reserved shared closeout surfaces | lifecycle task record and generated projections; submitted-handoff deletion belongs to the repository hook |
| Forbidden paths | other adapters; contracts; historical research/captures/releases; Goose serve/desktop/TUI/recipes/ACP-provider routes; live provider probes; release/tag/workflow files |
| Approved concurrent siblings | none on shared currentness surfaces; this campaign is serial |
| Worker capability class | evidence-first Rust currentness worker; exact Git-tag tree ledger and ACP boundary reconciliation; no provider credentials |
| Acceptance evidence | GitHub stable-release and five-tag identities including baseline; exact per-hop tree/module ledger; mutation-sensitive selection/CLI/ACP/authority/lifecycle fixtures; current docs/claim agreement |
| Review oracle | skipped stable, inferred range, unclassified mapped delta, changelog-only inference, serve/desktop/provider flattening, advertised-operation inference, lost builtin/mode/permission/provider-config/resource/cleanup boundary, provider reliance, or unjustified feature widening |
| Oracle gate | not required — Contract 029 and Research 148/250/253 settle this provider-free exact-pin comparison and its falsifiable stop conditions; no new auth, permission, secret, or execution policy is delegated |
| Stop conditions | official-channel disagreement; selected public lifecycle/authority/authentication/provider/permission/builtin/mode/failure change; new driver/facade required; provider/live evidence required for base compatibility |
| Escalation owner | operator via Chatterbox for new policy; Queue coordinator for mechanical blockers |

## Boundaries

One route family and one exact-pin decision. No prompt, inference, configure or
login, credential use, live ACP session, installation, host update,
downloaded-artifact execution, serve/desktop/TUI/provider work, release, tag,
publication, or consumer mutation. Do not infer compatibility from adjacent
tags, protocol crate semver, or advertised capabilities.

## Validation

- `cargo fmt -p swallowtail-adapter-goose -- --check`
- `effigy validate:focused swallowtail-adapter-goose`
- `effigy package:verify-affected swallowtail-adapter-goose`
- `effigy check:examples`
- `effigy package:api`
- `effigy qa:routes`
- `effigy qa:northstar`
- focused Goose identity, selection, CLI, ACP, activity, permission, and cleanup selectors
- relevant research/log/roadmap/number/lifecycle/next-action checks
- `git diff --check`

## Acceptance

- [x] official identity and all four published successors reproduce
- [x] every changed selected input is classified
- [x] identity evidence precedes the claim decision
- [x] exact QualifiedOnly shape and route boundaries remain truthful
- [x] advertised siblings and capabilities remain independently gated
- [x] official stable is rechecked at both boundaries

## Result

Stopped as a typed identity stop. Research 319 froze the exact `1.46.0`
baseline and all four published stable successors through official `1.50.0`
as exact tagged source trees with a 26-file mapped-module ledger, Darwin-arm64
asset digests hashed without extraction, and a mutation-sensitive ledger test;
no downloaded artifact was executed and no provider operation occurred. The
smallest exact counterexample is the hop `1.46.0..1.47.0`: provider-auth
failure becomes typed `auth_required` on `session/new` and `session/prompt`,
so the exact `QualifiedOnly` point stays at `1.46.0` with behavior
`goose.acp.stdio-v1` unchanged and no range, second point, or newer posture
added. Research 148/250/253 stand. The failure-binding policy returned to the
operator via Chatterbox and that ruling is now recorded in
[standing-lanes.md](../standing-lanes.md); the campaign continued serially with
Kiro.

## Next Task

The failure-binding ruling is recorded in
[standing-lanes.md](../standing-lanes.md): the route binds the typed
provider-authentication semantics, so the reopen lane needs a new behavior
revision, a re-frozen exact point at the then-current published stable, and the
typed failure shape recorded in the Goose prepared guide. Kiro followed this
family and is complete.
