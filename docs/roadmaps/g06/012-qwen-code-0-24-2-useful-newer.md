# g06.012 Qwen Code 0.24.2 Useful Newer

Owner: Tom
Created: 2026-09-21
Depends on: Contract 029; Research 310, 331/334; completed g05.060
Vision tags: route currentness, Qwen Code, useful-newer

## Outcome

Qualify the `qwen-code.package` / `qwen.headless` family from the qualified
ceiling `0.23.3` through official npm/GitHub stable `0.24.2`. Identity names the
segment shape first; the claim raises the published window.

## Why It Matters

g05.060 left the Qwen Code ceiling at `0.23.3`. Official stable moved to
`0.24.2`. AllowUnverified already admits later points; it is not permission to
leave the current official stable unverified.

## Ready-State Rubric

- [x] Official npm `@qwen-code/qwen-code` and the direct QwenLM/qwen-code tags
      agree through `0.24.2`.
- [x] Every published hop `0.23.4`, `0.24.0`, `0.24.1` and `0.24.2` was
      retrieved and inventoried.
- [x] The identity record names a compatible extension.
- [ ] The branch's failing nextest check is repaired through review.

## Decisions

- One family: `qwen-code.package` and its headless agent-core binding only.
- Compatible extension: keep baselines, claim ids, behavior revisions,
  `AllowUnverified`, historical gaps, and the exact Plan and
  reasoning/budget sets on their probed points.
- Preview and nightly channels are not stable authority.
- Later `UnverifiedNewer` is the next unpublished stable.
- Host `qwen` is absent from `PATH`. Missing host is not a gap. Do not install,
  and do not execute downloaded binaries. No provider prompt or live session.
- The failing `Stable nextest` check is evidence for review or revision, never
  a reason to bypass a merge gate.
- Do not edit the Next Task pointer, generation status, handoffs, or northstar
  loop state.

## Dispatch manifest

- **State:** ready; one family useful-newer lane entering at review, with a
  known failing check expected to be repaired through the revision path.
- **Owned mutable paths on revision:** this PR's existing Qwen selection source,
  Qwen identity and prepared-facade tests and fixtures under
  `crates/swallowtail-adapter-qwen/`, the Qwen headless prepared guide, the
  affected provider and activity matrix rows, the CHANGELOG `[Unreleased]`
  entry, the lane's research record and its indexes/logs.
- **Excluded:** other families, the Alibaba/Qwen hosted families, workflow,
  dependency, release, provider mutation, the Next Task pointer,
  `docs/roadmaps/README.md`, generation runway and lifecycle projection.
- **Escalation:** operator via Chatterbox for scope, number, or contract
  questions; Queue coordinator for mechanical blockers.

## Work

1. Freeze identity for `0.24.2` with the per-hop artifact and inventory
   evidence. No production claim.
2. Recheck official stable. If it moved before the claim landed, extend hops
   under Contract 029 In-Run Latest Movement.
3. Apply the compatible-extension claim to the published window.
4. Repair the failing nextest check within the lane's existing surfaces, then
   run focused adapter validation and the named docs gates.

## Acceptance and review oracle

| Invariant | Adversarial counterexample | Required proof |
| --- | --- | --- |
| The window extends through `0.24.2` | A range is inferred past the current stable | selection claims name the raised ceiling |
| The gap set is truthful | A never-published hop is treated as compatible | gap set with publication evidence |
| Selected headless surfaces are unchanged | The agent-core stream-json grammar or Plan shaping moved | frozen per-hop inventories and fixtures |
| The failing check is repaired honestly | The check is skipped or weakened to pass | the failing test is identified and its cause fixed |

## Stop conditions

Stop and ask if a hop changes the selected stream-json grammar, Plan shaping,
reasoning or budget authority, or retention; if the npm and tag channels
disagree; or if the failing check cannot be repaired inside the lane's existing
surfaces.

## Evidence

The lane's research record (renumbered from the colliding `331` on admission).
Fixtures under `crates/swallowtail-adapter-qwen/tests/fixtures/qwen-code-0.24.2/`.
Probe artifacts live outside the repository.

## External PR Review Entry Authority

Tom authorized the Queue Oracle on 2026-09-21 to prepare and take over the
remaining externally authored currentness PRs through review entry, accepted the
pilot-first sequence, attested Grok 4.6 as the author model for the cloud
sessions, directed that the Claude Agent ACP PR be held until last with its
contract change flagged, and explicitly directed that this failing Qwen lane be
admitted and repaired through the ordinary revision path.

The external work and its evidence are submitted claims, not independent
acceptance. Tom declared Grok 4.6 as the external author model; canonical
declaration: `xai/grok-4.6`, GitHub author `betterthanclay`, authoring system
Cursor cloud driven by Grok Bot.

Queue starts an independent reviewer, not a worker. Changes requested authorize
one ordinary revision loop in the same workspace and branch, scoped to this Qwen
qualification and its existing changed surfaces. Queue owns merge and lifecycle
closeout. Intake preparation may publish this planning and merge it into the
existing branch; the exclusions above constrain revision workers, not this
operator-approved planning preparation or hook-owned closeout.

Canonical handoff: `docs/handoffs/20260921-g06-012-external-pr-review-entry.md`.

Preserve the external branch and workspace after completion. Other external PRs
are not dispatched by this approval. Research numbers `331`, `332` and `333`
are retained by earlier lanes; this lane takes the next free number.
