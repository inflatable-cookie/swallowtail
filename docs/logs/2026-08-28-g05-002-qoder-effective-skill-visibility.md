# 2026-08-28 g05.002 Qoder Effective Skill Visibility

Status: complete
Owner: Tom
Card: 004
Research: 256

## Result

Card 004 closed with an honest empty deliver-now set.

Exact `@qoder-ai/qodercli@1.1.25` init builder emits `skills` as non-disabled
skill names and `plugins` as enabled `{name,path}` rows. Discovery reads
built-in, plugin, project `.qoder/skills`, and user `~/.qoder/skills` (plus
optional `.agents` paths). `npx skills -a qoder` targets those same path
classes; install membership is not selected-run visibility.

Selected `qoder.headless` remains prompt-bearing with blocking auth. Contract
058 therefore withholds binding. Fixture
`skill-roster-evidence.json` freezes the source review. Cards 005–006 stay
planned. No production code or Contract 058 edit.
