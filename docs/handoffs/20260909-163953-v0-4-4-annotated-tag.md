---
title: Swallowtail v0.4.4 annotated source tag
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
status: ready-to-launch
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Tom explicitly authorized local annotated-tag creation and tag push for v0.4.4 at exact SHA 49c9e3b291609c9ebf5b35a284c08302f3b8d5e3 on 2026-09-09, conditional on green CI; exact-SHA push run 34350208617 is green, and Tom separately approved the exact annotation text quoted in this handoff."
queue:
  capability: general
  skipPRReview: false
  notifyOriginOnCloseout: true
---

## What This Thread Was Doing

Execute the operator-authorized source-only `v0.4.4` annotated tag at exact
candidate SHA `49c9e3b291609c9ebf5b35a284c08302f3b8d5e3`, then publish the normal
tag record and planning closeout under `g05.036`.

## Why It Matters

The exact candidate has passed the source gates and the separate Desktop
18-cell acceptance gate. Consumers select immutable tags, so the release does
not exist until the authorized annotated tag is created and pushed at the
proved source identity.

## Current State

- Candidate commit: `49c9e3b291609c9ebf5b35a284c08302f3b8d5e3`.
- Candidate tree: `1a9db12742b68e839dfebe42f0633f5d1aa0265b`.
- Canonical branch and remote: `main` at
  `git@github.com:inflatable-cookie/swallowtail.git`.
- Exact-SHA push CI run `34350208617` completed successfully with all 11 jobs
  green, including Pinned MSRV floor and Pinned MSRV floor tests.
- Card 154's qualifying workflow-dispatch run `34345060452` also passed at
  reviewed head `1fb5b16c`, whose tree is byte-identical to the candidate.
- Desktop Card 323 accepted the same candidate/tree across all 18 ledger cells.
- Local and remote `v0.4.4` refs were absent at planning promotion.
- Current `main` contains later documentation-only closeout commits. It is not
  the tag target and must not replace the frozen candidate.

Canonical task: `docs/roadmaps/g05/036-v0-4-4-release-readiness.md`.

## Boundaries

Authorization covers exactly two external mutations: create one local
annotated tag `v0.4.4` at the named candidate SHA, then push that exact tag ref
to `origin`. It does not cover a GitHub Release, crates.io publication, binary,
sidecar, installer, model artifact, workflow edit, consumer repin, provider
call, or another tag. Never move, replace, delete, recreate, or force-push a
tag. Do not repeat Card 154 preparation or mutate the candidate tree.

The exact authorized annotation is:

```text
Swallowtail v0.4.4

Additive registered-tool qualification and Claude failure diagnostics. Canonical hosted CI run 34350208617 passed against 49c9e3b291609c9ebf5b35a284c08302f3b8d5e3.

The source-only release contains no crates.io publication, GitHub Release object, binary, sidecar, installer, or model artifact.
```

## Important Context

Card 154 already consumed the one authorized prepare and produced the frozen
candidate. Its archived workspace no longer retains Effigy's ephemeral
`.release-prepared.json`; do not fabricate or repeat prepared state. Use
Effigy's read-only release inspection where applicable, then create the exact
annotated Git object directly at the prepared candidate and push only
`refs/tags/v0.4.4`.

Immediately before mutation, fetch refs and verify: clean worker checkout;
candidate commit/tree and ancestry; workspace version `0.4.4`; exact release
baselines and note; local and remote tag absence; CI run `34350208617` exact
head/status/conclusion and eleven green jobs; prior immutable tag identities;
and canonical remote URL. Any mismatch stops before tag creation.

After push, verify the local and remote annotated tag object agree, both peel
to the exact candidate, and the annotation bytes equal the approved text. Wait
for the tag-triggered CI run and record its result. A red post-tag run does not
permit deletion, movement, recreation, or retry of the tag; report it as a
release failure under the next-patch rule.

## Suggested Next Move

Perform the read-only preflight once. If every invariant holds, create and push
the tag once, verify its immutable identity, then update current-main release,
contract, roadmap, and log surfaces in one documentation PR.

## Completion Protocol

Record tag object SHA, peeled commit, exact annotation, pre-tag CI run, tag-run
CI result, and local/remote agreement. Update Contract 036, the `0.4.4` release
note and index, `g05.036`, roadmap/generation front doors, and one annotated-tag
log. Preserve the later current-main documentation commits as post-tag history.

Run `effigy qa:docs`, `effigy qa:northstar`, `git diff --check`, and exact ref
checks for the documentation PR. Obtain independent exact-head review, merge
through Northstar Queue, and synchronize `main`. No consumer, provider, GitHub
Release, registry, binary, or publication work follows automatically.
