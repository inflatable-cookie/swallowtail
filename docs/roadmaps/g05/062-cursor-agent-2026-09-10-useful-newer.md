# g05.062 Cursor Agent 2026.09.10 Useful Newer

Owner: Tom
Created: 2026-09-14
Depends on: Contract 029; qualified Cursor Agent `2026.08.11-e8db854`; Research 135, 294, and 308; g05.061
Vision tags: route currentness, Cursor Agent, compatibility

## Outcome

Qualify current official Cursor Agent `2026.09.10-fd3934a` on the catalogue,
ACP, and headless axes if deterministic official artifact evidence admits each
route. Preserve exact date-plus-build identity, every historical milestone and
gap, the three distinct route claims, `AllowUnverified`, and all
feature-specific exact-version sets not independently proved at the new points.

Planning rechecked the official ACP registry at `2026.09.10-fd3934a`; the
host remains `2026.08.04-aaa8809`. Known later official observations include
`2026.08.31-4057e58` and `2026.09.02-c22c1a3`. Registry metadata is discovery,
not compatibility evidence. The worker must reconstruct the complete
officially published hop set after `2026.08.11-e8db854` and recheck latest.

## Ready-State Rubric

- [x] Tom authorized the complete Research 308 currentness campaign.
- [x] g05.061 completed and its lifecycle closeout is on canonical `main`.
- [x] Current claims, official channel, known intermediate identities, and
  host observation are known.
- [x] No prompt, login, installation, host update, or provider credit is needed.

## Work

1. Re-probe the official ACP registry and observe the installed host safely.
   Corroborate every later exact date-plus-build identity through Cursor's
   official downloadable artifacts, including the known `2026.08.31-4057e58`,
   `2026.09.02-c22c1a3`, and `2026.09.10-fd3934a` points. Retrieve artifacts
   into `/tmp`; freeze Research 312 and a secret-free complete identity/tree
   corpus without executing downloaded code.
2. Classify every changed shipped file feeding the selected catalogue, ACP,
   and headless commands; stream JSON and ACP wire; model controls; modes and
   authority; session, tool, activity, lifecycle, failure, configuration, and
   process behavior. Commit identity evidence before production claim edits.
   Changelog, registry descriptions, and calendar ordering are discovery only.
3. Record Contract 029 decisions for catalogue, ACP, and headless separately.
   Add only exact date-plus-build milestones directly admitted by evidence. Do
   not infer dates between them, widen Ask/high-effort/model-parameter sets, or
   turn advertised ACP list/load shapes into supported operations without their
   own exact proof.
4. A material public lifecycle or authority change stops with the smallest
   exact counterexample and a concrete follow-up. Recheck official latest
   immediately before the identity commit and push. Provider-free diagnosis
   and reruns remain allowed normally.

## Dispatch Manifest

| Field | Task 062 |
| --- | --- |
| Readiness | ready |
| Prerequisites | Contract 029; Research 135, 294, and 308; completed g05.061; clean pushed `main`; official Cursor ACP registry and download artifacts |
| Completion conditions | complete official hop set after `2026.08.11-e8db854` is frozen; every point binds exact date plus revision; identity precedes claim; catalogue, ACP, and headless each receive an honest decision; exact-head review and named gates pass |
| Owned mutable paths | Cursor selection/tests/fixtures under `crates/swallowtail-adapter-cursor/**`; Research 312 and one research-index line; exact Cursor prepared-guide, route-matrix, feature-matrix, architecture ceiling, standing-lane, `[Unreleased]`, identity-log, claim-log, and task-result lines; `PAPERCUTS.md` append only |
| Reserved shared closeout surfaces | lifecycle task record and generated projections; submitted-handoff deletion belongs to the repository hook |
| Forbidden paths | other adapters, contracts, historical research/releases, ACP load/resume support, new Cursor operations, public API expansion without a returned planning decision |
| Approved concurrent siblings | none on shared currentness surfaces; this campaign is serial |
| Worker capability class | evidence-first Rust currentness worker; official registry/download retrieval and deterministic wrapper/platform/source comparison; no provider credentials |
| Acceptance evidence | official metadata and artifact identities, host observation, complete mapped-surface ledger, mutation-sensitive fixtures, current docs/claim agreement |
| Review oracle | skipped official point, date-only identity, unclassified mapped delta, registry/changelog-only inference, lost historical milestone/gap, route flattening, or unjustified feature-specific widening |
| Stop conditions | official identity cannot be corroborated; major-line or identity-axis reset; mapped public lifecycle or authority change requiring new policy; provider/live evidence required |
| Escalation owner | operator via Chatterbox for new policy; Queue coordinator for mechanical blockers |

## Boundaries

One family only. No prompt, authenticated catalogue, live ACP or headless
session, login, credentials, install, host update, downloaded-artifact
execution, new public operation, release, tag, publication, or consumer
mutation. The npm `cursor-agent` package is a different identity axis.

## Validation

- `cargo fmt -p swallowtail-adapter-cursor -- --check`
- `effigy validate:focused swallowtail-adapter-cursor`
- `effigy package:verify-affected swallowtail-adapter-cursor`
- `effigy check:examples`
- `effigy package:api`
- `effigy qa:routes`
- `effigy qa:northstar`
- relevant research/log/roadmap/number/lifecycle/next-action checks
- `git diff --check`

## Acceptance

- [x] official identity and the complete later point set are reproducible
- [x] every accepted point binds its exact opaque build revision
- [x] every changed selected input is classified
- [x] identity evidence precedes production claim edits
- [x] catalogue, ACP, and headless remain separately truthful
- [x] historical milestones, gaps, exact feature sets, claims, and `AllowUnverified` survive
- [x] official latest is rechecked at both boundaries

## Result

Research 312 froze the official download identities for all three published
hops after `2026.08.11-e8db854` (`2026.08.31-4057e58`, `2026.09.02-c22c1a3`,
`2026.09.10-fd3934a`) with a complete tree inventory. The selected CLI
definitions, ACP initialize construction, and stream-json event keys are
text-identical modulo minifier renames; new flags attach only to unmapped
worker/controller commands and the SEA/native packaging refactor feeds no
selected surface. The compatible-extension claim adds the three exact
milestones through `2026.09.10-fd3934a`, keeps the three distinct route
claims, historical milestones and gaps, exact feature-specific sets, and
`AllowUnverified`, and leaves ACP load/replay and continuation recovery
blocked. No provider operation or host mutation occurred. Validation is
named in the handoff; PR review remains queue-owned.

## Next Task

Return the exact outcome to Chatterbox, then compile and dispatch Ollama as the
next Research 308 family without asking Tom to repeat authorization.
