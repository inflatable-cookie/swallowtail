---
title: g05.009 Contract 061 package completion worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Tom
created: 2026-08-31
updated: 2026-08-31
handoff_path: /Users/tom/Dev/projects/swallowtail/docs/handoffs/20260831-141547-g05-009-package-completion.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr]
---

## What This Thread Was Doing

The previous orchestrator closed the Contract 061 two-route proof through PR
131, then partitioned the remaining 716 census rows into 12 complete-package
candidates. Only candidate A passed the readiness rubric. This local
orchestrator verified the pushed Mac checkout and the Northstar authority
surface, then selected card 023 as the sole implementation lane.

This dispatches one bounded provider-free implementation lane. No transcript
or second prompt is part of the authority chain.

## Why It Matters

Card 023 completes the two adapter packages already started by PR 131. It must
publish exact prepared-operation truth for the remaining Codex Exec and OpenAI
Background rows without treating package ownership, documentation, another
route, reconciliation support, or successful execution as stronger evidence.

## Current State

- **Repository:** `/Users/tom/Dev/projects/swallowtail`
- **Planning branch:** `main`
- **Planning base commit:** `6619808d0a8c3931ebcc112a2ec039cbd4afd789`
- **Pushed main verification:** local `HEAD` and `origin/main` were equal at the
  planning base before this handoff was written; the worker must fetch the
  pushed handoff commit and require `HEAD == origin/main`
- **Planning checkout:** clean `main`; no implementation edits
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight
- **Planning artifacts included at the base:** Batch 9.4 package partition,
  ready card 023, sole roadmap Next Task, and the orchestrator continuation
  handoff
- **Worker branch:** `worker/g05-009-contract-061-package-completion`
- **Worker worktree:** Paseo-managed isolated worktree; accept the clean
  launcher-selected non-`main` worktree even when its generated path differs
- **Worktree creation command:** Paseo `branch-off` from `origin/main`; manual
  fallback only through the Completion Protocol and `.agents.local.env`
- **Required sibling worktree links:** none
- **Active spec lane:** none; Contract 061 is active
- **Roadmap milestone:**
  `/Users/tom/Dev/projects/swallowtail/docs/roadmaps/g05/009-contract-061-consumer-projection-realization.md`
- **Ready cards, in order:**
  `/Users/tom/Dev/projects/swallowtail/docs/roadmaps/g05/batch-cards/023-contract-061-codex-openai-package-completion.md`
- **Allowed runway:** exactly the remaining 35 `codex.exec` and 24
  `openai.background` census rows in one two-package PR
- **Remaining card budget:** one card; one reviewable PR; no auto-continuation
- **Dispatch topology:** serial single-card lane
- **Parallel safety check:** no other Northstar worker is authorized; PR 130 is
  separate and merge-withheld
- **Canonical refs:**
  `/Users/tom/Dev/projects/swallowtail/docs/contracts/001-working-rules.md`,
  `/Users/tom/Dev/projects/swallowtail/docs/contracts/037-prepared-consumer-integration.md`,
  `/Users/tom/Dev/projects/swallowtail/docs/contracts/047-configured-provider-instance-catalogue.md`,
  `/Users/tom/Dev/projects/swallowtail/docs/contracts/057-route-readiness-and-connection-admission.md`,
  and
  `/Users/tom/Dev/projects/swallowtail/docs/contracts/061-consumer-route-feature-and-control-projection.md`
- **Review oracle:** card 023, the Batch 9.4 package-expansion checkpoint, and
  the reviewed 767-row census
- **Model capability profile:** Paseo `Opus Worker`; complex Rust evidence and
  exact/negative-claim work, high reasoning
- **Tool/runtime restrictions:** no provider contact, live probe, external
  currentness work, watcher work, route expansion, or core/runtime/testkit API
  change
- **Required validation:** every command under card 023 `## Validation`; no
  live probe
- **PR base/head:** pushed `main` /
  `worker/g05-009-contract-061-package-completion`
- **PR URL:** pending
- **Review state:** awaiting implementation and exact-head orchestrator review
- **Merge authorisation:** withheld; the worker must not merge

## Boundaries

Please keep this run inside card 023:

- **In scope:** add the established contribution method to
  `CodexPreparedExec` and `OpenAiPreparedBackgroundRun`; implement independent
  exact 35-row and 24-row ledgers; add provider-free fixtures for the card's
  withholding, replacement, disagreement, lifecycle, state, and authority
  claims; create one reviewable PR
- **Out of scope:** every other census row; candidates B-L; Batch 9.5; PR 127
  or PR 130; provider contact; live probes; currentness; watcher or skill
  visibility; generation closeout; contract changes
- **Outcome shape:** one complete contract-valid implementation, deterministic
  proof, required validation, pushed branch, and reviewable PR; diagnostics
  alone do not complete the lane
- Do not add or change a runtime/testkit/core public type, fixed maximum,
  composer failure or rule, source kind, registry, route enumeration, callback,
  downcast, provider payload, active-observation seam, acknowledgement result,
  or provider operation.
- Work only in the clean worker worktree selected by the Completion Protocol.
  Never edit the planning checkout or an unrelated dirty checkout.
- Do not merge the PR. Merge remains separately operator-authorized.

## Important Context

- **Planning lineage:** Contract 061 promoted the reviewed 767-row census. PR
  131 merged the runtime composer, portable conformance, 36
  `codex.app-server` rows, and 15 `openai.realtime` rows. Batch 9.4 assigned the
  remaining 716 rows exactly once and promoted only candidate A as card 023.
- **Why this card is ready:** both named prepared facades already carry exact
  `PreparedOperationEvidence`; the established contribution boundary and
  `AdapterContribution` source kind are sufficient; the independent 35/24
  ledgers fit one two-package provider-free validation tranche.
- **Decisions and preferences:** emit only exact prepared-operation truth.
  Withhold documentation-only, incompatible-operation, route-wide, lifecycle,
  post-open, and unobserved rows at construction. Do not emit then filter.
  Keep requested, prepared, and provider-effective-unobserved states distinct.
- **Open tensions:** a census row may prove unsupported by the named prepared
  facade. Withhold it. If the exact ledgers require a new shared/public
  decision or truth borrowed from another route or operation, stop and return
  to planning.
- **Report after:** the coherent two-package implementation and deterministic
  ledgers are in place, then again after all card validation and PR creation;
  report sooner for a stop condition.
- **Report to:** the Paseo parent/orchestrator through the active control plane.

## Suggested Next Move

Run the Completion Protocol preflight before broad reads. Then read
`AGENTS.md`, card 023, g05.009, Contract 061, the Batch 9.4 checkpoint, and the
exact census rows for `codex.exec` and `openai.background`. Inspect the merged
app-server and Realtime contribution patterns, then implement both package
remainders as one coherent tranche.

## Completion Protocol

### Before you start

1. This handoff's `worker_mode: implementation` and
   `dispatch_authority: orchestrator` activate worker mode. Before broad reads,
   run `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. If the current root is a registered worktree, its status is empty, and its
   branch is not `main`, accept it as the launcher-provided worktree. Record its
   actual root and branch. Do not create another worktree because its generated
   path or branch differs from the placeholders above.
3. If the current context is `main`, dirty, unregistered, or unusable, report a
   launcher failure. Only then inspect the named fallback and
   `.agents.local.env`; require `AGENTS_WORKTREE_CONTAINER_DIR` before creating
   a unique manual worktree from `origin/main`. Never use `/tmp`, guess a path,
   or clean, reset, stash over, or discard another checkout.
4. From the selected worktree, run
   `GIT_SSH_COMMAND="ssh -o ConnectTimeout=10 -o BatchMode=yes" git fetch origin`.
   Require `HEAD == origin/main`, require planning base `6619808d` to be an
   ancestor, and require this repository-relative handoff in `HEAD`. Load it
   with `git show HEAD:docs/handoffs/20260831-141547-g05-009-package-completion.md`.
   If the absolute dispatch file differs, stop. The tracked `HEAD` copy is
   canonical.
5. Required sibling worktree links are `none`.
6. Read the active milestone, card 023, `AGENTS.md`, canonical contracts,
   Batch 9.4 checkpoint, and exact census rows.
7. Follow the repo's Effigy and Northstar Rust everyday-authoring routes. Read
   `PAPERCUTS.md` if present. Run cheap orientation checks and record what ran.

### While you work

- Execute card 023 only. Keep commits aligned with coherent implementation and
  proof chunks.
- Build the Codex and OpenAI emitted sets directly from exact prepared truth;
  ledgers must independently total 35 and 24 without exception filters or
  cross-package claims.
- Preserve source identity, exact access dimensions, lifecycle, omission,
  applicability, evidence, and negative truth. Do not infer provider-effective
  or acknowledgement state.
- Log a genuine small execution papercut before continuing, but do not fix it
  unless already in scope.
- Report meaningful chunks through Paseo with changed files, validation run,
  remaining work, risks, and blockers.
- Stop if a contract is missing, intent becomes ambiguous, scope expands, an
  exact ledger cannot be built honestly, a shared/public decision is needed,
  or validation changes the plan.

### When the assigned runway is complete

1. Run every command under card 023 `## Validation`. Do not run a live probe.
2. Try to falsify the diff. Enumerate the card's universal, exact, and negative
   claims; exercise every review-oracle counterexample; map all 35 and 24 rows
   to emitted or construction-time-withheld proof; confirm the existing 36 and
   15 row proofs remain exact.
3. Keep card 023 as the sole active Next Task through review. Add only honest
   execution evidence needed by the PR; do not promote candidates B-L or
   compile Batch 9.5.
4. Push the selected worker branch and open one PR against current pushed
   `main`.
5. Link card 023, g05.009, Contract 061, the Batch 9.4 checkpoint, changed
   surfaces, validation, and unresolved items in the PR body.
6. Report the PR URL and exact head through Paseo. Do not merge.

### Review and merge path

The orchestrator will review the exact PR head against Contract 061, card 023,
the Batch 9.4 checkpoint, the exact census rows, changed files, and settled
checks. If the shared GitHub identity prevents formal approval, the
orchestrator posts the verdict as a PR comment. A `planning-change` returns to
planning before revision. Requested changes are: none.

- **Closeout refs:** card 023, g05.009, the batch-card and g05/generation
  indexes, the log index, and the sole roadmap Next Task. Reconcile these only
  after accepted merge; do not move the pointer in the worker PR.

### Handoff closeout

Leave the branch, PR, card, roadmap, and Next Task honest. If either exact
ledger cannot stay inside the two prepared facades and existing Contract 061
public baseline, record the blocker and stop rather than narrowing or widening
the card.
