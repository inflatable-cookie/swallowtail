---
title: g05.051 OpenCode HTTP 1.18.30 useful-newer qualification
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
status: ready-to-launch
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Tom flagged OpenCode HTTP 1.18.29 -> 1.18.30 on 2026-09-11, then explicitly said Go after Chatterbox proposed g05.051 as one provider-free identity-before-claim qualification task."
queue:
  capability: general
  skipPRReview: false
  notifyOriginOnCloseout: true
---

## What This Thread Was Doing

Run the operator-approved one-family Contract 029 currentness task for
OpenCode HTTP, from qualified `1.18.29` through current official `1.18.30`.
Freeze exact identity before changing any claim, then qualify only the segment
the evidence admits.

## Why It Matters

Official stable has moved to `1.18.30`, while Swallowtail's latest qualified
OpenCode server ceiling remains `1.18.29`. `AllowUnverified` keeps the route
usable, but it does not substitute for support evidence.

## Current State

- Canonical task: `docs/roadmaps/g05/051-opencode-http-1-18-30-useful-newer.md`.
- Qualified axis: `opencode.server`, baseline `1.14.48`, latest `1.18.29`,
  behavior `opencode.http-sse.surface-19`, posture `AllowUnverified`.
- Official npm/GitHub stable at planning: `1.18.30`; this is the sole published
  stable after the ceiling. npm published it at `2026-09-09T03:33:55.588Z`;
  GitHub published `v1.18.30` at `2026-09-09T03:34:27Z`.
- npm integrity is
  `sha512-oLcOLQE4XzDKy6T5L5d1RdVJvXHXwVlD4hRF5V317JbUQorrl2EyDdGZk5kbgv675J9FXp8usg92MZbEWhh6gQ==`;
  GitHub tag commit is `3104c1428ec91f809e5ab86631300de41eb6952e`.
- Host `/Users/tom/.opencode/bin/opencode` reports `1.18.18`, SHA-256
  `4f5979c2dadb06fbff1335335afaaea274e58f92e79aa43cf2ed98618d555422`.
- The host was not installed, updated, or invoked beyond `--version`.

## Boundaries

Follow g05.051 exactly. One OpenCode HTTP family only. No provider prompt,
login, live server/session, credential use, install, host change,
downloaded-artifact execution, ACP or web-search work, Contract 061 projection
expansion, Gemini work, release, tag, publication, or consumer mutation.
Preserve every historical segment, gap, claim ID, and unrelated route. Do not
infer compatibility from changelog text.

## Important Context

Use the repository `version-currentness` skill, its `reference.md`, Contract
029, Research 292, and the frozen OpenCode corpora. The planning-time GitHub
tag comparison is diverged, not a linear descendant, so compare deterministic
complete source trees rather than trusting a commit-range summary. At minimum,
classify changed `provider/provider.ts`, `provider/transform.ts`, the added
Astra prompt, every selected route/handler input, and OpenAPI. Identity must be
its own commit before any production claim edit. Record compatible extension,
private milestone, or stop honestly. Apply In-Run Latest Movement at both
required rechecks.

The planning work reserves Research 304. If canonical pushed `main` has taken
that path before worker mutation, stop for Chatterbox to renumber; do not reuse
or overwrite another record.

## Suggested Next Move

Recheck official latest and repository ownership, retrieve the `1.18.29` and
`1.18.30` official artifacts into `/tmp`, then build the exact identity and
mapped-surface ledger before touching `selection.rs`.

## Completion Protocol

Meet every g05.051 acceptance row. Run its exact validation list, obtain an
independent exact-head review from a different provider/model identity, merge
through Northstar Queue, and synchronize `main`. Return exact npm/GitHub/host
identities, artifact and segment decision, identity and claim commit SHAs,
tests, review comment, PR/merge, canonical closeout, and the next currentness
state. No second family or release follows automatically.
