# 2026-09-02 Claude SDK Lifecycle Prerequisite Compilation

PR 188 exact-head review accepted the model, tool, platform, route-count, and
false-graceful repairs but rejected merge. The branch exposes
`claude-agent.sdk` as available even though close and post-expiry cleanup remain
unbounded and the host reports root exit without attesting tree emptiness.

g05.023 now owns the shared repair. Card 057 is ready for additive process-tree
completion evidence. Card 058 remains planned until the operator decides the
breaking interactive-session close seam. Card 055 and PR 188 are preserved,
paused, and serially dependent on both.

No SDK code, release state, provider session, credential, or tag changed in
this compilation.
