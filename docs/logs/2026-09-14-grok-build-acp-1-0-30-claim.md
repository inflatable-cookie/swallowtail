# 2026-09-14 Grok Build ACP 1.0.30 Claim

## Result

Grok Build ACP now qualifies official npm `@xai-official/grok` `1.0.30`
through all 25 published stable hops after the `1.0.5` ceiling. The
`grok-build.executable` window extends as a compatible extension on the
existing `grok-build.acp-v1.cached-token-model-4-6-v3` behavior revision and
the `grok-build.acp.executable-window-2` claim id. Deprecated
`0.2.114..=0.2.117`, gaps `0.2.118..=0.2.121` and `1.0.0..=1.0.3`,
baseline `0.2.114`, the `grok-4.6` binding, and `AllowUnverified` stay.
Published alpha `1.0.31` and the next unpublished stable `1.0.32` stay
visible `UnverifiedNewer`. Discovery now pins the exact source revision of
every qualified point through `1.0.30`.

The exact `1.0.25` `QualifiedOnly` catalogue claim does not move, and the
registered-tool courier is decoupled from the executable window: its
qualification is now keyed only to the accepted live capsules at exact
`1.0.4` and `1.0.5`, so later maintained ACP points stay unqualified for
registered tools. Identity evidence stays in the prior commit
(`1a764b96`): downloaded artifacts were hashed and never executed, every
hop's wrapper and platform tarball integrity and executable digest are
frozen, the mapped ACP literal presence map is byte-identical at every hop,
the embedded model document changes once only for the unread
`show_model_fingerprint` key, and the 62 mapped-core ACP modules persist.

Focused, affected-package, example, API, route, docs, Northstar, format, and
diff gates pass without provider work.

## Next

Return the exact outcome to Chatterbox, then compile and dispatch the Claude
Agent SDK tuple as the next Research 308 family without asking Tom to repeat
authorization.
