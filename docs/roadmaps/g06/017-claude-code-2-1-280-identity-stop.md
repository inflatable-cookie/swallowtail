# g06.017 Claude Code 2.1.280 Identity Stop

Owner: Tom
Created: 2026-09-23
Depends on: Contract 029; Research 331, 338; completed g06.009
Vision tags: route currentness, Claude Code, identity stop

## Outcome

Admit the standing-lane Claude Code `2.1.280` identity record as a reviewed
typed stop. The family's qualified ceilings stay at `2.1.278` on both axes and
no production claim moves.

## Why It Matters

The family was `AllowUnverified` official-newer after g06.009 raised both axes
through `2.1.278`. Official npm and GitHub moved to `2.1.280`, and the hop is
**not** a compatible extension: `--safe-mode` behaviour changes on the selected
headless surface. The honest result is a stop, and an unrecorded stop is the
thing that lets an unverified point look maintained.

## Ready-State Rubric

- [x] Official npm and GitHub agree on `2.1.280`; `2.1.279` and `2.1.281` are
      unpublished.
- [x] Both platform tarballs for the ceiling and the new point were retrieved,
      hashed, and inventoried, and the ceiling reproduces Research 331.
- [x] The selected-surface change is named by exact evidence rather than by a
      changelog.
- [x] The decision is a stop with no claim change.
- [ ] The branch's failing format check is repaired through review.

## Decisions

- **Stop. Do not claim `2.1.280`.** Keep both baselines, claim ids, behavior
  revisions and `AllowUnverified`.
- Headless and response-only `latest_qualified` stay at `2.1.278`; `2.1.279`
  stays the synthetic later `UnverifiedNewer` point and `2.1.280` becomes
  `UnverifiedNewer` under `AllowUnverified`.
- Keep every historical unpublished gap, watcher exact `2.1.251`, and every
  feature-specific exact version set.
- Do not flatten onto Claude Agent ACP or the Claude Agent SDK pin.
- The failing `Stable format and lint` check is evidence for review or
  revision, never a reason to bypass a merge gate.

## Dispatch manifest

- **State:** ready; standing-lane currentness record entering at review.
- **Owned mutable paths on revision:** this PR's existing surfaces only — the
  identity fixtures under
  `crates/swallowtail-adapter-claude-agent/tests/fixtures/claude-code-2.1.280/`,
  `tests/claude_code_2_1_280_identity.rs`, the `tests/integration/main.rs`
  registration, Research 338, and one research-index line.
- **Excluded:** any claim, matrix, admission, contract, or guide change; the
  Claude Agent ACP and SDK surfaces; the watcher; workflow, dependency,
  release, provider mutation; the Next Task pointer and generation runway.
- **Worker evidence:** the authenticated run result and the lane's designated
  evidence files. Do not edit this card's prose; the generated lifecycle block
  is hook-owned.
- **Escalation:** operator via Chatterbox for any scope question; Queue
  coordinator for mechanical blockers.

## Work

1. Verify the record against the frozen evidence: the two-hop identity,
   reproduced ceiling, unpublished-neighbour proof, and the named
   `--safe-mode` change on the selected headless surface.
2. Confirm the decision holds: a stop, ceilings unchanged, no flattened
   inference onto the neighbouring Claude families.
3. Repair the failing format check within the lane's existing surfaces.
4. Run the focused adapter validation and the named docs gates.

## Acceptance and review oracle

| Invariant | Adversarial counterexample | Required proof |
| --- | --- | --- |
| No claim moves | A matrix row or selection claim gains `2.1.280` | diff shows no claim, matrix, or admission change |
| The stop rests on selected-surface evidence | The change is inferred from a changelog or a version bump | the exact behavior is cited from frozen platform artifacts |
| Neighbouring families stay separate | An ACP or SDK pin is changed, or the hop is flattened onto one | no ACP or SDK surface in the diff |
| The recorded decision is internally consistent | `2.1.280` is treated as qualified or as compatible elsewhere | ceilings, gaps, and `UnverifiedNewer` agree across the record |

## Stop conditions

Stop and ask if the evidence shows a compatible extension rather than a change,
if the selected-surface delta cannot be named exactly, if the channel
observation cannot be reproduced from frozen artifacts, or if the record's
decision conflicts with an existing claim.

## Evidence

Research 338 and its fixtures. Focused and affected-package validation for
`swallowtail-adapter-claude-agent`, route and matrix QA, and the named docs
gates.

## External PR Review Entry Authority

Tom identified this PR as Grok Bot external work on 2026-09-23 and directed its
integration. External work and its evidence are submitted claims, not
independent acceptance. The external author declaration is `xai/grok-4.6`,
GitHub author `betterthanclay`, authoring system Cursor cloud driven by Grok
Bot, under Tom's cloud-session attestation of 2026-09-22 extended to this PR by
his identification of the author.

Queue starts an independent reviewer, not a worker. Changes requested authorize
one ordinary revision loop in the same workspace and branch, scoped to this
Claude Code record and its existing changed surfaces, including the failing
format check. Queue owns merge and lifecycle closeout. Intake preparation may
publish this planning and merge it into the existing branch.

Canonical handoff: `docs/handoffs/20260923-g06-017-external-pr-review-entry.md`.

Preserve the external branch and workspace after completion. The other open
external PRs are not dispatched by this approval.

## Next Task

Chatterbox reconciles the route after closeout: the ceilings stay at `2.1.278`,
so no claim work follows, and the only standing-lane consequence is that
`2.1.280` is a named `UnverifiedNewer` stop until the selected `--safe-mode`
behavior is either reverted upstream or ruled on.
