---
title: g04.064 Kimi Code headless v2 worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Tom
created: 2026-08-25
updated: 2026-08-25
planning_base: 3a65457c2724ca24a51c23cb0f9848a0769a1712
handoff_path: /Users/tom/Dev/projects/swallowtail/docs/handoffs/20260825-200324-g04-064-kimi-code-headless-v2.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, currentness]
---

## What This Thread Was Doing

The orchestrator reviewed and fast-forwarded PR 62, reconciled the g04.063
evidence stop, and compiled the exact follow-up exposed by that work. Kimi Code
headless remains qualified only through `0.37.2` under the legacy v1 corpus.
Exact official `0.38.0` is visible `UnverifiedNewer` because its actual default
`kimi -p` path is agent-core-v2 `runV2Print`, which has not been independently
qualified.

g04.064 and cards 179-180 now form one serial Contract 029 family lane. Card
179 owns exact v2 identity and protocol evidence. Card 180 runs only if that
evidence admits an adapter-private mapping behind the existing public
structured-run lifecycle. An incompatible stop is a valid result.

This is the complete handoff from the planning/orchestrator thread to one
bounded implementation thread. Start from this file without a copied
transcript or second prompt. Do not create internal subagents or parallel
worker lanes; the operator's harness owns dispatch.

Read the `northstar` skill, then `references/router.md` and
`references/modes/handoff.md`. Also read and follow the repository
`version-currentness` skill and its `reference.md`; this handoff activates one
family-mode run. Use Effigy for repository validation.

## Why It Matters

The route currently permits `0.38.0` as unverified even though it launches a
different engine from the qualified v1 mapping. That is the right fail-visible
posture, but it is not a finished currentness result. We need either an exact
v2 behavior milestone backed by source and secret-free corpus evidence, or a
named incompatibility explaining why the official stable cannot be qualified.

The work is not a request to chase features or force the old engine. It is a
compatibility truth exercise: prove the actual default executable behavior at
the selected route boundary and keep every parser, lifecycle, retry, terminal,
activity, and retained-state claim no stronger than the evidence.

## Current State

- **Repository:** `/Users/tom/Dev/projects/swallowtail`
- **Planning branch:** `main`
- **Planning base commit:**
  `3a65457c2724ca24a51c23cb0f9848a0769a1712`
- **Pushed main verification:** local `HEAD` and `origin/main` both resolved to
  the planning base before this handoff commit
- **Planning checkout:** clean after the planning commit and push
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight
- **Planning artifacts at the base:** g04.064, cards 179-180, Research 211
  reservation, compilation log, closeout reservation, PR62 reconciliation,
  programme/triage/index updates, and the sole Next Task
- **Worker branch:**
  `agent/g04-064-kimi-code-headless-v2-20260825-200324`
- **Worker worktree:**
  `/Users/tom/Dev/worktrees/swallowtail-g04-064-kimi-code-headless-v2-20260825-200324`
- **Worktree creation command:** `git worktree add
  /Users/tom/Dev/worktrees/swallowtail-g04-064-kimi-code-headless-v2-20260825-200324
  -b agent/g04-064-kimi-code-headless-v2-20260825-200324 origin/main`
- **Worker worktree policy:** first use the clean, dedicated, non-`main`
  registered worktree supplied by the launcher, even when its generated path
  or branch differs from these placeholders. Record the actual path/branch and
  never create a second worktree for that reason. If the current context is
  unusable, use the named worktree when it matches; only then read
  `.agents.local.env`, require `AGENTS_WORKTREE_CONTAINER_DIR`, and create a
  unique manual worktree/branch under that container from `origin/main`. Ask
  the operator first if the file or key is absent. Never use `/tmp`, `TMPDIR`,
  or a guessed path for a worktree.
- **Active lane:** Contract 029 one-family currentness inside active g04; it
  does not close or keep open the generation
- **Roadmap:**
  `docs/roadmaps/g04/064-kimi-code-0-38-0-headless-v2-useful-newer.md`
- **Ready cards, in order:** card 179, then conditional card 180
- **Allowed runway:** one exact route/version family, `kimi-code.headless` at
  official `@moonshot-ai/kimi-code@0.38.0`
- **Remaining card budget:** two serial cards; stop after 179 unless Research
  211 admits an adapter-private claim shape
- **Dispatch topology:** one serial worker lane; cards share the same official
  identity, fixture corpus, decoder, selection claim, guides, and closeout
- **Parallel safety check:** no parallel lane is authorised; both cards mutate
  the same adapter and truth surfaces
- **Route identity:** route `kimi-code.headless`, driver
  `swallowtail.kimi.headless`, axis `kimi-code.executable`, claim
  `kimi.headless.executable-window-2`
- **Current claim:** `0.29.0..=0.37.2` under
  `kimi.headless.stream-json.v1`; `AllowUnverified`; exact `0.38.0` currently
  `UnverifiedNewer`
- **Exact upstream identity:** npm `@moonshot-ai/kimi-code@0.38.0`, official
  tag commit `0999454bdcb5ddd98f39bffee434dcf0a810f394`; Research 179 and
  Research 210 carry existing hashes and correction lineage
- **Decisive routing evidence:** `isKimiV2Enabled() = !isLegacyEnabled()`;
  naked `0.38.0` headless dispatches `runPrompt` to `runV2Print` unless
  `KIMI_CODE_LEGACY_FLAG` is truthy; Swallowtail does not set that flag
- **Expected admitted shape:** preserve v1 through `0.37.2`; if proved, add
  exact `0.38.0` under an adapter-private revision expected to be
  `kimi.headless.stream-json.v2`; keep synthetic `0.38.1`
  `UnverifiedNewer`
- **Canonical refs:** Contracts 011, 029, 033, 037, 044, and 052; the version
  currentness checkpoint; Research 179 and 210; the Kimi prepared guide and
  route/feature matrices
- **Model capability profile:** frontier implementation and protocol audit;
  deterministic source/fixture evidence only
- **Tool/runtime restrictions:** official public source and packages may be
  inspected in disposable `/tmp`; no Kimi install, live prompt, account,
  credential, login, host mutation, or paid inference
- **Required validation:** `cargo fmt -p swallowtail-adapter-kimi`, `effigy
  validate:focused swallowtail-adapter-kimi`, `effigy
  package:verify-affected swallowtail-adapter-kimi`, `effigy qa:routes`,
  `effigy qa:northstar`, relevant research/log/roadmap/g04/batch-card and
  sole-next-action index gates, and `git diff --check`
- **Known doctor baseline:** 378 god-file findings: 332 warnings and 46
  errors; stale graph index; one generated-in-src warning. Do not increase it.
- **Planning validation:** `effigy tasks`, `effigy doctor`, `effigy test
  --plan`, `effigy qa:docs`, `effigy qa:northstar`, and `git diff --check` ran.
  Docs and Northstar passed; doctor reproduced the inherited baseline.
- **PR base/head:** current pushed `main` / selected worker branch
- **PR URL:** pending
- **Review state:** awaiting worker evidence and PR
- **Merge authorisation:** none; do not merge

## Boundaries

Please keep this run inside the named runway:

- **In scope:** `crates/swallowtail-adapter-kimi/**` for exact v2 identity,
  selected-source corpus, decoder/activity/terminal/pump comparison, behavior-
  revision selection, prepared headless checks, fixtures, tests, examples, and
  API baseline only as warranted; Research 211; g04.064; cards 179-180; Kimi
  guide; route and feature matrices; changelog; identity/claim logs; reserved
  closeout; currentness, programme, triage, and index surfaces
- **Out of scope:** Kimi headless reasoning effort, plan, permissions, raw
  environment or config; forcing `KIMI_CODE_LEGACY_FLAG`; Kimi ACP,
  local-server, Platform Chat, or Python `kimi-cli`; other version families;
  new portable capability, shared runtime abstraction, contract change, live
  provider work, release, publication, merge, generation rollover, or g04
  closure
- Card 179 makes no production claim edit. Reconfirm official stable and exact
  artifact identity before source comparison. If official latest has moved or
  identity cannot be corroborated, stop and report the stale lane.
- Audit the actual default v2 engine. Do not qualify selected legacy source,
  assume the ambient host supplies a legacy flag, or reuse v1 behavior merely
  because CLI flags still say `stream-json`.
- Freeze every selected source blob needed to explain JSONL emission, renderer
  shape, stderr, terminal success/failure/interruption, process exit, retry,
  tool activity, resume hints, cancellation, unknown/malformed input, and
  retained provider state. Record retrieval dates and hashes.
- Synthetic fixtures may test the Swallowtail parser, but they are not
  official evidence that Kimi emits a shape. Label source-proved,
  fixture-proved, and withheld behavior separately.
- If exact v2 is representable behind the existing public structured-run
  lifecycle, a new adapter-private behavior milestone is in scope. If it needs
  a new public operation, driver, facade identity, shared contract, or public
  lifecycle semantics, stop after card 179 for orchestrator review.
- If terminal, retry, activity, parser, interruption, or exit truth cannot be
  established without a live prompt or authenticated session, record the
  incompatible reason and stop. Do not run provider work to rescue the lane.
- Preserve v1 qualification through `0.37.2`. Exact `0.38.0` must never decode
  under the v1 revision. Preserve ACP and local-server `0.38.0` claims.
- Do not rewrite historical Research 179 or 210. Research 211 records the new
  decision and current docs link the lineage.
- Default QA must not resolve credentials, install Kimi, run a prompt, contact
  a provider, or use paid inference.
- Work only in the selected clean worker worktree. Never edit the orchestrator
  planning checkout or clean, reset, or stash over unrelated state.
- Do not create subagents or parallel workers. Do not merge the PR. Merge
  remains a separate operator-authorised action.
- Follow repository `AGENTS.md`, canonical authority, glue-light reporting,
  the `version-currentness` family workflow, and Effigy selectors. Work in one
  meaningful batch.

## Important Context

- **Planning lineage:** g04.032 initially qualified all three Kimi Code routes
  through `0.38.0`. g04.063/Research 210 found that only the selected v1 source
  had been audited while naked headless `0.38.0` defaults to v2. PR 62 landed
  the correction at `5f37ff6b`. g04.064 is the exact repair, not a new feature
  detour.
- **Why these cards are ready:** exact official identity and engine routing are
  already known, the production claim is safely retracted, the active
  generation exists, and Contract 029 defines identity-before-claim.
- **Selected source leads:** start with
  `apps/kimi-code/src/cli/experimental-v2.ts`, `run-prompt.ts`, and
  `cli/v2/run-v2-print.ts` at the exact tag, then follow the imports that own
  v2 JSONL writing, events, activity, lifecycle, and exit behavior. Do not stop
  at those three files if the selected path crosses packages.
- **Current Swallowtail leads:** `selection.rs`, `headless_events.rs`,
  `headless_activity.rs`, `headless_pump.rs`, `headless_validation.rs`,
  `prepared_headless/**`, and headless fixtures/tests. Current instance facade
  naming includes `kimi-headless-stream-json-v1`; treat any need to revise a
  public facade identity as a decision gate, not a mechanical rename.
- **Decisions and preferences:** manual operator-harness handoff only; no
  internal subagents. g04 remains open until the operator explicitly closes
  it. Currentness stays one family at a time. A named incompatible stop is
  better than a red or overstated claim.
- **Open tensions:** v2 may reuse familiar JSONL names while changing event or
  lifecycle meaning; stderr/exit behavior may be less statically provable than
  parsing; the public `ProtocolFacadeId` may expose a revision seam. Research
  211 must settle these instead of assuming compatibility.
- **Report after:** card 179 and Research 211, then card 180 only if admitted
- **Report to:** the operator, who relays progress to the orchestrator

## Suggested Next Move

This handoff explicitly activates worker mode. Start by reading it from the
top. Before broad repository reads, run the quick startup worktree-safety
preflight below. If the current context is a clean, dedicated, non-`main`
registered worktree, use it immediately, record its actual path/branch, and do
not create another worktree because its generated name differs from this file.

Read `AGENTS.md`, the Northstar handoff mode, the `version-currentness` skill
and reference, g04.064, cards 179-180, Research 179/210/211, Contract 029's
Upgrade Workflow, the currentness checkpoint, the Kimi prepared guide, and the
route-local files named above. Execute card 179 first. Promote Research 211
with a fresh official observation, exact selected v2 source/corpus, and one
honest segment decision. Continue automatically only if its gate admits an
adapter-private claim.

## Completion Protocol

### Before you start

1. Read this handoff path. Its `worker_mode: implementation` and
   `dispatch_authority: orchestrator` metadata activate worker mode. Run one
   quick read-only safety probe before broad repository reads: `git rev-parse
   --show-toplevel`, `git branch --show-current`, `git status --porcelain`, and
   `git worktree list --porcelain`.
2. If the current root is a registered worktree, its status is empty, and its
   branch is not `main`, accept it as the launcher-provided worktree. Record its
   actual root/branch. Do not compare it with the placeholder path/branch or
   create another worktree merely because they differ.
3. Only if the current context is `main`, dirty, unregistered, or otherwise
   unusable should you inspect the named worktree. If that also cannot be used,
   read `.agents.local.env` and require `AGENTS_WORKTREE_CONTAINER_DIR`; ask the
   operator if it is absent. Create a unique worktree and branch under that
   container from `origin/main`. Never use `/tmp`, `TMPDIR`, or a guessed path;
   never clean, reset, stash over, or discard another checkout's state. If the
   launcher supplied a dirty or `main` worktree, stop and report it rather than
   silently creating a second worktree.
4. From the selected worktree, confirm `HEAD == origin/main`, confirm planning
   commit `3a65457c2724ca24a51c23cb0f9848a0769a1712` is an ancestor of
   `HEAD`, and confirm this handoff exists in the selected `HEAD`.
5. Read the named skills, milestone, cards, evidence, contracts, guides, and
   route-local source before editing.
6. Run `effigy tasks`, `effigy doctor`, and `effigy test --plan`. Record the
   inherited doctor baseline; do not turn it into unrelated cleanup.

### Execute

1. Execute card 179 only. Re-observe the official channel, inspect exact
   official artifacts in disposable `/tmp`, freeze selected v2 evidence, add
   the secret-free corpus, promote Research 211, and name the decision. Make no
   production claim edit yet.
2. If the decision is incompatible, needs live provider evidence, needs a new
   public driver/facade/lifecycle, or official identity moved, stop honestly.
   Leave card 180 unexecuted, close g04.064 as a named stop, update the reserved
   closeout/current docs/indexes/sole Next Task, validate, commit, push, and
   open the evidence PR.
3. If Research 211 admits an adapter-private v2 milestone behind the existing
   public lifecycle, execute card 180 in the same serial lane. Split the claim
   by assessed behavior revision, bind the exact v2 decoder/lifecycle corpus,
   preserve v1 and sibling claims, and update current truth surfaces.
4. Mark roadmap/card/research/log/index status from actual evidence. Keep g04
   active. Do not invent the next route task beyond the closeout rule; leave
   one honest sole Next Task for orchestrator continuation.

### Validate and report

1. Run every card-specific gate and the final validation set named in Current
   State. Run specific index selectors rather than broad workspace QA. Default
   validation stays account-, credential-, install-, prompt-, and
   provider-free.
2. Run `git diff --check` against the planning base. Confirm no secrets,
   downloaded packages, executable artifacts, temporary corpus, or
   out-of-scope changes remain.
3. Commit cohesive work, push the selected worker branch, and open one PR
   against the current pushed `main` tip. Do not merge it.
4. In the PR body, link g04.064, cards 179-180, Research 211, changed surfaces,
   evidence, validation, and unresolved items.
5. Report exact branch, PR URL, head SHA, official identity observation,
   Research 211 decision, card 180 disposition, behavior/claim result or named
   stop, changed surfaces, validation, inherited baseline, and review concerns.
   State clearly that merge authority remains with the operator/orchestrator.

### Review and merge path

The orchestrator will review the PR against canonical refs, exact head, diff,
and checks. When formal self-approval is unavailable, it will post the verdict
as a PR comment. If changes are requested, make only those changes on this
branch, push again, and report back through the operator. The operator must
explicitly authorise any merge.

- **Closeout refs:** g04.064, cards 179-180, Research 211, the reserved
  g04.064 closeout, currentness standing lane, programme, triage, research/log/
  roadmap indexes, and the sole Next Task

### Handoff closeout

Before calling the runway complete, leave the card, roadmap, log, and next-task
state honest. If the work is blocked, record the exact incompatibility and stop
rather than making the handoff look more complete than it is. Do not create a
second handoff.
