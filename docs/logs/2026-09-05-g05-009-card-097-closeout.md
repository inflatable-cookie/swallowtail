# 2026-09-05 g05.009 Card 097 Closeout

Card 097 completed Candidate L across OpenCode and Pi: 69 ledger rows, 64
emitted and 5 withheld. The Pi SDK-sidecar plan widening was reverted; the
three unsupported sidecar rows are withheld at construction with anchored
reasons. The final implementation binds ledgers bidirectionally to real
prepared facades, uses the corrected `session-lifecycle` tuple, and proves
Pi RPC to SDK-sidecar mixed-assembly rejection in both directions.

PR 234 was independently accepted at exact head
`58f8495f44b917ac6d7ed0a5d58b68a5338705eb` and merged as
`f4e707f08e7bb1dd064914766f7b17449e3d2c6c`. All 11 hosted checks were green;
focused validation reached 239/239 and the named API, routes, docs,
Northstar, god-files, and diff checks passed.

Contract 061 now proves all 767 census rows. The per-turn authority audit note
remains in place for Chatterbox's reserved prune and was not deleted by this
closeout.
