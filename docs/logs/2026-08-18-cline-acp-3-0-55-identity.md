# 2026-08-18 Cline ACP 3.0.55 Identity

## Result

Card 262 froze official Cline ACP identity at npm `cline@3.0.55` /
GitHub `cli-v3.0.55` without installing, logging in, or sending a prompt.
The selected wire is `cline --acp` stdio ACP: initialize, `session/new`,
one bounded `session/prompt`, cancel, and joined cleanup. `--json`, TUI,
hub, `--id`, and `--auto-approve true` stay out. Named fixtures live under
the future adapter tree.

## Next

Implement the Cline ACP driver core (card 263).
