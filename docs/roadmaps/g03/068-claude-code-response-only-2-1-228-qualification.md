# 068 Claude Code Response-Only 2.1.228 Qualification

Status: completed
Owner: Tom
Created: 2026-08-12
Depends on: g03.066; Figmatic `g04.005` preparation evidence
Contract refs: 039, 044

## Problem

Figmatic's packaged `g04.005` mutation-runway smoke reaches isolated
preparation, but exact Claude Code `2.1.228` fails discovery because the
response-only route admits only `2.1.227`.

## Generation Runway

Advance g03's exact compatibility and consumer-proven hardening goals. Replace
one exact qualified release only after the existing protocol and authority
boundary passes deterministic and live evidence.

## Execution Plan

- [x] card 214: replace the exact compatibility point, move the deterministic
      corpus, preserve fail-closed neighbors, and pass package plus live proof

## Downstream Handoff

- [ ] card 215: link the exact implementation commit in Figmatic and run the
      packaged `g04.005` mutation-runway smoke

## Goals

- [x] qualify only exact Claude Code `2.1.228`
- [x] preserve ordinary response text, empty tools and MCP, validated and
      discarded private thinking, and Max/OAuth without an API key
- [x] keep the prepared API, command, route identity, and host authority
      unchanged
- [x] leave exact `2.1.227`, exact `2.1.229`, and all other releases rejected

## Acceptance Criteria

- [x] deterministic discovery, init, progress, private-thinking, text, result,
      failure, cancellation, and cleanup cases pass
- [x] exact-version assertions prove one point rather than a range
- [x] focused and affected-package validation pass
- [x] current guide, route matrix, architecture, testkit, and contract surfaces
      name exact `2.1.228`
- [x] the gated live probe passes through local Max/OAuth with
      `ANTHROPIC_API_KEY` absent

## Boundaries

- no prepared API, command flag, model, credential, host-service, capability,
  output, retry, continuation, or fallback change
- no inference from semantic-version adjacency
- no edit to immutable `v0.3.2`, historical research, release, or prior
  qualification records
- no Figmatic mutation in this repository
