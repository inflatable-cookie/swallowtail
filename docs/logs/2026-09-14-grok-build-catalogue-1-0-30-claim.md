# 2026-09-14 Grok Build Catalogue 1.0.30 Claim

## Result

Grok Build's separately prepared `grok-build.catalogue` route now admits only
exact installed `1.0.30`. The exact one-point claim is
`grok-build.catalogue.executable-1-0-30` under `QualifiedOnly`, with the
`grok-build.catalogue.models-text-v1` behavior revision unchanged; every older
and newer point fails closed before any process starts. The previous exact
`1.0.25` point is not retained as a second accepted point.

Research 316 (identity commit `f6379f8e`) reproduced the Research 314
`darwin-arm64` platform tarball, brotli payload, and decompressed executable
digests for `1.0.25` and every later published stable through official
`1.0.30`, without executing a downloaded artifact. The catalogue-specific
ledger proves the exact root flag `--no-auto-update`, the `models` subcommand
grammar with no `PROMPT` argument, the authentication preamble, the shipped
`*`/`-` bullet format literals with byte-identical counts, the
`xai-grok-pager/src/models.rs` module path, and the embedded
`default_models.json` document identical at every hop. The installed exact
`1.0.30` `--help` argv-grammar probes are provider-free exits and match the
Research 306 `1.0.25` record.

The provider-free parser, driver, fake-process, local-host, redaction, and
lifecycle gates passed before the live boundary. One prompt-free authenticated
`--no-auto-update models` observation of the installed exact `1.0.30`
executable then returned ordered `grok-4.6` default then `grok-4.5`: 113
stdout bytes, zero stderr, `outcome = "success"`, `process_joined = true`, and
no prompt, model session, inference, or tool dispatch. The stdout byte count
and SHA-256 reproduce the accepted `1.0.25` capsule exactly, and the redacted
`1.0.30` capsule is frozen at
`crates/swallowtail-adapter-grok/tests/fixtures/grok-1.0.30-catalogue/live-capsule.json`.
The live listing remains authoritative for membership, order, and default; the
frozen document supplements matching exact ids only; unknown valid ids pass
through and absent fields stay absent. Raw stdout and stderr never enter the
record.

The Grok ACP execution window, the `1.0.4`/`1.0.5` registered-tool courier,
and every historical corpus stay independently bounded. `GROK_BUILD_CATALOGUE_VERSION`
and the frozen `default_models_1_0_30.json` metadata moved with the claim.

## Next

Return the exact outcome to Chatterbox, then compile and dispatch Command Code
as the next Research 308 family without asking Tom to repeat authorization.
