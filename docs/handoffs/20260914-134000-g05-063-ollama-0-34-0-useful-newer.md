---
title: g05.063 Ollama 0.34.0 useful-newer qualification
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
status: ready-to-launch
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Tom instructed Swallowtail on 2026-09-14 to action every family in Research 308 and continue without repeated approval; g05.062 is complete and Ollama is next."
roadmap: docs/roadmaps/g05/063-ollama-0-34-0-useful-newer.md
queue:
  capability: general
  skipPRReview: false
  notifyOriginOnCloseout: true
---

## What This Thread Was Doing

Continue Tom's authorized Research 308 serial currentness campaign with Ollama.
Qualify every published stable after `0.32.15` through current official
`0.34.0` on the existing attached native-runtime route where complete tagged
source evidence admits it.

## Why It Matters

The maintained `ollama.runtime` claim ends at `0.32.15`, while the official
GitHub release is `v0.34.0` and the installed client is already `0.33.3`.
`AllowUnverified` keeps later stables visible but does not establish support.

## Current State

- Canonical task: `docs/roadmaps/g05/063-ollama-0-34-0-useful-newer.md`.
- Campaign authority: Research 308 and Tom's 2026-09-14 instruction to action
  all named families without repeated approval.
- Existing claim: maintained `0.14.0..=0.32.15`, exact exclusions `0.32.2`
  and `0.32.10`, claim `ollama.native-runtime-window-2`, behavior
  `ollama.native-text-v1`, posture `AllowUnverified`.
- Planning official point: GitHub `v0.34.0`. Published later stables are
  `0.33.0`, `0.33.1`, `0.33.2`, `0.33.3`, and `0.34.0`. Host client:
  `0.33.3`; no runtime was reachable during the promptless version check.

## Boundaries

Follow g05.063 exactly. Ollama attached native-runtime family only. No prompt,
model inference, live catalogue or runtime API call, server start, login,
credentials, install, host mutation, downloaded-artifact execution, model
management, other adapter change, release, tag, publication, or consumer
mutation. Provider-free retrieval, parsing, comparison, correction, and
validation may be rerun normally; impose no artificial attempt budget.

## Important Context

Use the repository `version-currentness` skill, Contract 029, Research 174 and
308, and the frozen `0.32.15` evidence. Inspect all five planning-time stable
hops plus any new one found by the required rechecks. Compare the complete
tagged-source inputs to the five selected HTTP operations, wire types, NDJSON,
errors, controls, transcript, cancellation, and lifecycle. Identity must be
committed before any claim edit. Preserve the exact `0.32.2` and `0.32.10`
holes, attached external-runtime ownership, claim identity, behavior revision,
historical corpus, and feature-specific boundaries. Do not infer compatibility
from release notes or flatten Ollama Cloud, unused APIs, model management,
server lifecycle, or llama.cpp into this family. Apply In-Run Latest Movement
before the identity commit and final push.

Research 313 is reserved; renumber cleanly if canonical main takes it first.
Do not add a task-level `Status:` header: lifecycle state belongs to the
repository hook.

## Suggested Next Move

Recheck GitHub latest and releases, retrieve all tagged source points into
`/tmp`, and build the tag/commit/tree and mapped-source ledger before touching
`selection.rs`.

## Completion Protocol

Meet every g05.063 oracle, run its named gates, obtain independent exact-head
review, and merge through Queue. The lifecycle hook owns closeout and handoff
consumption. Return official and host identities, segment decision, identity
and claim commits, checks, review, PR/merge, and canonical closeout.
Chatterbox then compiles Grok Build ACP from fresh main under the same standing
operator authority.
