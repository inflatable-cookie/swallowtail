# 2026-08-22 g04.035 Cursor Headless Model Parameters Compiled

## Change

- rechecked current official Cursor model-parameter documentation
- inspected all four exact qualified Cursor Agent help surfaces without a
  provider call
- compiled g04.035 and cards 095-097 for evidence, typed binding, and
  deterministic acceptance
- advanced the per-route feature programme to its first numbered milestone

## Decision

Cursor Fast, context, and effort stay adapter-local model parameters. The
worker must first freeze an exact model/parameter/value allowlist; the CLI
catalogue exposes plain model ids and cannot justify arbitrary combinations.

Typed selections render canonically into the exact model id bound by the
immutable plan and passed as one `--model` argument. Raw bracket grammar is
rejected. Qualified effort also binds portable reasoning selection when the
exact tuple is proved. Fast and context do not become portable aliases.

The milestone claims qualified dispatch only. Provider acceptance and
effective-value confirmation remain separate states under Contract 040.

## Evidence

- exact qualified builds `2026.07.01-41b2de7`,
  `2026.07.23-e383d2b`, `2026.08.04-aaa8809`, and
  `2026.08.11-e8db854` publish the same quoted bracket-override example
- official CLI and subagent documentation names `fast`, `context`, and
  `effort` parameters and says availability varies by model
- official TypeScript SDK documentation exposes model parameter descriptors,
  but that sibling surface is not the selected subscription CLI catalogue
- no prompt, authentication, live catalogue, install, or host update ran

## Next

Execute cards 095-097 serially in one worker PR. After g04.035 closes, compile
the Ollama attached `num_ctx` milestone.
