---
title: g04.053 Qoder headless maximum-turn worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Tom
created: 2026-08-24
updated: 2026-08-24
handoff_path: /Users/tom/Dev/projects/swallowtail/docs/handoffs/20260824-000717-g04-053-qoder-max-turns.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr]
---

## What This Thread Was Doing

The orchestrator merged and closed out g04.052, reassessed the remaining
promoted per-route feature inventory, and selected exact Qoder `1.1.25`
caller-decreasing maximum turns. g04.053 is compiled. Implementation has not
started. Card 148 is the evidence gate; cards 149-150 run only if Research 200
admits a non-empty exact deliver-now set.

This is one bounded implementation handoff. Start from this file without a
copied transcript or second prompt. Do not create internal subagents or
parallel worker lanes; the operator's harness owns dispatch.

## Why It Matters

`qoder.headless` already sends `--max-turns 8` on its one print child.
Consumers cannot choose a smaller conversation envelope. Research 151 proves
the exact package requires a positive native bound, treats flag omission as
unbounded, and emits distinct `error_max_turns` terminal truth. It does not
prove the exact counter semantics, off-by-one behavior, or public domain.

The target is narrow: exact caller-decreasing selection, omission
compatibility, and truthful native-limit behavior. It is not a portable
output-token or generic budget control and does not alter Qoder permission,
output, persistence, workdir, access, auth, deadline, cancellation, or cleanup.

## Current State

- **Repository:** `/Users/tom/Dev/projects/swallowtail`
- **Planning branch:** `main`
- **Planning base commit:** `f7593490ccf6f7f1f3cf741c04eb20b8112c09e5`
- **Pushed main verification:** local `HEAD` and `origin/main` both resolved to
  `f7593490ccf6f7f1f3cf741c04eb20b8112c09e5` before this handoff commit
- **Planning checkout:** clean after the planning commit and push
- **Done:** g04.052 shared closeout; g04.053; cards 148-150; Research 200
  reservation; compilation and closeout logs; triage selection; sole Next Task
- **Still open:** card 148 evidence; conditional cards 149-150; worker PR,
  review, merge, and orchestrator shared closeout. g04 remains active.
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight
- **Worker branch:** `agent/g04-053-qoder-max-turns-20260824-000717`
- **Worker worktree:** `/Users/tom/Dev/worktrees/swallowtail-g04-053-qoder-max-turns-20260824-000717`
- **Worktree creation command:** `git worktree add /Users/tom/Dev/worktrees/swallowtail-g04-053-qoder-max-turns-20260824-000717 -b agent/g04-053-qoder-max-turns-20260824-000717 origin/main`
- **Worker worktree policy:** first use a clean, dedicated, non-`main`
  registered worktree supplied by the launcher, even when its generated path
  or branch differs from these placeholders. Record the actual identity and do
  not create a second worktree. If unusable, inspect the named worktree; only
  then read `.agents.local.env`, require `AGENTS_WORKTREE_CONTAINER_DIR`, and
  create a unique fallback under that container. Ask the operator if missing.
  Never use `/tmp`, `TMPDIR`, or a guessed path.
- **Active spec lane:** per-route feature completion; no spec or contract edit
- **Roadmap milestone:**
  `docs/roadmaps/g04/053-qoder-headless-max-turns.md`
- **Current batch card:**
  `docs/roadmaps/g04/batch-cards/148-qoder-headless-max-turns-evidence.md`
- **Ready cards, in order:** card 148, then conditional 149, then conditional 150
- **Allowed runway:** exact Qoder `1.1.25` maximum-turn evidence, then only
  Research 200 deliver-now adapter-local caller-decreasing binding
- **Remaining continuation envelope:** three serial cards; 149-150 run only
  after their named gates
- **Lane budget / pause signal:** one PR. Stop after card 148 if Research 200
  has no deliver-now row or shared contract/currentness work is needed.
- **Dispatch topology:** one serial worker lane; no internal subagents
- **Canonical refs:** `docs/architecture/system-architecture.md`; Contracts
  008, 011, 029, 033, 037, 040, and 052
- **Route identity:** `qoder.headless`, driver `swallowtail.qoder.headless`,
  axis `qoder.package`, exact npm package `1.1.25`
- **Candidate mapping:** caller-selected positive maximum turns `1..=8`; this
  is an evidence candidate, not a prequalified public domain
- **Current mapping:** every run sends `qodercli --print --output-format
  stream-json --permission-mode dont_ask --max-turns 8
  --no-session-persistence --cwd <working resource> <prompt>`
- **Limit boundary:** exact fixtures already map `error_max_turns` / `Maximum
  turns exceeded` to `swallowtail.qoder.headless.max_turns` provider-failed.
  Card 148 owns counter definition, off-by-one truth, `num_turns`, process exit,
  partial events, and lifecycle.
- **Model capability profile:** exact-package, evidence-first, fail closed on
  value, version, plan/evidence, driver, command, count, result, stream,
  partial-event, or lifecycle ambiguity
- **Tool/runtime restrictions:** use Effigy selectors; no internal subagents;
  no install, login, setup, credential/account inspection, provider request,
  live Qoder prompt, browser login, or paid work. Current official public-source
  inspection and secret-free exact-package evidence are authorized by card 148.
- **Required validation:** card-specific gates plus final `cargo fmt -p
  swallowtail-adapter-qoder`, `effigy validate:focused
  swallowtail-adapter-qoder`, `effigy package:verify-affected
  swallowtail-adapter-qoder`, `effigy check:examples`, `effigy qa:routes`,
  `effigy qa:northstar`, research/logs/roadmaps/g04/batch-card/next-action index
  gates, `effigy package:api`, and `git diff --check`
- **Known doctor baseline:** 376 inherited god-file findings: 330 warnings and
  46 errors; stale graph index; one generated-in-src warning. Keep inherited
  findings separate from lane-created findings.
- **Planning validation:** `effigy qa:docs`, `effigy qa:northstar`, `effigy
  qa:routes`, and `git diff --check` passed before the planning commit
- **PR base/head:** current pushed `main` / selected worker branch
- **PR URL:** pending
- **Review state:** awaiting worker evidence and PR
- **Merge authorisation:** none; do not merge

## Boundaries

- **In scope:** cards 148-150 serially; Research 200; exact current official and
  npm `1.1.25` parser/counter/enforcement/result evidence; only Research 200
  deliver-now typed maximum turns; route-local source, fixtures, tests, guide,
  example, API baseline, milestone, cards, and closeout reservation
- **Out of scope:** values outside Research 200; permission-mode changes,
  yolo, safe mode, tools, model, prompt, output format, session persistence,
  workdir, access, auth, deadline, cancellation, or cleanup changes; Qoder ACP,
  SDK stdio, TUI, continuation/resume, teleport, IDE, login; shared capability
  or generic provider settings; another Qoder release/route; live work;
  currentness; shared architecture/matrices/indexes/changelog; release,
  publication, merge, generation rollover, or g04 closure
- The planning-base contracts are the full authorization boundary. Stop if
  exact evidence requires a contract change.
- Do not represent a turn limit as Contract 040 `OutputTokenLimit`, a generic
  budget, or proof of effective provider work.
- Do not normalize parser breadth into public API. Research 200 owns the exact
  admitted subset. Caller omission stays fixed `8`; flag omission is forbidden.
- Do not strengthen earlier assistant output or `num_turns` into successful
  completion after `error_max_turns`.
- Work only in the selected worker worktree. Never edit, clean, reset, stash
  over, or discard the orchestrator planning checkout or unrelated state.
- Do not merge. Merge remains a later explicit operator action.
- Follow `AGENTS.md`, use Effigy, work in one meaningful batch, and keep
  glue-light reporting.

## Important Context

- **Planning lineage:** Research 151 and the realized route freeze exact npm
  `1.1.25` and fixed `--max-turns 8`. g04.053 adds no version, route, transport,
  model, permission, or lifecycle claim.
- **Exact package identity:** npm tarball SHA-256
  `627749221c609bfb5514f4486fb42f464597cf49472ed52c087c36a1d2fbb4ab`;
  integrity and shasum are in Research 151. Do not substitute registry latest.
- **Evidence burden:** fetch current official CLI/run-in-scripts docs and the
  exact package. Record retrieval dates, complete digests, and decisive source
  paths. Freeze parser breadth, counter source, increment/check order,
  off-by-one behavior, child lifetime, `num_turns`, result, exit, partial
  events, cancellation, deadline, and joined cleanup.
- **Preservation burden:** stream-json, `dont_ask`, no-session-persistence,
  workdir, local config access, mandatory host deadline, auth posture, failure,
  cancellation, and cleanup remain fixed.
- **Honest stop:** an empty Research 200 deliver-now set is a successful result.
  Mark cards 149-150 blocked, finish the route-local stop record, validate, and
  open the evidence PR.
- **Generation boundary:** do not close or roll g04. The orchestrator will
  reconcile shared closeout and reassess the remaining inventory after merge.
- **Decisions and preferences:** manual operator-harness handoff only; no
  internal subagents. New routes do not pre-empt per-route feature work.
- **Report after:** card 148's exact domain/counting/result decision. Continue
  automatically only for a non-empty exact set and no stop condition, then
  report after the complete cards 149-150 implementation and validation chunk.
- **Report to:** the operator, who relays progress to the orchestrator

## Suggested Next Move

Read this handoff from the top. Before broad repository reads, run the quick
worktree-safety preflight in `## Completion Protocol`. Use the launcher-provided
clean non-`main` worktree when present; do not create another because its name
differs. If unusable, follow the named and configured fallback rules exactly.

Then read `AGENTS.md`, g04.053, cards 148-150, Research 151 and 200, the Qoder
prepared guide, exact command/preparation/driver/stream/terminal source and
fixtures, and the canonical contracts. Take card 148 as one coherent evidence
chunk without production edits. If Research 200 is empty, close 149-150 as
blocked and open the evidence PR. If an exact set survives, execute 149-150 in
order and open one implementation PR.

## Completion Protocol

### Before you start

1. Read this handoff. Its `worker_mode: implementation` and
   `dispatch_authority: orchestrator` metadata activate worker mode. Run only
   this quick safety probe before broad reads: `git rev-parse --show-toplevel`,
   `git branch --show-current`, `git status --porcelain`, and `git worktree list
   --porcelain`.
2. If the current root is a registered clean non-`main` worktree, accept it as
   launcher-provided. Record its path/branch and do not create another.
3. If current context is `main`, dirty, unregistered, or unusable, stop and
   report it rather than silently creating a second worktree. Use the named
   worktree only when the operator relaunches there. If manual fallback is then
   required, read `.agents.local.env`, require
   `AGENTS_WORKTREE_CONTAINER_DIR`, and ask the operator when absent. Never use
   `/tmp`, `TMPDIR`, a guessed path, reset, clean, or stash-over.
4. In the selected worktree, fetch origin. Confirm `HEAD == origin/main`,
   `git merge-base --is-ancestor f7593490ccf6f7f1f3cf741c04eb20b8112c09e5 HEAD`,
   this handoff exists, and the tree is clean.
5. Read the milestone, all assigned cards, `AGENTS.md`, and named refs.
6. Run `effigy tasks`, `effigy doctor`, and `effigy test --plan` once. Record
   inherited doctor findings separately.

### Work the cards

1. Execute card 148 without production code. Promote Research 200 with exact
   source-backed dispositions and a deliver-now or stop table.
2. Report that table to the operator. Continue only if non-empty and no stop
   condition applies.
3. If continuing, implement cards 149-150 as one coherent code/test/docs batch.
   Keep tests focused and below new god-file error thresholds.
4. Update only route-local worker surfaces. In the reserved closeout, list
   shared architecture, Contract 029, route/feature matrix, programme, index,
   changelog, milestone, and Next Task changes for the orchestrator.
5. Run the complete card-specific validation once after the coherent batch.

### PR loop

1. Review `git diff`, `git diff --check`, branch, and worktree state. Commit and
   push the worker batch.
2. Open one PR against current `main`. Its body must name g04.053, cards,
   Research 200 disposition, exact route/version, selected/omitted values,
   counting/result/terminal truth, validation, shared-closeout delta, and
   explicit withholds.
3. Do not merge. Report PR URL, exact head SHA, actual branch/worktree,
   deliver-now/stop table, cards complete/blocked, validation, inherited
   baseline, shared-closeout delta, and unresolved decisions.
4. If review requests changes, keep the same branch/worktree/PR. Fix only
   in-scope issues, rerun proportionate validation, push, and report new head.
5. Do not restack or merge without a later explicit operator request.

### Handoff closeout

Keep card, roadmap, Research 200, route-local closeout, and PR state honest. If
blocked, record the blocker and stop. Do not claim merge, release, currentness
movement, generation rollover, or g04 closure.
