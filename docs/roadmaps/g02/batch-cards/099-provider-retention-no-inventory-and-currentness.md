# 099 Provider Retention `No` Inventory And Currentness

Status: completed
Owner: Tom
Created: 2026-07-28
Milestone: `../030-provider-retention-feature-closure.md`
Depends on: card 096

## Objective

Revalidate every provider archive, restore, delete, and owned-remote-cleanup
`No` before selecting another implementation tranche.

## Scope

1. Parse the canonical matrix and freeze the exact 75-cell starting inventory.
2. Check every cell against:
   - the selected route and operation shape
   - its current maintained interface range
   - realized prepared and low-level roles
   - Contracts 014-017, 025, 029, 037, and 038
3. Distinguish:
   - reversible archive and restore
   - history removal, provider-data deletion, and hard deletion
   - provider-native close and local runtime close
   - user-selected provider-session management
   - operation-owned remote-resource cleanup
   - honest non-applicability
4. Revalidate plausible conversions against current official provider or
   maintained-project evidence.
5. Rank candidates by consumer value and architectural information.
6. Use no live credential, account, provider effect, container, or model
   server.

## Acceptance Criteria

- [x] all 75 starting cells are classified exactly once
- [x] composite solutions retain route-specific capability ownership
- [x] cleanup cannot mint user-directed destructive authority
- [x] deletion truth never exceeds exact provider wording
- [x] false negatives and non-applicable cells are corrected separately
- [x] one exact contract/corpus tranche or honest no-conversion result remains

## Evidence

- Research 054 classifies all 75 starting cells:
  - 58 operation-shape non-applicable
  - 12 selected-surface absence
  - two Gemini separate-transport corpus gaps
  - one realized OpenCode false negative
  - one contract-ready Claude Agent cleanup
  - one OpenAI shared contract/corpus gap
- OpenCode structured runs already prove confirmed operation-private session
  deletion through the public prepared facade.
- Gemini CLI `0.51.0..=0.52.0` contains one exact installed-executable
  session-delete route; its `0.51.0` documentation omission is not a behavior
  gap.
- the current OpenAI Responses API exposes deletion of the bound background
  response resource.
- Kimi deletion remains unsupported.

Matrix value changes remain in card 102 so the 75-cell starting inventory
stays stable through contract and implementation work.

## Result

Card 100 owns Gemini CLI bound deletion and optional cleanup, Claude Agent
optional cleanup, and OpenAI background-response cleanup corpora. No operator
decision is required.

## Stop Conditions

- provider wording does not distinguish archive from deletion
- the selected interface differs from the evidence-bearing interface
- route selection would require a new credential, endpoint, topology, or
  product-policy choice

## Auto-Continuation

Continue to card 100 only when one exact tranche is selected without an
operator decision.
