# Antigravity CLI 1.2.11 Identity Fixture

Frozen identity and release-note classification for official GitHub
`google-antigravity/antigravity-cli` `1.2.8`, `1.2.9`, `1.2.10`, and
`1.2.11` (Research 353). Adaptation run for the open
`antigravity.headless` currentness stop.

- `identity.json` — release/tag/asset identity for the four new stable
  points (linux-x64 and mac-arm64 tarball digests, extracted binary
  digests/sizes/build ids), the frozen `1.2.7` ceiling digests, no host
  install, the catalogue-advance / headless-stop decision, and the
  operator ruling request the headless stop now needs.
- `protocol.json` — selected-surface presence, the single-file archive
  inventory, the official release-note classification per claim under
  Tom's 2026-09-15 ruling, the retry and print-timeout traces, and the
  bounded retry-control lead. Public git hops change only `CHANGELOG.md`.
  Exhaustive binary scanning stays stopped.

No downloaded binary was executed. No provider, prompt, login,
credential, installation, or host-mutation operation occurred. The
decoder corpus remains `antigravity-cli-1.1.9`. The catalogue claim
advances to `1.2.11`; headless `LATEST_QUALIFIED` stays `1.1.17` and its
unqualified gap becomes `1.1.18..=1.2.11`.
