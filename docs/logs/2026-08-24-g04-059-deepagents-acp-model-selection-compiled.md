# 2026-08-24 g04.059 Deep Agents ACP Model Selection Compiled

## Change

- reassessed the remaining promoted per-route inventory after g04.058
- selected `deepagents.acp` model selection as the next coherent route-local
  family
- compiled g04.059 and cards 164-166 as one serial evidence-first worker lane
- reserved Research 206 and the route-local closeout before dispatch
- kept g04 active; standing currentness did not move the programme pointer

## Decision

Deep Agents model selection has the strongest remaining transport fit. The
production route owns one exact `deepagents-acp@0.1.25` child per session and
currently passes no extra argv. Official LangChain docs expose `--model` and a
`provider:model` value, so selection belongs naturally at that child boundary.

Current docs do not prove exact `0.1.25` grammar or behavior. Card 164 must
freeze the published parser, provider integration, default, aliases, fallback,
authentication agreement, any ACP confirmation, and session/restoration
lifetime. It must distinguish current docs from exact package source. Cards
165-166 continue only for a non-empty Research 206 deliver-now set.

The route's host-owned Anthropic/OpenAI key posture is the main decision gate.
An admitted provider prefix must agree with a named prepared access profile
before spawn without moving key bytes into Swallowtail. Missing or rejected
credentials cannot trigger provider/model fallback. Omission keeps empty argv.

Gemini CLI sandbox remains a higher-proof containment family with weak live
testability. Deep Agents skills/memory and OpenCode permissions widen host or
tool authority. Claude controls remain absent from exact help; Codex verbosity
lacks model-support evidence; llama.cpp reasoning remains model/template
semantic. None is selected here.

## Next

Execute g04.059 cards 164-166 serially in one isolated worker worktree and open
one PR. Stop honestly after card 164 if exact grammar, provider/access
agreement, fallback, or claim truth cannot be frozen without server execution,
key inspection, provider-prompt work, generic configuration, or
contract/currentness movement. Keep g04 open.
