# 2026-09-15 Oh My Pi 18.1.22 Claim

g05.079 extended the `oh-my-pi.rpc` package claim through official npm and
GitHub `18.1.22` from Research 327, the nineteenth and final Research 308
family.

The retained `17.x` segment extends from `17.2.9..=17.4.0` to
`17.2.9..=17.4.2` on the unchanged `oh-my-pi.rpc-v2-v17.2.9` behavior
revision, and a distinct adapter-private `18.x` segment `18.0.0..=18.1.22`
opens on the new `oh-my-pi.rpc-v2-v18.0.0` revision. `18.0.2` and `18.1.7`
are explicit exclusions; the npm-unpublished GitHub tags `v17.4.3` and
`v17.4.4` sit between the segments and stay incompatible. `18.1.23` stays
permitted `UnverifiedNewer` on the `18.x` revision. Contract 029 gives a claim
its own revision, so adding the milestone moves the claim id from
`oh-my-pi.rpc.package-window-1` to `oh-my-pi.rpc.package-window-2`; because the
`18.x` segment carries the newest behavior revision, the retained `17.x`
segment is labeled `Deprecated` by the derived support status while remaining
executable. Baseline `17.2.9`, `AllowUnverified`, the frozen
`oh-my-pi-rpc-17.2.9` decoder corpus, and every historical specimen stay, and
the driver accepts either segment behavior revision.

New mutation-sensitive coverage lands in
`crates/swallowtail-adapter-oh-my-pi/tests/oh_my_pi_18_1_22_identity.rs` plus
the frozen `oh-my-pi-18.1.22` corpus: the exact published ledger and tarball
digests, the complete per-hop shipped-tree delta and 1773-file identical set,
the mapped RPC digest groups, and a guard that fails if any changed mapped hop
is misclassified. The existing identity, compatibility, driver, and
prepared-facade suites now assert the two segments, the `Deprecated` /
`Maintained` labels, the exclusive gaps, and the `UnverifiedNewer` point. The
prepared guide, route and lifecycle matrices, feature matrix,
`system-architecture.md`, Contract 029's Oh My Pi Mapping paragraph, the
standing lane, and `[Unreleased]` agree. `pi.package` stays a separate axis.

Official latest was rechecked at npm and GitHub `18.1.22` at the push
boundary. No provider prompt, login, credential, installation, host update,
downloaded-artifact execution, Pi adapter mutation, release, tag, publication,
or consumer mutation occurred. Research 327, g05.079.
