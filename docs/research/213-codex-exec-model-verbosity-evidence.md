# 213 Codex Exec Model Verbosity Evidence

Status: reserved
Owner: Tom
Created: 2026-08-25
Card: g04.066 / 184

## Question

Which exact Codex CLI versions, providers, selected models, verbosity values,
and `codex.exec` profiles can dispatch `model_verbosity` through an adapter-
local prepared selection without ambient-config authority, silent unsupported-
model behavior, or live-provider inference?

## Decision

Pending card 184. No version, provider, model, value, or profile row is
prequalified by this reservation.

The evidence gate must reconcile the closed configuration value domain with
exact release-tag `support_verbosity` and `default_verbosity` model metadata,
Responses-provider request construction, config precedence, selected-model
binding, unsupported behavior, omission, and claim strength before cards
185-186 may run.
