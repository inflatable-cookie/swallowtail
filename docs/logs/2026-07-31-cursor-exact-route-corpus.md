# Cursor Exact Route Corpus

Date: 2026-07-31

## Outcome

g03 card 010 froze exact Cursor evidence before adapter implementation:

- installed `2026.07.01-41b2de7` launcher and runtime identities
- maintained ACP-registry `2026.07.23-e383d2b` archive identity
- one sanitized installed ACP v1 initialization transcript
- an authenticated 193-entry model-catalogue count and normalized digest
- installed help and official headless JSON/stream-JSON behavior
- the local `agent` command collision with Grok

The two executable points remain separate and unqualified. No continuous
calendar-version range is inferred.

## Boundaries

The live check sent ACP `initialize` only, then closed and joined the process.
It sent no authentication request, session creation, prompt, model invocation,
tool request, update, installation, or consumer effect. Account identity and
raw model output are not retained.

Cursor advertises load, list, image, and MCP capabilities. None becomes a
production route claim until card 012 adds exact session and turn evidence.
Headless print mode documents no token-usage field, cancellation event, JSON
schema flag, or thinking event; Swallowtail must not synthesize them.

## Validation

`effigy validate:focused swallowtail-protocol-acp` passed 88 tests plus
warnings-denied clippy in three seconds.

## Next

Execute card 011. Add the focused Cursor package, identity-safe discovery,
exact version posture, delegated local access, and auth-aware catalogue parser.

