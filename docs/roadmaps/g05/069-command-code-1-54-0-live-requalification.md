# g05.069 Command Code 1.54.0 Live Requalification

Status: stopped; typed live evidence failure on 2026-09-17
Owner: Tom
Created: 2026-09-14
Depends on: Contract 029; Contract 043; Research 116, 118, and 317; g05.068
Vision tags: route currentness, Command Code, live evidence, exact pin

## Outcome

Run one separately authorized live requalification gate for the exact
`command-code.headless` `1.54.0` point. Reproduce the accepted `1.15.1`
evidence shape for authenticated structured completion, tool lifecycle, usage,
credit failure, and the Contract 043 two-turn private exact-id continuation. On
acceptance, reconcile the corresponding live feature and activity cells to the
exact tuple; on a typed stop, keep the current version-bound gate and record
the reason. This task does not alter the provider-free currentness
qualification in g05.068.

## Ready-State Rubric

- [x] g05.068 names the exact `1.54.0` point and keeps Research 116 and 118
  bound to `1.15.1`.
- [x] Provider-operation authority is current: Tom re-confirmed on 2026-09-17
  that the separately authorized live gate still stands.
- [x] The gate's exact executable, account, model, prompt, and cleanup evidence
  are fixed by the authorized gate below before any provider operation.

## Dispatch manifest

- **State:** ready; provider-operation lane; one lane; no automatic successor.
- **Completion:** the exact-`1.54.0` gate runs once and either reconciles the
  live feature and activity cells to the exact tuple, or records a typed stop
  and preserves their version-bound gate.
- **Owned mutable paths:** the Command Code live-evidence record, the
  Command Code feature and activity matrix rows, the route guide, and this
  task's result lines.
- **Reserved closeout surfaces:** lifecycle task record and generated
  projections; submitted-handoff deletion belongs to the repository hook.
- **Worker:** live-evidence worker with provider-operation authority limited to
  the one exact gate below.
- **Excluded:** broad provider exploration, install, login, host update,
  consumer mutation, any second attempt or rerun, public catalogue, import,
  export, or resume operation outside the gate, and unrelated provider work.
- **Escalation:** operator via Chatterbox for any provider budget or policy
  question; Queue coordinator for mechanical blockers.

### Authorized gate

One authenticated `command-code.headless` `1.54.0` session using the cheapest
adequate model, reproducing the accepted `1.15.1` shape: structured
completion, tool lifecycle, usage accounting, the credit-failure path, and the
Contract 043 two-turn private exact-id continuation. Research 116 and 118 stay
immutable and bound to `1.15.1`; they are comparison evidence, not authority to
rewrite. No retry beyond provider-free recovery, and no second live attempt.

## Work

1. Run only the separately authorized exact-`1.54.0` live gate.
2. Compare the result with Research 116 and 118 without rewriting those
   immutable records. Move feature and activity cells only on matching accepted
   evidence; otherwise preserve their version-bound gate and record a typed
   stop.
3. Keep `QualifiedOnly`, the exact version tuple, private exact-id resume, and
   all route authority boundaries unchanged.

## Boundaries

One exact Command Code version and one authorized live gate. No range, release,
tag, publication, or downloaded-artifact execution outside the gate.

## Validation

Focused Command Code tests, route and activity/feature matrix QA, and the named
docs gates. The live gate supplies its own exact provider evidence.

## Acceptance

- [x] the exact-`1.54.0` live gate runs or records a typed stop
- [x] Research 116/118 remain immutable and version-bound
- [x] feature and activity cells preserve the version-bound gate

## Result

The exact-`1.54.0` gate ran once on `meituan/LongCat-2.0:free` after the
operator repaired the executable and reauthorized the attempt. The structured
turn reached the provider but stopped with
`swallowtail.command_code.headless.malformed_stream`. The live-derived feature
and activity cells remain version-bound to `1.15.1`; no interactive
continuation, retry, model substitution, or claim change followed. See the
[live stop log](../../logs/2026-09-17-g05-069-command-code-1-54-0-live-stop.md).

## Next Task

The queue dispatches g05.069 as one bounded live gate. On return, Chatterbox
reconciles the version-bound feature and activity cells from the accepted or
stopped outcome.
