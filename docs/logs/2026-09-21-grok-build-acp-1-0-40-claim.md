# 2026-09-21 Grok Build ACP 1.0.40 Claim

## Result

Grok Build ACP now qualifies official npm `@xai-official/grok` `1.0.40`
through all ten published stable hops after the `1.0.30` ceiling. The
`grok-build.executable` window extends as a compatible extension on the
existing `grok-build.acp-v1.cached-token-model-4-6-v3` behavior revision and
the `grok-build.acp.executable-window-2` claim id. Deprecated
`0.2.114..=0.2.117`, gaps `0.2.118..=0.2.121` and `1.0.0..=1.0.3`,
baseline `0.2.114`, the `1.0.4` floor, the `grok-4.6` binding, and
`AllowUnverified` stay. Unpublished `1.0.41` stays visible
`UnverifiedNewer`. Discovery now pins the exact source revision of every
qualified point through `1.0.40`.

The exact `1.0.30` `QualifiedOnly` catalogue claim does not move, and the
registered-tool courier stays keyed only to the accepted live capsules at
exact `1.0.4` and `1.0.5`. Identity evidence stays in the prior commit:
downloaded artifacts were hashed and never executed, every hop's wrapper
and linux-x64 platform identity plus the darwin-arm64 cross-check are
frozen, the mapped ACP literal presence map is byte-identical at every
hop, the embedded model document is unchanged, and the 62 mapped-core ACP
modules persist.

## Next

Focused adapter validation on `swallowtail-adapter-grok` only. Do not
reopen the catalogue exact pin.
