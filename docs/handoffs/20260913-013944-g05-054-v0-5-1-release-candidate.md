---
title: g05.054 v0.5.1 release candidate
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
status: ready-to-launch
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Tom authorized Swallowtail point-release preparation after Desktop g02.089 Phase A qualified exact source 0209dd7f; preserve that package tree and stop before the exact-SHA tag gate."
queue:
  capability: general
  skipPRReview: false
  notifyOriginOnCloseout: true
  completionNotificationAgentIds:
    - 5317069e-201f-4dea-94f3-af8f0b9faff2
tags: [coordination, handoff, worker, release, patch, desktop]
---

## What This Thread Was Doing

Prepare and merge the source-only Swallowtail `v0.5.1` candidate carrying the
Desktop-qualified g05.053 source without changing any package source.

## Why It Matters

Desktop g02.089 Phase A is green against exact Swallowtail `0209dd7f` and is
held for an ordinary released-pin proof. The release candidate must preserve
that qualified implementation rather than silently qualifying a different
tree.

## Current State

- Canonical task: `docs/roadmaps/g05/054-v0-5-1-release-candidate.md`.
- Qualified source: `0209dd7ffc19c457ca56d55683576e09f049f7ee`;
  implementation merge `3e9f29e739d029145a6e153cd8bdd35c829462a2`.
- Qualified `crates/` tree: `186f3ba42a4aa896c03a3bddcacb007b1afbd8cc`;
  Grok adapter tree: `1a6f777f84f9aef80c146badf6944811568e23cd`.
- Exact-source push CI run `34724808090` is green on all 11 jobs.
- Workspace version is `0.5.0`; read-only release status selects `0.5.1` and
  passes all seven local gates; `v0.5.1` is absent locally and remotely.
- Desktop PR #233 is open and intentionally held. This task does not touch it.

## Boundaries

Follow g05.054 exactly. Do not edit `crates/**`, earlier release notes or
baselines except the two exact `v0.5.0` tag restorations named below,
workflows, providers, Desktop, dependencies, or runtime behavior.
Do not create or push a tag, registry package, GitHub Release, binary, sidecar,
installer, model artifact, or any other publication. No provider contact.

## Important Context

Run Effigy prepare for `0.5.1` in the fresh queue workspace. A stale ignored
`.release-prepared.json` from another release is not evidence and must never be
reused. Candidate changes are coordinated version/dependency requirements,
lock, changelog, new `0.5.1` release note and baselines, install guidance, and
directly required release validation surfaces only.

This provider-free local prepare is not one-shot. If it fails, retain the exact
diagnostic, repair only within the manifest, and rerun in this isolated
workspace. Do not build a disposable rehearsal checkout merely to protect a
local allowance. Tag creation and push remain separate irreversible actions
with no authority in this task.

The release audit found a bounded post-tag baseline correction. Restore
`release-baselines/production-routes-0.5.0.txt` and
`release-baselines/public-api-0.5.0/swallowtail-adapter-grok.txt`
byte-for-byte from immutable tag `v0.5.0`, and prove both blobs match the tag.
Those two files currently include g05.053's post-tag route/API additions only
because current-source gates ran while the manifest still named `0.5.0`.
Carry those additions into fresh `0.5.1` baselines. This is the only authorized
prior-version change. Keep `docs/releases/0.5.0.md` and every other historical
surface untouched.

Prove no path under `crates/` differs from `0209dd7f`, and re-read the frozen
subtree identities after preparation and after merge. Preserve both real
`[Unreleased]` entries: OpenCode HTTP `1.18.30` and the authenticated
non-inference Grok `1.0.25` catalogue. Swallowtail packages remain
`publish = false`; do not imply crates.io or GitHub Release availability.

Use the repository-owned full candidate board and external Git-source consumer.
Run `effigy release execute --plan` only; execution and tag mutation are not
authorized in this task. Obtain independent exact-head review and exact-SHA
hosted CI. Stop rather than repair around a deterministic release gate.

## Suggested Next Move

Freeze the qualified source/subtrees, execute the one `0.5.1` prepare, prove
the candidate changes only release metadata, then open the candidate PR.

## Completion Protocol

Return the candidate PR/head/review/merge/closeout, candidate tree, exact
`crates/` and Grok subtree equality to `0209dd7f`, coordinated package version,
prepare receipt digest, source-consumer result, hosted run and all job results,
and verified absence of tag, registry publication, GitHub Release, or artifact.
Queue completion goes directly to Desktop Chatterbox agent
`5317069e-201f-4dea-94f3-af8f0b9faff2`. The next action is an explicit
exact-SHA tag decision from Tom; no release mutation follows automatically.
