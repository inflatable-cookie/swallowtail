# 2026-08-30 g05.003 Claude Watcher Post-Currentness Reassessment

Status: complete
Owner: Tom
Date: 2026-08-30

## Result

PR 121 landed g05.005 at `a70254fb`. Claude Code headless and response-only
are base-route qualified through official npm `2.1.251`, with unpublished
`2.1.244` and `2.1.249` excluded. The installed host and npm `latest` still
report exact `2.1.251` at this checkpoint.

Contract 060 and card 016 already provide the provider-neutral HTTP bridge,
operation-private bearer authority, exact host/operation/turn correlation,
closed watcher protocol surface, terminal barrier, and joined cleanup.
Research 260 freezes the exact `2.1.251` MCP, settings, skill, Stop, and hook
ingredients. Card 010 is therefore ready for credential-free binding and
deterministic provider-free fixtures.

## Exact Boundary

Card 010 admits only the exact `2.1.251` watcher candidate. Base-route
qualification does not widen the watcher gate across `2.1.220..=2.1.251` or
later stable points. Every other version must reject the watcher opt-in before
bridge open, private materialization, or provider process work.

Card 010 does not run Claude, use credentials, consume paid work, advertise
watcher support, or publish a route claim. It must preserve the existing empty
strict-MCP command exactly when watcher support is omitted. Card 011 alone owns
the separately authorized live same-turn proof and first exact watcher claim.

The open consumer route-feature projection census remains an independent
evidence-only triage lane. It does not authorize card 010 to promote that
facade or edit its census surfaces.

## Next

Implement card 010 as one manual worker lane and return one PR. Stop before
card 011 or any live provider turn.
