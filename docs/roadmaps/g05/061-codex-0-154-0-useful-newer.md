# g05.061 Codex 0.154.0 Useful Newer

Owner: Tom
Created: 2026-09-14
Depends on: Contract 029; qualified Codex `0.152.1`; Research 275 and 308; g05.020; g05.060
Vision tags: route currentness, Codex, compatibility

## Outcome

Qualify current official Codex `0.154.0` on the exec and app-server axes if
deterministic npm and GitHub artifact evidence admits each one. Preserve every
historical window and unpublished gap, both claim identities, lifecycle
segments, `AllowUnverified`, and every feature-specific exact version set not
independently proved at the new points.

Planning observed npm and GitHub stable `0.154.0`; the host reports
`codex-cli 0.153.3`. The worker must recheck official latest and include every
published stable after `0.152.1`.

## Ready-State Rubric

- [x] Tom authorized the complete Research 308 currentness campaign.
- [x] g05.060 completed and its lifecycle closeout is on canonical `main`.
- [x] Current claims, official channels, and host observation are known.
- [x] No prompt, login, installation, host update, or provider credit is needed.

## Work

1. Re-probe npm and the official GitHub release and observe the host safely.
   Retrieve every published stable after `0.152.1` through current stable into
   `/tmp`. Freeze Research 311 and a secret-free wrapper/platform/source-tree
   identity corpus without executing downloaded artifacts.
2. Classify every changed shipped file feeding selected exec JSONL,
   app-server RPC/schema, catalogue, session, dynamic tools, questions,
   approvals, history/reconciliation/management, workspace roots, lifecycle,
   failure, configuration, model controls, and process behavior. Commit
   identity evidence before production claim edits. Changelog text is
   discovery only.
3. Record the Contract 029 decision for exec and app-server separately. Raise
   both only where evidence admits them; otherwise keep the smaller honest
   segment and name the exact counterexample. Preserve feature-specific exact
   ranges unless their own selected surfaces are directly proved.
4. A material public lifecycle or authority change stops with the smallest
   exact counterexample and a concrete follow-up. Recheck official latest
   immediately before the identity commit and push. Provider-free diagnosis
   and reruns remain allowed normally.

## Dispatch Manifest

| Field | Task 061 |
| --- | --- |
| Readiness | ready |
| Prerequisites | Contract 029; Research 275 and 308; completed g05.060; clean pushed `main`; official npm/GitHub Codex stable |
| Completion conditions | every published stable after `0.152.1` is frozen; identity precedes claim; exec and app-server each receive an honest segment outcome; admitted support updates only this family; exact-head review and named gates pass |
| Owned mutable paths | Codex selection/tests/fixtures under `crates/swallowtail-adapter-codex/**`; Research 311 and one research-index line; exact Codex prepared-guide, route-matrix, feature-matrix, architecture ceiling, standing-lane, `[Unreleased]`, identity-log, claim-log, and task-result lines; `PAPERCUTS.md` append only |
| Reserved shared closeout surfaces | lifecycle task record and generated projections; submitted-handoff deletion belongs to the repository hook |
| Forbidden paths | other adapters, contracts, historical research/releases, new Codex operations, or public API expansion without a returned planning decision |
| Approved concurrent siblings | none on shared currentness surfaces; this campaign is serial |
| Worker capability class | evidence-first Rust currentness worker; official npm/GitHub retrieval and deterministic wrapper/platform/source comparison; no provider credentials |
| Acceptance evidence | official metadata and artifact identities, host observation, all-hop mapped-surface ledger, mutation-sensitive fixtures, current docs/claim agreement |
| Review oracle | skipped hop, unclassified mapped delta, changelog-only inference, lost historical window/gap, exec/app-server flattening, or unjustified feature-specific widening |
| Stop conditions | official-channel disagreement; major-line reset; mapped public lifecycle or authority change requiring new policy; provider/live evidence required |
| Escalation owner | operator via Chatterbox for new policy; Queue coordinator for mechanical blockers |

## Boundaries

One family only. No prompt, live Codex exec or app-server session, login,
credentials, install, host update, downloaded-artifact execution, new public
operation, release, tag, publication, or consumer mutation.

## Validation

- `cargo fmt -p swallowtail-adapter-codex -- --check`
- `effigy validate:focused swallowtail-adapter-codex`
- `effigy package:verify-affected swallowtail-adapter-codex`
- `effigy check:examples`
- `effigy package:api`
- `effigy qa:routes`
- `effigy qa:northstar`
- relevant research/log/roadmap/number/lifecycle/next-action checks
- `git diff --check`

## Acceptance

- [ ] official identity and every published stable are reproducible
- [ ] every changed selected input is classified
- [ ] identity evidence precedes production claim edits
- [ ] exec and app-server remain separately truthful
- [ ] historical windows, gaps, exact feature sets, claim identities, and `AllowUnverified` survive
- [ ] official latest is rechecked at both boundaries

## Next Task

Return the exact outcome to Chatterbox, then compile and dispatch Cursor Agent
as the next Research 308 family without asking Tom to repeat authorization.
