# g05.068 Command Code 1.54.0 Useful Newer

Owner: Tom
Created: 2026-09-14
Depends on: Contract 029; Research 116, 118, 308, and 316; g05.067
Vision tags: route currentness, Command Code, headless, exact pin

## Outcome

Reopen the exact `command-code.npm` pin and qualify current official npm
`1.54.0` for `command-code.headless` only if complete provider-free package,
entrypoint, payload, invocation, NDJSON, lifecycle, and continuation evidence
admits it. Preserve `QualifiedOnly` and one exact maintained point; do not infer
a range across the 67 published stable releases after `1.15.1`.

Planning rechecked npm `latest=1.54.0`, published
`2026-09-14T01:59:24.573Z`; alpha, beta, and rc tags are separate prerelease
channels. Installed `command-code --version` remains exact `1.15.1` and is an
observation only. The current Node `22.23.2` satisfies the latest package's
declared Node `>=22` engine. The worker must recheck the official channel and
include every stable added before the identity commit.

## Ready-State Rubric

- [x] Tom authorized the complete Research 308 currentness campaign, including
      reopening exact-pin families.
- [x] g05.067 completed and its lifecycle closeout is on canonical `main`.
- [x] Official `1.54.0`, installed `1.15.1`, the 67-hop span, and Node engine
      are identified separately.
- [x] Existing structured-run and private interactive-continuation boundaries
      are explicit in Research 116 and 118.
- [x] No prompt, login, installation, host update, or provider credit is needed
      for the identity and claim decision.

## Work

1. Re-probe npm stable/dist-tags, full published versions, package metadata,
   and installed `--version`. Retrieve exact `1.15.1`, all 67 published stable
   successors, and `1.54.0` into `/tmp`. Freeze Research 317 and exact tarball,
   integrity, package-tree, entrypoint, payload, dependency, engine, and
   selected-file identities without executing downloaded artifacts.
2. Derive a deterministic per-hop shipped-tree ledger. Classify every changed
   input feeding the selected route: `-p`, JSON output, permission mode,
   onboarding/update/trust/skills/session flags, explicit model and effort,
   max turns, stdin prompt delivery, structured `--no-session`, private exact
   `--resume`, AgentEvent NDJSON event/result/error shapes, correlation, usage,
   exit codes, redaction, cancellation, process ownership, deadlines, and
   joined cleanup. Treat launcher aliases, TUI, Provider API, mods/taste,
   session catalogue/export, and other unselected additions as unmapped only
   after proving they cannot affect selected behavior. Changelogs are discovery
   only. Commit identity before production claim edits.
3. If the selected contract is unchanged or mechanically adaptable without a
   new public lifecycle, authority, or operation, rebind the exact version and
   exact claim point to `1.54.0`. Keep the existing behavior revision only when
   the mapped protocol is unchanged. Do not retain `1.15.1` as a second exact
   point merely to hide incomplete current evidence.
4. Reconcile evidence-backed features honestly. Research 116's authenticated
   completions, tool lifecycle, usage, and credit-failure observations and
   Research 118's two-turn exact-id continuation ran on `1.15.1`; they do not
   automatically transfer to `1.54.0`. Preserve those immutable records. Gate
   any feature whose acceptance requires matching live evidence and name a
   separate follow-up instead of projecting the old run onto the new pin.
   Provider-free decoder and command evidence may transfer only where the full
   all-hop ledger proves the selected surface.
5. Stop with the smallest exact counterexample if a mapped wire, lifecycle,
   permission, authentication, retention, model, failure, or continuation
   boundary changed materially or the base decision requires a live provider
   turn. Recheck official stable immediately before identity commit and push.
   Provider-free retrieval, classification, correction, and validation may
   rerun normally without artificial attempt limits.

## Dispatch Manifest

| Field | Task 068 |
| --- | --- |
| Readiness | ready |
| Prerequisites | Contract 029; Research 116, 118, 308, and 316; completed g05.067; clean pushed `main`; official npm artifacts |
| Completion conditions | every published stable after `1.15.1` is frozen and classified; identity precedes claim; one honest exact-`1.54.0` decision lands; QualifiedOnly and route-family boundaries survive; old live evidence does not migrate; exact-head review and named gates pass |
| Owned mutable paths | Command Code selection, prepared route, decoder, tests, and fixtures under `crates/swallowtail-adapter-command-code/**`; Research 317 and one research-index line; exact Command Code prepared-guide, route/activity/feature matrices, architecture ceiling, standing-lane, `[Unreleased]`, identity-log, claim-log, and task-result lines; `PAPERCUTS.md` append only |
| Reserved shared closeout surfaces | lifecycle task record and generated projections; submitted-handoff deletion belongs to the repository hook |
| Forbidden paths | other adapters; contracts; historical research/captures/releases; provider API route; new public catalogue/import/export/resume operations; live provider probes; release/tag/workflow files |
| Approved concurrent siblings | none on shared currentness surfaces; this campaign is serial |
| Worker capability class | evidence-first Rust/Node currentness worker; large automated npm tree ledger and exact-pin evidence reconciliation; no provider credentials |
| Acceptance evidence | npm dist-tags/version set; 68-point package identities including baseline; exact per-hop inventory and mapped-surface ledger; mutation-sensitive decoder/argv/lifecycle fixtures; current docs/claim agreement |
| Review oracle | skipped stable, inferred range, unclassified mapped delta, changelog-only inference, launcher/TUI/API flattening, lost exact retention/continuation boundary, stale live evidence projected onto `1.54.0`, provider reliance, or unjustified public feature widening |
| Stop conditions | official-channel disagreement; selected public lifecycle/authority/authentication/permission/model/retention/failure change; new driver/facade required; provider/live evidence required for base compatibility |
| Escalation owner | operator via Chatterbox for new policy; Queue coordinator for mechanical blockers |

## Boundaries

One route family and one exact-pin decision. No prompt, inference, login,
credential use, live structured run, live interactive turn, installation, host
update, downloaded-artifact execution, Provider API work, release, tag,
publication, or consumer mutation. Do not infer compatibility from the
installed old version or from package semver continuity. Prerelease channels
are not stable authority.

## Validation

- `cargo fmt -p swallowtail-adapter-command-code -- --check`
- `effigy validate:focused swallowtail-adapter-command-code`
- `effigy package:verify-affected swallowtail-adapter-command-code`
- `effigy check:examples`
- `effigy package:api`
- `effigy qa:routes`
- `effigy qa:northstar`
- focused Command Code identity, selection, argv, decoder, session, and cleanup selectors
- relevant research/log/roadmap/number/lifecycle/next-action checks
- `git diff --check`

## Acceptance

- [ ] official identity and all 67 published successors reproduce
- [ ] every changed selected input is classified
- [ ] identity evidence precedes the claim decision
- [ ] exact QualifiedOnly shape and route boundaries remain truthful
- [ ] live evidence stays bound to the exact version that produced it
- [ ] official stable is rechecked at both boundaries

## Result

Ready for dispatch.

## Next Task

Return the exact outcome to Chatterbox, then compile and dispatch Qoder as the
next Research 308 family without asking Tom to repeat authorization.
