# 181 Claude Code Headless Ultracode Evidence

Status: complete; evidence stop
Owner: Tom
Created: 2026-08-25
Updated: 2026-08-25
Milestone: [g04.065 Claude Code Headless Ultracode](../065-claude-code-headless-ultracode.md)
Depends on: Research 175, 192, 202; Contracts 029, 033, 040, 044

## Goal

Determine the exact Claude Code headless versions, selected models, and
operation profile on which Ultracode can be selected without flattening it
into portable effort, widening authority, or relying on live-provider
inference. Promote an honest empty set if any required fact remains unproved.

## Work

1. Retrieve and digest current official Claude Code model-configuration,
   workflows, CLI, headless, access, billing, and release sources. Record dates
   and decisive body or package hashes in Research 212.
2. Inspect exact official packages/binaries across the claimed first-support
   point and current qualified ceiling `2.1.241`. Freeze `--version`, `--help`,
   parser/local-validation, embedded option, and selected command evidence in
   a disposable directory without installation or a prompt.
3. Reconcile the documentation lead `--effort ultracode` with the exact
   `2.1.241` help corpus, which advertises only
   `low|medium|high|xhigh|max`. Classify accepted, rejected, hidden, aliased,
   gated, and stale-documentation outcomes.
4. Freeze exact model eligibility, access/entitlement and billing posture,
   session lifetime, precedence, settings interaction, and failure behavior.
   Do not infer support from model names or general Claude product prose.
5. Separate the documented `xhigh` effort component from dynamic workflow
   orchestration. Identify every tool, subagent, teammate, process, settings,
   persistence, stream, activity, usage, and terminal effect the exact
   headless command can produce.
6. Determine whether fixed `Plan`, `Read,Glob,Grep`, empty MCP, and
   `--no-session-persistence` disable or contain dynamic workflow effects.
   Any extra process must remain inside the route's owned child/task cleanup
   truth or the row is withheld.
7. Audit current prepared input, immutable evidence, command builder, driver,
   parser, activity, usage, cancellation, deadline, cleanup, fixtures, guide,
   and API baseline. Name the smallest safe adapter-local delta.
8. Classify omission and conflict with every existing portable reasoning
   value. Ultracode may not be represented as `ReasoningMode::xhigh` or a
   seventh value.
9. Separate planned, dispatched, accepted, effective, and observed claims.
   Successful text or usage cannot confirm effective Ultracode behavior.
10. Promote Research 212 with one exact deliver-now table or explicit empty
    set. Update the milestone/card state and reserved closeout honestly.

## Acceptance Criteria

- [x] exact official sources, selected packages, dates, and hashes are recorded
- [x] first supported version and exact selected-model rows are explicit
- [x] parser, help, settings, precedence, alias, fallback, and failure truth is
      settled
- [x] access, entitlement, billing, and no-live-proof limits are explicit
- [x] dynamic workflow tools/processes/persistence/disclosure are bounded or
      named as a stop
- [x] Ultracode remains distinct from portable reasoning and ordinary `xhigh`
- [x] claim strength is split by planned/dispatched/accepted/effective/observed
- [x] Research 212 contains a non-empty exact table or honest empty set
- [x] no production code, public API, shared contract/runtime, currentness,
      release, merge, generation rollover, or g04 closure changes
- [x] `effigy validate:focused swallowtail-adapter-claude-agent`, `effigy
      qa:northstar`, relevant indexes, and `git diff --check` pass

## Stop Conditions

- exact parser, model, entitlement, workflow, or process-topology truth remains
  ambiguous
- delivery needs a live prompt, account inspection, raw settings, config
  mutation, generic argv/environment, shared authority, or a public lifecycle
  change

## Out Of Scope

- production binding, Fast mode, teams, response-only/ACP promotion, live
  access, currentness, release, merge, rollover, or g04 closure
