# 148 Claude SDK Degraded-Cleanup Oracle Reconciliation

Status: complete; Desktop PR 175 merged at `f8bff5a2b927eaf23e0220ac4a0640835e4a2dfd`
Owner: Desktop live-evidence harness owner
Created: 2026-09-08
Milestone: `../035-shared-harness-capability-and-producer-boundary.md`
Depends on: card 147 return; Research 298; Contract 019

## Goal

Align Desktop's Claude SDK proof runner with the route-qualified ordinary macOS
`Degraded` cleanup posture without weakening cleanup truth.

## Scope

1. Freeze the Card 312 capsule and prove its `degraded` close plus joined
   reapers and zero leases, listeners, survivors, or dispatcher activity
   matches Contract 019's accepted root-only posture.
2. Repair the provider-free runner oracle so `Degraded` is an accepted close
   only for the exact qualified route/platform posture. Preserve the label as
   `degraded`; never promote it to `clean` or owned-tree-empty evidence.
3. Keep `Failed`, unconfirmed root completion, surviving resources, unjoined
   reapers, dispatcher activity, or missing evidence on the defect branch.
4. Add focused fixtures for Clean, NotApplicable, accepted Degraded, failed
   cleanup, and every adverse companion invariant.
5. Record that Card 312 observed successful open at zero credit but did not
   exercise a prompt turn. Keep both Contract 061 cells unqualified.

Owned mutable paths: Desktop's Card 132/312 runner and wrapper, focused tests,
the new Desktop card/log/result, and additive proof documentation. Do not edit
the immutable Card 312 capsule. Queue closeout owns shared Desktop indexes.

Forbidden: live Claude/native provider execution; prompt, turn, tool, control,
retry, reconnect, or respawn; Swallowtail runtime changes; qualification or
matrix availability; account top-up; candidate, tag, release, or dependency-pin
changes.

## Acceptance Criteria

- [x] the oracle accepts route-qualified `Degraded` only with every companion cleanup invariant satisfied
- [x] accepted `Degraded` remains distinguishable from `Clean` and never implies `OwnedTreeEmpty`
- [x] Failed, surviving-resource, unjoined-reaper, dispatcher, and missing-evidence fixtures remain defects
- [x] Card 312's immutable capsule and digest remain byte-identical
- [x] no live attempt occurs and both Contract 061 cells remain unqualified

## Validation

- Desktop focused runner checks
- Desktop docs and Northstar QA
- `git diff --check`
- exact-head independent review

## Review Oracle

Smallest counterexample: changing `degraded` to `clean`, or accepting a
degraded close while any survivor/listener/lease remains or reaper join fails.

## Stop Conditions

- the typed cleanup result cannot distinguish the qualified degraded posture
  from failed or unconfirmed cleanup;
- repair requires a Swallowtail runtime or Contract 019 change; or
- any test or documentation rewrites the immutable Card 312 observation.

## Auto-Continuation

No. After provider-free repair and closeout, the credited suite still requires
restored credit and separate live authority.

## Result

Desktop task `bd2791d2-5bc6-4764-aa34-ada94d3a2aa0` completed through PR 175.
Accepted head `dff630578b6e3cedf163916ae07e5e3f1a30b997`; review comment
`5592097198`; merge `f8bff5a2b927eaf23e0220ac4a0640835e4a2dfd`; canonical
Desktop closeout `71d1b295423b4ef3eaf26959d8e84e66dd79df34`.

Provider-free checks passed 12/12. The repaired oracle accepts only bounded
code `swallowtail.claude-agent.sdk.close_root_only_degraded` with joined
reapers and zero leases, listeners, survivors, or dispatcher calls. Foreign
degraded codes, `Failed`, residue, and unjoined reapers remain defects. Card
312's capsule stayed byte-identical and no provider process ran.
