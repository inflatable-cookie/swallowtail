# g05.065 Claude Agent SDK 0.3.270 Tuple Useful Newer

Owner: Tom
Created: 2026-09-14
Depends on: Contract 029; exact Claude Agent SDK `0.3.259` / native `2.1.259` tuple; Research 278, 280, 287, 301, and 308; g05.064
Vision tags: route currentness, Claude Agent SDK, native sidecar, compatibility

## Outcome

Rebind the Claude Agent SDK route to the current exact official tuple — SDK
wrapper `0.3.270` carrying native Claude `2.1.270` — only if complete
provider-free package, declaration, implementation, manifest, and platform
artifact evidence admits it. Keep the Node `22.23.2`, private wire, and current
Swallowtail sidecar-source axes independent. Preserve `QualifiedOnly` one-point
claims: this task does not create a range or transfer compatibility from the
separate Claude Code routes.

Planning observed official npm `latest=next=0.3.270`; its manifest declares
`claudeCodeVersion=2.1.270`. The published wrapper points after `0.3.259` are
`0.3.260`, `0.3.261`, `0.3.263`, and `0.3.265..=0.3.270`; `0.3.262` and
`0.3.264` are unpublished gaps. Installed Node is the qualified `22.23.2`;
installed Claude Code is `2.1.258` and does not identify the bundled native
target. The worker must recheck official npm and include every added stable.

## Ready-State Rubric

- [x] Tom authorized the complete Research 308 currentness campaign.
- [x] g05.064 completed and its lifecycle closeout is on canonical `main`.
- [x] Wrapper, native, Node, source-tag, and wire axes are identified
  separately.
- [x] The exact live-qualified registered-tool evidence boundary is known and
  cannot silently migrate to a new tuple.
- [x] No prompt, SDK session, login, installation, host update, or provider
  credit is needed.

## Work

1. Re-probe npm stable/dist-tags and observe installed Node and Claude through
   version commands only. Retrieve wrapper and relevant platform packages for
   `0.3.259` and every published stable through `0.3.270` into `/tmp`. Freeze
   Research 315 and exact tarball, integrity, package-tree, manifest, native
   revision, platform-payload, and selected-file identities without executing
   downloaded artifacts.
2. Classify every changed shipped file feeding the route's `.` entry point and
   sidecar: exported query/session APIs, spawn hook and signal semantics,
   initialize/system records, supported models, account-source labels, model
   and effort controls, permission modes and `canUseTool`, native and MCP tool
   calls, registered servers/status, session listing/resume, activity and
   results, rate limits, structured provider failures, cancellation, process
   exit, stderr drain, cleanup, and settings/config isolation. Check `/bridge`
   and `/browser` only to preserve their prohibition. Changelogs are discovery
   only. Commit identity evidence before production edits.
3. If the selected contract is unchanged or can be mechanically adapted
   without public behavior or authority change, update the sidecar and
   fixtures, then rebind only `claude-agent.sdk.package` and
   `claude-agent.sdk.native` to exact `0.3.270` / `2.1.270`. Keep Node
   `22.23.2`, the private wire, current source tag, claim posture, and
   one-point shape unless direct evidence requires an honest stop.
4. Reconcile tuple-specific features explicitly. Research 301's
   registered-tool qualification and other live capsules belong to exact SDK
   `0.3.259` / native `2.1.259`; they do not transfer to `0.3.270` / `2.1.270`.
   Preserve their immutable evidence and either gate the newer compiled tuple
   as unqualified with a named live requalification follow-up, or retain a
   feature only where independent evidence actually covers the new tuple. Do
   not make old and new exact base claims simultaneously just to keep a live
   feature green.
5. A material public lifecycle, authority, authentication, permission, model,
   selected-wire, or native-control change stops with the smallest exact
   counterexample and concrete follow-up. Recheck npm stable immediately
   before the identity commit and push. Provider-free diagnosis and reruns are
   normally allowed.

## Dispatch Manifest

| Field | Task 065 |
| --- | --- |
| Readiness | ready |
| Prerequisites | Contract 029; Research 278, 280, 287, 301, and 308; completed g05.064; clean pushed `main`; official npm wrapper/platform artifacts |
| Completion conditions | every published wrapper after `0.3.259` is frozen with its coupled native identity; identity precedes claim; one honest exact-tuple decision lands; five-axis separation and QualifiedOnly posture survive; tuple-specific live evidence does not migrate; exact-head review and named gates pass |
| Owned mutable paths | Claude SDK selection, sidecar, registered-tool gating, tests, and fixtures under `crates/swallowtail-adapter-claude-agent/**`; Research 315 and one research-index line; exact Claude SDK prepared-guide, route-matrix, feature-matrix, architecture ceiling, standing-lane, `[Unreleased]`, identity-log, claim-log, and task-result lines; `PAPERCUTS.md` append only |
| Reserved shared closeout surfaces | lifecycle task record and generated projections; submitted-handoff deletion belongs to the repository hook |
| Forbidden paths | Claude Agent ACP, Claude Code headless/response/watcher claims, other adapters, contracts, historical research/releases or live capsules, new public operations, live registered-tool requalification |
| Approved concurrent siblings | none on shared currentness surfaces; this campaign is serial |
| Worker capability class | evidence-first Rust/Node currentness worker; official npm retrieval, deterministic package/platform comparison, and private-sidecar adaptation; no provider credentials |
| Acceptance evidence | npm dist-tags/version set, wrapper/platform identities, host observations, complete all-hop selected-surface ledger, mutation-sensitive declarations/fixtures, sidecar check-only corpus, current docs/claim agreement |
| Review oracle | skipped stable, inferred native equality, unclassified mapped delta, changelog-only inference, lost unpublished gap, range widening, Claude Code claim transfer, old live evidence projected on the new tuple, provider reliance, or unbounded sidecar adaptation |
| Stop conditions | official-channel disagreement; major-line reset; mapped public lifecycle/authority/authentication/permission/model change requiring policy; live/provider evidence required for the base tuple |
| Escalation owner | operator via Chatterbox for new policy; Queue coordinator for mechanical blockers |

## Boundaries

One route family and one exact tuple decision. No prompt, inference, SDK open,
live session, login, credential use, install, host update, downloaded-artifact
execution, live registered-tool attempt, release, tag, publication, or consumer
mutation. Node and host Claude are observations only. No range, `AllowUnverified`
posture, or compatibility transfer from Claude Code or Claude Agent ACP.

## Validation

- `cargo fmt -p swallowtail-adapter-claude-agent -- --check`
- `effigy validate:focused swallowtail-adapter-claude-agent`
- `effigy package:verify-affected swallowtail-adapter-claude-agent`
- `effigy check:examples`
- `effigy package:api`
- `effigy qa:routes`
- `effigy qa:northstar`
- relevant sidecar check-only, research/log/roadmap/number/lifecycle/next-action checks
- `git diff --check`

## Acceptance

- [ ] official wrapper and coupled native identities are reproducible for every hop
- [ ] every changed selected input is classified
- [ ] identity evidence precedes production edits
- [ ] all five base axes remain exact and independent
- [ ] live-qualified feature evidence remains bound to the tuple it actually ran
- [ ] official stable is rechecked at both boundaries

## Result

Ready for Queue dispatch.

## Next Task

Return the exact outcome to Chatterbox, then compile and dispatch the Grok
catalogue family as the next Research 308 family without asking Tom to repeat
authorization.
