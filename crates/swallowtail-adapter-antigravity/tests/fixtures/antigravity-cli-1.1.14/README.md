# Antigravity CLI 1.1.14 currentness corpus

This secret-free identity corpus freezes host Antigravity `1.1.9` and
official GitHub `google-antigravity/antigravity-cli` `1.1.14` before
Swallowtail widens the `antigravity-cli.release` claim.

Exact GitHub asset and selected help identities live in `identity.json`.
Selected flags, exact `--conversation`, and `models` remain. `1.1.14`
adds unmapped `--input-format`. Gemini API-key sign-in stays unused.

`1.1.8` stays independently unqualified. No provider prompt. No live
catalogue. No live print run. The host install was not replaced.

No fixture contains a credential, host path, account identity, provider
payload, or real conversation id.
