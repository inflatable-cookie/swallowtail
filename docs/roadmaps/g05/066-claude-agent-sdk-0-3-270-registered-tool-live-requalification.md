# g05.066 Claude Agent SDK 0.3.270 Registered-Tool Live Requalification

Status: planned; awaiting Chatterbox compilation and queue dispatch — this stub names the follow-up only and claims no execution authority
Owner: Tom
Created: 2026-09-14
Depends on: Contract 029; Contract 061; Contract 063; Research 301; Research 315; g05.065
Vision tags: route currentness, Claude Agent SDK, registered tools, live evidence

## Outcome

Run the separately authorized live registered-tool requalification on the
exact rebound tuple — SDK wrapper `0.3.270`, native `2.1.270` Darwin arm64,
Node `22.23.2`, current sidecar source tag, private-loopback mediated-stdio,
MCP `2025-11-25` — in the same Desktop Card 318 shape as Research 301
(exact Allow dispatching once with unchanged `{}` and correlating its fixed
result; Deny, cancellation, and stale/foreign controls dispatching zero
times; joined cleanup with no survivors). On acceptance, move the frozen
live tuple to `0.3.270`/`2.1.270` and re-open the `registered_tools` and
`consumer_tool_exchange` feature-matrix cells. On any other outcome, keep
the unqualified truth and record the typed stop.

## Ready-State Rubric

- [x] g05.065 rebound the route tuple and froze Research 315 without
  transferring Research 301.
- [ ] Chatterbox has compiled the live gate (Desktop task, capsule shape,
  review bar) and the queue has dispatched it.
- [ ] No prompt, login, installation, host update, release, tag, or consumer
  mutation beyond the separately authorized live gate.

## Work

1. Run the authorized live gate only; no provider work beyond it.
2. On acceptance, update the frozen live-tuple point, the projection gate,
   the two matrix cells, and the route guide. On any other outcome, keep
   `live_tuple_not_compiled` and record the stop.
3. Never widen the tuple, add a range, or transfer compatibility from
   Claude Code or Claude Agent ACP.

## Boundaries

One live gate on one exact tuple. No release, tag, publication, or consumer
mutation.

## Validation

Focused and affected-package gates for `swallowtail-adapter-claude-agent`,
route and matrix QA, and the named docs gates. No live probes beyond the
authorized gate.

## Acceptance

- [ ] the live gate ran on the exact rebound tuple or a typed stop is recorded
- [ ] the frozen live point moves only on acceptance
- [ ] both matrix cells agree with the projected truth

## Result

Planned; not started.

## Next Task

Chatterbox compiles and dispatches this follow-up after the Research 308
campaign work it sequences first.
