# 2026-09-24 g06.028 Live Gate Disposition

Owner: Tom (Chatterbox planning ruling)
Date: 2026-09-24
Task: g06.028; PR #369

## Decision

The one authorized OpenCode ACP HTTP MCP attempt is **accepted as HTTP
honouring evidence** on exact installed `1.18.18`, `http` form, loopback URL,
one bearer header. It connected, listed the tool, completed one call, and the
turn ended `Completed` with `Clean` cleanup.

The card's exact-model requirement was provenance, not a property of the
claim. Honouring is OpenCode's client behaviour: it opens the declared server
and routes the call whichever model drives the turn. The model is therefore
recorded as **the host's default agent model, unverified** (configured
`kimi-for-coding/k3` present, not proven effective), and the claim does not
depend on it.

- The `opencode.acp` HTTP MCP placement cells move to available for exact
  `1.18.18` only, citing the record and disclosing the unverified model. Other
  window points stay unqualified for honouring.
- The record renumbers to **Research 349**; 348 belongs to Claude Code
  g06.022.
- The corrected model resolver and its `no_usable_model` stop stay as the
  harness for any future attempt.
- No rerun. The consumed attempt is closed by this ruling.

## Why

Three review rounds correctly refused to close on a condition the evidence
could not meet. The condition was a planning choice, so planning resolves it
rather than spending another operator attempt to re-prove model-independent
behaviour.
