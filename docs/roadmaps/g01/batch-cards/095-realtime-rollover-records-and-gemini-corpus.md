# 095 Realtime Rollover Records And Gemini Corpus

Status: ready
Owner: Tom
Updated: 2026-07-22
Milestone: `../033-gemini-live-realtime-portability-proof.md`

## Objective

Realize the minimum explicit planned-rollover boundary and freeze the exact
Gemini Live preview corpus before production WebSocket work.

## Readiness Gate

Research 021 selects the route. Contract 027 fixes rollover meaning, private
handle scope, access, handoff, cancellation, and cleanup. No operator product
policy remains unresolved in the bounded subset.

## Governing References

- Research 021
- Contracts 005, 006, 009, 011, 014, 016, 020, 026, and 027
- roadmap 033
- official Gemini Live raw WebSocket, capability, session, API-reference,
  key, and pricing evidence accessed 2026-07-22

## Scope

- disabled-or-bounded planned connection rollover policy
- matching capability and exact maximum-one constraint
- immutable preflight agreement across requirements, instance, route, and open
  request
- private replaceable zeroized provider-handle fixture type or equivalent
  adapter-owned boundary
- provider-neutral rollover assertion pack over the existing realtime profile
- frozen Gemini Live setup, activity, audio, output, transcript, usage,
  resumption-update, GoAway, malformed, unknown, and failure fixtures
- exact authorization-key access profile, preview facade, model, asymmetric
  formats, voice, thinking, activity, turn, and connection bounds
- no production network client, credential, browser, device, or paid request

## Ordered Work

1. Add the smallest capability, constraint, and request-policy records required
   by Contract 027; default existing behavior to disabled.
2. Extend pure preflight fixtures for disabled, missing, mismatched, zero, and
   maximum-one rollover posture.
3. Add a provider-neutral assertion pack for one idle-boundary handoff, no
   replay, exhaustion, failure, and credential-last joined cleanup.
4. Freeze the dated raw Gemini setup and server corpus without embedding a real
   endpoint query or credential.
5. Prove handle redaction, replacement, clearing, and absence from
   `SessionResumeBinding`, events, outcomes, and diagnostics.
6. Run focused core, runtime, testkit, and Gemini tests plus warnings-denied
   clippy before card 096 becomes ready.

## Acceptance Criteria

- [ ] planned rollover is distinct from resume, reconnect, reattachment,
      recovery, retry, and durable state
- [ ] disabled remains the default for every existing driver and request
- [ ] one positive bound is visible before provider effects
- [ ] handles are private, replaceable, redacted, and operation-scoped
- [ ] the common realtime role and eleven existing profiles do not widen
- [ ] the corpus fixes exact setup, manual activity, formats, voice, thinking,
      usage, warning, handle, and terminal semantics
- [ ] raw authenticated URLs, keys, handles, audio, transcripts, provider ids,
      and payloads remain outside stable diagnostics
- [ ] no live provider or credential state enters default QA

## Evidence Required

- public policy, capability, constraint, and pure-preflight tests
- provider-neutral rollover assertion report
- handle redaction and lifecycle tests
- exact frozen Gemini client/server frame assertions
- unchanged OpenAI realtime profile and production tests
- focused tests and warnings-denied clippy output

## Validation

- focused core, runtime, testkit, Gemini, and OpenAI realtime tests
- focused warnings-denied clippy
- `effigy qa:docs`
- `git diff --check`

## Stop Conditions

- rollover needs generic reconnect or consumer resume
- handle state must become public or durable
- existing OpenAI semantics must change to enable Gemini
- exact preview model, authorization-key route, or manual activity behavior is
  no longer supported
- fixture work needs a credential, paid request, browser, or device

## Auto-Continuation

No. Confirm the shared records and corpus before production WebSocket work.
