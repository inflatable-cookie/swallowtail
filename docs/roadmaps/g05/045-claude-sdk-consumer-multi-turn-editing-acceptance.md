# g05.045 Claude SDK Consumer Multi-Turn Editing Acceptance

Status: ready; provider-limitation recorded from merged Desktop g02.049; awaiting exact-head review
Owner: Tom
Created: 2026-09-09
Depends on: Contracts 017, 019, 023, 029, 041, and 063; stopped g05.029
audit; released `v0.4.4`; Northstar Queue task
`bc4acd98-91a3-4e14-86e0-38376b037da7`
Vision tags: Claude route, consumer parity, native mediation, registered tools

## Outcome

Consume Desktop g02.049's three merged immutable capsules provider-free and
record the exact native SDK limitation. On SDK `0.3.259` / native `2.1.259`, an
in-workspace native `Read` completed without `canUseTool`; the qualified native
tool set therefore cannot satisfy universal consumer mediation.

Freeze the Desktop merge, review, capsule digests, released Swallowtail source
identity, tuple, prompt, observed proposal/result order, unchanged fixture,
zero decisions, and cleanup as Research 303. Correct the guide, matrix notes,
and g05.029 follow-up without weakening the historical failed clause or the
route's narrower answerable permission-exchange claim.

## Ready-State Rubric

- [x] g05.029 names the exact missing consumer multi-turn clause.
- [x] Desktop produced three bounded immutable attempts and spent every
      authorized provider budget without a passing capsule.
- [x] The final capsule distinguishes proposal visibility from native `Read`
      completion and records zero `canUseTool` decisions.
- [x] Upstream SDK issue 401 reports the same default-allowed native-read
      behavior.
- [x] Tom authorized negative-evidence landing and provider-limitation
      classification on 2026-09-10.
- [x] This task has no provider authority.

## Decisions

- The original g05.029 audit remains historically `stopped`; this follow-up
  explains why the native route cannot close its every-call clause.
- SDK `default` does not make every native tool answerable. `canUseTool` is a
  permission request, not a universal pre-tool hook; default-allowed
  in-workspace reads may execute without it.
- Keep `permission_exchange=Yes` scoped to calls the SDK actually offers. Do
  not project it as universal mediation or bounded workspace writing.
- Every call on the qualified registered-tool MCP carrier remains mediated;
  that separate mechanism is the supported route for strict before-dispatch
  consumer authority.
- Preserve all three Desktop capsules byte-for-byte. No fourth call, observer
  patch, model change, or native-tool workaround follows.

## Dispatch manifest

- **State:** ready; dispatch only after queue dependency
  `bc4acd98-91a3-4e14-86e0-38376b037da7` reaches `done`.
- **Completion:** Research 303, one deterministic negative-evidence
  fixture/test, corrected guide and matrix notes, one log, and a bounded
  g05.029 follow-up.
- **Owned mutable paths:** this task; `docs/research/303-*.md` and its index;
  one fixture and focused test under
  `crates/swallowtail-adapter-claude-agent/tests/**`; the exact evidence note in
  `docs/guides/claude-agent-sdk-prepared-integration.md`; the Claude matrix-row
  notes only; one appended `## Follow-up evidence` subsection in g05.029; one
  new log and indexes; `PAPERCUTS.md` append only.
- **Reserved closeout surfaces:** `docs/roadmaps/README.md`,
  `docs/roadmaps/g05/README.md`, and `docs/roadmaps/generation-index.md`.
- **Worker:** evidence-focused Rust/documentation worker; no provider access.
- **Excluded:** runtime or sidecar behavior; public API; dependency/version or
  range changes; matrix Yes/No values; Desktop mutation; provider call;
  credentials; release, tag, or publication.
- **Escalation:** Chatterbox owns evidence semantics; Tom owns any later live
  acceptance or broader route authority.

## Work

1. Wait for the dependency to reach `done`. Resolve its exact Desktop PR head,
   independent review, merge, canonical closeout, all capsule paths, and
   SHA-256 values. Use only the merged tree.
2. Recompute all three capsule digests. For the final capsule, correlate the
   native `Read` proposal and result, zero callbacks/decisions, unchanged
   fixture digest, attempt count, and cleanup. Distinguish absence from an
   unobserved field.
3. Write Research 303 and a deterministic fixture/test binding that exact
   negative evidence. Correct claims that every admitted native tool crosses
   `canUseTool`; retain the narrower permission-exchange and registered-tool
   truths. Add the historical g05.029 follow-up.
4. Validate only the affected Claude adapter and documentation surfaces. Open
   one PR for independent exact-head review. The queue owns merge and closeout.

## Limitation and review oracle

| Invariant | Adversarial counterexample | Required proof |
| --- | --- | --- |
| Desktop is the consumer | a standalone Swallowtail runner | capsule producer and call path resolve to merged Desktop agent service |
| Attempt history is exact | omit earlier failures or infer another retry | all three capsule digests and final one-open/zero-retry count |
| Native bypass is observed | proposal visibility misclassified as execution | final capsule shows native `Read` result with zero callback decisions and unchanged fixture |
| Claim narrows correctly | mark all permission exchange unavailable | native universal mediation is unavailable; SDK-emitted callbacks and registered MCP calls retain their qualified truths |
| Scope stays bounded | Bash, Git, web, outside path, fallback, retry, or hidden turn | capsule counters/events prove zero forbidden effects |
| Evidence belongs to released source | local checkout or mismatched lock/tag | Desktop lock and capsule resolve Swallowtail to released `v0.4.4` source |
| Cost and lifecycle are bounded | Opus/fallback, extra attempt, or hidden retry | admitted Sonnet, one session, one stopped turn, zero retries, and recorded cleanup |

## Stop conditions

- Stop if the Desktop task does not merge or any capsule is missing, mutable,
  redaction-unsafe, source-mismatched, or independently unaccepted.
- Stop if the native result/callback absence cannot be proven from the merged
  capsule. Do not infer it from issue prose alone.
- Stop if the correction requires runtime, sidecar, public API, matrix-value,
  compatibility, consumer, provider, release, or tag mutation.
- Return to Chatterbox with the exact failed row. Do not rerun Claude.

## Operator rulings

Tom authorized the corrected execution boundary and one final Sonnet session
on 2026-09-09. That successor capsule then observed native `Read` complete
without any `canUseTool` callback. Tom authorized landing the result as
definitive negative evidence on 2026-09-10, correcting universal native-tool
mediation claims, and using registered tools for the separate strict editing
route. No fourth Claude call belongs to this task.

## Evidence

Desktop g02.049 task `bc4acd98-91a3-4e14-86e0-38376b037da7`; PR #205 accepted
head `8002833f45a66e63df1071f5bb99053d6bf85922`; independent exact-head
review `5610373055`; merge `e0217483ed8584ddca1bc4f4fdfedce53bcbaa76`;
closeout `941dfcdb5361d673dfd5c7adb54950cacbbbe478`.

Capsules recomputed from the merged Desktop tree:

- `docs/proofs/claude-mediated-multi-turn-editing.capsule.json` SHA-256
  `10b77ede7a85b68831967731e6d5636c0d44b72a56b1774b29938ed4fd519cd6`
- `docs/proofs/claude-mediated-multi-turn-editing-renewed.capsule.json`
  SHA-256
  `8921fa5edc0ba869a8d565e2834fd9e138178dd87bd0bbec8e1561205ffc1386`
- `docs/proofs/claude-mediated-multi-turn-editing-successor.capsule.json`
  SHA-256
  `4f23e55c548469ae61666052266497ae70b864566e0e3cc7de1c3a208a777dba`

Released Swallowtail `v0.4.4` peel
`49c9e3b291609c9ebf5b35a284c08302f3b8d5e3`. Tuple: SDK `0.3.259`, native
`2.1.259` darwin-arm64, Node `22.23.2`, sidecar `0.4.4`,
`permissionMode` `default`, `claude-sonnet-5`. Successor: one open, one
stopped turn, zero retries/fallbacks/reconnects; chain is one native `Read`
proposal; allow 0, deny 0; no `canUseTool` field; fixture digest stayed
`sha256:b6a98d9ce9a2d9149288fa3df42d377c3e42737afdcdaf714e33c0a100b51060`;
cleanup degraded accepted. Binding:
`crates/swallowtail-adapter-claude-agent/tests/fixtures/claude-agent-sdk-0.3.259/native-read-mediation-limitation.json`.

Retained non-claims: `permission_exchange=Yes` for SDK-emitted callbacks;
registered MCP still Contract 063-mediated; bounded workspace writing still
`No`; no matrix Yes/No, version, range, runtime, release, or tag change.
Swallowtail review and merge belong to the queue.

## Next task

Close g05.045 as a provider-limitation reconciliation. Desktop g02.051 builds
the registered-tool editing route provider-free. Any later live acceptance
requires a separate operator gate; no provider rerun, range work, release, or
tag follows automatically.
