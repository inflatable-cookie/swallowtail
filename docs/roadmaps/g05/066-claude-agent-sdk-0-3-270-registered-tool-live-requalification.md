# g05.066 Claude Agent SDK 0.3.270 Registered-Tool Live Requalification

Status: ready; typed setup stop recorded; separately authorized live gate
remains unqualified
Owner: Tom
Created: 2026-09-14
Depends on: Contract 029; Contract 061; Contract 063; Research 301; Research 315; g05.065
Vision tags: route currentness, Claude Agent SDK, registered tools, live evidence

## Outcome

Run the separately authorized live registered-tool requalification on the
exact rebound tuple — SDK wrapper `0.3.270`, native `2.1.270` Darwin arm64,
Node `22.23.2`, current sidecar source tag, private-loopback mediated-stdio,
MCP `2025-11-25` — in the same Desktop Card 318 shape as Research 301. On
acceptance, move the frozen live tuple to `0.3.270`/`2.1.270` and re-open the
`registered_tools` and `consumer_tool_exchange` feature-matrix cells. On any
other outcome, keep the unqualified truth and record the typed stop.

## Ready-State Rubric

- [x] g05.065 rebound the route tuple and froze Research 315 without
  transferring Research 301.
- [x] Chatterbox has compiled the live gate (capsule shape and review bar).
- [x] Provider-operation authority is current: Tom re-confirmed on 2026-09-17
  that the separately authorized live gate still stands. It covers one exact
  tuple and nothing else.

## Dispatch manifest

- **State:** ready; provider-operation lane; one lane; no automatic successor.
- **Completion:** the gate runs once on the exact rebound tuple and either
  moves the frozen live point to `0.3.270`/`2.1.270` with both matrix cells
  reconciled, or records a typed stop and keeps `live_tuple_not_compiled`.
- **Owned mutable paths:** the frozen live-tuple record, the Contract 061
  projection gate, the `registered_tools` and `consumer_tool_exchange` matrix
  cells, the route guide, and this task's result lines.
- **Reserved closeout surfaces:** lifecycle task record and generated
  projections; submitted-handoff deletion belongs to the repository hook.
- **Worker:** live-evidence worker with provider-operation authority limited to
  the one exact gate below.
- **Excluded:** any second attempt, fallback, or rerun; any other tuple, range,
  or compatibility transferred from Claude Code or Claude Agent ACP; install,
  login, host update, release, tag, or consumer mutation.
- **Escalation:** operator via Chatterbox for any provider budget or policy
  question; Queue coordinator for mechanical blockers.

### Authorized gate

One mediated-stdio session on the exact rebound tuple: an exact Allow
dispatches once with an unchanged `{}` and its fixed result is correlated;
Deny, cancellation, and stale or foreign controls dispatch zero times; cleanup
is joined with no survivors. Use the cheapest adequate model. No retry beyond
provider-free recovery, and no second live attempt.

## Work

1. Run the authorized live gate only; no provider work beyond it.
2. On acceptance, update the frozen live-tuple point, the projection gate, the
   two matrix cells, and the route guide. On any other outcome, keep
   `live_tuple_not_compiled` and record the stop.
3. Never widen the tuple, add a range, or transfer compatibility from Claude
   Code or Claude Agent ACP.

## Boundaries

One live gate on one exact tuple. No release, tag, publication, or consumer
mutation.

## Validation

Focused and affected-package gates for `swallowtail-adapter-claude-agent`,
route and matrix QA, and the named docs gates. No live probes beyond the
authorized gate.

## Acceptance

- [x] the live gate returned the typed setup stop recorded in Research 329
- [x] the frozen live point did not move without acceptance
- [x] both matrix cells remain producer-gap `live_tuple_not_compiled`, agreeing
  with the projected truth

## Result

Compiled and dispatched by Chatterbox on 2026-09-17 under Tom's re-confirmed
provider-operation authority. The Swallowtail courier build and mediated-stdio
preflight passed. The existing Desktop runner's provider-free check stopped at
`setup.static_tuple_mismatch`: it remains bound to SDK `0.3.259` / native
`2.1.259`, while this route is compiled at SDK `0.3.270` / native `2.1.270`.
No provider work started; Research 329 records the typed stop. The frozen live
point and both matrix cells remain unchanged.

## Next Task

The queue dispatched g05.066 as one bounded live gate. The typed stop closes
this attempt without a rerun. A future separately authorized consumer update
must bind the existing runner to the exact rebound tuple before another live
operation can be considered.
