# Ollama 0.32.15 currentness corpus

This secret-free identity corpus freezes official GitHub `v0.32.15` before
Swallowtail widens the `ollama.runtime` native attached claim. Host `ollama`
was not installed.

Exact GitHub tag identity lives in `identity.json`. `api/types.go` is
byte-identical to `v0.32.14`. Selected chat, inventory, process, show, and
options structs stay byte-identical. The five selected native routes remain
registered. `server/routes.go` moved on unselected scheduler cache and
chat/generate parser-error cancel. Desktop onboarding, metadata cache, Qwen
3.8 system-message normalize, and MLX/llama.cpp dependency updates stay
unmapped.

GitHub still marks plain `v0.32.2` and `v0.32.10` as prereleases. Those stay
named exclusions. The attached server was not started. Official app archives
were not downloaded. The frozen `0.32.14` corpus stays.

No fixture contains a credential, host path, account identity, provider
payload, real model observation, or live inference response.
