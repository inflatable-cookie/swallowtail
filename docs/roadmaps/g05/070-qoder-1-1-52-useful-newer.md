# g05.070 Qoder 1.1.52 Useful Newer

Owner: Tom
Created: 2026-09-14
Depends on: Contract 029; Research 151, 256, 308, and 317; g05.068
Vision tags: route currentness, Qoder, headless, exact pin

## Outcome

Reopen the exact `qoder.package` pin and qualify current official npm `1.1.52`
for `qoder.headless` only if complete provider-free package, entrypoint,
invocation, stream-JSON, authority, and lifecycle evidence admits it. Preserve
`QualifiedOnly` and one exact maintained point; do not infer a range across the
27 published stable releases after `1.1.25`.

Planning rechecked npm `latest=1.1.52`, published
`2026-09-14T12:07:54.560Z`; `1.1.35-beta.2` remains on the separate beta
channel. `qodercli` is absent from this host. The latest package still exposes
`qodercli` through `bundle/qodercli.js`, the separate `qoder` dispatcher, and
Node `>=20.0.0`; host Node `22.23.2` satisfies that engine. Research 308's
`1.1.51` observation moved before planning, so `1.1.52` is the task target.

## Ready-State Rubric

- [x] Tom authorized the complete Research 308 currentness campaign, including
      reopening exact-pin families.
- [x] g05.068 completed and its lifecycle closeout is on canonical `main`.
- [x] Official `1.1.52`, absent host CLI, the 27-hop span, and Node engine are
      identified separately.
- [x] Research 151 fixes the selected headless wire and Research 256 keeps
      skill visibility withheld rather than inferred.
- [x] No prompt, login, installation, host update, or provider credit is needed
      for the identity and claim decision.

## Work

1. Re-probe npm stable/dist-tags, full published versions, package metadata,
   and installed `--version` availability. Retrieve exact `1.1.25` and every
   stable `1.1.26..=1.1.52` into `/tmp`. Freeze Research 318 and exact tarball,
   integrity, package-tree, entrypoint, dependency, engine, and selected-file
   identities without installing or executing downloaded artifacts.
2. Derive a deterministic per-hop shipped-tree ledger. Classify every changed
   input feeding `qodercli --print`: headless-vs-ACP/SDK/TUI dispatch, prompt
   operand, stream-JSON output, `dont_ask`, historical `--max-turns 8`,
   `--no-session-persistence`, `--cwd`, init/assistant/result/error envelopes,
   correlation, cancellation, deadline, process ownership, and joined cleanup.
   Prove whether the formerly inert max-turns argv remains inert. Treat the
   `qoder` IDE dispatcher, skills commands, hidden prompt and permission
   aliases, resume/session/worktree/remote flags, partial messages, ACP, and SDK
   stdio as unmapped only after proving they cannot affect the selected route.
   Changelogs are discovery only. Commit identity before production claim edits.
3. If the selected contract is unchanged or mechanically adaptable without a
   new public lifecycle, authority, or operation, rebind the one exact claim
   point to `1.1.52`. Keep the existing behavior revision only when mapped wire
   and lifecycle are unchanged. Do not retain `1.1.25` as a second exact point
   merely to hide incomplete current evidence.
4. Preserve Research 151 and its historical decoder specimens. Do not turn
   package skill files, init `skills`/`plugins`, or `qoder skills list` into
   selected-run visibility: Research 256's empty deliver-now ruling and
   g05.039/g05.040 gate remain independent. Reconcile current feature, route,
   lifecycle, guide, and architecture truth only where the all-hop ledger
   supports it. No live feature may be inferred from source artifacts.
5. Stop with the smallest exact counterexample if a mapped wire, lifecycle,
   permission, authentication, retention, failure, or process-authority
   boundary changed materially, or if the base decision requires a prompt or
   live provider run. Recheck official stable immediately before identity
   commit and push. Provider-free retrieval, classification, correction, and
   validation may rerun normally without artificial attempt limits.

## Dispatch Manifest

| Field | Task 070 |
| --- | --- |
| Readiness | ready |
| Prerequisites | Contract 029; Research 151, 256, 308, and 317; completed g05.068; clean pushed `main`; official npm artifacts |
| Completion conditions | every published stable after `1.1.25` is frozen and classified; identity precedes claim; one honest exact-`1.1.52` decision lands; QualifiedOnly and route-family boundaries survive; skill visibility is not inferred; exact-head review and named gates pass |
| Owned mutable paths | Qoder selection, prepared route, decoder, tests, and fixtures under `crates/swallowtail-adapter-qoder/**`; Research 318 and one research-index line; exact Qoder prepared-guide, route/activity/feature matrices, architecture ceiling, standing-lane, `[Unreleased]`, identity-log, claim-log, and task-result lines; `PAPERCUTS.md` append only |
| Reserved shared closeout surfaces | lifecycle task record and generated projections; submitted-handoff deletion belongs to the repository hook |
| Forbidden paths | other adapters; contracts; historical research/captures/releases; Qoder ACP, SDK, TUI, IDE, skills-list or skill-visibility implementation; live provider probes; release/tag/workflow files |
| Approved concurrent siblings | none on shared currentness surfaces; this campaign is serial |
| Worker capability class | evidence-first Rust/Node currentness worker; automated npm tree ledger and exact-pin evidence reconciliation; no provider credentials |
| Acceptance evidence | npm dist-tags/version set; 28-point package identities including baseline; exact per-hop inventory and mapped-surface ledger; mutation-sensitive selection/argv/decoder/lifecycle fixtures; current docs/claim agreement |
| Review oracle | skipped stable, inferred range, unclassified mapped delta, changelog-only inference, dispatcher/ACP/SDK/TUI flattening, lost permission/retention/deadline boundary, skill visibility inference, provider reliance, or unjustified public feature widening |
| Stop conditions | official-channel disagreement; selected public lifecycle/authority/authentication/permission/retention/failure change; formerly inert max-turns becomes selected authority; new driver/facade required; provider/live evidence required for base compatibility |
| Escalation owner | operator via Chatterbox for new policy; Queue coordinator for mechanical blockers |

## Boundaries

One route family and one exact-pin decision. No prompt, inference, login,
credential use, live print run, installation, host update, downloaded-artifact
execution, skill scan, ACP/SDK/TUI/IDE work, release, tag, publication, or
consumer mutation. Do not infer compatibility from semver continuity or infer
effective skill visibility from package membership. Beta is not stable
authority.

## Validation

- `cargo fmt -p swallowtail-adapter-qoder -- --check`
- `effigy validate:focused swallowtail-adapter-qoder`
- `effigy package:verify-affected swallowtail-adapter-qoder`
- `effigy check:examples`
- `effigy package:api`
- `effigy qa:routes`
- `effigy qa:northstar`
- focused Qoder identity, selection, argv, decoder, activity, and cleanup selectors
- relevant research/log/roadmap/number/lifecycle/next-action checks
- `git diff --check`

## Acceptance

- [ ] official identity and all 27 published successors reproduce
- [ ] every changed selected input is classified
- [ ] identity evidence precedes the claim decision
- [ ] exact QualifiedOnly shape and route boundaries remain truthful
- [ ] Research 256's skill-visibility stop remains independent
- [ ] official stable is rechecked at both boundaries

## Result

Not started.

## Next Task

Return the exact outcome to Chatterbox, then compile and dispatch Goose as the
next Research 308 family without asking Tom to repeat authorization.
