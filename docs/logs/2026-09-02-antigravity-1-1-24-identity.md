# 2026-09-02 Antigravity 1.1.24 Identity

## Result

Card 050 froze official GitHub `google-antigravity/antigravity-cli`
`1.1.24` against qualified `1.1.17`. Host `agy` was not installed.
Downloaded linux-x64 binaries were hashed and never executed. Selected
flags, exact `--conversation`, and `models` remain as in-binary literals
on every hop `1.1.17` through `1.1.24`. Public git is changelog-only.
Every hop ships one `antigravity` file; the binaries are distinct.
Segment shape for card 051 is a compatible extension of the existing
catalogue and stream-json revisions. `mic-serve`, `mcp`, and
`--input-format` stay unmapped. No provider prompt.

Next Task stays on the post-Codex all-route checkpoint. Northstar-loop
state was not changed.

## Next

Card 051 raises `antigravity-cli.release` through `1.1.24`.
