---
title: g05.045 Claude native mediation limitation reconciliation
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
status: ready-to-launch
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Tom answered 'Yes' on 2026-09-10 to land the Desktop negative evidence, correct universal native-tool mediation claims, and classify the exact SDK behavior as a provider limitation. This task is provider-free."
queue:
  dependsOn: [bc4acd98-91a3-4e14-86e0-38376b037da7]
  capability: general
  skipPRReview: false
  notifyOriginOnCloseout: true
---

## What This Thread Was Doing

Replace queued task `fcdb4a71-6651-4a2a-aec0-84f8a919f3f3`, whose immutable
handoff still sought a passing native multi-turn capsule. Reconcile g05.045 as
the exact provider-limitation result after Desktop g02.049 merges.

## Why It Matters

The final source-linked Desktop capsule proved native in-workspace `Read` may
complete without `canUseTool`. Swallowtail must not describe an answerable
permission callback as universal pre-tool mediation.

## Current State

Desktop PR #205 retains the default-mode mapping, check-only harness, and three
immutable capsules. Its final successor capsule has SHA-256
`4f23e55c548469ae61666052266497ae70b864566e0e3cc7de1c3a208a777dba`.
The queue dependency must reach `done` before this task dispatches. Desktop
g02.051 separately owns a provider-free registered-tool editing route.

## Boundaries

Follow `docs/roadmaps/g05/045-claude-sdk-consumer-multi-turn-editing-acceptance.md`.
Own Research 303 and index, one negative-evidence fixture/test, the exact
Claude integration guide and matrix-row notes, g05.029 follow-up, one log and
indexes. Preserve permission-exchange and registered-tool truths. Do not edit
runtime/sidecar/API behavior, matrix Yes/No values, versions or ranges,
Desktop, credentials, releases, or tags. Do not contact any provider.

## Important Context

Score absence literally: native `Read` produced a result with zero
`canUseTool` callbacks/decisions while the fixture stayed unchanged. This is
not the earlier proposal-versus-execution ambiguity. Record universal native
mediation as unavailable on the qualified tuple. Registered MCP calls remain
separately qualified through the Contract 063 before-dispatch bridge.

## Suggested Next Move

After Desktop closes, freeze its exact PR/review/merge/closeout and recompute
all capsule digests. Bind the final negative evidence provider-free, then
correct only the claims it disproves.

## Completion Protocol

Run `effigy validate:focused swallowtail-adapter-claude-agent`, `effigy
package:verify-affected swallowtail-adapter-claude-agent`, `effigy qa:docs`,
`effigy qa:northstar`, and `git diff --check`. Open one PR for independent
exact-head review. Return both repositories' identities, all capsule digests,
the exact limitation, validation, review, merge, and closeout. No provider
call, compatibility change, release, or tag follows.
