# 135 Cursor Agent 2026.08.04 And 2026.08.11 Identity

Status: promoted
Owner: Tom
Date: 2026-08-18
Card: g03 batch 244

## Question

After g03.077 qualified Oh My Pi through `17.3.7`, which AllowUnverified
family should move first, and are host Cursor `2026.08.04-aaa8809` and ACP
registry `2026.08.11-e8db854` compatible exact milestones of the July
`cursor-agent.release-date` claim, a new behavior revision, or a stop?

## Remaining AllowUnverified rank

Oh My Pi is done. Remaining host-drifted families, Research 127 numbers
unless noted:

| Rank | Family | Host | Qualified bound | Why this order |
| --- | --- | --- | --- | --- |
| 1 | Cursor Agent | `2026.08.04-aaa8809` | exact July points; no inferred gap | named next after Oh My Pi |
| 2 | OpenCode HTTP | `1.18.18` | through `1.18.10` | later family |
| 3 | Kimi ACP / headless / local-server | `0.34.0` | through `0.31.1` | later family |
| 4 | Ollama attached | `0.32.9` | `0.14.0..=0.32.1` excluding `0.32.2` | later family |

Host still on a qualified bound (registry newer only): Claude Agent ACP,
Pi, Qwen, Antigravity. Rank those after host-drifted families.

Gemini stays deferred. npm `cursor-agent@1.0.3` is a different axis.

## Method

Compared host `cursor-agent --version` / `--help`, prompt-free `acp`
initialize on the host binary, ACP registry Cursor `2026.08.11`, the
downloaded darwin-arm64 archive `2026.08.11-e8db854`, extracted
`--version` / `--help` / initialize, selected catalogue/ACP/headless
argv, and the production `cursor-agent.release-date` claim.

No provider prompt, authenticated catalogue, session creation, host
install, update, or claim edit in this research card.

## Identity

| Fact | Value |
| --- | --- |
| host CLI | `2026.08.04-aaa8809` |
| host runtime index SHA-256 | `65cb83494b6134b1b1c78139f24ac77d12943d3ba2e540d24e45eef17ee10bef` |
| host ACP chunk | `8869.index.js` `5c702c38a79ca379d19b6aaf7cf13545fc5b8d887842e90f02e3e942fb5da178` |
| ACP registry | Cursor `2026.08.11` |
| official archive | `https://downloads.cursor.com/lab/2026.08.11-e8db854/darwin/arm64/agent-cli-package.tar.gz` |
| official archive SHA-256 | `46044d6d7bcbd7b49a0cf1cd01aa4ca79aaa2ea5f2c7a32965fc0ebe29841790` |
| extracted CLI | `2026.08.11-e8db854` |
| extracted runtime index SHA-256 | `6aceb24b7c7ecddb1993946ebb18a7dd4d025842e6efda955eb0c13255b1e5f0` |
| extracted ACP chunk | `2996.index.js` `db023030987a1c0a54b2c7102e27faea657a4ad1320b2da153ed590ddd21eb39` |

Version parse still requires exactly `YYYY.MM.DD-` plus a 7-hex lowercase
build. Qualified dates still reject a mismatched build.

## Protocol comparison

Selected Swallowtail argv and commands remain documented on host and
official help:

- catalogue: `models`
- ACP: `acp`
- headless: `--print`, `--output-format stream-json`, `--model`, `--trust`,
  `--mode plan` for read-only

Prompt-free initialize on both binaries returned the same result:
protocol v1, `cursor_login`, advertised load/list, image prompt, HTTP/SSE
MCP, no `agentInfo`, zero stderr. Advertisement still grants no new
Swallowtail operation. Load remains blocked: both source bundles still
advertise load without a proven complete replay.

Unselected deltas remain unused: `--auto-review`, `worker`, `create-chat`,
`generate-rule`, `--force` / `--yolo`, `--sandbox`,
`--stream-partial-output`.

## Segment decision for card 245

Exact milestones. Same axis `cursor-agent.release-date`. Reuse the three
route behaviors. Keep AllowUnverified. Do not infer calendar dates.

Keep `2026-07-01`/`41b2de7` and `2026-07-23`/`e383d2b`. Add host
`2026-08-04`/`aaa8809` and official `2026-08-11`/`e8db854`. After
qualification, synthetic later-stable UnverifiedNewer is `2026-08-12`.
Dates between the four exact points stay incompatible.

No new public operation. No provider prompt. No host install or update.
