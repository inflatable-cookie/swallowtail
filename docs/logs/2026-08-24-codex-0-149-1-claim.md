# 2026-08-24 Codex 0.149.1 Claim

## Result

Card 152 raised `codex.cli` through exact `0.149.1` as a compatible
extension of the existing exec, app-server, lifecycle, and thread-catalogue
revisions. No new milestone. Later stables stay AllowUnverified; the
synthetic later point is now `0.149.2`. Gaps, exclusions, and prerelease
rejection are unchanged. `fork` / `thread/fork` / `--thread-source` /
Bedrock stay unmapped.

g04.054 is standing currentness, completed. Next Task stays on g04.053.

## Validation

- `cargo fmt -p swallowtail-adapter-codex` passed
- `scripts/validate-focused-packages.sh swallowtail-adapter-codex` passed
  (191 tests, clippy `-D warnings`)
- `scripts/verify-affected-packages.sh swallowtail-adapter-codex` failed
  when `$validation_repo_root` is `/workspace`: the archive audit `rg`s
  that path, and existing `tests/fixtures/activity/app-server.jsonl`
  already uses `/workspace` as fixture `cwd`/`path`. Not a new leak.
  Same script passed from `/tmp/st-verify/swallowtail` (`affected package
  proof passed`). Decoder/activity specimens were left alone.

No `effigy` binary on this host; the named scripts are the task
equivalents.

## Next

Resume g04.053 Qoder max turns.
