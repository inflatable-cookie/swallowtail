---
title: g05.044 Pi RPC 0.85.1 useful-newer qualification
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
status: ready-to-launch
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Tom stated on 2026-09-09 that Pi 0.84.4 -> 0.85.1 is an upgrade Swallowtail needs to support and asked this Chatterbox to take over because the Grok bot could not create a PR after Cursor credits were exhausted."
queue:
  capability: general
  skipPRReview: false
  notifyOriginOnCloseout: true
---

## What This Thread Was Doing

Run the operator-approved one-family Contract 029 currentness task for Pi RPC,
from qualified `0.84.4` through current official `0.85.1`. Freeze exact identity
before changing any claim, then qualify only the segment the evidence admits.

## Why It Matters

The installed Pi host and official stable have moved to `0.85.1`, while
Swallowtail's latest qualified RPC ceiling remains `0.84.4`. `AllowUnverified`
keeps the route usable, but it does not substitute for support evidence.

## Current State

- Canonical task: `docs/roadmaps/g05/044-pi-rpc-0-85-1-useful-newer.md`.
- Qualified axis: `pi.package`, baseline `0.80.10`, latest `0.84.4`, claim
  `pi.rpc.package-window-2`, posture `AllowUnverified`.
- Official npm/GitHub stable at planning: `0.85.1`; published hops after the
  ceiling are `0.85.0` and `0.85.1`; npm gitHead is
  `d981de1229ef899957bbe968bc8dcda02a21f477`.
- Host observation: `/opt/homebrew/bin/pi` reports `0.85.1`, SHA-256
  `e6d7fcf36a239cf3746e67ddf4222081ac01a601b85a3ee688bdfe9c161d754c`.
- The host was not installed, updated, or invoked beyond `--version`.
- `pi.sdk-sidecar` remains a separate QualifiedOnly exact `0.84.2` tuple.

## Boundaries

Follow g05.044 exactly. One Pi RPC family only. No provider prompt, login, live
RPC session, credential use, install, host change, downloaded-artifact
execution, sidecar widening, Oh My Pi work, Gemini work, release, tag,
publication, or consumer mutation. Preserve every historical segment, gap,
claim ID, and unrelated route. Do not infer compatibility from changelog text.

## Important Context

Use the repository `version-currentness` skill, its `reference.md`, Contract
029, Research 268, and the frozen Pi corpora. Compare both official hops and
derive a deterministic shipped-tree inventory when mapped inputs are not
byte-identical or the tree refactors. Identity must be its own commit before
any production claim edit. Record compatible extension, private milestone, or
stop honestly. Apply In-Run Latest Movement at both required rechecks.

The planning work reserves Research 302. If canonical pushed `main` has taken
that path before worker mutation, stop for Chatterbox to renumber; do not reuse
or overwrite another record.

## Suggested Next Move

Recheck official latest and repository ownership, retrieve the `0.84.4`,
`0.85.0`, and `0.85.1` official artifacts into `/tmp`, then build the exact
per-hop identity ledger before touching `selection.rs`.

## Completion Protocol

Meet every g05.044 acceptance row. Run its exact validation list, obtain an
independent exact-head review from a different provider/model identity, merge
through Northstar Queue, and synchronize `main`. Return exact npm/GitHub/host
identities, artifact and segment decision, identity and claim commit SHAs,
tests, review comment, PR/merge, canonical closeout, and the next currentness
state. No second family or release follows automatically.
