# kimi-local-server-2.1.1

Research 353 froze this corpus. No production claim moved.

`identity.json` carries official npm/GitHub identity for every published
stable after the Research 326 observation (`0.43.1`, `2.0.0`, `2.0.1`,
`2.0.2`, `2.1.0`, `2.1.1`) plus the `0.43.0` revalidation point. The
`0.43.0` tarball, `dist/main.mjs`, annotated tag, commit, and tree
reproduce Research 326. Official latest at probe is `2.1.1`. The installed
host stays `kimi 0.34.0` at the frozen digest, observed only through
`--version` and its digest.

`protocol.json` carries the selected-file git-blob ledger over
`0.43.0..=2.1.1`, the Bash `cwd` authority trace, the existing
`disabled_tools` control, and the four rulings this adaptation needs.
The uncontained `0.40.0` `resolve` blob is byte-identical through
`2.1.1`. Pinning `disabled_tools: ["Bash"]` can hide that tool; Contract
023 says a denylist does not contain the `AmbientHost` process.

No downloaded artifact was executed, installed, or authenticated. No
prompt, catalogue call, login, credential, host update, live session, or
local-server start occurred.
