# g05 Agent Runtime Surfaces And Route Truth — Archive Roll-up

Kind: roll-up
Status: archived
Generation: g05 (closed at 82 numbered tasks, operator rollover 2026-09-17; g06 active)
Canonical rollover task: g05.084 (folded into this roll-up and
[docs/logs/](../../logs/README.md) before its file was removed; preservation
manifest section in [preservation-manifest.md](preservation-manifest.md);
lifecycle closure in `.northstar/lifecycle/v1/generations/g05.closure.json`
and the terminal-record receipt `g05.json`)

## Intent and boundary

Give consuming applications dependable in-turn watcher control, bounded skill
inventory plus exact harness-effective visibility, lifecycle-aware feature
truth, and production routes that keep pace with qualified harness and agent
versions, while keeping distribution membership, host configuration, model
visibility, process authority, activity projection, turn completion, feature
lifecycle, and compatibility evidence distinct. Not a shell, daemon manager,
arbitrary process controller, ambient skill registry, or consumer UI; prompt
text is not enforcement. Boundary: 82 numbered tasks — 77 terminal (including
this rollover task) and five planned tasks carried forward to g06.

## Shipped outcomes

- **Skill and watcher vocabulary** — Research 255/256 censuses promoted into
  Contracts 058 (effective skill visibility) and 059 (watcher lifecycle);
  bounded installed/discoverable inventory split off into Contract 062.
- **Provider-neutral operation bridge** — Contract 060 and the host service,
  private authority, closed HTTP/MCP surface, terminal barrier, and joined
  cleanup; lossless watcher lifecycle delivery and panic-safe workspace
  cleanup repaired credential-free.
- **Honest watcher evidence** — the exact-Haiku live turns never produced a
  host-observed watcher; no exact isolation shape survives provider-free
  counterexamples; claims stay unpublished and mechanism change unplanned.
- **Consumer projection truth** — Contract 061 composes Contracts 037/047/057;
  all 767 census rows proved across every production route with honest empty
  deliver-now sets where no adapter facade exists; no production binding
  follows without an accepted gate.
- **Registered-tool capability** — Contract 063 and the shared
  registration/lease/dispatch kernel with Claude, Codex, and Grok route
  bindings; card 153's exact-tuple provider-free qualification; the Desktop
  card 318 four-control live acceptance on the frozen `0.3.259`/`2.1.259`
  tuple (Research 301).
- **Session lifecycle** — Contracts 019 amendments: caller-bounded close,
  reserved reapable-task runtime, exact-host/scope relinquishment, and the
  operator-accepted macOS root-only degraded cleanup posture.
- **Route currentness** — the standing Contract 029 lane plus the completed
  nineteen-family Research 308 campaign: Claude Agent ACP `0.76.0`, Qwen
  `0.23.3`, Codex `0.154.0`, Cursor `2026.09.10`, Ollama `0.34.0`, Grok Build
  ACP `1.0.30`, Claude Agent SDK `0.3.270`/native `2.1.270`, Grok catalogue
  `1.0.30`, Command Code `1.54.0`, Qoder `1.1.54` (adapter-owned eight-turn
  bound), Goose `1.50.1` (typed `auth_required` binding), Kiro `2.21.4`,
  Mistral Vibe `2.25.4`, Deep Agents `0.1.30`, Antigravity catalogue `1.2.2`
  with the `1.1.22` headless stop, Gemini `0.59.0`, Kimi headless `0.43.0`
  with the A2 ACP cap at `0.38.0`, Kimi local server `0.39.1` with
  `QualifiedOnly` fail-closing above, and Oh My Pi `18.1.22` on a distinct
  `18.x` revision.
- **Source-only releases** — annotated tags `v0.4.0` (`56f3913a`), `v0.4.1`
  (`c3cce750`), `v0.4.2` (`f94dd16f`), `v0.4.3` (`cbd4ddc8`), `v0.4.4`
  (`49c9e3b2`), `v0.5.0` (`582d01d6`), and `v0.5.1` (`e9140b46`); each
  created and pushed only on explicit operator exact-SHA authority; no
  publication.
- **Claude routes** — dual-route parity with the restored `claude-agent.sdk`
  sidecar on the reserved-reap seam (PR 196 at `493f8194`), the
  registered-only session profile, and the provider-limitation reconciliation
  of the unproven consumer multi-turn editing session.

## Current canonical destinations

- Effective skill visibility → docs/contracts/058-effective-harness-skill-visibility.md
- Watcher lifecycle → docs/contracts/059-operation-scoped-process-watchers.md
- Operation bridge → docs/contracts/060-operation-scoped-watcher-http-bridge.md
- Consumer projection → docs/contracts/061-consumer-route-feature-and-control-projection.md
- Skill inventory → docs/contracts/062-bounded-skill-inventory.md
- Registered tools → docs/contracts/063-registered-tool-operation-bridge.md
- Task lifecycle and reserved reap → docs/contracts/019-embedded-sdk-and-cloud-client-boundary.md (descendant-tree join and reapable-task amendments)
- Version qualification → docs/contracts/029-interface-version-qualification-and-compatibility.md
- Standing currentness lane → docs/roadmaps/standing-lanes.md
- Research evidence → docs/research/255-329 under the research index
- Shared-harness spec lineage → docs/specs/014-shared-harness-capability-and-producer-boundary.md
  and the carried-forward g06.001 task

## Migrations, compatibility limits, retained risks

- Watcher first-route claims remain withheld: the same-turn oracle was never
  proved live and prototype head `49f2692f` stayed unmerged; instruction
  delivery separate from ambient skill discovery still needs a planning lane.
- Kimi Code ACP stays `QualifiedOnly` at `0.38.0` (A2 cap; exact `0.39.0` /
  `0.39.1` excluded as recorded evidence; `0.40.0..=0.43.0` posture-rejected)
  with one artifact-level upstream reopen trigger; Kimi local server fails
  closed above `0.39.1` on the uncontained `cwd` widening; Antigravity
  headless holds the `1.1.22` provider-managed-retry stop with
  `1.1.18..=1.2.2` the named unqualified gap; Command Code live-derived
  evidence stays bound to `1.15.1`; Claude Agent SDK live evidence stays
  bound to `0.3.259`/`2.1.259` (`live_tuple_not_compiled`).
- g05.029's consumer multi-turn Claude SDK editing session closed as an
  honest evidence stop; native mediation limitation recorded provider-free.
- Release lanes g05.031/g05.034 stopped as honest evidence stops (CI latency
  and all-green targets unmet); audits merged with no follow-ups.
- Contract 061 Kimi candidate F stopped on the provider-operation observation
  decision; its corrected 89-row ledger remains reassessment evidence.

## Deferred or rehomed items with new destinations

- g05.035 shared harness capability and producer boundary → g06.001
  (planned; Batch A promoted; folded card evidence intact).
- g05.039/g05.040 Qoder skill visibility binding and acceptance → g06.002 /
  g06.003 (planned; gated on a non-empty Research 256 deliver-now
  disposition).
- g05.041 persistent permission grant admission → g06.004 (planned; operator
  promotion absent).
- g05.042 registered-tool adoption for remaining ACP routes → g06.005
  (planned; consumer requirement and operator direction absent).
- g05.066's follow-on — bind the Desktop runner to the rebound
  `0.3.270`/`2.1.270` tuple before any further live operation — stays held at
  the stopped parent's recorded typed setup stop (Research 329); it needs a
  future separately authorized consumer update, so it is recorded here rather
  than carried as an executable task. The claude-agent.sdk matrix cells cite
  the held real-route gate at g06.001.
- Gated follow-ons that stayed with their stopped parents: the Kimi local
  server follow-on claim (behind g05.026's gate), the Antigravity headless
  claim (behind g05.027's gate), and Contract 061's held Kimi gate (behind
  g05.009's task-owned decision).
- Standing currentness → docs/roadmaps/standing-lanes.md (not a generation
  runway goal; keeps no generation open).

## Selected evidence

- Contract promotions: Contracts 058-063 with Spec 012/013 archived; Contract
  061 realization compiled through g05.009 (PRs 131, 133, 138, 141, 144 and
  the breadth-audit series).
- Release lane: `v0.4.0` runs 33853812785/33870017023; `v0.5.0` runs
  34464717829/34467974791; `v0.5.1` runs 34731113171/34743711387; candidate
  and consumer proofs recorded per tag in docs/releases and docs/logs.
- Currentness campaign: PRs #321-#330 merged serially with lifecycle
  closeouts and exact-head reviews recorded per family; Research 308-328
  freeze identity and selected-source ledgers.
- Claude SDK lane: PR 196 at `493f8194` (restored route), PR 309 at
  `7736a358` (registered-only profile), PR #328 at `0c4f86ed` (tuple rebound),
  and g05.066's Research 329 typed setup stop.
- Census: the g05 generation-index census records "g05 closed at 82 numbered
  tasks … 77 terminal … five planned tasks carried forward to g06".

## Rollover record (g05.084)

The operator authorized the structural rollover on 2026-09-17 ("roll over and
compact"). g05.084 closed the generation: it wrote the preservation manifest
section, carried the five planned tasks to g06.001-g06.005, committed the
lifecycle closure record with the terminal-record tasks digest
`sha256:cfe29cbb46d182207cc1d60b2701301c1e27a0ccc8a093ab03976b96ee69754f`,
consumed the 26 terminal fragments into the g05 receipt, regenerated the
front-door projections for g06, rewired the docs index policy, and removed
`docs/roadmaps/g05/**` last. Documentation and planning surfaces only; no
product, release, tag, or consumer authority followed.

## Successor

g06 (sole active generation; carried-forward route truth and registered
capability; wider focus owned by Chatterbox and the operator).
