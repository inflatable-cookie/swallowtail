# Subagent Topology Route And Package Acceptance

Date: 2026-07-30

Card 160 closes provider-wide route truth and selected package proof.

## Matrix

The 57-row provider activity matrix now records:

- exact child observation fidelity
- exact parentage fidelity
- child-authored activity attribution
- visible provider-owned collaboration actions
- direct operator control

Four operation rows claim qualified topology:

- Codex app-server interactive session
- Codex exec structured run
- Kimi local-server interactive session
- Kimi local-server structured run

Zero rows claim direct operator control. Every other ordinary operation is
explicitly unavailable. Non-ordinary operations remain not applicable.

## Package Proof

Core, runtime, Codex, and Kimi archives assemble and audit independently. Each
archive then compiles as its own root through one shared target against local
unpublished Swallowtail dependencies. The proof completed in eight seconds.

The first interdependent-package run exposed a lock identity collision when
selected extracted packages and root-source development dependencies entered
one temporary workspace. The selector now retains independent extracted roots
and shares only compilation artifacts. This preserves archive evidence and
works for dependency-related selections.

Route QA, selector argument tests, public-API declarations, docs, and diff
hygiene pass. No provider call, consumer edit, candidate replacement, or
publication ran.

## Next

Card 161 publishes exact graph projection guidance and the Nucleus child-work
handoff.
