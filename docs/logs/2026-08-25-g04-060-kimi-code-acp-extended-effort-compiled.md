# 2026-08-25 g04.060 Kimi Code ACP Extended Effort Compiled

Status: compiled
Owner: Tom
Milestone: g04.060
Cards: 167-169

## What Changed

- reassessed the remaining promoted per-route feature inventory after g04.059
- selected Kimi Code ACP catalogue-declared `xhigh` and `max` effort levels
- compiled g04.060 and cards 167-169 as one serial evidence-first worker lane
- reserved Research 207 and the route-local closeout before dispatch
- kept g04 open and Contract 029 currentness standing

## Selection Basis

Swallowtail already negotiates Kimi ACP reasoning from one exact `thinking`
select option, sends one `session/set_config_option`, and requires the response
snapshot to confirm the effective value. The adapter currently treats any row
above `high` as malformed.

Exact official Kimi Code `0.38.0` source projects the current model's
`support_efforts` into that option. Exact source tests include `xhigh` and
`max`. This is a selected-transport control, not a UI label, model-name guess,
new route, or authority widening.

The version floor remains an evidence gate. The present maintained claim spans
`0.29.0..=0.38.0`; card 167 must find the first exact version with the full
extended-effort snapshot/set/confirmation path and split compatibility behavior
if required. Cards 168-169 continue only for Research 207's non-empty exact set.

## Reassessment Notes

Codex app-server plan-mode effort was not selected. The current route already
sends the chosen effort both as `turn/start.effort` and inside Plan
`collaborationMode.settings.reasoning_effort`; the inventory does not yet prove
a separate missing wire control. Codex verbosity still lacks exact model
support evidence. Ollama `think=max` remains withheld because exact `0.32.15`
maps it to `high` for the relevant family. Permission, multi-agent, skills,
memory, and sandbox families remain authority or containment gates.

## Validation Plan

Card 167 uses exact public source and secret-free fixtures only. If it promotes
a non-empty set, cards 168-169 run focused Kimi formatting, validation,
affected-package verification, examples, route/docs/API/index checks, doctor,
and diff checks. No Kimi install, OAuth mutation, login, provider prompt,
external inference request, or paid work is part of default QA.

## Next Move

Execute g04.060 cards 167-169 serially in one isolated worker worktree and open
one PR. Stop honestly after card 167 if Research 207 is empty or the exact
version/confirmation gate fails. Do not merge or close g04.
