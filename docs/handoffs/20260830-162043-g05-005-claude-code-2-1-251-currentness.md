---
title: g05.005 Claude Code 2.1.251 currentness worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Tom
created: 2026-08-30
updated: 2026-08-30
handoff_path: /Users/tom/Dev/projects/swallowtail/docs/handoffs/20260830-162043-g05-005-claude-code-2-1-251-currentness.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, rust, claude-code, currentness]
---

## What This Thread Was Doing

The provider-neutral watcher HTTP bridge landed through PR 120. The required
post-bridge reassessment found a circular gate: watcher card 010 depended on a
live same-turn proof that later card 011 could only run after card 010 existed.

The orchestrator separated the gates. g05.005 cards 017-018 now own one bounded
Contract 029 currentness lane for the existing Claude Code headless and
response-only routes. This worker qualifies exact `2.1.251` if the mapped
stream remains compatible. It does not wire or claim watcher support.

## Why It Matters

The installed host and npm `latest` are exact `2.1.251`, while both production
Claude Code route claims stop at `2.1.241`. Watcher binding should not be built
on an unqualified base route. Closing currentness first gives card 010 one
current exact route identity without pretending that base-route evidence proves
MCP, Stop-hook, skill, or same-turn watcher behavior.

## Current State

- **Repository:** `inflatable-cookie/swallowtail`
- **Planning branch:** `main`
- **Planning base commit:** `946ae36c6d28d4975153fa34e2556ac61b685ad8`
- **Pushed main verification:** local `HEAD` and `origin/main` both resolved to
  `946ae36c6d28d4975153fa34e2556ac61b685ad8` before this handoff was created
- **Planning checkout:** clean before the handoff commit
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight
- **Planning artifacts included at the base:** ready g05.005 and cards 017-018;
  revised watcher cards 010-011; post-bridge reassessment log; reconciled g05,
  generation, batch-card, log, and sole Next Task surfaces
- **Worker branch:** `worker/g05-005-claude-code-2-1-251-currentness`
- **Worker worktree:** `/Users/tom/Dev/worktrees/swallowtail-g05-005-claude-code-2-1-251-currentness`
- **Worktree creation command:** `git worktree add -b worker/g05-005-claude-code-2-1-251-currentness /Users/tom/Dev/worktrees/swallowtail-g05-005-claude-code-2-1-251-currentness origin/main`
- **Worker worktree policy:** first use the clean, dedicated, non-`main`
  registered worktree supplied by the launcher, even when its generated path
  or branch differs from the placeholders above. Do not create a second
  worktree for a naming difference. If that context is unusable, use the named
  worktree; only then read `.agents.local.env`, require
  `AGENTS_WORKTREE_CONTAINER_DIR`, and create a unique fallback there. Never
  use `/tmp` or a guessed worktree path
- **Required sibling worktree links:** none
- **Active spec lane:** none; Contract 029 and the standing currentness lane are
  canonical
- **Roadmap milestone:** `docs/roadmaps/g05/005-claude-code-2-1-251-useful-newer.md`
- **Ready cards, in order:**
  `docs/roadmaps/g05/batch-cards/017-claude-code-2-1-251-identity.md`, then
  `docs/roadmaps/g05/batch-cards/018-claude-code-2-1-251-claim.md`
- **Allowed runway:** identity card 017, then claim card 018 only if card 017
  admits compatible-extension or an already-governed private milestone
- **Remaining card budget:** two serial cards; one reviewable PR
- **Dispatch topology:** serial identity then claim
- **Parallel safety check:** both cards edit the same Claude adapter corpus,
  claims, guide, matrices, and indexes. They cannot run independently. Watcher
  cards 010-011 remain outside this worker
- **Canonical refs:** `docs/contracts/001-working-rules.md`,
  `docs/contracts/029-interface-version-qualification-and-compatibility.md`,
  `docs/guides/version-currentness-checkpoint.md`,
  `docs/roadmaps/standing-lanes.md`, and the repository
  `version-currentness` skill
- **Evidence refs:** `docs/research/202-claude-code-2-1-241-identity.md`,
  `docs/research/257-claude-code-watcher-seam-evidence.md`, and
  `docs/research/260-claude-code-watcher-bridge-transport.md`
- **Primary code surfaces:**
  `crates/swallowtail-adapter-claude-agent/src/claude_code_selection.rs`,
  `crates/swallowtail-adapter-claude-agent/src/claude_code_response_selection.rs`,
  adapter fixtures and tests, Claude prepared guide, current route/feature
  matrices, and standing-currentness closeout surfaces
- **Observed current point:** installed `/Users/tom/.local/bin/claude` reports
  `2.1.251 (Claude Code)` and SHA-256
  `625869b01e0050f260b2980fac248fd9cef9e462612bded4ec9d3d49ff8969a5`;
  npm `latest` for wrapper and Darwin native packages is `2.1.251`
- **Published points after `2.1.241`:** `2.1.242`, `2.1.243`, `2.1.245`,
  `2.1.246`, `2.1.247`, `2.1.248`, `2.1.250`, and `2.1.251`
- **Unpublished gaps:** `2.1.244` and `2.1.249`; keep both incompatible
- **Synthetic later point:** unpublished `2.1.252` remains
  `UnverifiedNewer` if compatible-extension lands
- **Model capability profile:** capable coding model with medium reasoning;
  exact artifact and compatibility judgment required
- **Tool/runtime restrictions:** no subagents, provider prompt, login,
  authentication, live session, live probe, install, host update, watcher
  wiring, watcher claim, workflow edit, release work, or merge. Official
  artifacts may be downloaded and extracted only under `/tmp`
- **Inherited health baseline:** doctor reports 384 god-file findings: 337
  warnings and 47 errors, plus one generated-in-source warning and a stale
  graph index. Do not widen this lane into structural cleanup
- **Required validation:** `cargo fmt -p swallowtail-adapter-claude-agent`;
  `effigy validate:focused swallowtail-adapter-claude-agent`;
  `effigy package:verify-affected swallowtail-adapter-claude-agent`;
  `effigy qa:routes`; `effigy qa:northstar`;
  `effigy qa:docs:index:research`; `effigy qa:docs:index:logs`;
  `effigy qa:docs:index:roadmaps`; `effigy qa:docs:index:roadmaps:g05`;
  `effigy qa:docs:index:roadmaps:batch-cards`;
  `effigy qa:docs:next-action:roadmaps`; `git diff --check`
- **PR base/head:** current pushed `main` / worker branch
- **PR URL:** pending
- **Review state:** awaiting implementation and exact head
- **Merge authorization:** not authorized

## Boundaries

- **In scope:** exact wrapper/native identity, published intermediate and gap
  census, selected mapped headless/response-only comparison, secret-free
  `2.1.251` corpus, Research 261, compatible claim extension if admitted,
  focused tests, current guide/matrix/changelog updates, currentness logs and
  indexes, and one PR.
- **Out of scope:** watcher MCP/settings/Stop/skill configuration, Contract 060
  binding, watcher capability advertisement, live same-turn proof, Claude
  Agent ACP, provider prompts, login, paid work, install, host mutation,
  maximum-turn or other feature-range widening without its own evidence,
  mapping unused additions, Gemini, skill cards 005-006, consumer feature
  projection, workflow/release changes, and merge.
- **Outcome shape:** complete the identity-to-claim currentness loop if and only
  if the selected mapped subset supports an admitted Contract 029 shape. If
  identity records stop or new-driver-or-facade, do not edit claims; return the
  identity evidence and exact blocker in the PR.
- Do not turn npm `latest`, host version, help membership, or watcher candidate
  flags into a compatibility claim by themselves. Corroborate official
  artifacts and compare the selected mapped behavior.
- Keep unpublished `2.1.244` and `2.1.249` as gaps. A semantic interval must
  exclude them explicitly rather than claiming artifacts that never existed.
- Keep watcher and feature-specific evidence narrower than base route
  currentness. Research 260 proves candidate flag parsing at exact `2.1.251`;
  it does not prove a live watcher segment.
- Work only in the selected clean worker worktree. Preserve unrelated state.
  Do not merge the PR.

## Important Context

- **Planning lineage:** g04.055 qualified both Claude Code stream-JSON axes
  through `2.1.241`. Research 260 later froze exact host/native `2.1.251` and
  watcher candidate flags without a prompt. PR 120 landed the provider-neutral
  bridge. The current task is only the base-route currentness prerequisite.
- **Why these cards are ready:** official and host identity agree at `2.1.251`;
  the family, axes, old ceiling, published points, gaps, evidence method,
  validation, and stop conditions are explicit. No provider access is needed.
- **Operator preference:** continue the Northstar manual worker/PR loop. Do not
  spawn internal agents. Docker and hostile-process containment are not part of
  watcher work, and no watcher wiring starts in this lane.
- **Open tension:** native package layout and help may have changed since
  `2.1.241`. Compare the selected mapped subset rather than forcing a compatible
  extension. New unmapped flags stay unmapped. If mapped framing or lifecycle
  changed, stop with the exact disposition.
- **Roadmap boundary:** g05.005 is standing currentness and does not displace the
  generation's post-currentness reassessment pointer. Close the milestone and
  cards, but do not mark watcher card 010 ready; the orchestrator does that
  after merge.
- **Report after:** card 017 identity/segment disposition, then card 018 final
  validation and PR closeout
- **Report to:** the operator, who will relay progress and the PR to the
  orchestrator

## Suggested Next Move

Start with the worker preflight. Re-read the repository `version-currentness`
skill and its reference, then cards 017-018 and the prior `2.1.241` corpus.
Re-probe npm `latest` before downloading anything. Extract official artifacts
under `/tmp`, compare only selected mapped behavior, and freeze Research 261.

If card 017 admits compatible-extension, auto-continue through card 018 in the
same worker and PR. If it does not, stop before any production claim edit and
return the evidence-backed disposition.

## Completion Protocol

### Before you start

1. Read this handoff. Its worker metadata activates implementation mode. Before
   broad reads, run `git rev-parse --show-toplevel`,
   `git branch --show-current`, `git status --porcelain`, and
   `git worktree list --porcelain`.
2. If the current root is a clean, registered, dedicated non-`main` worktree,
   accept it as launcher-provided. Record the actual root and branch. Do not
   create another because its names differ from this handoff.
3. If the launcher supplied a dirty or `main` worktree, stop and report it.
   Only for another unusable context, inspect the named worktree and then
   `.agents.local.env`; require `AGENTS_WORKTREE_CONTAINER_DIR` before a unique
   fallback. Never clean or reset another checkout and never use `/tmp` for a
   worktree.
4. In the selected worktree, fetch origin. Confirm `HEAD == origin/main`,
   confirm
   `git merge-base --is-ancestor 946ae36c6d28d4975153fa34e2556ac61b685ad8 HEAD`,
   and load the tracked handoff with
   `git show HEAD:docs/handoffs/20260830-162043-g05-005-claude-code-2-1-251-currentness.md`.
   If the absolute file differs, stop. The tracked copy is canonical.
5. Required sibling worktree links are `none`.
6. Read `AGENTS.md`, `PAPERCUTS.md`, the repository
   `version-currentness` skill and reference, g05.005, cards 017-018,
   Contract 029, the checkpoint guide, Research 202, Research 260, the current
   claim files, fixtures, guide, matrices, and Rust-quality profile before
   edits.
7. Run `effigy tasks`, `effigy doctor`, and `effigy test --plan`. Record the
   inherited baseline; do not execute the broad workspace plan.

### While you work

- Execute card 017 first. Re-probe official `latest`; if it moved, stop and
  report rather than retargeting silently.
- Keep downloaded/extracted official artifacts in `/tmp`. Never install or
  update the host. Return to the repository worktree before Effigy or Cargo.
- Compare selected mapped headless and response-only behavior. Do not map new
  flags, watcher surfaces, or provider features because they appear in help or
  changelog output.
- Write the identity fixture and Research 261 before any claim edit. If the
  segment is admitted, continue directly to card 018. Otherwise stop.
- Keep both unpublished gaps incompatible and feature-specific version sets
  independent from the base-route ceiling.
- Report the card 017 disposition through the operator, then finish card 018
  and final validation as one coherent second chunk.
- Do not spawn internal agents. The operator owns thread parallelism in their
  harness.
- Stop on a moved official point, artifact mismatch, mapped protocol/lifecycle
  change outside the existing revision, missing contract, or scope expansion.

### When the assigned runway is complete

1. Run every command in `## Current State` under Required validation. Do not
   substitute workspace QA, broad docs QA, live probes, MSRV, or consumer
   checks.
2. Mark cards 017-018 and g05.005 complete only if the claim lands. Write exact
   identity and claim logs; index Research 261 and the logs; reconcile g05,
   batch-card, generation-index, standing-lane, matrices, guide, changelog, and
   sole Next Task surfaces.
3. Keep the Next Task on orchestrator reassessment of watcher card 010 after
   this currentness PR lands. Do not mark card 010 ready, start watcher wiring,
   authorize card 011, or advertise watcher support.
4. Push the selected worker branch and open one reviewable PR against current
   pushed `main`.
5. In the PR body, link g05.005, cards 017-018, Contract 029, Research 261,
   identity/claim logs, fixtures, claim changes, guide/matrices, validation,
   and any stop disposition.
6. Report the PR URL and exact head to the operator. Do not merge.

### Review and merge path

The orchestrator will review the exact PR head against Contract 029, the
currentness skill, cards 017-018, official artifact evidence, corpus, claims,
docs, matrices, and hosted checks. With the shared GitHub identity, the
canonical verdict may be a PR comment rather than formal self-approval.
Requested changes are `none` at dispatch. Merge remains separately authorized
by the operator.

- **Closeout refs:** g05.005; cards 017-018; Research 261; Claude Code
  `2.1.251` identity and claim logs; currentness and roadmap front doors
- **Currentness:** this handoff is the one-family Claude Code run. Do not start
  a second family.

### Handoff closeout

Return one PR and stop. If the current official point moved, selected mapped
behavior changed, or identity cannot be corroborated, return the identity-only
evidence and exact blocker without changing production claims.
