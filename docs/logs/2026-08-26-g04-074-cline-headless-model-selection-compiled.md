# 2026-08-26 g04.074 Cline Headless Model Selection Compiled

Status: compiled
Owner: Tom
Milestone: g04.074
Cards: 204-206

## What Changed

- reconciled the stale 85-item inventory with the delivered programme after
  g04.073
- selected exact Cline headless model routing as the next dependency-shaped
  route-local lead
- compiled g04.074 and cards 204-206 as one serial evidence-first worker lane
- reserved Research 221 before dispatch
- kept g04 open and Contract 029 currentness standing

## Selection Basis

Exact qualified `cline@3.0.55` source parses `--model`, places explicit model
identity ahead of persisted and catalogue/default state, and carries the
resolved model into the one-run config. The production route already owns the
exact JSON child, optional Plan flag, read-only working resource, deadline,
events, cancellation, and joined cleanup. Model selection is also the named
dependency behind the earlier Cline thinking evidence stop.

The source exposes material gates. Provider identity resolves independently
from explicit argv, last-used settings, or `cline`. Model membership can come
from mutable provider resolution. The CLI attempts to persist the resolved
provider/model before the run. The current prepared route binds no model route,
exposes no catalogue, and uses `Ambient` configuration, which grants no
mutation authority.

Card 204 must therefore prove a closed provider/model pair, preflight-valid
agreement with the instance and access audience, fail-closed invalid behavior,
and configuration legality. An adapter-fixed provider argument is eligible
only if exact evidence derives it from existing route facts. Caller provider
selection, open model strings, live catalogue data, and new settings authority
stay out.

## Validation Plan

Card 204 uses official exact-tag/package source, existing secret-free fixtures,
and deterministic adapter tests only. It performs no install, login, account
inspection, catalogue request, provider prompt, configuration mutation, or
paid request. If Research 221 promotes a non-empty set, cards 205-206 run
focused Cline adapter validation, affected-package verification, examples,
API, Northstar, indexes, doctor, and diff checks.

## Next Move

Execute g04.074 cards 204-206 serially in one isolated worker worktree and open
one PR. Stop honestly after card 204 if Research 221 is empty or any provider,
membership, fallback, or configuration gate fails. Do not merge, roll the
generation, or close g04.
