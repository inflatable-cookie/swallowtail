# 050 Antigravity 1.1.24 Identity

Status: completed
Owner: Tom
Created: 2026-09-02
Updated: 2026-09-02
Milestone: `../021-antigravity-1-1-24-useful-newer.md`
Depends on: Contract 029; Research 274; official stable `1.1.24`

## Goal

Freeze exact official Antigravity `1.1.24` identity and classify its
selected catalogue and headless surfaces without changing a claim or
executing a downloaded binary.

## Scope

1. Recheck GitHub latest stable, published intermediates after `1.1.17`,
   tarball digests, extracted-binary digests, sizes, and ELF Build IDs.
2. Keep the host observation-only. Do not install or update it. Do not
   execute downloaded official binaries.
3. Compare the complete linux-x64 shipped tree `1.1.17` through `1.1.24`.
4. Classify changelog extras as unmapped unless they change a selected
   mapped flag.
5. Add Research 276 and one secret-free `1.1.24` identity/protocol/inventory
   corpus with a delta-ledger test.
6. Commit identity evidence before any selection, matrix, guide, or
   changelog claim edit.
7. Record compatible extension, private milestone, new revision, or stop.

## Out Of Scope

Production claim edits, another family, provider contact, login, install,
host update, live probe, Gemini deferral, Claude Code, Next Task, generation
pointer, execution of downloaded binaries, or broad workspace work.

## Acceptance Criteria

- official identity is corroborated through independent official channels
- mapped and material unmapped additions are explicit
- current production claims are byte-for-byte unchanged in this commit
- fixture provenance, digests, and negative boundaries are load-bearing
- card 051 continues only for an admitted segment

## Validation

- `cargo fmt -p swallowtail-adapter-antigravity -- --check`
- `effigy validate:focused swallowtail-adapter-antigravity`
- `effigy qa:northstar`
- `git diff --check`

## Auto-Continuation

Yes, to card 051 only after an admitted segment is recorded.

## Result

Official stable remained exact `1.1.24`. Host `agy` was not installed.
Research 276 and the frozen corpus landed in the identity-only commit;
production claims remained `1.1.9..=1.1.17`. Downloaded binaries were
hashed and never executed. Every hop ships one `antigravity` file. Selected
mapped flag literals stay. `mic-serve` stays unmapped. Already-mapped
print/catalogue hang and exit-code repairs are compatible-extension
fixes. The selected protocol classifies as a compatible extension, so
card 051 continued.
