# 2026-09-15 Kimi Code 0.43.0 Installed Claim

g05.077 requalified the Kimi Code installed routes through official npm and
GitHub `0.43.0` from Research 325 and moved only the headless ceiling.

`kimi.headless.executable-window-2` extends from maintained `0.33.0..=0.39.1`
to maintained `0.33.0..=0.43.0` on the unchanged
`kimi.headless.stream-json.v2` behavior revision with baseline `0.33.0` and
`AllowUnverified`. `KIMI_HEADLESS_LATEST_QUALIFIED_VERSION` becomes `0.43.0`.
The selected argv, the JSONL roles and meta types, the `system.version`
preamble, the `session.resume_hint`, the retry and tool records, the terminal
outcome, retention, cancellation, and cleanup are unchanged; only internal
dependency-injection, telemetry, shutdown, and prompt-submission mechanics
move. Unpublished `0.43.1` stays the visible `UnverifiedNewer` point.

`kimi.acp.executable-window-5` does not move. The g05.017 A2 containment
ruling stays binding: `acpTerminalRunner.ts` and the bundled `AcpProcessService`
are byte-identical at every point through `0.43.0`, the `terminal: false`
branch still spawns a local host process, and nothing in the adapter or runtime
contains it. The ceiling stays `0.38.0` with exclusions exactly `0.39.0` and
`0.39.1`; `0.40.0..=0.43.0` is the posture-rejected published gap and no new
exclusion is added. No new ACP behavior revision, public operation, or shared
type.

The `0.39.1` historical identity suite was updated to record its own frozen
destination while asserting the advanced live ceiling, and the
`0.36.1`/`0.38.0` windows, the local-server corpus, and the local-server
identity suite were repointed at `0.43.1` as the first later unverified-newer
point. `kimi-code.local-server` stays Research 282's separate family at
`0.38.0` and was not edited.

Official latest was rechecked at npm and GitHub `0.43.0` at the identity
boundary. No provider prompt, login, credential, installation, host update,
downloaded-binary execution, local-server start, release, tag, publication, or
consumer mutation occurred. Research 325, g05.077.
