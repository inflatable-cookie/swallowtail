# Kiro ACP 2.18.1 effort evidence corpus

Secret-free empty-set evidence for g04.089 card 254 / Research 251.

Official ACP docs for `kiro-cli acp` do not document `--effort`. Official
`--effort low|medium|high|xhigh|max` belongs to `kiro-cli chat`. Exact
`2.18.1` platform archives were not recoverable (HTTP 403) on 2026-08-28.

No live ACP initialize. No provider prompt. No install. No host `kiro-cli`
mutation. Do not promote chat/interactive effort onto `kiro.acp`.

No fixture contains a credential, host path, account identity, provider
payload, or real session id.
