# Cursor Discovery And Catalogue Foundation

Date: 2026-07-31

## Outcome

g03 card 011 adds the `swallowtail-adapter-cursor` package and the first
production Cursor route foundation:

- automatic candidate name `cursor-agent`, never generic `agent`
- exact host-approved target probing through joined `--version` execution
- calendar-date compatibility with a separate exact build-revision gate
- provider-owned local subscription access without credential extraction
- bounded `cursor-agent models` catalogue parsing
- stable, sanitized discovery diagnostics with non-zero exit status

The qualified installed point is `2026.07.01-41b2de7`. Later valid release
dates remain visible as unverified newer. Unknown builds on the qualified date
do not inherit support.

Catalogue output preserves ids, display names, and Cursor's default marker. It
does not infer provider identity, model invocability, or a model route.

## Evidence

Deterministic fixtures cover local-authoritative and remote-authoritative
hosts, opaque host-approved executable names, exact and newer releases,
joined cleanup, non-zero stderr sanitization, model parsing, immutable
preflight, and host mismatch rejection.

`effigy validate:focused swallowtail-adapter-cursor` passes 12 tests plus
warnings-denied clippy. No live prompt, session creation, model invocation,
account mutation, consumer edit, or broad workspace suite ran.

## Next

Finish active card 019, then execute card 012. Freeze exact Cursor ACP session
transcripts before enabling interactive session behavior.
