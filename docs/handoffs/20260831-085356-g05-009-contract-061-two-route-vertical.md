---
title: g05.009 Contract 061 two-route vertical worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-revise
owner: Tom
created: 2026-08-31
updated: 2026-08-31
handoff_path: /home/box/Dev/projects/swallowtail/docs/handoffs/20260831-085356-g05-009-contract-061-two-route-vertical.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr]
---

## What This Thread Was Doing

The orchestrator compiled Contract 061 realization planning from the reviewed
767-row census. The operator selected runtime-owned composition, immutable
adapter contributions, fixed library bounds, and a Codex plus OpenAI Realtime
first proof. Batch 9.1 then closed the shared baseline and one final Realtime
API fork: an additive adapter-owned prepared-open result preserves exact
`session.updated` truth while the existing `open_session` remains unchanged.

The same worker opened PR 131. Exact-head review found three planning gaps and
three execution misses. The operator accepted the complete review packet, the
canonical gate and card were repaired on pushed `main`, and this same handoff
now authorizes one revision on the existing worker branch and PR. No second
worker or replacement PR is authorized.

This dispatches card 022 only. No transcript or second prompt is part of the
authority chain.

## Why It Matters

Consumers need one bounded projection of exact route-feature and lifecycle
control truth without adapter downcasts or inferred provider state. The first
implementation must prove both a four-lifecycle route and an exact
acknowledgement route; the common kernel or either route alone is insufficient.

## Current State

- **Repository:** `/home/box/Dev/projects/swallowtail`
- **Planning branch:** `main`
- **Planning base commit:** `59029aaeafbf7db26f5bb8767ff98009bbc199cf`
- **Original implementation base:** `be98c30d682bea9ab01c5fa5e9af46e7180d4fbc`
- **Pushed main verification:** the repaired gate/card commit
  `59029aaeafbf7db26f5bb8767ff98009bbc199cf` was pushed; local `HEAD` and
  `origin/main` were verified equal before this revision authorization
- **Planning checkout:** `main`; clean after the repaired planning commit and
  before this handoff update
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight
- **Planning artifacts included at the revision base:** repaired Batch 9.1
  gate, repaired ready g05.009/card 022, and reconciled front doors
- **Worker branch:** `worker/g05-009-contract-061-two-route-vertical`
- **Worker worktree:** existing registered worktree at
  `/home/box/.herdr/worktrees/swallowtail/worker-g05-009-contract-061-two-route-vertical`
- **Worktree creation command:** none; reuse the existing clean PR worktree and
  branch; do not create another
- **Required sibling worktree links:** none
- **Active spec lane:** none; Contract 061 is active
- **Roadmap milestone:**
  `/home/box/Dev/projects/swallowtail/docs/roadmaps/g05/009-contract-061-consumer-projection-realization.md`
- **Ready cards, in order:**
  `/home/box/Dev/projects/swallowtail/docs/roadmaps/g05/batch-cards/022-contract-061-composer-and-two-route-vertical.md`
- **Allowed runway:** card 022's six accepted review repairs inside the same
  four-package runtime/testkit plus exact `codex.app-server` and
  `openai.realtime` 51-row tranche
- **Remaining card budget:** one card; one reviewable PR
- **Dispatch topology:** serial single-card lane
- **Parallel safety check:** no other worker is authorized; do not touch
  watcher, skill visibility, currentness, package expansion, PR 127, or parked
  work
- **Canonical refs:**
  `/home/box/Dev/projects/swallowtail/docs/contracts/001-working-rules.md`,
  `/home/box/Dev/projects/swallowtail/docs/contracts/037-prepared-consumer-integration.md`,
  `/home/box/Dev/projects/swallowtail/docs/contracts/047-configured-provider-instance-catalogue.md`,
  `/home/box/Dev/projects/swallowtail/docs/contracts/057-route-readiness-and-connection-admission.md`,
  and
  `/home/box/Dev/projects/swallowtail/docs/contracts/061-consumer-route-feature-and-control-projection.md`
- **Review oracle:** repaired card 022, repaired Batch 9.1 gate, exact census,
  and the measured 391/7/42/342 god-file baseline
- **Model capability profile:** Rust public-API and lifecycle-refactor capable;
  concrete worker selection remains operator-owned
- **Tool/runtime restrictions:** no provider contact, live probes, external
  research, new route, compatibility work, or runtime package outside the four
  named by card 022
- **Required validation:** the nine exact commands in repaired card 022; no
  live probe
- **PR base/head:** pushed `main` / existing
  `worker/g05-009-contract-061-two-route-vertical` branch; reviewed head
  `6f30a542ff69ded641158999c21192cad96f13a1`
- **PR URL:** `https://github.com/inflatable-cookie/swallowtail/pull/131`
- **Review state:** changes requested; repaired planning published; same-worker
  revision authorized by this handoff
- **Merge authorisation:** withheld until exact-head checks, orchestrator
  review, and a merge-authorized GitHub verdict; the worker never merges

## Boundaries

Please keep this run inside card 022:

- **In scope:** the runtime-owned Contract 061 composer, testkit conformance,
  nine named Codex prepared-facade contribution methods, the preserved and
  additive OpenAI Realtime open paths, and exact 51-row provider-free
  disposition proof, limited to the six accepted review repairs in the
  repaired gate/card; matrix-only and out-of-tranche rows remain withheld
- **Out of scope:** core or contract changes; another public Realtime route;
  the remaining 716 rows; package expansion; provider contact or live probes;
  watcher or skill-visibility work; generation closeout; PR 127
- **Outcome shape:** one coherent revision pushed to existing PR 131, then stop
  for the orchestrator's two-route checkpoint
- Do not invent architecture, change a fixed maximum or public signature, add
  a registry/callback/downcast/provider payload, or turn projection into
  execution or mutation authority.
- This handoff represents one worker lane. Stop if another lane creates shared
  mutable scope.
- Work only in the clean worker worktree selected by the Completion Protocol.
- Do not merge the PR.

## Important Context

- **Planning lineage:** Contract 061, the realization-readiness inventory, all
  four accepted option 1 decisions, g05.009, the repaired Batch 9.1 gate,
  repaired card 022, and the operator's accepted PR 131 review packet
- **Why the card is ready:** package direction, public names/signatures, source
  identity, fixed maxima, failures, replacement, 51-row coverage, fixtures,
  validation, and stops are all closed; the operator accepted the original
  Realtime seam and the complete PR 131 repair packet
- **Decisions and preferences:** preserve existing `open_session`; give
  projected-open separate prepared and active-observation source IDs; add
  `ConsumerMediatedPerTurn`; retain the five exact access-state dimensions;
  compare record/plan policy and access fail closed; withhold the two
  out-of-tranche Codex features at construction; split all nine new god-file
  findings and restore at most 391/7/42/342
- **Open tensions:** the implementation may discover that a selected public
  signature cannot satisfy a named contract invariant. That is a planning
  stop, not permission to revise the API or narrow the 51-row tranche.
- **Report after:** the complete six-item revision is pushed, all nine card
  checks are recorded, and PR 131 is ready for exact-head rereview
- **Report to:** the operator, who will relay progress to the orchestrator

## Suggested Next Move

Run the revision-specific Completion Protocol preflight before broad reads.
Then integrate pushed `origin/main` into the existing PR branch, verify this
tracked handoff, and read `AGENTS.md`, Contract 061, g05.009, the repaired Batch
9.1 gate, card 022, and the exact census. Revise the six accepted items as one
coherent chunk on PR 131.

## Completion Protocol

### Before you start

1. This handoff's `worker_mode: implementation` and
   `dispatch_authority: orchestrator` activate worker mode. Before broad reads,
   run `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. This is an existing-PR revision, so reuse only the clean registered
   `/home/box/.herdr/worktrees/swallowtail/worker-g05-009-contract-061-two-route-vertical`
   worktree on `worker/g05-009-contract-061-two-route-vertical`. If the launcher
   did not select that worktree and branch, or it is dirty, stop and report;
   do not create, clean, reset, or substitute another worktree.
3. Run `GIT_SSH_COMMAND="ssh -o ConnectTimeout=10 -o BatchMode=yes" git fetch origin`.
   Before integrating planning, confirm local `HEAD` equals
   `origin/worker/g05-009-contract-061-two-route-vertical` and that
   `6f30a542ff69ded641158999c21192cad96f13a1` is an ancestor. Confirm
   `59029aaeafbf7db26f5bb8767ff98009bbc199cf` is an ancestor of `origin/main`
   and this repository-relative handoff exists in `origin/main`.
4. Merge current `origin/main` into the existing worker branch with ordinary
   non-destructive Git. Resolve only conflicts inside card 022's existing
   closeout surfaces. Stop if the integration exposes another planning or
   scope conflict. Do not rebase, reset, force-push, or open a replacement PR.
5. Confirm `59029aaeafbf7db26f5bb8767ff98009bbc199cf` and the pre-revision PR head
   are ancestors of the integrated `HEAD`. Load this handoff with `git show
   HEAD:docs/handoffs/20260831-085356-g05-009-contract-061-two-route-vertical.md`.
   If the absolute file differs, stop. The tracked copy is canonical.
6. Required sibling links are `none`.
7. Read the active milestone, repaired card, `AGENTS.md`, and canonical refs,
   then run the repository's cheap orientation checks and record them.

### While you work

- Execute card 022 only and keep commits aligned with coherent code and proof
  chunks.
- Preserve the existing public Realtime open method and make both methods
  delegate to one private lifecycle.
- Implement all six accepted repairs together: distinct Realtime sources,
  consumer-mediated per-turn authority, exact access applicability,
  fail-closed cross-access agreement, construction-time Codex withholding,
  and god-file splitting back to the measured baseline.
- Report the named meaningful chunks through the operator with changed files,
  validation run, remaining work, risks, and blockers.
- Stop if a contract is missing, intent becomes ambiguous, scope expands, a
  selected signature or bound cannot hold, or validation changes the plan.
- Do not quietly turn an implementation problem into new architecture.

### When the assigned runway is complete

1. Run every validation command named by card 022. Do not run a live probe.
2. Try to falsify the diff against the card: map every fixed bound and failure
   kind to portable proof, every exact census row to one adapter fixture, and
   every Realtime state to exact acknowledgement evidence. Prove there is no
   claim for the remaining 716 rows.
3. Reconcile card, milestone, batch-card index, g05/generation indexes, and the
   sole Next Task. Stop and return any new product threshold, contract choice,
   or acceptance rule to planning.
4. Push the existing worker branch normally and update PR 131 against current
   pushed `main`. Do not force-push or open a replacement PR.
5. Update the PR body with the repaired gate/card, changed surfaces, exact
   51-row evidence, validation, and the restored god-file counts.
6. Report PR 131's new exact head to the operator. Do not merge.

### Review and merge path

The orchestrator reviews the exact PR head against Contract 061, card 022, the
Batch 9.1 gate, census, diff, and checks. If the shared GitHub identity prevents
formal approval, the orchestrator posts the verdict as a PR comment. A
`planning-change` returns to planning before revision. The accepted planning
changes are now published; no further architecture or scope is authorized.
Helm merges only after a new merge-authorized GitHub verdict following checks
and exact-head rereview.

- **Requested changes:** separate prepared/active Realtime source IDs; add
  `ConsumerMediatedPerTurn`; expose exact access states; fail closed across
  configured policy and access/readiness; withhold the two out-of-tranche Codex
  features before emission; split all nine new god-file findings and restore at
  most 391 total / 7 critical / 42 high / 342 warning
- **Closeout refs:** card 022, g05.009, the batch-card and generation indexes,
  and the sole roadmap Next Task

### Handoff closeout

Leave card, milestone, and Next Task honest. If the repaired 51-row tranche
cannot stay inside the selected signatures, fixed baseline, and Contract 061,
record the blocker and stop rather than narrowing or widening the card. PR 131
remains unmerged until a later operator-authorized verdict.
