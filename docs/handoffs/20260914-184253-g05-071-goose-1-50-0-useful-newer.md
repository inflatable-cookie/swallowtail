---
title: g05.071 Goose 1.50.0 useful-newer qualification
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
status: ready-to-launch
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Tom instructed Chatterbox to action the full Research 308 currentness campaign, including exact-pin families, Kimi and Oh My Pi, and continue without repeated approval; g05.070 is closed and Goose is next."
roadmap: docs/roadmaps/g05/071-goose-1-50-0-useful-newer.md
queue:
  capability: general
  skipPRReview: false
  notifyOriginOnCloseout: true
---

# g05.071 Goose 1.50.0 Useful Newer

## What This Thread Was Doing

Chatterbox closed Qoder's honest typed stop and compiled the eleventh Research
308 family. Goose remains exact-qualified at GitHub `v1.46.0`; official stable
is `v1.50.0`, four published stable releases later.

## Why It Matters

The exact old pin rejects the current official harness. Compatibility cannot
be inferred from adjacent tags or ACP crate semver: every changed source input
feeding the selected stdio route must be classified without flattening Goose's
serve, desktop, builtin, provider, or advertised session-operation surfaces.

## Current State

- Canonical `main` is g05.070 closeout
  `20dd62a014b7fb22c694cf2d4d0100539c81ce24`.
- PR #331 merged accepted head
  `c8551d1f3a72c75b95001ffcf79a51a830e57f4c` as
  `bd26f573e980e835b7bfd1e283402147ae3c30c3`; review comment
  `5668136570`.
- Qoder's point stayed exact `1.1.25` because `--max-turns 8` becomes the real
  AgentLoop ceiling at `1.1.30`; that separate operator decision does not block
  this campaign.
- GitHub latest stable is `v1.50.0`, published `2026-09-08T19:32:41Z`, tag
  commit `4cc49a8485f7960efeceba37ac5f572459c52c7c`.
- Stable successors are `v1.47.0` (`f9c7aacc`), `v1.48.0` (`25021517`),
  `v1.49.0` (`71fc4be1`), and `v1.50.0` (`4cc49a84`). `goose` is absent
  locally.
- Current claim is exact `1.46.0`, `QualifiedOnly`, behavior
  `goose.acp.stdio-v1`.

## Boundaries

One route family and one exact-pin decision. No prompt, inference, configure/
login, credentials, live ACP session, installation, host update, downloaded-
artifact execution, serve/desktop/TUI/provider work, release, tag, publication,
or consumer mutation. Provider-free work may rerun normally. Do not infer a
range or public operation from advertised capability.

## Important Context

Read Contract 029, Research 148, 250, 253, 308, and 318, g05.071, the Goose
prepared guide, `selection.rs`, and all Goose ACP corpora before editing. Use
the repository `version-currentness` skill and Effigy selectors. Recheck
official latest at both boundaries. Retrieve exact tagged source trees for
`v1.46.0..=v1.50.0` to `/tmp`; freeze Research 319 and a mutation-sensitive
per-hop source-tree and mapped-module ledger before claim edits.

Classify CLI dispatch, builtin/scheduler defaults, initialize/capabilities,
session new/prompt/cancel, content, stop reasons, updates, permission callbacks,
modes, provider/model failure, correlation, cwd/resource authority, deadlines,
process/task ownership, and cleanup. Keep serve, desktop/TUI, recipes,
ACP-provider adapters, client MCP, and advertised load/list/close independent.
On a compatible provider-free result, move the one exact point to `1.50.0`;
otherwise land the smallest typed stop.

Oracle gate: not required. Contract 029 and Research 148/250/253 settle this
mechanical comparison and its stop oracle; the worker receives no new auth,
permission, secret, or execution-policy decision.

## Suggested Next Move

Build the five-tag identity and per-hop tree/module ledger, commit identity,
then make the smallest honest exact-pin decision. Validate, push, open a PR,
and hand the exact head to independent review.

## Completion Protocol

The Queue owns worker launch, independent review, merge, lifecycle closeout,
and handoff deletion. Return official/tag/asset identities, complete hop set,
Research 319 path, exact claim/behavior result, validation, review marker/
comment, merge SHA, and closeout SHA. Next is Kiro. No release or tag follows.
