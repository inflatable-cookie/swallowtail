# 2026-09-14 Kiro ACP 2.21.4 Claim

g05.072 advanced the exact `kiro-cli.release` point from `2.18.1` to
official stable-manifest `2.21.4` after Research 320 froze all eleven
published stable successors as exact official archives and classified the
complete selected ACP surface. The `kiro.acp.release-window-1` claim remains
one exact `QualifiedOnly` maintained point with the unchanged
`kiro.acp.stdio-v1` behavior revision. No range, second exact point, or
unverified-newer posture was added; `2.21.5` is not published.

Every selected wire and lifecycle input is byte-stable across the chain:
initialize capability shape, `session/new` cwd authority, the `prompt`
field (the official docs `content` example is still stale at `2.21.4`),
cancel, session-update kinds, permission kinds with `allow_always`
unselected, stop reasons, and joined cleanup. The `_kiro.dev/*` extension
set and the advertised `session/load` stay unmapped additions with named
non-effect reasons. Research 251 and 254 keep their empty deliver-now sets;
their package/source gates are now closed by per-hop binary evidence. The
frozen `kiro-acp-2.18.1` decoder corpus still covers the selected wire, so
its fixtures stand unchanged. No downloaded artifact was executed, no ACP
message was sent, and no install, login, credential use, or host mutation
occurred.
