# Handoff — g05 Card 055 Claude Agent SDK Foundation

## Objective

Implement card 055 exactly: the provider-free `claude-agent.sdk` foundation
using the official TypeScript SDK through a bounded Node sidecar. Open one PR;
do not merge it.

## Base And Authority

- planning base: `997002bb38c11d7f54ef71b8a5c6ce351b2d7d21`
- card: `docs/roadmaps/g05/batch-cards/055-claude-agent-sdk-provider-free-foundation.md`
- evidence: Research 278 and the SDK contract gate
- contracts: 019 and 029 at the planning base
- user authorized this implementation lane, but not provider contact, login,
  release preparation, tag creation, or push of a tag

Verify the exact handoff blob and that the planning base is an ancestor before
editing. Work from current pushed `main`. Preserve unrelated changes.

## Worker Class

Frontier implementation worker. This is justified by both required conditions:
exceptional post-planning reasoning remains in the cross-language private wire,
credential non-custody, and descendant process-tree join; and it is the highest
priority/material release blocker because v0.4.0 evidence cannot restart until
the Claude foundation reaches an accepted boundary.

## Required Shape

- Re-read card 055, Research 278, the SDK gate, Contracts 010/017/019/029/041,
  and the Pi SDK sidecar precedent before editing.
- Recheck the official Anthropic subscription article and npm stable first.
  Stop if policy changed or `0.3.258` moved; do not silently retarget.
- Prefer the existing host-owned POSIX process-group / Windows descendant-tree
  authority, combined with an independently joinable native-child handle in
  the sidecar. Prove the exact supported-platform behavior; do not assert it.
- Keep SDK-only types and wire private. Use the `.` entry only. No token value,
  `/bridge`, `/browser`, API-key helper, ambient settings, Bash, or terminal.
- Use provider-free fake sidecar/native children. Do not execute the downloaded
  official binary or open an authenticated session.
- If a public API is genuinely required, make it the smallest additive surface
  and call it out. Do not invent shared vocabulary where existing contracts fit.
- Keep modules below the god-file threshold. Apply the Northstar everyday Rust
  rules and run the card's exact validation after one coherent batch.

## Parallel And Serial Edge

Card 056 runs in parallel and lands first. It owns ACP model parsing/session
retention/projected-open contribution files. Avoid those files where possible.
Shared adapter manifest, `lib.rs`, route matrix, guide, changelog, milestone,
card index, and log index are expected restack surfaces. Do not merge; after
card 056 lands, restack and reconcile without dropping either route.

## Closeout

Return exact base/head, PR, changed surfaces, route/version identities, sidecar
wire, credential negative proof, descendant-tree lifecycle proof, test counts,
public API/god-file results, policy/latest recheck, residuals, and next move.
Stop for frontier exact-head review.
