# 2026-08-18 Copilot CLI ACP 1.0.80 Identity

## Result

Card 270 froze official GitHub Copilot CLI ACP identity at npm
`@github/copilot@1.0.80` without installing, logging in, or sending
`initialize`. The selected wire is public-preview `copilot --acp --stdio`:
initialize, `session/new`, one bounded `session/prompt`, cancel, and
joined cleanup. TCP `--port`, `--yolo`, `--allow-all`, server-start
tool/effort flags, interactive-only slash commands, GitHub login, and
prerelease `1.0.81-*` stay out. Initialize result is unrecovered from
public source. Named fixtures live under the future adapter tree. No
production claim. Current source stays 34 packages and 42 routes.

## Next

Implement the GitHub Copilot CLI ACP driver core (card 271).
