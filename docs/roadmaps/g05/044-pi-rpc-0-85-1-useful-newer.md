# g05.044 Pi RPC 0.85.1 Useful Newer

Status: ready; operator named Pi RPC `0.85.1` for support
Owner: Tom
Created: 2026-09-09
Updated: 2026-09-09
Depends on: Contract 029; qualified Pi RPC `0.84.4`; Research 268; g05.015
Vision tags: route currentness, Pi RPC, compatibility

## Outcome

Qualify current official npm/GitHub `@earendil-works/pi-coding-agent`
`0.85.1` for the exact `pi.package` RPC axis if deterministic artifact evidence
admits it. Preserve identity-before-claim, every historical segment and gap,
the separate `pi.sdk-sidecar` exact `0.84.2` pin, and `AllowUnverified`.

Official npm and GitHub stable were both `0.85.1` at planning. Published hops
after the qualified `0.84.4` ceiling are exactly `0.85.0` and `0.85.1`. The
installed host reports `0.85.1`; that is observation input, not qualification
or install authority.

## Ready-State Rubric

- [x] The operator named `0.84.4` -> `0.85.1` as support work.
- [x] npm and GitHub agree on official stable `0.85.1`; npm gitHead is
      `d981de1229ef899957bbe968bc8dcda02a21f477`.
- [x] Current `pi.rpc` is `AllowUnverified` through qualified `0.84.4` on
      `pi.rpc.strict-lf-v0.84.0-message-update-delta`.
- [x] Exact hops, host observation, boundaries, evidence, validation, and stops
      are explicit.
- [x] No provider call, host change, sidecar widening, release, or consumer
      mutation is required.

## Work

1. Re-probe npm and GitHub stable, observe the existing host safely, and freeze
   Research 302 plus a secret-free `0.85.1` identity corpus. Compare official
   `0.84.4` -> `0.85.0` -> `0.85.1` artifacts and every shipped file feeding
   mapped RPC wire shape, framing, lifecycle, failure, usage, capability,
   session cwd, configuration, and argv behavior. Use a deterministic full
   tree inventory whenever mapped files differ or the package tree refactors.
2. Commit identity evidence before any production claim edit. Name exactly one
   Contract 029 outcome: compatible extension, adapter-private milestone, new
   driver/facade stop, or evidence stop. Keep changelog claims subordinate to
   the shipped artifacts.
3. Only for an admitted segment, update `selection.rs`, mutation-sensitive
   tests, the Pi prepared guide, exact Pi route/feature-matrix version truth,
   `[Unreleased]`, standing-lane state, and one claim log in a second commit.
   Preserve all old gaps and claim IDs unless the evidence requires a private
   milestone. Keep `pi.sdk-sidecar` exact `0.84.2`.
4. Re-probe official latest immediately before the identity commit and final
   push. Apply Contract 029's In-Run Latest Movement rule without weakening the
   identity-before-claim boundary. A stable first observed after the identity
   commit remains `UnverifiedNewer` and does not reopen this claim.

## Dispatch Manifest

| Field | Task 044 |
| --- | --- |
| Readiness | ready |
| Prerequisites | Contract 029; Research 268 and frozen Pi `0.84.4` evidence; clean pushed `main`; npm/GitHub consensus on `0.85.1` |
| Completion conditions | Research 302 and a secret-free corpus freeze exact `0.84.4` -> `0.85.0` -> `0.85.1` identity; identity is committed before any claim edit; one segment outcome is recorded; an admitted outcome raises only the proved `pi.rpc` claim and current downstream truth; a stopped outcome leaves claims unchanged; exact-head review and named validation pass |
| Owned mutable paths | `crates/swallowtail-adapter-pi/src/selection.rs`; Pi selection and identity tests/fixtures under `crates/swallowtail-adapter-pi/tests/**`; `docs/research/302-*.md` and one research-index line; `docs/guides/pi-rpc-prepared-integration.md`; exact Pi rows/cells in `docs/guides/provider-route-matrix.md` and `provider-solution-feature-matrix.csv`; `CHANGELOG.md` `[Unreleased]`; one new Pi identity log and, if admitted, one Pi claim log plus their index lines; this task's result/status; the Pi currentness paragraph in `docs/roadmaps/standing-lanes.md`; `PAPERCUTS.md` append only |
| Reserved shared closeout surfaces | `docs/roadmaps/README.md`, `docs/roadmaps/g05/README.md`, `docs/roadmaps/generation-index.md`, and the rest of `docs/roadmaps/standing-lanes.md`; queue coordinator edits these at closeout |
| Forbidden paths | `crates/swallowtail-adapter-pi/src/sidecar/**`; existing sidecar fixtures and `pi.sdk-sidecar` pins; every other adapter; contracts; historical research/release notes; provider-wide activity fixture unless a named route gate proves it stale |
| Approved concurrent siblings | g05.034, g05.031, and g05.029 only when queue serialization protects shared closeout surfaces; implementation paths are disjoint |
| Worker capability class | evidence-first Rust currentness worker; npm/GitHub artifact retrieval and deterministic tree comparison; no provider credentials |
| Acceptance evidence | exact npm metadata/tarballs, GitHub tag/release identity, host version/digest observation, per-hop artifact inventory and mapped-surface classification, mutation-sensitive fixtures/tests, and exact current-doc agreement |
| Review oracle | the smallest counterexample is an uninspected changed mapped file, a changelog-only compatibility claim, a lost historical gap, sidecar widening, a range beyond the admitted segment, or current docs disagreeing with `selection.rs` |
| Stop conditions | official-channel disagreement; major-line reset; mapped public lifecycle/capability/authority change; unclassified mapped wire/failure delta; new driver/facade required; provider/live evidence required |
| Escalation owner | operator via Chatterbox for policy/authority; queue coordinator for mechanical blockers |

## Boundaries

One family only. No prompt, login, live RPC session, provider contact, package
install, host update, sidecar package/source/Node/wire change, Oh My Pi work,
Gemini deferral lift, release, tag, publication, or consumer mutation. Do not
execute downloaded official artifacts.

## Validation

- `cargo fmt -p swallowtail-adapter-pi -- --check`
- `effigy validate:focused swallowtail-adapter-pi`
- `effigy package:verify-affected swallowtail-adapter-pi`
- `effigy check:examples`
- `effigy package:api`
- `effigy qa:routes`
- `effigy qa:northstar`
- research, logs, roadmaps, g05, roadmap-number, status, and next-action checks
- `git diff --check`

## Acceptance

- [ ] official identity is reproducible for both published hops
- [ ] every changed shipped file feeding selected behavior is classified
- [ ] identity evidence lands before any production claim edit
- [ ] only the admitted segment changes the claim; a stop changes no claim
- [ ] historical segments and gaps, claim ID, `AllowUnverified`, and unrelated
      Pi surfaces survive
- [ ] `pi.sdk-sidecar` remains exact `0.84.2`
- [ ] official latest is rechecked at both required boundaries
