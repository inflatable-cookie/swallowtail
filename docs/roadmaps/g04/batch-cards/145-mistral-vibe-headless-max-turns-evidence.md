# 145 Mistral Vibe Headless Maximum-Turn Evidence

Status: complete
Owner: Tom
Created: 2026-08-23
Updated: 2026-08-23
Milestone: [g04.052 Mistral Vibe Headless Maximum Turns](../052-mistral-vibe-headless-max-turns.md)
Depends on: Research 150; exact route `mistral-vibe.headless` `2.24.2`

## Goal

Freeze exact current official and Mistral Vibe `2.24.2` maximum-turn behavior,
then define the smallest caller-decreasing positive subset whose dispatch,
counting, limit terminal, and cleanup Swallowtail can represent exactly.

## Method

1. Fetch the current official README/CLI documentation and exact official
   `v2.24.2` source. Record retrieval dates, page/source identity, stable URLs,
   complete specimen digests, and decisive parser, middleware, agent-loop, and
   programmatic-terminal paths.
2. Freeze current `mistral-vibe.headless` command construction, prepared input,
   immutable plan/evidence, driver agreement, streaming decoder, terminal,
   cancellation, deadline, failure, and cleanup behavior. Do not install Vibe
   or run a provider prompt.
3. Classify caller omission, candidates `1..=8`, zero, negative, fractional,
   overflow, and values above eight. Separately classify upstream omission of
   `--max-turns`. Do not normalize argparse acceptance into the public API.
4. Establish the exact turn definition, counter source, increment point,
   pre-turn check, off-by-one behavior, and one-child reset/lifetime. Keep
   assistant turns, model/tool cycles, tool calls, retries, compaction, and
   Swallowtail operation turns distinct.
5. Establish the exact at-limit and over-limit observable: process exit, stderr,
   streaming records, last/partial assistant or tool events, portable terminal
   classification, failure diagnostic, cancellation, deadline, and joined
   cleanup. Do not infer successful completion from earlier output.
6. Decide whether zero or negative values have any useful exact route meaning.
   The expected public candidate remains positive `1..=8`; deviations require
   explicit evidence and roadmap review before production binding.
7. Decide the exact typed adapter-local input and immutable plan/evidence
   carrier needed for any admitted subset. Do not add production code or a
   shared capability on this card.
8. Decide whether exact `2.24.2` needs a feature-local behavior/evidence
   revision while leaving the Contract 029 exact release membership and
   qualified-only posture unchanged.
9. Replace Research 199's reservation with source-backed domain, counting,
   terminal, and lifecycle dispositions plus a deliver-now table or honest
   empty set. Do not edit shared closeout surfaces.

Current public documentation and secret-free exact-release/repository evidence
are authorized. No login, credential/account inspection, setup, catalogue,
provider request, prompt, paid work, package installation, executable archive
extraction, or host configuration change is authorized.

## Acceptance Criteria

- [x] exact official and `2.24.2` evidence is frozen with source identity and
      complete specimen digests
- [x] upstream parser domain and Swallowtail caller-decreasing domain are distinct
- [x] omission, `1..=8`, zero, negative, fractional, raised, overflow, and
      unbounded states have explicit dispositions
- [x] turn definition, check/increment order, off-by-one boundary, and
      child-local lifetime are explicit
- [x] process exit, stderr, stream, terminal, partial-event, cancellation,
      deadline, failure, and cleanup truth is explicit or withheld
- [x] plan/evidence representation and feature-local revision are explicit
- [x] Research 199 is promoted with a deliver-now table or honest empty set
- [x] no production code, shared capability, matrix, contract, currentness, or
      sibling-route change lands
- [x] `effigy validate:focused swallowtail-adapter-mistral-vibe` passes
- [x] `effigy qa:northstar` and `effigy qa:docs:index:research` pass
- [x] `git diff --check` passes

Auto-continue to card 146 only when Research 199 admits a non-empty exact
value/profile set with truthful counter and terminal semantics.

## Stop Conditions

- exact counting, enforcement, terminal, or partial-event behavior needs live
  provider work
- a candidate cannot be represented without implying successful completion or
  effective work
- selection needs a shared capability, contract/currentness change, sibling
  route, ambient setting, synthetic config root, or breaking API
- output mode, agent, trust, workdir, access, deadline, cancellation, failure,
  or cleanup would change

## Out Of Scope

- production binding, shared closeout, another Vibe route/version/control,
  live work, release, publication, or merge
