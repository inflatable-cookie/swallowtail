# g05.072 Kiro 2.21.4 Useful Newer

Owner: Tom
Created: 2026-09-14
Depends on: Contract 029; Research 156, 251, 254, 308, and 319; g05.071
Vision tags: route currentness, Kiro, ACP, exact pin

## Outcome

Reopen the exact `kiro-cli.release` pin and qualify official stable-manifest
`2.21.4` for `kiro.acp` only if complete provider-free artifact, executable,
CLI, ACP wire, authority, and lifecycle evidence admits it. Preserve
`QualifiedOnly` and one exact maintained point. Do not infer a range from the
moving manifest or changelog families.

Planning rechecked the official stable manifest on 2026-09-14. It reports
`2.21.4`; the universal macOS DMG digest is
`954a48e8a5796507c6733c0b67e1b4a3fea97a4b24a3bdb37c43a4e9da2f95b7`
and the Linux aarch64 headless archive digest is
`f582eac0e002b4d11bbd061d41fcb49d1d37626a2c1fe230373fcbf97755df6f`.
The official changelog names `2.19.0`, `2.20.0`, and the `2.21.x` family after
the exact `2.18.1` baseline. `kiro-cli` remains absent from this host.

## Ready-State Rubric

- [x] Tom authorized the complete Research 308 currentness campaign, including
      reopening exact-pin families.
- [x] g05.071 completed as an honest typed stop and its lifecycle closeout is
      on canonical `main`.
- [x] Official stable-manifest `2.21.4`, its selected platform artifacts, the
      intervening changelog families, and absent host CLI are identified.
- [x] Research 156 fixes the selected ACP wire; Research 251 and 254 fix the
      effort and agent-profile authority boundaries.
- [x] No prompt, login, installation, downloaded-artifact execution, or
      provider credit is needed for the identity and claim decision.

## Work

1. Re-probe the official stable manifest, installer, CLI changelog, ACP docs,
   and installed `--version` availability. Enumerate every published stable
   successor after `2.18.1` that the official channel can reproduce. Download
   the exact baseline and successor macOS/Linux artifacts into `/tmp`, verify
   manifest hashes, extract without installation, and freeze Research 320 with
   archive, executable, build/version, help, and embedded-protocol identities.
   Never execute a downloaded artifact.
2. Build a deterministic per-hop artifact/executable ledger and mapped-binary
   closure for `kiro-cli acp`. Classify every changed input feeding initialize,
   `session/new`, one `session/prompt`, cancel, JSON-RPC correlation, content
   blocks, stop reasons, activity/update frames, permission callbacks,
   authentication failures, cwd/resource authority, task/process ownership,
   deadlines, and joined cleanup. Where source is unavailable, use exact
   executable evidence and mutation-sensitive fixtures; changelog prose is
   discovery only.
3. Keep `kiro-cli chat --no-interactive`, `--cloud`, `--agent`, `--v2`/`--v3`,
   `--trust-all-tools`, TUI `kiro-cli-chat`, custom agents, cloud configuration,
   `_kiro.dev/*`, session load/model/mode extensions, and unofficial npm/PyPI
   names separate unless exact non-effect on the selected ACP entrypoint is
   proven. Do not turn advertised capabilities into Swallowtail operations.
4. If the selected contract is unchanged or mechanically adaptable without a
   new public lifecycle, authority, or operation, rebind the one exact claim
   point to `2.21.4`. Keep the behavior revision only when mapped wire and
   lifecycle are unchanged. Do not retain `2.18.1` as a second exact point or
   infer a compatibility range.
5. Stop with the smallest exact counterexample if a mapped wire, lifecycle,
   permission, authentication, profile/mode, failure, or process-authority
   boundary changed materially, exact artifact evidence is unavailable, or
   base compatibility requires a live provider session. Recheck the official
   stable manifest immediately before identity commit and push. Provider-free
   retrieval, classification, correction, and validation may rerun normally.

## Dispatch Manifest

| Field | Task 072 |
| --- | --- |
| Readiness | ready |
| Prerequisites | Contract 029; Research 156, 251, 254, 308, and 319; completed g05.071; clean pushed `main`; official Kiro installer, manifest, changelog, ACP docs, and selected artifacts |
| Completion conditions | every reproducible published stable successor is frozen and classified; identity precedes claim; one honest exact-`2.21.4` decision lands; QualifiedOnly and route-family boundaries survive; exact-head review and named gates pass |
| Owned mutable paths | Kiro selection, prepared route, ACP decoder/connection, tests, and fixtures under `crates/swallowtail-adapter-kiro/**`; Research 320 and one research-index line; exact Kiro prepared-guide, route/activity/feature matrices, architecture ceiling, standing-lane, `[Unreleased]`, identity-log, claim-log, and task-result lines; `PAPERCUTS.md` append only |
| Reserved shared closeout surfaces | lifecycle task record and generated projections; submitted-handoff deletion belongs to the repository hook |
| Forbidden paths | other adapters; contracts; historical research/captures/releases; Kiro headless/cloud/TUI/custom-agent routes; live provider probes; release/tag/workflow files |
| Approved concurrent siblings | none on shared currentness surfaces; this campaign is serial |
| Worker capability class | evidence-first Rust currentness worker; exact closed-artifact/executable ledger and ACP boundary reconciliation; no provider credentials |
| Acceptance evidence | current and baseline official-manifest identities; complete reproducible successor ledger; exact selected artifacts and executable hashes; mutation-sensitive selection/CLI/ACP/authority/lifecycle fixtures; current docs/claim agreement |
| Review oracle | skipped successor, inferred range, unclassified mapped delta, changelog-only inference, artifact execution, headless/cloud/TUI/profile flattening, advertised-operation inference, lost permission/auth/resource/cleanup boundary, provider reliance, or unjustified feature widening |
| Oracle gate | not required — Contract 029 and Research 156/251/254 settle this provider-free exact-pin comparison and its falsifiable stop conditions; no new auth, permission, secret, or execution policy is delegated |
| Stop conditions | official-channel disagreement or incomplete exact artifacts; selected public lifecycle/authority/authentication/permission/profile/mode/failure change; new driver/facade required; provider/live evidence required for base compatibility |
| Escalation owner | operator via Chatterbox for new policy; Queue coordinator for mechanical blockers |

## Boundaries

One route family and one exact-pin decision. No prompt, inference, login,
credential use, live ACP session, installation, host update, downloaded-artifact
execution, headless/cloud/TUI/custom-agent work, release, tag, publication, or
consumer mutation. Do not infer compatibility from adjacent manifest points,
changelog families, embedded library versions, or advertised capabilities.

## Validation

- `cargo fmt -p swallowtail-adapter-kiro -- --check`
- `effigy validate:focused swallowtail-adapter-kiro`
- `effigy package:verify-affected swallowtail-adapter-kiro`
- `effigy check:examples`
- `effigy package:api`
- `effigy qa:routes`
- `effigy qa:northstar`
- focused Kiro identity, selection, CLI, ACP, activity, permission, and cleanup selectors
- relevant research/log/roadmap/number/lifecycle/next-action checks
- `git diff --check`

## Acceptance

- [x] official identity and every reproducible published successor reproduce
- [x] every changed selected input is classified
- [x] identity evidence precedes the claim decision
- [x] exact QualifiedOnly shape and route boundaries remain truthful
- [x] advertised siblings and capabilities remain independently gated
- [x] official stable manifest is rechecked at both boundaries

## Result

Research 320 freezes the official `2.18.1` baseline and all eleven
published stable successors through `2.21.4` as exact Linux aarch64
archives (baseline digest reproduces Research 156; `2.21.4` matches the
planning digests) plus read-only-mounted baseline and official DMGs. The
selected ACP surface is byte-stable across every hop: ACP library pins
`sacp-11.0.0` and `agent-client-protocol-0.10.4` never move, the selected
argv stays exactly `kiro-cli` + `acp`, the prompt field stays `prompt`,
and initialize, `session/new`, `session/prompt`, `session/cancel`,
session-update kinds, permission kinds, stop reasons, and cleanup are
unchanged. The exact `kiro.acp.release-window-1` claim now binds one
maintained `2.21.4` point with the unchanged `kiro.acp.stdio-v1` behavior
revision and `QualifiedOnly` posture; `2.21.5` is not published.

Bounded unmapped deltas: the `_kiro.dev` extension-request handler appears
at `2.19.2` and gains `mcp/startup_status` at `2.20.2`; the serve method
list gains `session/load` at `2.20.2`; and the `2.21.4` chat JS rebuild
swaps the bridge method map while the selected literals hold. The official
ACP docs page still shows the stale `content` prompt-field example.
Research 251 and 254 keep their empty deliver-now sets with their
package/source gates closed by the per-hop binary evidence. No provider
operation, host mutation, prompt, login, or downloaded-artifact execution
occurred. Validation is named above; PR review and merge remain
queue-owned.

## Next Task

Return the exact outcome to Chatterbox, then compile and dispatch Mistral Vibe
as the next Research 308 family without asking Tom to repeat authorization.
