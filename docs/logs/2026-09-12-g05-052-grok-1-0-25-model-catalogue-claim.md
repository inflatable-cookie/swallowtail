# 2026-09-12 g05.052 Grok 1.0.25 Model Catalogue Claim

## Result

Admitted the exact installed Grok `1.0.25` pre-session model catalogue as a
separately prepared `ModelCatalog` operation. One bounded provider-suppressed
`models` process with stdin closed unwritten reports ordered exact model ids
and the source default; display name, description, input token limit, and
reasoning modes project only from the frozen exact-`1.0.25` default-model
document by exact id equality, unknown ids pass through with empty metadata,
no provider id is attached, and malformed, duplicate, empty, over-limit, and
ambiguous-default catalogues fail closed with joined cleanup. Preparation
requires the exact `1.0.25` observation, delegated subscription access
readiness, and the immutable catalogue plan; execution never retries, updates
the CLI, opens ACP, sends a prompt, or falls back to an execution default.
There is no consumer cancellation surface: the driver enforces elapsed-deadline
rejection and mid-read timeout with joined stop/wait cleanup instead.

The new `QualifiedOnly` claim `grok-build.catalogue.executable-1-0-25`
(`grok-build.catalogue.models-text-v1`) admits no older or newer point. The
ACP execution claim, `grok_build_model_for_version`, and released `v0.5.0`
are unchanged: `1.0.25` stays permitted `UnverifiedNewer` for ACP execution
and never becomes a qualified execution milestone through this task.

No catalogue command was executed, no provider was contacted, no credential
was read, no host was changed, and no Desktop, release, tag, or publication
follows. Research 305. g05.052.

## Validation

`cargo fmt -p swallowtail-adapter-grok -- --check`, `effigy validate:focused
swallowtail-adapter-grok`, `effigy package:verify-affected
swallowtail-adapter-grok`, `effigy check:examples`, `effigy package:api`,
`effigy qa:routes`, `effigy qa:northstar`, the research, logs, roadmaps, g05,
roadmap-number, status and next-action checks, and `git diff --check`.

The 2026-09-12 Blocker Ruling resolved the route-50 inventory split under
Contract 036 and the current/previous baseline roles: current source and its
working `0.5.0` baselines validate at 50 routes with `grok-build.catalogue`
additive post-`v0.5.0`, while tagged `v0.5.0` and Research 281 validate
independently at frozen 49 routes with Card050/051 wording byte-identical.
`qa:routes`, `qa:consumer-docs`/pre-push, cross-classification, activity,
coverage, and docs-links all pass; strict-extension and ledger-membership
assertions fail if current and tagged/historical inventories are collapsed
again. No historical baseline, release note, tag, publication, consumer pin,
or Desktop mutation is carried by this task.

## Merge And Closeout

Worker PR targets `main` from the queue-owned branch; merge, review, and
closeout SHAs are recorded here by the queue at closeout. Desktop Chatterbox
may adopt the merged source SHA only under a separately authorized
exact-source pin; `v0.5.0` does not contain the seam and no tag follows
automatically.
