---
title: g04.024 Kimi Platform Chat worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Tom
created: 2026-08-21
updated: 2026-08-21
handoff_path: /Users/tom/Dev/projects/swallowtail/docs/handoffs/20260821-141451-g04-024-kimi-platform-chat.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr]
---

## What This Thread Was Doing

g04.023 and the five currentness families are merged on `main`. Research 171
classified `kimi-platform.chat` as the first post-023 implementation tranche.
The existing prepared facade already fixes the hosted Platform audience,
endpoint, provider/model identity, and one-attempt inference shape. g04.024 now
adds the Contract 057 descriptor and deterministic admission/lifecycle path.

This is the handoff from the planning/orchestrator thread to one bounded
implementation thread. It is written so the worker can start from this file
without a copied transcript or a second prompt.

## Why It Matters

`kimi-platform.chat` is prepared but not addable. Consumers cannot yet discover
the route, collect its Platform API key as an opaque `CredentialRef`, admit an
instance, or carry that admitted identity through refresh, catalogue,
inference, and 047 assembly. This runway proves that path without widening the
existing facade or importing hosted OAuth, provider state, retry, or another
Kimi audience.

## Current State

Here is the state the worker is inheriting:

- **Repository:** `/Users/tom/Dev/projects/swallowtail`
- **Repository posture:** strict-ready Northstar
- **Planning branch:** `main`
- **Planning base commit:** `2ab2203efd931935383a27762af6538c745f6fdd`
- **Pushed main verification:** run `git fetch origin`, then confirm local
  `HEAD == origin/main`; the current tip contains this handoff file after the
  later handoff commit. The recorded planning base above is the planning commit
  *before* this file existed.
- **Planning checkout:** clean on `main` at the planning base; this handoff is a
  follow-up commit on the same branch
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight
- **Planning artifacts included at the base:** Contracts 011, 014, 020, 037,
  047, 052, and 057; Research 171; completed g04.023; g04.024 milestone and
  ready cards 076-078; the merged currentness/research closeout
- **Worker branch:** `g04-024-kimi-platform-chat`
- **Worker worktree:** launcher-provided dedicated worktree first. Manual
  fallback path is
  `$AGENTS_WORKTREE_CONTAINER_DIR/swallowtail-g04-024-kimi-platform-chat`
- **Worktree creation command:** only if the current context is unusable and
  `.agents.local.env` defines `AGENTS_WORKTREE_CONTAINER_DIR`:
  `git worktree add -b g04-024-kimi-platform-chat "$AGENTS_WORKTREE_CONTAINER_DIR/swallowtail-g04-024-kimi-platform-chat" origin/main`
- **Worker worktree policy:** first use the clean, dedicated, non-`main`
  registered worktree supplied by the launcher, even if its generated path or
  branch differs from these handoff placeholders. Record the actual
  path/branch and never create a second worktree for that reason. If the
  current context is unusable, use the named worktree when it matches; only
  then read `.agents.local.env`, require `AGENTS_WORKTREE_CONTAINER_DIR`, and
  create a unique manual worktree/branch under that container from
  `origin/main`. Ask the operator first if the file or key is absent; never use
  `/tmp`, `TMPDIR`, or a guessed path.
- **Active spec lane:** none. The named contracts and realized Kimi Platform
  prepared integration are the authority.
- **Roadmap milestone:**
  `docs/roadmaps/g04/024-hosted-api-key-kimi-platform-chat.md`
- **Ready cards, in order:**
  `docs/roadmaps/g04/batch-cards/076-kimi-platform-chat-addable-descriptor.md`,
  then
  `docs/roadmaps/g04/batch-cards/077-kimi-platform-chat-admission-and-prepare.md`,
  then
  `docs/roadmaps/g04/batch-cards/078-kimi-platform-chat-refresh-catalogue-and-047.md`
- **Allowed runway:** g04.024 cards 076 -> 077 -> 078. Stop after the exact
  Kimi Platform Chat descriptor, admission/prepare handoff, and deterministic
  refresh/catalogue/inference/047 lifecycle are realized and documented.
- **Remaining card budget:** three cards, one PR
- **Dispatch topology:** serial; one worker, one worktree, one PR
- **Parallel safety check:** all three cards touch the same route identity,
  admitted refs, preparation boundary, lifecycle evidence, and closeout state.
  No parallel lane.
- **Canonical refs:** Contracts 011, 014, 020, 037, 047, 052, 057; Research
  171; `docs/guides/kimi-platform-prepared-integration.md`;
  `docs/guides/connection-lifecycle.md`; the existing
  `prepare_kimi_platform_direct` implementation and tests
- **Exact route binding:** route `kimi-platform.chat`; driver
  `swallowtail.kimi-platform.direct-chat`; facade
  `kimi-platform-chat-2026-07-21`; provider/model `moonshot` / `kimi-k3`;
  approved audience `api.moonshot.ai`
- **Model capability profile:** capable coding model, medium reasoning
- **Tool/runtime restrictions:** no live provider, install, login, billing,
  or account work. No hosted OAuth. No Kimi Membership, Kimi Code, local
  server, or regional Platform credential. No OpenHands production wiring.
  No provider state, reusable sessions, tools, retries, fallback, cloud SDK,
  realtime, or owned-server route. Do not change Contracts 047 or 057, the
  production route matrix, or `release-baselines/public-api-0.3.3`.
- **Orientation baseline:** `effigy tasks` passed. `effigy doctor` reported the
  known repository baseline of 348 god-file findings (40 errors, 308 warnings),
  plus stale-graph and generated-in-src warnings. Treat that as inherited
  evidence, not permission to widen this PR; report any new or changed finding.
- **Required validation:** card 076:
  `effigy validate:focused swallowtail-adapter-kimi-platform swallowtail-runtime`,
  `git diff --check`, and `effigy package:api` if public API changes. Card 077:
  `effigy validate:focused swallowtail-adapter-kimi-platform swallowtail-runtime swallowtail-host-local`,
  `git diff --check`, and `effigy package:api` if public API changes. Card 078:
  `effigy validate:focused swallowtail-adapter-kimi-platform swallowtail-runtime swallowtail-host-local swallowtail-testkit`,
  `effigy package:verify-affected swallowtail-adapter-kimi-platform swallowtail-runtime swallowtail-host-local swallowtail-testkit`,
  `effigy qa:routes`, `effigy qa:guides`, `effigy qa:northstar`,
  `git diff --check`, and `effigy package:api` if public API changes.
- **PR base/head:** `main` / selected worker branch
  (`g04-024-kimi-platform-chat` unless the launcher supplied a different
  dedicated branch)
- **PR URL:** pending
- **Review state:** awaiting worker PR
- **Merge authorisation:** not granted; merge is a separate
  operator-authorised action

## Boundaries

Please keep this run inside the named runway:

- **In scope:** adapter-local hosted addable descriptor; secret Platform API-key
  field with no invented environment name; opaque host-owned endpoint config
  field; Contract 057 admission and API-key collection; route-local retyping of
  admitted refs into `KimiPlatformPreparationInput`; host-supplied refresh;
  subject `Absent`; existing catalogue and one explicit K3 attempt; exact 047
  assembly; the two named guide updates; honest card/milestone/log closeout.
- **Out of scope:** raw key or endpoint values in portable state or diagnostics;
  browser URL-open, loopback, device-code, or login-helper ports; Membership,
  Code, local-server, or regional Kimi access; provider/account/billing probes;
  retries, tools, sessions, provider state, fallback; hosted OAuth; OpenHands;
  another descriptor tranche; route-matrix or Contract 047/057 changes;
  GitHub Release, crates.io, or tag mutation.
- Topology is hosted and remains distinct from `ExecutionLayer`. Descriptor
  availability depends on the Credential host service and must name that
  missing service when unavailable.
- Portable records may carry only opaque endpoint and credential references.
  Preparation follows admission and must fail closed on route, endpoint-ref,
  credential-ref, audience, or host drift.
- Keep exact facade, audience, provider/model identity, explicit reasoning
  choice, output bound, and one-attempt authority. Selection readiness remains
  the Contract 047 formula; overlay cannot change it. Provider defaults remain
  separate from consumer defaults.
- This handoff represents one worker lane. Do not edit another lane's assigned
  scope. If shared mutable scope or a hidden dependency expands the runway,
  stop and report it through the operator.
- Work only in the selected clean worker worktree: prefer the current
  launcher-provided worktree and record its actual path/branch; otherwise use
  the named fallback created by the startup preflight. Never edit the
  orchestrator's planning checkout or an unrelated dirty checkout.
- Do not merge the PR. Merge remains a separate operator-authorised action.

## Important Context

- **Planning lineage:** g04.023 landed the 047 `instance_label` projection.
  Research 171 named Kimi Platform Chat as the first post-023 addable tranche.
  Five currentness families landed after it without changing this ready task.
- **Why these cards are ready:** the prepared adapter already binds the exact
  direct-chat facade and operation shape. Contracts 014, 037, 047, and 057
  settle the secret, prepare, readiness, and admission boundaries. The hosted
  API-key lifecycle has existing proved shapes to follow.
- **Decisions and preferences:** reuse `prepare_kimi_platform_direct`; no raw
  secrets or endpoint URL in portable state; no invented environment variable;
  subject remains `Absent`; route inventory remains unchanged; hosted OAuth
  stays parked.
- **Open tensions:** none currently. Stop rather than invent policy if the
  existing Contract 057 descriptor/admission surface cannot represent the
  exact Platform audience and opaque refs. There is no `.agents.local.env` on
  the planning machine; if the launcher does not supply a worktree, ask the
  operator for `AGENTS_WORKTREE_CONTAINER_DIR`.
- **Report after:** card 076 descriptor; card 077 admission/prepare handoff;
  card 078 lifecycle, closeout, and PR
- **Report to:** the operator, who will relay progress to the orchestrator

## Suggested Next Move

This handoff explicitly activates worker mode. Start by reading it from the
top. Before broad repository reads, run the quick startup worktree-safety
preflight in `## Completion Protocol`. If the current context is a clean,
dedicated, non-`main` registered worktree, use it immediately, record its actual
path/branch, and do not compare its generated path/branch with this handoff or
create another worktree. If it is `main`, dirty, unregistered, or otherwise
unusable, use the named worktree if it matches; only then read
`.agents.local.env`, require a valid `AGENTS_WORKTREE_CONTAINER_DIR`, ask the
operator if it is absent, and create a unique manual worktree and branch under
that container from pushed `origin/main`. Never fall back to `/tmp` or
`TMPDIR`. Do not run broad repo orientation before this decision.

Once the worktree is safe, read `AGENTS.md`, the milestone, cards 076-078, the
canonical contracts, Research 171, and the two named guides. Take card 076
first. When its descriptor is green, continue through 077 and 078 in order.
After the complete lifecycle and docs are green, close the roadmap evidence,
open one PR, and stop. Do not select the next descriptor tranche.

## Completion Protocol

### Before you start

1. Read this handoff path. Its `worker_mode: implementation` and
   `dispatch_authority: orchestrator` metadata activate worker mode. Then run
   one quick read-only safety probe before broad repository reads:
   `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. If the current root is a registered worktree, its status is empty, and its
   branch is not `main`, accept it as the launcher-provided worktree. Record its
   actual root/branch and do not compare them with the placeholders above or
   create another worktree merely because they differ.
3. Only if the current context is `main`, dirty, unregistered, or otherwise
   unusable should you inspect the named worktree. If that also cannot be used,
   read `.agents.local.env` and require `AGENTS_WORKTREE_CONTAINER_DIR`; if it
   is absent, ask the operator before creating the file or worktree. Then
   create a unique worktree and branch under that container from pushed
   `origin/main`, record the actual path and branch, and run all subsequent
   commands there. Never use `/tmp`, `TMPDIR`, or a guessed path; never clean,
   reset, stash-over, or discard the original checkout's dirty state. If the
   launcher supplied a dirty or `main` worktree, stop and report it instead of
   silently creating a second worktree.
4. From the selected worktree, run `git fetch origin`, confirm
   `git rev-parse HEAD` equals `git rev-parse origin/main`, confirm
   `git merge-base --is-ancestor 2ab2203efd931935383a27762af6538c745f6fdd HEAD`
   succeeds, and confirm this handoff file exists in the selected `HEAD`.
5. Read the active milestone, assigned cards, `AGENTS.md`, canonical refs,
   Research 171, and named guides.
6. Run `effigy tasks`, `effigy doctor`, and `effigy test --plan`. Record what
   actually ran and distinguish inherited doctor findings from new ones.

### While you work

- Execute cards 076, 077, and 078 in order. Keep commits aligned with the
  descriptor, admission/prepare, and final lifecycle/closeout chunks.
- Follow existing hosted API-key descriptor and lifecycle patterns where the
  contracts permit it. Keep the Kimi-specific audience and facade exact.
- After each card, report through the operator with changed files, validation
  actually run, remaining cards, new risks, and blockers.
- Stop and say so if a contract is missing, intent is ambiguous, scope expands,
  authority/access is missing, or validation changes the plan.
- Do not quietly turn an open question into new architecture.
- Do not start another descriptor tranche, Kimi Code wiring, hosted OAuth, or
  OpenHands production wiring.

### When the assigned runway is complete

1. Run the complete required validation named for card 078, plus
   `effigy package:api` if any public API changed. Update
   `release-baselines/public-api-unreleased/` if required; never rewrite the
   `public-api-0.3.3` baseline.
2. Update cards 076-078, g04.024, roadmap/log indexes, and the `## Next Task`
   pointer honestly. Record merge reality as pending because the worker PR is
   not yet merged. Do not select or begin the next descriptor tranche.
3. Push the selected worker branch.
4. Open a reviewable PR against the current pushed `main` tip. The handoff's
   planning base `2ab2203efd931935383a27762af6538c745f6fdd` is the planning
   commit before the handoff was created, not a self-referential hash for the
   commit that contains this file.
5. In the PR body, link g04.024, cards 076-078, Contracts 011, 014, 020, 037,
   047, 052, and 057, Research 171, changed surfaces, evidence, validation,
   baseline doctor findings, and unresolved items.
6. Report the PR URL, exact head commit, and evidence to the operator. Do not
   merge.

### Review and merge path

The orchestrator will review the PR against the canonical refs, diff, and
checks. Current review state: awaiting worker PR.

The orchestrator records an evidence-backed verdict in the provider's review
surface. When the orchestrator and worker share a GitHub identity, formal
self-approval is unavailable, so the orchestrator posts the verdict as a PR
comment; that comment is the canonical review record. If changes are
requested, make only those changes on this branch, push again, and report back
through the operator. Requested changes are: none yet. The operator must
explicitly authorise any merge.

- **Closeout refs:** cards 076-078, g04.024, `docs/roadmaps/README.md`,
  `docs/logs/README.md`, `docs/guides/kimi-platform-prepared-integration.md`,
  `docs/guides/connection-lifecycle.md`

### Handoff closeout

Before calling the runway complete, leave the cards, milestone, log, guides,
and next-task state honest. If the work is blocked, record the blocker and stop
rather than making the handoff look more complete than it is. After the PR
lands, the orchestrator will record merge reality before choosing another
descriptor tranche. Hosted OAuth stays parked.
