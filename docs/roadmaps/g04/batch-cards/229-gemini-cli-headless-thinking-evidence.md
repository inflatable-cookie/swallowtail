# 229 Gemini CLI Headless Thinking Evidence

Status: complete
Owner: Tom
Created: 2026-08-27
Updated: 2026-08-27
Milestone: [g04.082 Parallel Per-Route Feature Qualification](../082-parallel-per-route-feature-qualification.md)
Depends on: g04.034; g04.081 closeout

## Goal

Freeze exact Gemini CLI headless thinking-configuration model, value,
precedence, isolation, dispatch, and confirmation truth, then promote Research
230 with a non-empty deliver-now table or an honest empty set.

## Work

1. [x] Keep route `gemini-cli.headless`, exact qualified `0.51.0..=0.56.0`,
       stream-JSON behavior v1, enterprise Developer API-key access, explicit
       model, one prompt, and best-effort transcript cleanup unchanged.
2. [x] Freeze official `0.56.0` documentation, schema, tagged source, and tests
       for `modelConfig.thinkingConfig`, value vocabulary, model membership,
       defaults, validation, precedence, and request application. Current docs
       are leads, not proof for earlier points.
3. [x] Determine whether one caller-bound, process-private configuration seam
       can override ambient settings without reading or mutating host config.
       Trace CLI argv, environment, settings loading, model resolution, prompt
       dispatch, stream events, transcript retention, and cleanup.
4. [x] Classify exact versions, models, values, and omission. Do not translate
       UI labels or promote Gemini Live thinking evidence.
5. [x] Prove unsupported rows can reject before process, credential, resource,
       or provider work and that any claimed selection has an exact dispatched
       or effective confirmation channel.
6. [x] Audit prepared headless inputs/evidence, plan/request agreement, command
       construction, child environment, decoder, fixtures, guide, matrices,
       and API baseline without changing production surfaces.
7. [x] Promote Research 230 and complete the reserved lane log. Do not edit
       shared milestone, inventory, programme, triage, matrices, or indexes.

## Acceptance Criteria

- [x] exact configuration, model/value membership, precedence, and isolation are frozen
- [x] exact version/model/value table or honest empty set is recorded
- [x] omission preserves current command/environment and makes no selection claim
- [x] no consumer-account login or live provider test is required
- [x] no production code, public API, shared authority, currentness, release,
      merge, rollover, or g04 closure changes

## Validation

```sh
effigy validate:focused swallowtail-adapter-gemini
effigy qa:northstar
git diff --check
```

## Stop Conditions

- configuration remains ambient, durable, account-owned, or mutable outside
  the child process
- model/value membership depends on an authenticated catalogue or mutable
  account fact
- applied thinking cannot be confirmed without a provider prompt
- proof needs login, credential, install/update, host config mutation, or a
  shared-contract change

## Out Of Scope

Gemini ACP, Gemini Live, sandbox, output limits, consumer login, production
binding, live provider work, currentness, release, merge, shared closeout,
rollover, or g04 closure.

## Outcome

Research 230 promoted with an empty deliver-now set. Exact `0.56.0` thinking
configuration is settings-backed through `modelConfigs.generateContentConfig.thinkingConfig`;
the qualified route binds no thinking seam; `GEMINI_CLI_HOME` can redirect user
settings and a last system layer can replace earlier override arrays, but the
adapter injects neither; stream-json exposes no thinking confirmation. Frozen evidence:
`crates/swallowtail-adapter-gemini/tests/fixtures/gemini-cli-headless-0.56.0-thinking/thinking-evidence.json`.
