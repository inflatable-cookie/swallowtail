# 2026-08-26 g04.072 Grok Build ACP Subagents Disabled Compiled

Status: compiled
Owner: Tom
Milestone: g04.072
Cards: 198-200

## What Changed

- reassessed the remaining per-route inventory after g04.071 stopped
- rejected Claude Code `--max-turns` because exact qualified `2.1.241` help
  does not expose the moving-docs flag
- selected Grok Build ACP launch-time subagent suppression as the strongest
  bounded route-local lead
- compiled g04.072 and cards 198-200 as one serial evidence-first worker lane
- reserved Research 219 and the route-local closeout before dispatch
- kept g04 open and Contract 029 currentness standing

## Selection Basis

Exact installed Grok Build `1.0.5` help accepts `--no-subagents` as a global
option before `agent stdio`. The maintained route already owns the child
process, exact argv, immutable preparation evidence, both ACP operation shapes,
fresh replacement, cancellation, and joined cleanup. The flag is independent
of model, reasoning, billing, and provider-tool identity.

That is a credible evidence path, not a delivery decision. Parser acceptance
does not prove complete suppression. Card 198 must tie exact package/source to
every ACP subagent registry and spawn path, settle ambient configuration and
session override behavior, and prove applicability across structured runs,
interactive sessions, later prompts, recovery, and replacement without a
provider prompt.

The candidate is disabled-only and adapter-local. It does not add child
observation or direct control, grant permission, remove ordinary process tools,
or establish sandbox, filesystem/network, read-only, or OS descendant-process
containment.

## Validation Plan

Card 198 uses official documentation, exact `1.0.4`/`1.0.5` package artifacts,
exact-version source where available, local help/version/parser cases, and
deterministic/no-prompt fixtures only. It performs no install, login, account
inspection, provider prompt, tool/subagent execution, or paid request. If
Research 219 promotes a non-empty set, cards 199-200 run focused Grok adapter
validation, affected-package verification, examples, API, Northstar, indexes,
doctor, and diff checks.

## Next Move

Execute g04.072 cards 198-200 serially in one isolated worker worktree and open
one PR. Stop honestly after card 198 if Research 219 is empty or exact effective
suppression cannot be proved. Do not merge, roll the generation, or close g04.
