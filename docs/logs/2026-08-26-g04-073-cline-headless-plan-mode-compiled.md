# 2026-08-26 g04.073 Cline Headless Plan Mode Compiled

Status: compiled
Owner: Tom
Milestone: g04.073
Cards: 201-203

## What Changed

- reassessed the remaining per-route inventory after g04.072 stopped
- rejected Kimi headless Plan because exact prompt-mode evidence already marks
  the selected command incompatible
- rejected Cline ACP Plan because exact `3.0.55` returns into ACP without
  carrying the parsed root mode
- selected exact Cline headless fixed-argument Plan as the strongest bounded
  route-local lead
- compiled g04.073 and cards 201-203 as one serial evidence-first worker lane
- reserved Research 220 and the route-local closeout before dispatch
- kept g04 open and Contract 029 currentness standing

## Selection Basis

Exact qualified `cline@3.0.55` source parses `--plan` into an explicit mode
that wins over persisted settings. The selected JSON path carries it into the
one-run config, system prompt, mode-tagged turn, tool preset, and a command
guard that runs before approval. The route already owns the exact child argv,
read-only working resource, host deadline, structured events, cancellation,
and joined cleanup.

That is a credible evidence path, not a delivery decision. Card 201 must prove
the complete exact Plan behavior, every write-capable tool seam, Plan-to-Act
paths, ambient precedence, retained state, and fixed-argument equivalence to
portable `HarnessMode::Plan`. The selected JSON wire does not report mode
without unselected verbosity, so no effective observation may be invented.

Plan remains provider behavior under Contracts 023 and 034. It does not grant
permission or establish filesystem, network, shell, process, sandbox, or
descendant containment. `Ambient` configuration and `AmbientHost` isolation
remain exact.

## Validation Plan

Card 201 uses official exact-tag source, published artifact identity, local
parser/source fixtures, and deterministic adapter tests only. It performs no
install, login, account inspection, provider prompt, arbitrary tool execution,
or paid request. If Research 220 promotes a non-empty set, cards 202-203 run
focused Cline adapter validation, affected-package verification, examples,
API, Northstar, indexes, doctor, and diff checks.

## Next Move

Execute g04.073 cards 201-203 serially in one isolated worker worktree and open
one PR. Stop honestly after card 201 if Research 220 is empty or exact Plan
equivalence cannot be proved. Do not merge, roll the generation, or close g04.
