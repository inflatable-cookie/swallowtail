# 2026-07-24 Generation Boundary And Grok Range Selection

## Outcome

g01 stays active. Roadmap 047 takes the generation to 47 numbered roadmaps,
inside its normal 30-50 range. No g02 is created.

Grok Build is the next maintained-range harness proof. The first boundary is
an exact host-approved executable over ACP v1 stdio, pre-existing delegated
OAuth, subscription allowance, restrictive provider-native permissions,
explicit ambient-host isolation, isolated host-approved state, suppressed
auto-update, and no sandbox claim.

Roadmap 047 and cards 137-140 own the exact corpus, installed discovery and
private dispatch, production driver, and cross-topology closeout. Card 137 is
the sole ready task.

## Evidence Delta

- ACP v1 has added stable optional lifecycle surfaces.
- ACP v2 and interactive authentication status remain Draft.
- Rust SDK `2.0.0` is an SDK API major that retains stable ACP v1 wire.
- The live ACP registry contains local binary, `npx`, and `uvx`
  distributions. It does not publish provider-hosted remote ACP endpoints.
- The registry's Grok entry pins `@xai-official/grok@0.2.111` and
  `grok agent stdio`.
- Public package metadata contains 111 published stable `0.2.x` points from
  `0.2.0` through `0.2.111`, with `0.2.48` unpublished.
- xAI documents exact version observation, ACP stdio, update suppression,
  pre-existing authentication, and permissions separately from sandboxing.

The package span is compatibility pressure, not a support claim. Card 137 must
inspect exact artifacts and freeze behavior milestones before any release or
segment becomes qualified.

## Contract Decision

Contracts 012, 013, 015, 017, 023, 029, 032, 033, and 034 cover the proposed
restrictive subset. No new shared contract is required before corpus work.

The lane excludes installation, updates, API-key injection, login and logout,
workspace writes, shell, web search, subagents, memory, plugins, hooks, MCP
mutation, custom endpoints, sandbox claims, and implicit fallback. A material
lifecycle or authority difference found in the corpus stops implementation
and returns to contract planning.

## Remaining Risks

- Grok platform artifacts remain black-box until exact package inspection.
- No continuous supported range can be inferred from semver or release cadence.
- ACP registry and package license metadata disagree and remain source-scoped.
- Pre-existing delegated OAuth has not been exercised.
- Model selection, local retention, ambient configuration, and exact
  permission behavior still need artifact evidence.
- No provider-specific remote ACP endpoint or stable interactive-auth status
  route is currently qualified.

## Validation

- `effigy qa:docs` passes.
- `effigy qa:northstar` passes.
- `git diff --check` passes.
- `effigy doctor` reports the inherited 19 oversized-file findings:
  12 warnings and 7 errors. No new doctor category or count appeared.

## Continuation

Execute card 137. Freeze exact artifacts and raw ACP behavior first. Promote
card 138 only if the evidence proves at least one safe release and requires no
new shared contract.
