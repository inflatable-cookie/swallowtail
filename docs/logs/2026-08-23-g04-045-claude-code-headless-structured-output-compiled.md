# 2026-08-23 g04.045 Claude Code Headless Structured Output Compiled

## Change

- reassessed the remaining promoted per-route feature inventory after g04.044
- rejected Kimi headless thinking/plan as the next lane because the exact
  TypeScript `0.38.0` corpus does not prove thinking and earlier exact evidence
  says prompt mode cannot compose with `--plan`
- selected JSON Schema structured output on `claude-code.headless`
- compiled g04.045 and cards 124-126 as one serial evidence-first worker lane
- reserved Research 192 and the route-local closeout before dispatch

## Decision

The existing headless route already has the bounded structured-run request,
structured-output descriptor vocabulary, exact model, optional reasoning,
read-only Plan-mode tools, activity, usage, cancellation, and cleanup needed
to state the candidate. Current official Claude Code docs name
`--json-schema` for print mode.

That does not establish delivery. The exact `2.1.238` currentness corpus reused
earlier selected help and does not freeze the flag. Response-only Research 121
also found a model-visible schema tool, retries, and successful exit with a
null structured result. Card 124 must therefore settle exact package syntax,
dialect, enforcement source, attempt budget, terminal result, and composition
before any binding. Earlier versions and the response-only route inherit
nothing.

Cards 125-126 may continue only for a non-empty Research 192 deliver-now set.
An evidence stop is valid. No implementation, capability, matrix, or
compatibility claim was made during compilation.

## Next

Execute g04.045 cards 124-126 in one isolated worker worktree and open one PR.

