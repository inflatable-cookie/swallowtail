# 2026-07-31 Antigravity Headless Structured Driver

## Changed

- promoted Research 079 from official headless documentation, exact installed
  help, and deterministic documentation-derived fixtures
- added the separate `swallowtail.antigravity.headless` structured-run driver
- bound explicit model, working resource, optional effort, optional inline
  JSON Schema, ambient or provider-enforced isolation, and request-review mode
- projected bounded assistant, tool, completion-only subagent, usage, typed
  failure, and exact terminal evidence
- added force-stop cancellation, deadline expiry, joined event delivery, and
  cleanup across local and remote-authoritative host identities

## Boundary

The driver never selects dangerous permission bypass or implicit sandboxing.
Tool arguments, output, errors, provider stderr, workspace paths, prompt text,
and subagent URIs remain private. Permission-required tools are not
consumer-approved through this route.

The documented print interface places the prompt in process arguments, so the
execution host can observe it. Continuation, prepared facade acceptance,
interactive callbacks, and provider-session management remain separate.

## Validation

- focused validation: 22 tests across three binaries plus warnings-denied
  checking in one second
- no live prompt, account mutation, credential read, consumer edit, broad
  workspace suite, or publication ran

## Current State

Card 016 is complete. Card 017 is the sole next task and owns exact
conversation-id continuation with one joined process per turn.
