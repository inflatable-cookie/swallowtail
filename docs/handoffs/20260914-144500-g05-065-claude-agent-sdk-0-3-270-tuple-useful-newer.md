---
title: g05.065 Claude Agent SDK 0.3.270 tuple useful-newer qualification
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
status: ready-to-launch
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Tom instructed Chatterbox to action the full Research 308 currentness campaign, including Kimi and Oh My Pi, and continue without repeated approval; g05.064 is complete and the Claude Agent SDK tuple is next."
roadmap: docs/roadmaps/g05/065-claude-agent-sdk-0-3-270-tuple-useful-newer.md
queue:
  capability: general
  skipPRReview: false
  notifyOriginOnCloseout: true
---

# g05.065 Claude Agent SDK 0.3.270 Tuple Useful Newer

## What This Thread Was Doing

Chatterbox closed g05.064 and compiled the seventh Research 308 family.
Official npm has moved the SDK wrapper from exact `0.3.259` to `0.3.270`, whose
manifest carries native `2.1.270`. The route has five independent one-point
axes and substantial tuple-specific live evidence, so this is an exact rebind
and evidence-reconciliation task, not a range bump.

## Why It Matters

The current SDK route rejects the official wrapper/native tuple. Updating only
constants would be unsafe: selected declarations and implementation may have
moved, and Research 301's registered-tool acceptance ran only on
`0.3.259`/`2.1.259`. The new base tuple needs complete provider-free evidence
and the old live proof must remain honestly bounded.

## Current State

- Canonical `main` is g05.064 closeout
  `90d9dfc408bb689122edf8f554b1aae1f8a768f0`.
- PR #327 merged accepted head `0179a96492bdb0be96d0780615a06a41b2179f84`
  as `578acda90bfe31d32e5bf42fa4ac62f0a63c8a45`; review comment
  `5664881281`.
- npm `latest=next=0.3.270`; its `package.json` declares
  `claudeCodeVersion=2.1.270` and Node `>=18.0.0`.
- New published wrappers are `0.3.260`, `0.3.261`, `0.3.263`, and
  `0.3.265..=0.3.270`; `0.3.262` and `0.3.264` are gaps.
- Installed observations: Node `22.23.2`; Claude Code `2.1.258`.
- Existing base tuple: SDK `0.3.259`, native `2.1.259`, Node `22.23.2`,
  private wire v1, current sidecar source tag; all five claims are exact
  `QualifiedOnly` one-point segments.
- Research 301 registered-tool qualification is frozen to SDK `0.3.259`,
  native `2.1.259`, Node `22.23.2`, Darwin arm64, MCP `2025-11-25`, and its
  exact capsule/source identities.

## Boundaries

One family and one exact tuple. No prompt, inference, SDK open/live session,
login, credentials, install, host update, downloaded-artifact execution, live
registered-tool requalification, release, tag, publication, or consumer
mutation. No range or `AllowUnverified`. Do not transfer Claude Code or Claude
Agent ACP compatibility. Preserve immutable capsules and historical research.

Provider-free retrieval, unpacking, comparison, sidecar adaptation, fixture
repair, and validation may rerun normally. Do not impose artificial attempt
limits on them.

## Important Context

Read Contract 029, Research 278, 280, 287, 301, 308, and g05.065 before
editing. Use the repository `version-currentness` skill and Effigy selectors.
Inventory every published wrapper hop and its declared/coupled native, not
only npm `latest`. Compare the full shipped package tree and all selected
declarations/implementation used by the source-tagged sidecar. The installed
Claude `2.1.258` is not the native target. Changelogs are discovery only.

The base SDK route may rebind provider-free under the Research 280 precedent
if its selected contract is unchanged or mechanically adaptable within the
existing public behavior. Tuple-specific live projections may not follow it
without matching evidence. If rebind would make Research 301's feature claim
false, publish the honest unqualified state and name a separate live
requalification follow-up; do not retain the old base point alongside the new
one merely to hide that loss.

## Suggested Next Move

Freeze Research 315 and the mutation-sensitive all-hop corpus first. Commit
identity. Then make the smallest sidecar/claim/projection changes needed for
one truthful exact-tuple outcome, run the full named provider-free gate, push,
open a PR, and hand the exact head to an independent reviewer.

## Completion Protocol

The Queue owns worker launch, independent review, merge, lifecycle closeout,
and handoff deletion. Return the exact wrapper/native/Node/source/wire outcome,
classified hop set, Research 315 path, feature-evidence disposition,
validation, review marker/comment, merge SHA, and closeout SHA. Next is the
separate Grok catalogue family. No release or tag follows from completion.
