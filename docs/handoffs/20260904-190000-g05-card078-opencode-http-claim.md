---
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
---

# g05.028 Card 078 OpenCode HTTP 1.18.28 Claim

## Objective

Apply only the compatible `surface-19` segment admitted by Card 077 to the
OpenCode HTTP claim, raising the qualified ceiling to `1.18.28` while keeping
the `1.14.48` baseline, every historical gap, and `AllowUnverified` truth.

## Authority

Operator-confirmed direction from Chatterbox; promoted base is
`904785d43031c855583d8b77a2566324e5364686`. Card 077 merged at
`99e91aa8ca7f7e47315728f0796b1f62234fe8` and admitted `surface-19`.

## Scope

Own only the Card 078 manifest paths: OpenCode selection source/tests, claim
fixtures, prepared guide, OpenCode cells in route and feature matrices,
architecture ceiling lines, `CHANGELOG.md` `[Unreleased]`, the standing-lane
OpenCode claim paragraph, and one claim log. Identity and claim are two commits
in one PR. No public API, Contract 061, Candidate L, or unrelated route edits.

## Execution

Fetch origin before preflight. Re-probe official latest before the identity
commit and record any later movement as `UnverifiedNewer`, never a reopen.
Preserve all gaps and the baseline. Use Effigy selectors and package-scoped
validation; do not run provider contact or execute downloaded artifacts.

## Validation and review

Run the manifest-named focused, package, docs, Northstar, formatting, diff,
and required CI gates. Push one reviewable PR, then request an independent
cross-model exact-head review in this same worker workspace. Do not merge.

## Handoff

Report both commit SHAs, exact PR head, qualified ceiling, preserved gaps, and
all validation results. Coordinator owns merge gate, reserved closeout, and
frontier recomputation.
