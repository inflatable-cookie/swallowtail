---
title: g04.086 Cline ACP Plan mode worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Tom
created: 2026-08-27
updated: 2026-08-27
handoff_path: /Users/tom/Dev/projects/swallowtail/docs/handoffs/20260827-215656-g04-086-cline-acp-plan-mode.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr]
---

## What This Thread Was Doing

The orchestrator reviewed and fast-forwarded g04.085's four evidence lanes,
then promoted their shared result. Three candidates closed with honest empty
sets. Research 240 qualified one exact `cline.acp` `3.0.55`
`HarnessMode::Plan` row.

The orchestrator closed g04.085 and compiled g04.086 cards 242-243 for that one
positive set. This is one bounded implementation thread. Start from this file
without a copied transcript or second prompt. Do not spawn internal agents; the
operator owns worker execution and parallelism in their harness.

## Why It Matters

`cline.acp` already owns exact `3.0.55`, `cline --acp`, ACP v1 stdio, a
read-only working resource, observational permission handling, active-turn
cancellation, fresh context-losing replacement, and joined cleanup.

Research 240 proves that the exact ACP path advertises Plan, accepts one
set-config request, confirms `mode.currentValue = plan`, and builds the first
runtime manager from that stored mode. Swallowtail currently rejects every ACP
harness-mode selection. This lane can expose useful portable Plan without
borrowing headless argv, generic config, permissions, or containment claims.

## Current State

- **Repository:** `inflatable-cookie/swallowtail`
- **Planning branch:** `main`
- **Planning base commit:** `abdaefd2bef66a6818953128e07388acb0dcbd6e`
- **Pushed main verification:** local `HEAD` and `origin/main` both resolved to
  that SHA before the planning batch and this handoff were created.
- **Planning checkout:** clean before the planning batch.
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight.
- **Planning artifacts included by the planning commit:** g04.085 closeout;
  g04.086; cards 242-243; inventory, programme, triage, research/log/card/
  roadmap indexes; sole Next Task; this handoff.
- **Worker branch:** `worker/g04-086-cline-acp-plan-mode`
- **Worker worktree:** prefer the launcher worktree. Named fallback:
  `/Users/tom/Dev/worktrees/swallowtail-g04-086-cline-acp-plan-mode`
- **Worktree creation command:** only if preflight permits:
  `git worktree add -b worker/g04-086-cline-acp-plan-mode /Users/tom/Dev/worktrees/swallowtail-g04-086-cline-acp-plan-mode origin/main`
- **Worker worktree policy:** first use the clean, dedicated, non-`main`
  registered worktree supplied by the launcher, even when its generated path
  or branch differs from these placeholders. Record the actual path/branch and
  do not create a second worktree. If current context is unusable, use the
  named worktree when it matches; only then read `.agents.local.env`, require
  `AGENTS_WORKTREE_CONTAINER_DIR`, and create a unique manual worktree/branch
  under that container from `origin/main`. Ask the operator if the key is
  absent; never use `/tmp`, `TMPDIR`, or a guessed path.
- **Active spec lane:** per-route feature completion programme.
- **Roadmap milestone:**
  `docs/roadmaps/g04/086-cline-acp-plan-mode.md`
- **Ready cards, in order:**
  `242-cline-acp-plan-mode-binding.md`, then
  `243-cline-acp-plan-mode-acceptance.md`.
- **Allowed runway:** bind and prove Research 240's one exact new-session Cline
  ACP Plan row; one PR.
- **Remaining card budget:** two cards.
- **Dispatch topology:** one serial worker lane; no subagents.
- **Parallel safety check:** keep serial. Cards 242-243 share Cline prepared,
  driver, fixture, guide, matrix, API, milestone, and log surfaces.
- **Canonical refs:** `docs/architecture/system-architecture.md`; Contracts
  011, 012, 017, 023, 029, 033, 034, 037, 047, and 052; Research 240; Cline ACP
  prepared integration guide.
- **Model capability profile:** exact route-local Rust implementation and
  deterministic ACP/conformance proof; no provider access.
- **Tool/runtime restrictions:** use Effigy selectors; no credential, account,
  provider prompt, paid work, install, update, live probe, or ambient Cline
  settings mutation.
- **Required validation:** `cargo fmt -p swallowtail-adapter-cline`;
  `effigy validate:focused swallowtail-adapter-cline`;
  `effigy package:verify-affected swallowtail-adapter-cline`;
  `effigy check:examples`; `effigy qa:routes`; `effigy qa:northstar`;
  `effigy package:api`; `git diff --check`.
- **Inherited doctor baseline:** `scan.god-files` reports 380 findings (334
  warnings, 46 errors); `scan.generated-in-src` reports one warning. Do not
  widen or repair that unrelated baseline.
- **PR base/head:** current pushed `main` / selected worker branch.
- **PR URL:** pending.
- **Review state:** awaiting worker implementation and PR.
- **Merge authorisation:** absent; do not merge.

## Boundaries

- **In scope:** optional typed `HarnessMode::Plan` on
  `ClineSessionProfileInput`; exact capability/constraint/plan/evidence/request
  agreement; `session/new` mode/config snapshot parsing; one correlated
  `session/set_config_option` `mode=plan`; exact returned
  `mode.currentValue=plan` confirmation before readiness; omission; same-
  session turns; fresh context-losing replacement; failure cleanup; route
  guide/matrices/example as needed; package API baseline; changelog; Research
  240; cards 242-243; g04.086; reserved route-local log.
- **Out of scope:** Cline headless, root `--plan`, `session/set_mode`, provider
  Act/Yolo/Zen, post-readiness mutation, Plan-to-Act switching, model/thinking,
  auto-approve, generic provider config, `session/load`, resume, transcript
  restoration, effective provider behavior beyond exact source, live provider
  work, currentness, contracts, release, shared inventory/programme/triage/
  indexes/Next Task closeout, generation rollover, g04 closure, or merge.
- Admit only portable `HarnessMode::Plan` on exact `cline.acp` `3.0.55`.
- Do not change `cline.acp.stdio-v1`; Research 240 requires no behavior-revision
  split. Preserve qualified-only compatibility.
- Require unique Plan advertisement and exact set-config confirmation before a
  usable handle or first prompt. Missing, malformed, duplicate, foreign,
  rejected, or mismatched truth fails and joins owned work.
- Omission sends no mode request and retains current frames. Do not turn native
  default Act into a selected/default public claim.
- Fresh replacement is a new session and must renegotiate the same immutable
  selection. It remains context-losing `SessionReplaced`, not load or resume.
- Plan is provider behavior. It grants no permission, auto-approve, tool,
  filesystem, network, shell, process, descendant, sandbox, resource, model,
  account, credential, or isolation authority.
- Do not invent architecture, change contracts, widen the roadmap, or choose an
  unresolved product/API/persistence/security decision.
- Work only in the selected clean worker worktree. Never edit the orchestrator
  planning checkout or an unrelated dirty checkout.
- Do not merge the PR. Merge remains a separate operator-authorised action.

## Important Context

- **Planning lineage:** g04's per-route feature programme; g04.085 card 241 and
  Research 240 provide the exact evidence gate; g04.086 owns delivery.
- **Current prepared seam:** `ClineSessionProfileInput` carries only request id
  and working resource. `prepare_session` advertises no
  `HarnessModeSelection`, and `OpenSessionRequest` carries empty options.
- **Current validation seam:** `driver/validation.rs` rejects any
  `request.options().harness_mode()`.
- **Current open seam:** `start_session` initializes, sends `session/new`,
  parses only `sessionId`, stores it, and returns the handle. Insert selection
  and confirmation after new-session truth and before handle construction.
- **Current fixture seam:** `tests/support/agent.rs` returns Plan/Act mode
  metadata but does not yet return the exact mode config option or handle
  `session/set_config_option`. Keep fixtures deterministic and bounded.
- **Structural precedent:** Kimi ACP has a negotiated Plan implementation. It is
  a shape reference only; do not import Kimi reasoning, lifecycle, model, or
  version behavior into Cline.
- **Exact native row:** new session advertises `plan`; request uses
  `session/set_config_option` with `configId=mode`, `value=plan`; response must
  contain one mode option whose `currentValue` is `plan`.
- **Lifecycle tension:** exact Cline source bakes the mode into the first
  session manager. A post-start config update can diverge and stays absent.
  Same-session turns use the existing manager; fresh replacement negotiates a
  new manager from the immutable request.
- **Omission tension:** the native snapshot reports current Act, but omission
  must remain no-selection truth. Do not expose Act or claim a public default.
- **Permission tension:** Plan's tool preset and command guard are not
  containment. Existing observational permission handling and rejection of
  host writes remain authoritative.
- **Decisions and preferences:** portable `HarnessMode::Plan` only; no raw
  option escape hatch; no live proof; no sibling-route promotion; no implicit
  mode fallback.
- **Report after:** card 242 binding and focused validation, then card 243
  acceptance and the PR. Report earlier only for a real stop condition.
- **Report to:** the operator, who will relay progress to the orchestrator.

## Suggested Next Move

Read this file from the top, then run the worktree-safety preflight in
`## Completion Protocol` before broad repository reads. Accept a clean
launcher-provided non-`main` worktree even if its generated path or branch
differs from the placeholders. Do not create a second worktree or spawn
internal agents.

Start with card 242. Trace Plan from prepared input through capability,
preflight plan, evidence, session options, request agreement, new-session
snapshot, set-config request, exact confirmation, failure cleanup, and fresh
replacement. Continue to card 243 only after explicit Plan is confirmed before
returning a usable session and omission stays exact.

## Completion Protocol

### Before you start

1. Read this handoff. Its `worker_mode: implementation` and
   `dispatch_authority: orchestrator` metadata activate worker mode. Then run
   `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain` before broad
   repository reads.
2. If the current root is a registered worktree, its status is empty, and its
   branch is not `main`, accept it as launcher-provided. Record its actual
   root/branch and do not compare it with the placeholders or create another
   worktree merely because they differ.
3. Only if current context is `main`, dirty, unregistered, or unusable should
   you inspect the named worktree. If that also cannot be used, read
   `.agents.local.env` and require `AGENTS_WORKTREE_CONTAINER_DIR`; ask the
   operator if absent. Create a unique worktree and branch there from
   `origin/main`. Never use `/tmp`, `TMPDIR`, or a guessed path; never clean,
   reset, stash over, or discard another checkout. If the launcher supplied a
   dirty or `main` worktree, stop and report it instead of silently creating a
   second worktree.
4. From the selected worktree, fetch origin, confirm `HEAD == origin/main`,
   confirm `git merge-base --is-ancestor abdaefd2bef66a6818953128e07388acb0dcbd6e HEAD`,
   and confirm this handoff exists in `HEAD`.
5. Read `AGENTS.md`, g04.086, cards 242-243, Research 240, the named contracts,
   and the Cline ACP guide.
6. Run the repo's cheap orientation checks and record what you ran.

### While you work

- Execute cards 242-243 in order. Keep commits aligned with binding and
  acceptance chunks.
- After each meaningful chunk, report changed files, validation actually run,
  remaining work, risks, and blockers through the operator.
- Stop if a contract is missing, intent is ambiguous, scope expands,
  confirmation cannot be exact, or validation changes the plan.
- Do not quietly turn an open question into a new architecture.

### When the assigned runway is complete

1. Run the final validation named in `Current State`.
2. Update Research 240, cards 242-243, g04.086, and the reserved route-local log
   with actual evidence. Record shared closeout deltas without editing the
   inventory, programme, triage, shared indexes, or sole Next Task.
3. Push the selected worker branch.
4. Open one reviewable PR against current pushed `main`. The planning base above
   predates this handoff commit and is intentionally not self-referential.
5. Link g04.086, cards 242-243, Research 240, changed surfaces, validation, and
   unresolved items in the PR body.
6. Report the PR URL. Do not merge.

### Review and merge path

The orchestrator will review metadata, commits, diff, checks, and changed files
against g04.086, cards 242-243, Research 240, and the governing contracts.
Shared GitHub identity prevents formal self-approval, so the orchestrator's PR
comment is the canonical review record. Requested changes: none yet. The
operator must explicitly authorise merge.

- **Closeout refs:** Research 240; cards 242-243; g04.086; reserved route-local
  log; Cline ACP guide; feature/route matrices; example; package API baseline;
  changelog; shared inventory/programme/index/Next Task after merge.

### Handoff closeout

Leave assigned card, milestone, Research, and log state honest. If blocked,
record the exact blocker and stop. Do not make the lane look complete or widen
the feature to get a green check.
