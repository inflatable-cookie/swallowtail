# Claude Code 2.1.281 Identity

Research 341 froze official npm/GitHub `2.1.280` and `2.1.281`, both platform
artifacts, the complete built-in hook inventory, and the settings-parser
decision. The `2.1.281` `agents-md` prompt-context hook is on by default and
can add project instructions. Writable `--settings` cannot disable built-in
plugins, and `disableAllHooks` outside managed policy retains them.

g06.018 therefore takes branch 3. Both current claims remain at `2.1.278`
pending an operator ruling on the narrowed response-only isolation guarantee.
Downloaded binaries were hashed and read, not executed. No live provider work.
