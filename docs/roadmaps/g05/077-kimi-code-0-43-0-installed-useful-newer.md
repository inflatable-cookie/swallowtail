# g05.077 Kimi Code 0.43.0 Installed Useful Newer

Owner: Tom
Created: 2026-09-15
Depends on: Contracts 017, 023, and 029; Research 179, 210, 211, 270, 308,
and 324; g05.016, g05.017, and g05.076
Vision tags: route currentness, Kimi Code, ACP, headless, process authority

## Outcome

Complete. A split outcome through official stable `0.43.0`. Research 325
froze npm and GitHub `0.39.1`, `0.40.0`, `0.40.1`, `0.41.0`, `0.42.0`, and
`0.43.0` with tarball, tag, commit, tree, source-archive, and release-asset
digests, one deterministic tagged-source inventory per point, a
mutation-sensitive selected-file blob ledger, and a bundle oracle, before any
claim moved.

The Kimi Code installed headless claim advances from maintained
`0.33.0..=0.39.1` to maintained `0.33.0..=0.43.0` on
`kimi.headless.executable-window-2` with the `kimi.headless.stream-json.v2`
behavior revision, baseline `0.33.0`, and `AllowUnverified` unchanged. The
`dispatchNativeEvent` switch is byte-identical across all six points — the
same ten case labels and writer calls — and `prompt-render.ts` and
`options.ts` are byte-identical git blobs, so the JSONL roles, meta types,
`system.version` preamble, `session.resume_hint`, retry record, tool record,
terminal shape, retention, cancellation, and cleanup do not move. Only
internal dependency-injection, telemetry, shutdown-quiescence,
prompt-submission, and cancellation mechanics change, plus the `0.42.0`
deletion of the v1 legacy body and the `KIMI_CODE_LEGACY_FLAG` gate. The
`0.41.0` auto-permission-mode dangerous-command guard drop and the `0.42.0`
trust-gated MCP stderr warning stay unmapped. Unpublished `0.43.1` is the
visible `UnverifiedNewer` point.

`kimi-code.acp` holds at `QualifiedOnly` `0.38.0`. The g05.017 A2 ruling is
binding and unchanged: `acpTerminalRunner.ts` is git blob `9016d48b` at every
point from `0.39.1` through `0.43.0`, and the bundled `AcpProcessService` is
SHA-256 `7c58e045` at all six — the frozen `0.39.0`/`0.39.1` digest — so the
`terminal: false` local host-process spawn is still the branch taken. No exact
containment or removal exists. The exact `0.39.0` and `0.39.1` exclusions do
not grow, `0.40.0..=0.43.0` is the posture-rejected published gap, and no new
ACP behavior revision, public operation, or shared type is required.
`kimi-code.local-server` stays Research 282's separate family at `0.38.0`.

No provider prompt, login, credential, catalogue call, live session,
installation, host update, downloaded-binary execution, local-server start,
release, tag, publication, or consumer mutation occurred.

## Ready-State Rubric

- [x] Tom authorized every Research 308 family, explicitly including Kimi.
- [x] g05.076 completed; canonical `main` is clean and synchronized.
- [x] Research 270 and g05.017 freeze the split installed-route result and A2
      ACP containment rule.
- [x] Official npm/GitHub `0.43.0`, the five-hop stable sequence, and the
      installed-host observation are known separately.
- [x] No prompt, login, provider session, installation, or host update is
      needed.

## Work

1. Re-probe npm and GitHub latest plus installed `kimi --version`. Retrieve the
   official npm packages and matching tagged source for `0.39.1`, `0.40.0`,
   `0.40.1`, `0.41.0`, `0.42.0`, and `0.43.0` into `/tmp`; corroborate
   package/tag identity and freeze Research 325 plus one mutation-sensitive
   selected-file ledger before changing claims.
2. Use official release notes to discover candidate behavior changes. Inspect
   only shipped source that contains or feeds the selected installed routes.
   ACP owns `kimi acp`, capability advertisement, initialization, session and
   prompt lifecycle, callbacks, reasoning/mode/model mapping, permission and
   terminal authority, failure, cancellation, process ownership, and cleanup.
   Headless owns `kimi --model ... --prompt ... --output-format stream-json`,
   selected event and terminal records, reasoning/model/config inputs,
   retries, retention, failure, cancellation, process ownership, and cleanup.
   Build one deterministic changed-file inventory. Do not repeat whole-package
   scans, decompile binaries, or inspect unrelated UI/web/local-server code.
3. Classify all five hops independently per claim. For ACP, prove at every hop
   whether the `terminal: false` path still permits the uncontained local
   host-process spawn that forced the `0.38.0` QualifiedOnly cap. Admit only an
   exact bounded/removed replacement; otherwise preserve the cap and record
   the published gap without growing a redundant deny-list. For headless,
   prove the selected v2 argv, stream grammar, retry ownership, terminal
   outcome, retention, cancellation, and cleanup or stop at the first change.
4. Keep `kimi-code.local-server`, Remote Control/web, Tower/subagents, MCP and
   dynamic tools, skills/hooks, session UI/management, model service behavior,
   and Python `kimi-cli` separate unless exact dependency tracing proves they
   feed a selected installed route. Do not transfer local-server authority
   findings onto ACP/headless or widen provider features from release notes.
5. Land the maximal honest independent segments. Preserve baselines, claim ids,
   behavior revisions, exclusions, and newer-version posture unless exact
   evidence requires the smallest change. Update selected tests/fixtures,
   Kimi prepared guide, route/activity/feature matrices, architecture ceiling,
   `[Unreleased]`, Research 325, identity/claim logs, standing lane, this task,
   g05 index/census, and the sole Next Task pointer.
6. Recheck official latest before the identity commit and immediately before
   push. Follow Contract 029's in-run movement rule. Stop on identity
   disagreement, unresolved process authority, selected public lifecycle
   change, provider/live evidence, or operator policy. Ordinary provider-free
   retrieval, comparison, correction, and validation may rerun normally.

## Dispatch Manifest

| Field | Task 077 |
| --- | --- |
| Readiness | ready |
| Prerequisites | Contracts 017, 023, and 029; Research 179, 210, 211, 270, 308, and 324; completed g05.016, g05.017, and g05.076; clean pushed `main`; official npm/GitHub channels; installed official `kimi 0.34.0` |
| Completion conditions | Research 325 freezes `0.39.1..=0.43.0` identity before claims; all five stable hops receive independent ACP/headless selected-path classifications; the A2 authority boundary is proved or remains fail-closed; maximal honest segments or exact stops land; current docs and claims agree; exact-head review and named gates pass |
| Owned mutable paths | Kimi installed ACP/headless selection, prepared profiles, commands, drivers/decoders, tests, and fixtures under `crates/swallowtail-adapter-kimi/**`; Research 325 and one research-index line; exact Kimi prepared-guide, route/activity/feature matrices, architecture ceiling, standing-lane, `[Unreleased]`, identity-log, claim-log, task-result, g05 index/census, and Next Task lines; `PAPERCUTS.md` append only |
| Reserved shared closeout surfaces | lifecycle task record and generated projections; submitted-handoff deletion belongs to the repository hook |
| Forbidden paths | Kimi local-server behavior/claim/guide/tests; Python `kimi-cli`; other adapters; contracts; historical research/captures/releases; provider prompts/live probes; host install/update; release/tag/workflow files |
| Approved concurrent siblings | none on shared currentness surfaces; this campaign is serial |
| Worker capability class | evidence-first Rust currentness worker; npm/source artifact comparison; no provider credentials |
| Acceptance evidence | npm/GitHub/package/tag identities for all six compared points; mutation-sensitive selected-file ledger; exact ACP/headless per-hop classification; explicit A2 containment result; current docs/claim agreement |
| Review oracle | claim edit before identity; skipped stable hop; ACP/headless coupling; unclassified changed mapped file; admission across uncontained host-process authority or unbounded provider retry; local-server flattening; inferred range; provider reliance; repeated broad or binary scanning; unjustified operation/authority widening |
| Oracle gate | not required — Research 308 records Tom's Kimi campaign authority and g05.017 records the A2 policy; Contracts 017/023/029 settle identity-first qualification, while new product policy still returns to Chatterbox |
| Stop conditions | official identity disagreement; uncontained process authority or unbounded retry at the next point; selected wire/auth/permission/failure/resource/lifecycle change without exact mapping; new public operation/driver/facade required; provider/live evidence required; operator policy needed |
| Escalation owner | operator via Chatterbox for new policy; Queue coordinator for mechanical blockers |

## Boundaries

One installed package family with two existing claims. Provider-free
exact-artifact evidence and the smallest necessary private mapping/claim change
only. No prompt, inference, catalogue call, login, credential access,
installation, downloaded-binary execution, host update, live provider work,
local-server mutation, release, tag, publication, or consumer mutation.

## Validation

- `cargo fmt -p swallowtail-adapter-kimi -- --check`
- `effigy validate:focused swallowtail-adapter-kimi`
- `effigy package:verify-affected swallowtail-adapter-kimi`
- `effigy check:examples`
- `effigy package:api`
- `effigy qa:routes`
- `effigy qa:northstar`
- focused Kimi installed identity-ledger, ACP/headless selection, permission,
  terminal, failure, retry, cancellation, retention, activity, and cleanup tests
- relevant research/log/roadmap/number/lifecycle/next-action checks
- `git diff --check`

## Acceptance

- [x] official identities reproduce for all compared releases and channels
- [x] every published selected-path change is classified independently per claim
- [x] identity evidence precedes any claim or mapping change
- [x] ACP never crosses the A2 authority boundary without exact containment proof
- [x] maintained segments and incompatible gaps are maximally honest
- [x] local server and unrelated Kimi surfaces remain independently bounded
- [x] official latest is rechecked at identity and push boundaries

## Next Task

Return the exact outcome to Chatterbox, then compile and dispatch the separate
Kimi Code local-server family from fresh canonical `main` without asking Tom to
repeat authorization.
