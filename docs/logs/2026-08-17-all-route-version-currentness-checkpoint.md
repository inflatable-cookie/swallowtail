# 2026-08-17 All-Route Version Currentness Checkpoint

## Result

Research 127 compared every production route family's qualified bound against
local `--version` and official stable points. No bound moves. No catch-all
g03 range-maintenance milestone. g03 stays evidence-gated.

Most installed harnesses now sit above their qualified ceiling. That is
already UnverifiedNewer where the posture allows it, and rejected where the
posture is QualifiedOnly. Hosted API facade ids are unchanged. ACP schema is
still `v1.20.0`.

Two optional single-family cards exist if the operator promotes later: Codex
`0.147.0` (now stable, local, and already seen by Nucleus) and Grok `1.0.x`
(major-line reset on the same npm package). This checkpoint compiles neither.

## Unchanged Gates

- Gemini CLI range requalification remains deferred
- exact-pin routes (Command Code, Muse, DeepSeek Harness, ZCode, llama.cpp)
  do not inherit unverified-newer
- ZCode later gates remain later gates
- immutable `v0.3.2` stays 30 packages / 36 routes

## Validation

- no runtime, provider, authentication, consumer, install, or publication
  effect ran
- docs index gates run after this log is linked

## Next

Keep the front-door Next Task on the ZCode later-gate reassessment. Promote a
single-family range card only if the operator asks.
