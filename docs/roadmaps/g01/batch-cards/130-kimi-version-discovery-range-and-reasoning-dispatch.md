# 130 Kimi Version Discovery, Range, And Reasoning Dispatch

Status: completed
Owner: Tom
Updated: 2026-07-24
Milestone: `../043-kimi-code-capability-range.md`

## Objective

Bind exact Kimi executable evidence to private ACP behavior and apply one
preflight-selected reasoning mode on new sessions.

## Scope

- bounded non-authenticating Kimi installed-executable observation
- exact configured-instance, requirement, plan, discovery, and ACP handshake
  agreement
- publish exact `0.28.1` and `0.29.0` qualified segments
- permit exact stable newer releases as unverified through the `0.29.0`
  behavior revision
- private legacy-boolean and declared-effort dispatch
- one correlated `session/set_config_option` request after `session/new`
- exact effective reasoning confirmation before readiness
- no reasoning selection on load or resume
- empty-options regression coverage for the complete existing persistent
  session lifecycle

## Acceptance Criteria

- [x] discovery grants no install, auth, configuration, or execution authority
- [x] handshake drift rejects before session work
- [x] each exact qualified point selects only its frozen private behavior
- [x] unverified newer remains visible and unqualified
- [x] reasoning setup cannot switch model, agent mode, provider, or route
- [x] failure joins process, resource, callback, and credential work

## Validation

- focused Kimi discovery, driver, range, and lifecycle tests
- workspace all-target check
- workspace warnings-denied clippy
- `git diff --check`

## Auto-Continuation

Yes, after both qualified behaviors and unverified-newer drift fail
deterministically.

## Outcome

The Kimi descriptor now publishes installed-executable discovery and an exact
two-point compatibility claim. One host-approved `kimi --version` process
returns only an exact semantic version. The observation retains authoritative
host identity and distinguishes qualified, incompatible, malformed,
cancelled, and unverified-newer results without acquiring credentials or a
working resource.

Interactive preflight now requires one exact executable binding. `0.28.1`
selects the private legacy boolean mapping. `0.29.0` selects declared effort
levels. Stable `0.30.0` remains visibly unverified and uses only the latest
qualified private behavior. Initialization must corroborate the planned exact
version before session allocation.

New sessions may apply one preflight-bound reasoning value through one
correlated `session/set_config_option` request. Readiness requires exact
effective confirmation. Missing, duplicate, malformed, unsupported, rejected,
unconfirmed, or drifted options fail without fallback and join the allocated
attachment. Load and resume reasoning reject before host effects. Empty
options retain the existing new, load, resume, replay, write, cancellation,
disconnect, and cleanup behavior.

## Evidence

- focused Kimi unit, discovery, range, reasoning, and persistent-lifecycle
  tests passed
- workspace all-target check passed
- workspace warnings-denied clippy passed
- format check and `git diff --check` passed
